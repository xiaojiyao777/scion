# Scion: OR × LLM 算法自动改进框架

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)

**Scion**（分支/嫁接）是一个面向组合优化问题的 **LLM 驱动算法自动改进框架**。它旨在通过 LLM 的先验知识与推理能力，在人类定义的“算法沙盒”内自主探索、验证并迭代启发式算子。

## 核心理念

与传统的纯进化算法不同，Scion 强调 **“推理驱动”** 而非随机变异：
- **LLM 作为推理主体**：利用 LLM 对业务逻辑的理解，提出改进假设，而不仅仅是修改代码。
- **严格的实验协议**：三级过滤机制（Screening -> Validation -> Frozen Holdout）控制过拟合风险。
- **契约式治理**：通过静态 Contract Gate 与动态 Verification Gate 强制约束算法边界，抑制 LLM 幻觉。

## 关键特性

- 🚀 **闭环迭代**：算子提出 (Proposal) → 契约校验 (Contract) → 仿真验证 (Experiment) → 决策采纳 (Decision) 的全自动化闭环。
- 🛡️ **安全沙盒**：多层 Gate 机制确保生成的代码符合物理约束（如库存不为负、车辆不超载）。
- 📊 **决策一致性**：Decision Layer 仅接收数值化的 DecisionFeatures，屏蔽 LLM 的文本干扰，实现客观评估。
- 🧬 **分支管理**：模拟 Git 工作流的演化路径管理，支持分支探索、回退与合并。
- 🏗️ **工程化支撑**：内置分布式仿真引擎接口、SQLite 线索注册表、Pydantic v2 配置校验。

## 系统架构

Scion 采用分层控制架构，确保“创造力”与“确定性”的平衡：

1. **Creative Layer (LLM, Tainted)**: 负责 Hypothesis 提出与代码生成。
2. **Gate Layer (Static & Dynamic)**: 负责语法检查、契约校验（C1-C10）与基础验证。
3. **Protocol Layer (Statistical)**: 执行三级实验协议，计算统计显著性。
4. **Decision Layer (Oracle)**: 根据字典序多目标评价体系，决定分支的命运。

## 快速开始

### 安装

```bash
git clone https://github.com/xiaojiyao777/or-autoresearch-agent.git
cd or-autoresearch-agent/scion
pip install -e .
```

### 运行 Campaign

```bash
# 初始化工作区
scion init-workspace --problem problems/warehouse_delivery

# 开启自动优化任务
scion run --campaign-id "WD_OPTIM_001" --max-rounds 10
```

## 目标问题示例：仓配协同

Scion 0.1 版本已在“仓配协同 VNS + Solution Pool”场景下完成验证：
- **Surrogate Solver**: 针对大规模订单分配与路径规划的启发式算法。
- **Operator Pool**: 自动优化订单级（Move/Swap）与车辆级（Split/Merge）算子。
- **目标函数**: 优先确保业务齐套率（聚合度），其次优化物流总成本与路径效率。

## 开发路线

- [x] **v0.1 MVP**: 核心循环、Contract Gate、三级实验协议、SQLite Registry。
- [ ] **v0.2 (P1)**: 增强 Verification Gate（深度业务逻辑校验）、多模型协同 (Opus Architect + Sonnet Coder)。
- [ ] **v0.3 (P2)**: 引入 RAG 记忆模块，实现跨 Campaign 的经验沉淀。

## 开源协议

基于 MIT License 开源。

---
*Generated by Cris with ⚙️ precision for BigBOSS.*
