# Scion v0.3 Final Validation Checklist

Date: 2026-04-26

## Code Gate

- [x] Production verification entrypoints require adapter-backed runtime checks.
- [x] Production experiment entrypoints require objective `metric_specs`.
- [x] `ObjectiveBreakdown` and `compare_with_breakdown()` removed.
- [x] Campaign `status.json` includes stage-level progress heartbeat during experiments.
- [x] `fix_code` light retry path logs at INFO.
- [x] Core regression subset passes.
- [x] Validation runner preserves rich `CampaignManager` summary and writes
      `validation_runner_summary.json` separately.
- [x] Experiment lineage records `raw_metrics_ref`, `case_ids`, and `seed_set`.
- [x] Branch lifecycle state is persisted for abandon/promote/reconcile paths.
- [x] Screening expansion is capped to one pre-registered expand per candidate.
- [x] Weight-only updates record invalidation audit and conservatively stale
      active non-frozen branches.
- [x] Metrics files are checkpointed during long validation/frozen stages.
- [x] `parameter_search.execution` supports `sync` and `async`.
- [x] Formal validation runner defaults to sync weight optimization on the
      current 2-core server.
- [x] `status.json` publishes weight-optimization phase, active runs,
      evaluation counts, estimated solver runs, and final improvement state.
- [x] Synthetic/production protocol mismatch fails fast in
      `run_validation_campaign.py`.
- [x] Objective steering is injected when higher-priority objectives are
      stable/saturated, guiding lower-priority optimization with explicit
      protection.

## Experiment Gate

Completed before this hardening pass:

- [x] `claude-sonnet-4-6`, synthetic, 100-round cap, independent process.
- [x] `gpt-5.4-mini`, synthetic, 100-round cap, independent process.

Run this after the hardening pass, before the full 6-campaign rerun:

- [x] Short confirmation campaign verifies new `status.json`,
      `campaign_summary.json`, `validation_runner_summary.json`, DB lineage,
      and partial metrics checkpoint behavior.

Proceed to the full 6-campaign rerun only if the confirmation run:

- complete without traceback;
- write `campaign_summary.json`;
- keep `status.json` updating during the run;
- preserve `stopped_reason`;
- populate `experiment_events.raw_metrics_ref`, `case_ids`, and `seed_set`;
- keep branch lifecycle states coherent in SQLite;
- do not hit production wiring errors for adapter / metric specs / verification.

Full closure rerun:

- [x] Six synthetic campaigns completed under
      `~/research/scion-experiments/v03-closure-validation-20260425/`.
- [x] All six wrote rich summaries, runner summaries, status files, DB lineage,
      and metrics checkpoints.
- [x] CPLEX final results imported as the report-only MILP benchmark source.
- [x] Known CPLEX-infeasible data-generation instances are absent from
      `surrogate/milp_bounds/`; residual manifest references were removed from
      synthetic/mixed manifests.
- [ ] Promotion-cadence target was not met; this is recorded as a search
      effectiveness issue for v1/v0.4, not a framework closure blocker.

## Sync Weight Optimization Gate

Completed before starting formal 12-campaign validation:

- [x] Synthetic confirmation campaign promoted to `v2`, completed sync weight
      optimization, improved to `v2_r1`, recorded `weight_optimizations=1`,
      and continued structure search.
- [x] Production confirmation campaign promoted to `v2`, completed sync weight
      optimization with no further improvement, recorded
      `weight_optimizations=1`, and continued structure search.
- [x] Both confirmation campaigns loaded the expected protocol:
      synthetic -> `protocol.yaml`; production -> `protocol_prod.yaml`.
- [x] LLM traces include objective policy, objective opportunity profile, and
      exploration/exploitation guidance.
- [x] Production traces include objective steering that treats stable
      `subcategory_splits` as protected and targets `total_cost`.

Formal v0.3 validation started:

- [x] Base dir:
      `~/research/scion-experiments/v03-final-sync-12campaign-20260426/`
- [x] Matrix:
      `claude-sonnet-4-6`, `gpt-5.4-mini` x `synthetic`, `production`
      x seeds `11`, `29`, `47`
- [x] Queue parallelism: 2
- [x] Weight optimization: sync
- [x] Queue started from commit `6ca1c13`.

## Two-Model Validation Post-Run Analysis

Completed after both 100-round synthetic campaigns:

- [x] Reconstruct each campaign lifecycle from `status.json`, `campaign.log`,
      `scion.db`, `metrics/*.json`, `champions/*`, and `archive/*`.
- [x] Summarize per model:
      promotions, final champion version, stopped reason, verification failure
      profile, branch abandon/promote paths, validation/frozen pass quality,
      and objective-level improvement.
- [x] Check design alignment against v3/v0.3:
      branch lifecycle, hierarchical CI gates, frozen holdout semantics,
      generic objective comparison, adapter-backed verification, failure
      routing, and lineage auditability.
- [x] Decide whether the result is sufficient to start the 6-campaign rerun:
      search loop yes, evidence layer required hardening first.

### Observed Framework Fix Candidates

These issues were observed during the live two-model validation and have been
handled in the validation hardening patch unless explicitly marked P1:

1. [x] `status.json` needs stage-level heartbeat, not only step-level snapshots.
   Long validation/frozen steps must expose current stage, branch, case/seed,
   completed pair count, step start time, and last progress timestamp.
2. [x] Branch lifecycle persistence needs synchronization. `branches.state` must
   reflect abandon/promote/frozen transitions consistently with hypotheses and
   campaign logs.
3. [x] Metrics lineage needs durable DB links. `experiment_events.raw_metrics_ref`
   should point to the corresponding `metrics/*.json` evidence file for every
   experiment event.
4. [x] Expanded screening needs de-duplication and hard accounting. The same
   candidate should not repeatedly rerun identical expanded screening splits
   without a clear budget reason.
5. [x] Long campaigns need partial summaries/checkpoints. A running or interrupted
   campaign should still expose enough structured evidence to audit in-progress
   validation/frozen work and support controlled resume/retry.
6. [ ] P1: Stagnation signals need stronger downstream effect. Plateau/oscillation
   should bias or constrain proposal/scheduler behavior more clearly, instead
   of remaining mostly advisory log output.

## Deferred To v1.0

- Remaining deprecated compatibility surface:
  - `CaseAggregateFeedback` alias fields;
  - scalar `compute_delta()` compatibility path;
  - `_MECHANISM_KEYWORDS` fallback paths.
- Full observability beyond minimal `status.json`.
- FCMCNF + Benders second real problem.
- Multi-campaign queue / restart orchestration beyond current launcher.
- Further `campaign.py` responsibility extraction by cohesive service boundaries.
