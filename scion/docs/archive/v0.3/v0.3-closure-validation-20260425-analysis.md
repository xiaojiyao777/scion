# v0.3 Closure Validation and CPLEX Benchmark Audit

**Date**: 2026-04-26
**Campaign dir**: `~/research/scion-experiments/v03-closure-validation-20260425/`
**CPLEX package**: `surrogate/cplex-final-20260426/`

## 1. Campaign Completion

All six synthetic closure campaigns completed without launcher or campaign crash.

| Campaign | Rounds | Experiments | Final champion | Promotions | Stop reason |
|---|---:|---:|---:|---:|---|
| `sonnet-4-6_synthetic_seed11` | 25 | 23 | v2 | 1 | idle ratio 76.0% exceeds 60% |
| `sonnet-4-6_synthetic_seed29` | 26 | 26 | v2 | 1 | idle ratio 61.5% exceeds 60% |
| `sonnet-4-6_synthetic_seed47` | 36 | 33 | v2 | 1 | idle ratio 61.1% exceeds 60% |
| `gpt-54-mini_synthetic_seed11` | 43 | 35 | v3 | 2 | no promotion in 25 rounds with stagnation plateau |
| `gpt-54-mini_synthetic_seed29` | 46 | 38 | v2 | 1 | idle ratio 60.9% exceeds 60% |
| `gpt-54-mini_synthetic_seed47` | 25 | 22 | v2 | 1 | idle ratio 88.0% exceeds 60% |

Aggregate:
- Sonnet promotions: 3 total, 1.0 average.
- GPT-mini promotions: 4 total, 1.33 average.
- Validation stages executed: 14.
- Frozen stages executed: 8.
- Frozen passes: 7.
- All campaigns produced `campaign_summary.json`, `validation_runner_summary.json`,
  `status.json`, `scion.db`, and checkpointed `metrics/*.json`.

## 2. Pre-Registration Outcome

| Criterion | Target | Result | Outcome |
|---|---:|---:|---|
| Sonnet avg promotions | >= 2.0 | 1.0 | Not met |
| GPT-mini avg promotions | >= 1.5 | 1.33 | Not met |
| Early-stop round, if any | >= 25 | 25-46 | Met |
| All 6 campaigns complete without crash | required | 6/6 | Met |

Interpretation:
- The engineering closure succeeded: the long-running campaign stack completed
  cleanly and the evidence chain is complete.
- The promotion-cadence target was not restored to the pre-registered threshold.
  This should be treated as an algorithm/search-efficiency finding, not as a
  framework integrity failure.

## 3. CPLEX Benchmark Replacement

The report-only MILP comparison has been switched from the historical HiGHS
package to the CPLEX final package.

CPLEX package summary:
- Total results: 83.
- `optimal`: 68.
- `feasible`: 9.
- `timeout`: 1.
- `infeasible`: 5.
- Verified incumbents: 77.
- CPLEX improves over historical HiGHS on 16 matched instances.

`surrogate/milp_bounds/` was regenerated from
`surrogate/cplex-final-20260426/{production,synthetic}/`:
- Written benchmark files: 78.
- Skipped infeasible files: 5.
- Status breakdown in compact bounds: 68 `optimal`, 9 `feasible`, 1 `timeout`.

The compact files retain CPLEX provenance fields:
`solver`, `source_package`, `source_result`, `milp_status`, `milp_exact`,
`milp_verified`, `oracle_feasible`, `n_orders`, and `scale`.

Deep-analysis caveat: a later fixed frozen post-run evaluation found current
baseline/final VNS solutions that are feasible under the runtime oracle but
better than several CPLEX-reported reference f1 values. CPLEX results also mix
`optimal`, `feasible`, and `timeout` statuses. Until the MILP model, oracle, and
data-version provenance are reconciled, CPLEX must remain report-only reference
data rather than a strict exact-gap certificate. See
`docs/v0.3-closure-validation-20260425-deep-analysis.md` and
`docs/v0.3-closure-validation-20260425-deep-analysis.zh.md`.

## 4. Infeasible Instance Audit

CPLEX identified five data-generation infeasible instances:

| Family | Split | Instance | Manifest status |
|---|---|---|---|
| production | frozen | `instance_prod_fro_x02.json` | already absent from prod manifest; removed from mixed |
| production | screening | `instance_prod_scr_m02.json` | already absent from prod manifest |
| production | screening | `instance_prod_scr_ml03.json` | already absent from prod manifest |
| production | validation | `instance_prod_val_l03.json` | already absent from prod manifest |
| synthetic | frozen | `instance_v4_fro_x06.json` | removed from synthetic and mixed manifests |

Formal synthetic closure metrics did not include any of these instances:
- Distinct cases touched by metrics: 30.
- Intersection with infeasible/unverified CPLEX list: none.

`instance_prod_val_lx02.json` is not infeasible, but CPLEX timed out and returned
an unverified feasible incumbent. It remains in the production manifest as a
runtime case, but its compact benchmark status is `timeout`, not `optimal`.

## 5. Current Conclusion

v0.3 now has a clean engineering closure for the framework:
- generic objective comparison and adapter-backed verification are active;
- evidence artifacts are sufficient for post-run audit;
- branch lifecycle and metrics lineage are coherent;
- report-only MILP/CPLEX comparison is separated from campaign gates.

The remaining concern is search effectiveness: the closure run promoted less
often than the pre-registered target. This is v1/v0.4 algorithm work, not a
reason to block the v0.3 framework baseline.
