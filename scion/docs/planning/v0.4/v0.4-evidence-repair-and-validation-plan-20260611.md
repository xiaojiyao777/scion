# Scion v0.4 Evidence Repair And Validation Plan

*Date: 2026-06-11*
*Branch: `codex/v04-evidence-repair-plan`*
*Status: Proposal trajectory report artifacts accepted; CVRP still low-power*

This plan translates the 2026-06-09 and 2026-06-11 audits into v0.4 work.
v0.5 should run broad controlled experiments; v0.4 must first make those
experiments scientifically interpretable and restore meaningful VRP/warehouse
research capability.

## Current Execution Status

- Phase 0 postrun baseline is now captured in
  [`../../experiments/v0.4/v04-evidence-verify-4r-gpt55-20260611-phase0-postrun.md`](../../experiments/v0.4/v04-evidence-verify-4r-gpt55-20260611-phase0-postrun.md).
  The paired CVRP/warehouse 4R verification runs from commit `0a6a2f5` both
  finished cleanly. CVRP completed `4/4` screening rows with no validation or
  frozen evidence; warehouse completed a full screening -> validation -> frozen
  promotion path and reached champion v2.
- Phase 1 A/A calibration is complete. Warehouse create/modify A/A screening
  calibrations are complete. The first CVRP safe-root run failed under uniform
  30s on `M/M-n200-k17.vrp`; a corrected uniform-60s CVRP A/A run finished as
  a legacy first estimate; and the repaired protocol-time CVRP A/A run finished
  as the formal Phase 1 gating artifact. The final report is
  [`../../experiments/v0.4/v04-phase1-aa-calibration-20260611.md`](../../experiments/v0.4/v04-phase1-aa-calibration-20260611.md).
  CVRP protocol-time MDE is `9.9` raw `total_distance` with false-pass rate
  `0.0`; warehouse MDEs are `1725.0` raw `total_cost` for create and `577.5`
  raw `total_cost` for modify, both with false-pass rate `0.0`.
- R1/R2 implementation is owned by the main thread in this branch. The first
  patch adds problem-owned measurement declarations, resolves practical deltas
  into protocol config, threads problem-v1 measurement through `init` and
  `optimize-weights`, validates measurement metric names against declared
  objectives, and separates comparative runtime from budget-exhausting runtime.
- R3/R4 observability is delegated to worker `019eb708-4c13-7191-8fa0-07ff3f99633f`
  with a narrow write scope: campaign summary/state and prompt/evidence
  manifest diagnostics only; no lifecycle, scheduling, protocol gate, or
  `DecisionFeatures` changes.
- Focused verification already run for the main-thread patch:
  `test_config.py`, `test_problem_bridge.py`, `test_problem_adapter.py`,
  `test_cli_run_options.py`, `test_protocol_stats_gates.py`, and
  `unit/core/test_runtime_budget_diagnostics.py`.
- CVRP focused regression already run for the existing actionability/runtime
  patch set: `test_cvrp_adapter_core.py`, `test_cvrp_protocol_smoke.py`,
  `test_cvrp_solver_algorithm_runtime.py`,
  `unit/test_cvrp_active_solver_map_provider.py`, and
  `unit/test_cvrp_solver_design_provider.py`.
- P0 A/A calibration now has a reusable numeric library and
  `scion/tools/calibrate_aa_noise.py`. A minimal CVRP controlled smoke passed at
  5s/10s with `n_pairs=1`; the earlier 1s failure was a time-budget issue, not
  path or solver breakage. Worker F landed the Phase 1 prerequisite tooling
  repair: A/A artifacts include replayable pair/runtime evidence, selected
  cases/seeds, seed offsets, case resolution, safe data roots, and runtime
  policy metadata; the CLI can run with
  `--runtime-policy protocol_time_limits`. The final formal CVRP protocol-time
  artifact finished from commit `a43dc2be371b5f2f209477df54883708b8750055`
  with `n_pairs=96`, `mde_at_power_80=9.9` raw `total_distance`,
  `false_pass_rate_at_current_gate=0.0`, and `recommended_min_seeds=8`.
  Read-only subagent cross-checks accepted that the Phase 0 CVRP candidates
  were below measured screening power (`median_delta=0.0` for all four
  candidates; best CI upper bound `8.0` versus final MDE `9.9`), not disproven
  by the failed screening outcomes.
- P1 runtime governance now includes `measurement.runtime_model`,
  budget-exhausting saturation as non-actionable info, disabled fresh-replay
  for budget-exhausting runtime ties, and V9 budget-compliance semantics.
- R4 context-density repair now exposes `Compact Research Signals` before broad
  rules in hypothesis prompts, compresses the safety/rules block, and reports
  `research_signal_ratio` / governance share in prompt manifests.
- Phase 2 Worker C is integrated: deterministic `pairing_validity` is resolved
  from problem measurement declarations; trajectory-divergent low-SNR screening
  can expand below `0.5` aggregate win rate when evidence is tie-heavy or weak
  non-negative; hard negative, loss-heavy, candidate-failure, timeout, and true
  runtime-regression evidence still fails closed; lifecycle limits are relaxed
  only for trajectory-divergent low-SNR research.
- Phase 2 Worker D/E is integrated: proposal context exposes tainted
  problem-owned measurement/noise/opportunity diagnostics as research signal,
  filters raw calibration rows and holdout/detail leaks, and prompt manifests
  now record code-phase source visibility guarantees for target and required
  integration source after compression.
- Integrated verification passed with `311 passed` across config, protocol,
  Decision, lifecycle, runtime, prompt/context, source-visibility, and proposal
  manifest focused tests. The v3 generic-layer boundary test also passes after
  removing a legacy generic-core `vns` hardcode.
- Phase 3 measurement readiness is integrated. `measurement_readiness_status()`
  resolves `calibration_ref`, reports missing/unreadable/incompatible/
  incomplete/stale/ready states, and reduces compatible A/A artifacts to
  deterministic readiness status. Compact Phase 1 artifacts are installed at
  `scion/scion/problems/cvrp/formal/calibration/aa_noise_floor.json` and
  `surrogate/calibration/aa_noise_floor.json`; they include source artifact
  refs and SHA256 hashes but omit raw `pair_evidence`. CVRP and warehouse refs
  both resolve as `ready` on 2026-06-11, with `low_power` signal tiers.
- Focused Phase 3 verification passed with `34 passed` across measurement
  readiness, problem bridge, config, and hypothesis-context tests.
- Phase 4 first-rung focused validation is launched from commit `32ab596` with
  local `gpt5.5`: CVRP formal 4R at
  `/home/clawd/research/scion-experiments/v04-phase4-focused-cvrp-measreadiness-20260611-4r-gpt55-20260611T224916Z-claw`
  and warehouse production 4R at
  `/home/clawd/research/scion-experiments/v04-phase4-focused-warehouse-measreadiness-20260611-4r-gpt55-20260611T225004Z-claw`.
- Warehouse first-rung result is invalid for Phase 4 validation: four attempts
  were `CANARY_FAILED` before protocol screening, with
  `screening_protocol_results=0`, `effective_protocol_rounds=0`, no formal
  candidate index, and missing protocol `raw_metrics_ref`.
- Worker H's generic canary evidence/accounting repair is accepted in the
  canary accounting repair commit. Canary vetoes now persist structured details,
  do not count as formal/protocol rows, preserve legacy reported counters under
  `legacy_*_reported`, and terminal zero-protocol-row runs are invalidated as
  `invalid_no_protocol_rows`.
  Acceptance: full core unit suite `489 passed`, protocol/canary suite
  `31 passed`, and `git diff --check`. Next gate: rerun warehouse Phase 4 from
  the repair commit while the CVRP run continues from `32ab596`.
- Warehouse accounting verification rerun from repair commit `a6a34d6` finished
  with wrapper exit 0 at
  `/home/clawd/research/scion-experiments/v04-phase4-focused-warehouse-canaryfix-foreground-20260611-4r-gpt55-20260611T234843Z-claw`.
  It consumed four attempts, produced zero experiments, correctly reported
  `formal_screened_candidates=0`, `effective_protocol_rounds=0`,
  `screening_protocol_results=0`, `protocol_metric_results=0`, and
  `run_validity.reason=invalid_no_protocol_rows`. This verifies the accounting
  repair, but the run is still not warehouse research evidence because canary
  resolution now fails explicitly on
  `artifact:instance_prod_can_s01.json#64a747f955e8` as
  `absolute_outside_roots`.
- Curie's warehouse safe-root repair is accepted. The production split declares
  `safe_data_roots: ../../../../scion-data`, keeping the fix in the
  problem-owned warehouse manifest while generic core/Decision semantics remain
  unchanged. Acceptance: protocol/CLI/e2e focused subset `12 passed` and
  `git diff --check`. Next gate: commit this repair and rerun warehouse Phase 4
  from the new commit.
- Warehouse safe-root Phase 4 rerun from commit `63f01d7` finished valid at
  `/home/clawd/research/scion-experiments/v04-phase4-focused-warehouse-saferoot-20260611-4r-gpt55-20260612T000035Z-claw`.
  It produced `effective_protocol_rounds=4`, `formal_screened_candidates=4`,
  `protocol_evaluated_candidates=4`, `screening_protocol_results=5`,
  `protocol_metric_results=5`, `validation_protocol_results=0`,
  `frozen_protocol_results=0`, and champion v1. The fifth screening row is a
  non-counted fresh-runtime replay closure; candidate reconciliation reports
  four formal candidate artifacts. Next gate: warehouse postrun audit plus CVRP
  completion/audit before governance on/off.
- Warehouse postrun audit is complete:
  [`../../experiments/v0.4/v04-phase4-warehouse-saferoot-4r-postrun-20260612.md`](../../experiments/v0.4/v04-phase4-warehouse-saferoot-4r-postrun-20260612.md).
  It accepts the run as valid no-promotion evidence. Candidate 1 was weak mixed
  signal below warehouse A/A MDE, candidates 2/3 were zero-effect
  same-mechanism follow-ups, and the clean fork was negative. v3 boundaries and
  prompt/source visibility looked healthy. Phase 5 design should keep
  fresh-runtime replay out-of-band and avoid counting replay closures as
  research candidates.
- CVRP first-rung Phase 4 run from commit `32ab596` completed valid at
  `/home/clawd/research/scion-experiments/v04-phase4-focused-cvrp-measreadiness-20260611-4r-gpt55-20260611T224916Z-claw`.
  It produced `effective_protocol_rounds=4`, `formal_screened_candidates=4`,
  `screening_protocol_results=4`, `protocol_metric_results=4`,
  `validation_protocol_results=0`, `frozen_protocol_results=0`, and champion
  v1. Decisions: `expand_screening`, `continue_explore`, `expand_screening`,
  `abandon`.
- CVRP postrun audit is complete:
  [`../../experiments/v0.4/v04-phase4-cvrp-measreadiness-4r-postrun-20260612.md`](../../experiments/v0.4/v04-phase4-cvrp-measreadiness-4r-postrun-20260612.md).
  It accepts the run as valid first-rung evidence. Formal config, A/A
  calibration, replayable patch artifacts, protocol metric rows, verification,
  lineage, and deterministic decisions reconcile. Runtime-governance repairs
  worked for budget-exhausting CVRP: no fresh runtime replay drain, saturation
  remained info-only, and V9 checked budget compliance rather than standalone
  speed. Branch mechanics improved enough that both mechanisms received
  same-mechanism expansion under the 4R budget.
- Phase 4 closeout interpretation: v0.4 now has a healthier CVRP evidence path,
  but CVRP quality-improvement evidence is still underpowered under the current
  4-seed screening protocol. `route_limit_aware_repair` was all-tie/zero-effect;
  `double_bridge_relink_vns` had meaningful movement (`17/9/6` pair evidence
  initially and `22/19/7` after expansion) but ended with median `-1.25`, CI
  `[-5.75, 7.25]`, and high CI below the Phase 1 CVRP MDE `9.9`. Next gate:
  design a CVRP 8-seed or otherwise power-adjusted screening check, and use
  warehouse or another effect-measurable problem as the first governance on/off
  candidate unless the CVRP measurement check clears.
- Next-rung design is complete. CVRP gets a staged power check first:
  8 selected screening cases x 8 screening seeds x 3 replicates, with
  protocol-resolved runtime limits, temporary run-root protocol/seed/split
  copies, and no LLM calls. The active run is
  `/home/clawd/research/scion-experiments/v04-phase4-cvrp-8seed-aa-saferoot-20260612T011824Z-claw`.
  A prior attempt
  `/home/clawd/research/scion-experiments/v04-phase4-cvrp-8seed-aa-20260612T011722Z-claw`
  failed before solver execution because copied protocol config no longer
  activated formal data-root wiring; the active saferoot run declares the repo
  `vrp` directory in the run-root split copy.
- CVRP 8-seed A/A is complete and audited:
  [`../../experiments/v0.4/v04-phase4-cvrp-8seed-aa-postrun-20260612.md`](../../experiments/v0.4/v04-phase4-cvrp-8seed-aa-postrun-20260612.md).
  It finished with wrapper exit `0`, `n_pairs=192`, MDE `9.6` raw
  `total_distance`, false-pass rate `0.0`, and `recommended_min_seeds=16`.
  This only slightly improves the Phase 1 MDE `9.9` and remains `4.8x` above
  CVRP `practical_delta_screen=2.0`. Therefore CVRP remains a low-power
  diagnostic/runtime/research-mechanics pressure test and should not be the
  next formal governance ON/OFF target or long promotion campaign without
  another pre-registered measurement change. Caveat: `CMT4` has file dimension
  `151` but received the default `30s`; exact protocol-time fidelity needs CMT
  dimension handling repair or explicit pre-registration.
- Governance on/off first candidate is warehouse production saferoot. CVRP can
  become a later governance candidate only if the power-adjusted check lowers
  MDE near the accepted effect scale or a larger pre-registered effect scale is
  accepted. The minimal `measurement_governance` record-only switch is now
  implemented and accepted, so the OFF arm can record reduced readiness without
  letting measurement declarations drive protocol/runtime/lifecycle/context
  behavior.
- Warehouse governance ON/OFF shakedown is complete and audited:
  [`../../experiments/v0.4/v04-phase5-warehouse-governance-onoff-8r-postrun-20260612.md`](../../experiments/v0.4/v04-phase5-warehouse-governance-onoff-8r-postrun-20260612.md).
  Both arms completed valid/complete from commit `f604e81` with `8/8`
  effective protocol rows, `6` screening rows, `1` validation row, `1` frozen
  row, and champion v2. This validates the switch and the warehouse promotion
  path, but it is not a formal governance-value conclusion because LLM
  trajectories and promoted `operators/merge_vehicles.py` patches diverged.
  Record-only suppresses `problem_measurement_diagnostics` but still exposes
  opportunity/runtime/cross-branch research signals, so the ablation contract
  must be tightened before formal Phase 5 experiments.

## Task Split

1. Main thread: finish R1/R2 measurement and runtime semantics, keep CVRP/VRP
   semantics in problem-owned declarations/providers, and own focused protocol
   tests.
2. R3/R4 workers: add read-only research-shape and context-density diagnostics
   to summaries/manifests/prompts, with tests and no decision/scheduling
   behavior changes.
3. Next: design the post-Phase-4 measurement rung and first governance on/off
   candidate. CVRP needs an 8-seed or otherwise power-adjusted screening check
   before any long promotion campaign; warehouse or another effect-measurable
   problem is the safer first governance-value ablation unless CVRP clears that
   measurement check.
4. Implementation complete: the minimal `measurement_governance` switch is
   accepted. Default ON preserves current behavior. Record-only/OFF computes
   and persists reduced measurement-readiness status while preventing
   measurement declarations from driving practical deltas, runtime model,
   pairing validity, low-SNR lifecycle, or prompt-visible measurement
   diagnostics. Acceptance passed focused config/CLI/context (`40 passed`),
   protocol/Decision/lifecycle/runtime (`124 passed`), full `unit/core`
   (`489 passed`), problem/boundary/context subset (`113 passed`), Python
   compile on touched files, and `git diff --check`.
5. Completed and audited: first warehouse governance ON/OFF shakedown from commit `f604e81`
   with production saferoot protocol/split/seeds, local `gpt5.5`, 8 rounds per
   arm, and matched runtime/agentic settings. ON arm:
   `/home/clawd/research/scion-experiments/v04-phase5-governance-warehouse-on-pilot-8r-gpt55-20260612T013119Z-claw`.
   Record-only/OFF arm:
   `/home/clawd/research/scion-experiments/v04-phase5-governance-warehouse-record_only-pilot-8r-gpt55-20260612T013119Z-claw`.
   Both arms reached validation/frozen and promoted champion v2. Treat this as
   a successful switch/evidence-path shakedown, not a causal governance-value
   result. The immediate framework repair follow-up is now accepted:
   `campaign_summary.json` records `measurement_governance`; manifest/context
   tests assert record-only measurement diagnostic suppression and code-phase
   source visibility; CVRP `CMT4` is explicitly pre-registered for the 45s
   screening budget; expanded-screening borderline advance is an explicit
   protocol policy; fresh-runtime replay pressure reports scheduler eligibility
   instead of replay materialization only; and same-branch hypotheses persist
   `parent_hypothesis_id` outside `DecisionFeatures`. Acceptance: targeted
   repair suite `296 passed`, full `unit/core` `494 passed`, protocol/adapter
   subset `99 passed`, Python compile on touched core/config files, and
   `git diff --check`. The fixed-candidate replay and measurement-only OFF gate
   is now accepted. `scion report fixed-candidate-replay-manifest` builds a
   deterministic `scion.fixed_candidate_replay_manifest.v1` from
   `formal_candidates/index.jsonl` and `candidate.patch.json`, so identical
   patches can be compared under `measurement_governance=on` and `record_only`.
   Real warehouse shakedown artifacts generated manifests for both source arms
   with `5` candidates and `0` omitted rows each, without leaking code body,
   prompt text, LLM rationale, raw measurement diagnostics, BKS/gap, or raw A/A
   rows. Fixed-order proposal replay is deferred because it would need a new
   pre-registered scheduler/proposal trace contract and still would not safely
   control nondeterministic LLM output unless stored proposals are reused.
   `record_only` is measurement governance OFF, not all governance OFF; formal
   reports must say that non-measurement objective/runtime/cross-branch/source
   signals remain visible. Acceptance: focused replay/OFF/report suite
   `117 passed`, core+report suite `513 passed`, config/model/context/protocol/
   Decision suite `212 passed`, Python compile on touched implementation files,
   and `git diff --check`.
   The executor half of this control is now accepted as well: `scion report
   fixed-candidate-replay` replays manifest candidates under `on` and
   `record_only` and writes `scion.fixed_candidate_replay_comparison.v1` without
   mutating campaign, scheduler, Decision, lifecycle, or promotion state. Real
   warehouse smoke artifact:
   `/home/clawd/research/scion-experiments/v04-phase5-fixed-candidate-replay-smoke-warehouse-20260612T0508Z-claw/fixed_candidate_replay_comparison.v1.json`
   with `row_count=2`, `error_count=0`, no forbidden-field leakage, and both
   rows completed. Acceptance: focused replay/report suite `22 passed`, broader
   core/config/context regression `592 passed`, Python compile on touched files,
   and `git diff --check`.
   Full warehouse fixed-candidate replay is now complete and audited:
   [`../../experiments/v0.4/v04-phase5-warehouse-fixed-candidate-replay-postrun-20260612.md`](../../experiments/v0.4/v04-phase5-warehouse-fixed-candidate-replay-postrun-20260612.md).
   It replayed all five ON-shakedown formal screening candidates under both
   arms, producing `row_count=10`, `error_count=0`, no forbidden-field leakage,
   and identical ON/record-only outcomes. This validates fixed-candidate causal
   pairing but not LLM trajectory governance value. The next Phase 5 gate should
   design a trajectory-aware prompt/context or stored-proposal control on
   warehouse; CVRP remains low-power for formal governance value under the
   current measurement setup.
   The first trajectory-aware control is now implemented as report-only
   artifacts, not LLM replay. `scion report proposal-trajectory-manifest` and
   `proposal-trajectory-compare` summarize session/trace/prompt/candidate
   fingerprints with v3 guardrails and no raw prompt/response/patch leakage.
   Real warehouse ON/OFF artifacts were generated under
   `/home/clawd/research/scion-experiments/v04-phase5-proposal-trajectory-warehouse-onoff-20260612T0550Z-claw/`.
   Report:
   [`../../experiments/v0.4/v04-phase5-warehouse-proposal-trajectory-compare-20260612.md`](../../experiments/v0.4/v04-phase5-warehouse-proposal-trajectory-compare-20260612.md).
   They show equal top-level counts (`10` sessions, `32` traces, `5` formal
   candidates each) but divergent proposal mechanisms and target distributions.
   The comparison is explicitly `observational_only=true`. Acceptance:
   proposal/fixed-candidate/report suite `27 passed`, py_compile, real artifact
   smoke, exact leakage scan, and `git diff --check`.
   Proposal-visible context ablation is now implemented as the next control
   surface. `scion run --proposal-context-ablation` supports `full`,
   `no-measurement-diagnostics`, and `minimal-research-context`; all three arms
   keep `measurement_governance=on` so protocol/runtime/lifecycle/Decision
   semantics remain matched. The switch only affects tainted hypothesis prompt
   visibility, and prompt manifests plus proposal trajectory fingerprints record
   the selected mode. Acceptance: context/control/trajectory suite `23 passed`,
   proposal pipeline/source-visibility suite `46 passed`, CLI/config suite
   `31 passed`, py_compile, and `git diff --check`. Next gate: warehouse 3-arm
   shakedown; CVRP remains excluded from formal governance-value conclusions
   until measurement power improves.
6. Git management: keep branch and commits sliced by logical track; do not
   mix pre-existing CVRP actionability changes with generic measurement/runtime
   framework changes unless a test requires the combined behavior.

## Phase 2 Worker Brief Catalog

These briefs are execution slices for Phase 2 now that Phase 1 A/A is
complete. Every worker brief must begin by requiring the worker to read
`scion/design/scion-architecture-v3.md` and state how the change preserves the
v3 boundary.

### Worker A - Measurement Integration Closure

Purpose: close integration gaps around problem-owned measurement declarations,
not reimplement the already-landed practical-delta path.

Current state:

- `ProblemSpecV1.measurement` and `MeasurementSpec` already define
  `runtime_model`, `pairing_validity`, effect metric/unit, practical deltas,
  and calibration refs.
- `ProtocolConfig.with_problem_measurement()` already resolves problem-owned
  practical deltas and runtime model into protocol config.
- CVRP already declares `runtime_model: budget_exhausting`,
  `pairing_validity: trajectory_divergent`, `practical_delta_screen: 2.0`,
  and `practical_delta_validate: 1.0`.

Write scope:

- `scion/scion/config/protocol_config.py`
- `scion/scion/problem/spec.py`
- `scion/scion/problem/bridge.py`
- `scion/scion/cli/commands/init_run.py`
- tests only where needed under `scion/scion/tests/`

Acceptance:

- Add or extend tests proving formal CVRP and warehouse protocol loading
  consumes problem-owned measurement values, not synthetic-only fixtures.
- Assert measurement/calibration raw diagnostics do not enter
  `DecisionFeatures`.
- Suggested command:
  `pytest scion/scion/tests/test_config.py scion/scion/tests/test_problem_bridge.py scion/scion/tests/test_cvrp_formal_readiness.py`

### Worker B - Runtime Semantics Closure

Purpose: finish evidence/feedback integration for comparative vs
budget-exhausting runtime semantics.

Current state:

- Budget-exhausting runtime already suppresses fresh champion replay for cached
  runtime ties in the gate path.
- Runtime budget saturation already downgrades to info and clears actionable
  reason codes under `runtime_model: budget_exhausting`.
- V9 perf guard already treats budget-exhausting runtime as budget compliance.

Write scope:

- `scion/scion/core/runtime_budget_diagnostics.py`
- `scion/scion/protocol/gates.py`
- `scion/scion/protocol/experiment/stages.py`
- `scion/scion/verification/perf_guard.py`
- `scion/scion/proposal/context_manager/runtime.py`

Acceptance:

- Stage-level protocol results do not surface budget-exhausting saturation as
  actionable gate reason codes.
- Runtime feedback does not tell the agent to chase meaningless saturation on
  budget-exhausting anytime solvers.
- Quality-tie runtime speedup remains possible only from deterministic,
  non-regressive quality evidence and valid runtime evidence.
- Suggested command:
  `pytest scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/unit/core/test_runtime_budget_diagnostics.py scion/scion/tests/test_verification_gate_integration.py scion/scion/tests/unit/test_runtime_feedback_guidance.py`

### Worker C - Low-SNR Expand And Lifecycle Depth (Completed)

Purpose: make low-SNR, trajectory-divergent research continue or expand when
evidence is inconclusive but not negative, while hard negatives still close.

Completed state:

- `pairing_validity` is resolved into `ProtocolConfig` from problem-owned
  measurement declarations.
- Screening gate and Decision can expand trajectory-divergent, tie-heavy or
  weak non-negative evidence below `0.5` aggregate win rate.
- Lifecycle thresholds are relaxed only for trajectory-divergent low-SNR
  research so same-mechanism follow-up can continue longer.
- Warehouse trajectory-stable behavior remains unchanged.

Completed write scope:

- `scion/scion/protocol/gates.py`
- `scion/scion/core/decision.py`
- `scion/scion/core/branch_lifecycle_policy.py`
- `scion/scion/core/scheduler.py` only if required

Acceptance status:

- For declared trajectory-divergent/low-SNR problems, high-tie or weak
  non-negative evidence can expand or continue without direct abandon.
- Negative median delta, loss-heavy evidence, candidate failure, infeasibility,
  timeout/crash, or runtime regression still fail close.
- Same-mechanism follow-up is possible for multiple low-SNR attempts so branch
  depth can grow beyond one-off candidates.
- Focused verification:
  `pytest scion/scion/tests/test_decision_screening.py scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/unit/core/test_branch_lifecycle_policy.py scion/scion/tests/unit/core/test_scheduler_runtime_evidence_pressure.py`
  passed as part of the integrated Phase 2 suite.

### Worker D - Context Signal Density (Completed)

Purpose: improve proposal-visible research signal without moving diagnostics
into Decision.

Compressible material:

- Raw cross-branch audit/session metadata.
- Long `branch_lesson_usage_requirement` boilerplate.
- Repeated runtime saturation/low-confidence boilerplate.
- Generic governance rules, duplicate "Do Not Claim" text, and raw tool
  observation payloads when compact summaries exist.

Write scope:

- `scion/scion/proposal/context_manager/*`
- `scion/scion/proposal/engine/hypothesis_context_profiles.py`
- `scion/scion/proposal/engine/hypothesis_prompts.py`
- `scion/scion/proposal/prompt_manifest_accounting.py`

Acceptance status:

- Prompt manifests show improved problem-domain research-signal density.
- Proposal context receives measurement/noise/opportunity diagnostics only as
  tainted problem-owned diagnostics excluded from `DecisionFeatures`.
- Validation/frozen per-case details remain hidden from proposal context.
- Focused verification:
  `pytest scion/scion/tests/unit/test_hypothesis_context_profiles.py scion/scion/tests/unit/test_prompt_manifest_accounting.py scion/scion/tests/unit/test_cross_branch_research.py`
  passed as part of the integrated Phase 2 suite.

### Worker E - Source Visibility Protection (Completed)

Purpose: make context compression phase-aware so it cannot remove the research
object code the agent must edit or understand.

Protected material:

- Hypothesis phase: champion/current branch research code, problem object,
  solver mechanics, active algorithm facts, research surface/interface.
- Code phase: approved target file source, required integration file source,
  API manifest, source digests, editable/frozen patterns, and grounding needed
  to write a valid patch.

Write scope:

- `scion/scion/proposal/engine/code_prompts.py`
- `scion/scion/proposal/target_intent_binding.py`
- `scion/scion/proposal/prompt_manifest_visibility.py`
- `scion/scion/proposal/agentic_session_budget_runtime.py`

Acceptance status:

- Code-phase prompt manifests explicitly prove target source and required
  integration source are present after compression.
- Compression may remove generic governance text, duplicated raw logs, and
  raw cross-branch payloads, but not source/code material needed to modify the
  champion or current branch.
- Focused verification:
  `pytest scion/scion/tests/unit/test_agentic_solver_design_prompt_payloads.py scion/scion/tests/unit/test_agentic_target_file_grounding.py scion/scion/tests/unit/test_agentic_session_tool_selection.py`
  passed as part of the integrated Phase 2 suite.

### Worker F - Calibration Tooling Evidence Closure (Completed)

Purpose: make A/A calibration artifacts replayable and diagnostic enough to
support Phase 1 conclusions and later measurement-aware governance.

Completed state:

- `tools/calibrate_aa_noise.py` can produce `scion.aa_noise_floor.v1` with MDE,
  false-pass rate, power curve, and per-case summaries.
- CVRP formal calibration exposed a tooling gap: the initial CLI did not reuse
  the declared problem data root wiring used by campaign launch, so the legacy
  uniform-60s run required a copied split with local `safe_data_roots`.
- The repaired CLI wires declared problem data roots and can use
  `--runtime-policy protocol_time_limits`.
- The repaired payload persists pair evidence, candidate seeds, elapsed
  runtime, selected case/seed lists, replicate count, seed offset, bootstrap
  samples, runtime policy, and case-resolution evidence.
- Residual caveat: the final artifact has elapsed runtime and pair time limits,
  but no explicit per-row budget-hit/saturation flag.

Completed write scope:

- `scion/tools/calibrate_aa_noise.py`
- `scion/scion/measurement/aa_calibration.py`
- `scion/scion/cli/commands/data_roots.py` only if reuse helpers need a small
  public wrapper
- `scion/scion/config/protocol_config.py` only if calibration needs an existing
  runtime time-limit resolver exposed
- tests under `scion/scion/tests/unit/test_aa_calibration.py`,
  `scion/scion/tests/unit/test_cli_data_roots.py`, and
  `scion/scion/tests/test_cvrp_formal_readiness.py`

Acceptance status:

- CVRP formal A/A can resolve `cvrplib/...` cases through declared problem data
  roots without manually copying or editing the split manifest.
- The artifact records each pair's case, ledger seed,
  candidate seed, replicate, outcome, delta, raw delta, candidate/champion
  values, elapsed runtime, and case resolution.
- The top-level payload records selected cases, selected seeds, replicate
  count, seed offset, bootstrap samples, selected surface, runtime policy, and
  safe data roots used.
- Calibration can optionally use protocol runtime time-limit rules so CVRP
  large-case screening budgets match campaign execution.
- The artifact remains `problem_owned_measurement_diagnostic` and explicitly
  excluded from `DecisionFeatures`.
- Focused verification:
  `pytest scion/scion/tests/unit/test_aa_calibration.py scion/scion/tests/unit/test_cli_data_roots.py scion/scion/tests/test_cvrp_formal_readiness.py`
- Final artifact:
  `/home/clawd/research/scion-experiments/v04-phase1-aa-cvrp-screening-modify-r3-protocoltime-20260611T191356Z-claw/aa_noise_floor.json`.

## Governing Constraints

- Preserve the v3 boundary: LLM output is tainted, and Decision reads only
  deterministic `DecisionFeatures`.
- Keep CVRP/VRP semantics problem-owned. BKS, gap, case hardness, runtime model,
  and measurement readiness may guide proposals and preflight, but must not
  become generic Decision inputs.
- Do not treat "no promotion" as a framework failure without the 2026-06-09
  audit checklist: copied configs, prompt visibility, pair-level metrics,
  Protocol evidence, safe features, and branch state must be reconciled.
- Runtime-improvement promotion remains a valid VRP research mode when quality
  is tied or non-regressive and runtime evidence is complete and materially
  better.

## V0.4 Repair Tracks

### R1 Measurement And Practical Delta

Goal: know what each problem/protocol can detect before spending long rounds.

- Add problem-owned measurement/readiness declarations for effect scale,
  practical deltas, pairing validity, runtime model, and calibration refs.
- Replace the hard-coded `ProtocolConfig.min_practical_delta == 0.001` with
  resolved problem/protocol values.
- Build A/A noise-floor calibration for CVRP and warehouse using champion
  replays, producing MDE and false-pass estimates.
- Surface measurement diagnostics in preflight/status/proposal context, not
  `DecisionFeatures`.

Exit criteria:

- CVRP and warehouse have full calibration reports. This criterion is now met
  for Phase 1; Phase 2 must consume those reports rather than rerun blind gate
  tuning.
- CVRP 6/10 screening failures are explained as below measured MDE, not only
  by aggregate win-rate labels.

### R2 Runtime Governance For Budget-Exhausting Solvers

Goal: make anytime ALNS/VNS runtime evidence meaningful.

- Add `runtime_model: comparative | budget_exhausting` semantics.
- In `budget_exhausting`, treat saturation as expected info unless there is a
  timeout/crash or budget violation.
- Disable meaningless `RUNTIME_TIE_FRESH_CHAMPION_REQUIRED` replay paths for
  budget-exhausting tie cases.
- Preserve quality-tie runtime speedup promotion when candidate/champion quality
  is non-regressive and runtime evidence is fresh enough for the declared model.
- Mark saturated runtime-regression-rate style fields as not-applicable when
  they compare millisecond jitter near the time limit.

Exit criteria:

- Focused tests cover both comparative and budget-exhausting runtime semantics,
  including V9 budget-compliance behavior.
- CVRP reruns no longer spend effective rounds on runtime replay that cannot
  add decision evidence.

### R3 Branch Depth And Mechanism Continuity

Goal: Scion should pursue a research direction deeply enough to learn.

- Add branch-depth distribution and mechanism-family x effect summaries to
  campaign status/summary.
- Adjust low-signal lifecycle defaults so marginal/non-regressive evidence can
  drive same-mechanism follow-up rather than immediate churn to shallow sibling
  branches.
- Keep clear regressions and boundary/runtime failures fail-closed.
- Ensure next-hypothesis context shows branch-local failures, mechanism ids,
  and follow-up constraints as concise research guidance.

Exit criteria:

- Post-run analysis can reconstruct branch depth, mechanism continuity, and
  whether follow-up hypotheses used prior evidence.
- A focused campaign demonstrates at least one branch with meaningful depth
  beyond one-off attempts without violating v3 Decision boundaries.

### R4 Context Signal Density

Goal: the agent sees enough research signal without drowning in governance logs.

- Profile hypothesis/code prompts by block category: source, governance,
  tool observations, branch-local history, runtime/screening feedback, and
  cross-branch lessons.
- Compact cross-branch maps and runtime feedback into mechanism-level guidance.
- Keep full artifacts for audit, but avoid rendering duplicate raw logs as
  default model-visible context.
- Do not remove active solver facts, target source, branch state, or feedback
  merely to reduce token count.

Exit criteria:

- Prompt profile reports show improved research-signal ratio.
- Focused prompt tests confirm compact research signals are visible before
  broader rules, safety invariants remain present, and prompt manifests report
  research-signal/governance ratios. End-to-end campaign smoke remains a follow-up.

### R5 Focused VRP/Warehouse Validation

Goal: v0.4 must still show meaningful research on current problem families.

- After R1-R4, run focused CVRP/VRP campaigns that explicitly test:
  quality improvement, quality-tie runtime speedup, and branch-depth research.
- Revisit warehouse under the current framework: verify whether it can produce
  repeated promotions or whether stagnation has a measurement/context cause.
- Audit every run with the 2026-06-09 guide and pair-level metrics.

Exit criteria:

- Either VRP produces valid improvement/runtime evidence, or the report
  quantifies why the current protocol cannot detect it and what variant is
  needed.
- Warehouse has a reproducibility/stagnation analysis, not just a one-run
  promotion anecdote.

## V0.5 Hand-Off

Only after v0.4 exits R1-R5 should v0.5 run the larger value-analysis program:

- governance ablation: full Scion vs naive vs mid-profile;
- reproduction matrices across warehouse, VRP/variant, and a third problem;
- prompt/context ablations;
- branch-depth and mechanism-trajectory studies;
- final kill/survive criteria for Scion's value claim.
