# Scion v0.4 Evidence Repair And Validation Plan

*Date: 2026-06-11*
*Branch: `codex/v04-evidence-repair-plan`*
*Status: v0.4 framework repairs are accepted enough for focused follow-up; live
campaign launch is currently blocked by `gpt-5.5` auth. See the current
execution snapshot below and `scion/docs/status/current-state.md`.*

This plan translates the 2026-06-09 and 2026-06-11 audits into v0.4 work.
v0.5 should run broad controlled experiments; v0.4 must first make those
experiments scientifically interpretable and restore meaningful VRP/warehouse
research capability.

## Current Execution Status

This section is the current execution snapshot, not a chronological log.
Detailed launch/postrun facts live in
[`../../experiments/v0.4/`](../../experiments/v0.4/), and milestone history
lives in [`../../status/v0.4-history.md`](../../status/v0.4-history.md).

- Current operational truth lives in
  [`../../status/current-state.md`](../../status/current-state.md). Keep this
  plan section short; update the status file when the operating truth changes.
- Warehouse has an accepted v0.4 positive research-path checkpoint. The open
  question is continuous follow-on improvement, not basic viability.
- CVRP framework behavior is improved enough for focused follow-up, but CVRP
  still has not produced solver improvement or promotion. The next mechanism
  must avoid the rejected/default-avoid list in `scion/TASK.md` unless it adds a
  genuinely new causal path or direct effect attribution.
- A large-instance intra-route two-opt external-control seed is now available,
  but only as a problem-owned research focus. It needs a deadline-aware
  implementation or explicit soft-budget protocol treatment before any solver
  update or promotion claim.
- The current CVRP prepared root now carries that seed in proposal-only
  `research_focus`, marks the unbounded fallback as default-avoid, and carries
  structured `large_instance_two_opt_constraints` for deadline-aware bounded
  implementation and pair-level evidence. CVRP and warehouse code-generation
  prompts now receive problem-owned active subject code constraints, and launch
  readiness requires the prepared prompt-context handoff to prove the
  family-specific code-constraint bridge. Current-run postrun analysis can also
  audit actual code prompt traces for the active subject constraints section via
  section status, digest, and count fingerprints without storing raw prompts.
  CVRP and warehouse current-run delegated-analysis readiness now require that
  trace to be present and full-visible whenever a matching code trace exists.
  Code traces also require protected target/integration/algorithm source
  visibility; missing required source paths and partial required hypothesis
  target-source visibility prevent delegated current-run review readiness.
- CVRP postrun analysis briefs now include `cvrp_large_twoopt_summary` so the
  bounded two-opt follow-up cannot be called review-ready from protocol rows
  alone when review inputs, incomplete handoff, a qualifying large two-opt
  mechanism signal, or direct activation/effect/phase telemetry on a matching
  top effect row are missing. Generic, cross-route, unbounded/fallback, VNS, and
  two-opt-star family labels are now reported as rejected two-opt-like signals
  rather than review-ready mechanism evidence. Required review questions now
  explicitly ask reviewers to check incomplete handoff before accepting
  review-ready interpretations, and prepared briefs now filter warehouse/CVRP
  specialist questions by problem family so delegated review does not carry
  irrelevant plateau/two-opt prompts. Prepare-only briefs now use
  launch/readiness/handoff required answers instead of current-run
  research-quality questions, and their minimum delegated-analysis markdown
  guidance likewise omits current-run branch/LLM/Protocol inspection steps.
- Prepared contract/inventory coverage requires the seed and its
  unbounded-fallback default-avoid line before static handoff coverage is
  complete.
- Measurement integration, budget-exhausting runtime semantics,
  low-signal/same-branch continuation, prepared-root handoff, launch readiness,
  warehouse follow-up analysis briefs, research-continuity shape projection,
  mechanism-family effect summaries, branch-lesson semantic diagnostics,
  research-context actionability summaries, prompt-side research-shape signals,
  prepared prompt/context readiness handoff, and postrun acceptance bundles have
  accepted regression/report coverage.
- Problem-owned CVRP and warehouse opportunity diagnostics are now visible in
  ordinary proposal measurement context, not only in prepared launch handoffs.
  They remain tainted proposal diagnostics and excluded from `DecisionFeatures`.
- Adapter-owned measurement/opportunity diagnostics are redacted before prompt
  exposure for raw pair/calibration rows, BKS/gap details, holdout/case details,
  prompt ratios, and LLM text.
- Postrun evidence isolation now covers `invalid_infra_only` roots as well as
  prepared-only and preflight-failed roots, so copied or partial artifacts cannot
  be treated as warehouse/CVRP research-quality evidence.
- Launcher postrun rebuilds now write a `POSTRUN_REPORTS_EXIT_STATUS` marker so
  report-bundle rebuild failures are visible to delegated review without
  changing campaign validity or solver evidence.
- Launcher postrun paths now also write report-only postrun acceptance
  readiness JSON/Markdown and a `POSTRUN_READINESS_EXIT_STATUS` marker, so
  delegated review can distinguish current-run analysis readiness from
  prepared-only, invalid-infra-only, or preflight-failed artifacts.
- Postrun acceptance readiness requires `warehouse_followup_summary` for
  warehouse current runs and `cvrp_large_twoopt_summary` for CVRP current runs;
  missing problem-specific summaries are not review-ready.
- Postrun readiness also fails on blocking problem-summary gaps such as missing
  review inputs, incomplete handoff, launch-only or infra-only state, and no
  protocol evidence. Valid negative conclusions remain delegated-analysis ready.
- Warehouse/CVRP postrun readiness also requires current-run prompt/source
  visibility trace accounting, including hypothesis target-source visibility,
  in the analysis brief before delegated current-run review is considered ready.
- Postrun readiness now selects the analysis brief through the rebuild manifest
  output list and checks run identity for both artifacts, so stale or
  lexically-later report files cannot satisfy delegated current-run readiness.
- CVRP bounded two-opt postrun review readiness now requires a qualifying
  large/two-opt protocol-effect row signal in measurement evidence plus direct
  activation/effect/phase telemetry on a matching top effect row.
  Research-continuity family mentions remain context only, and generic or
  default-avoid two-opt-like family labels are rejected before review-ready
  interpretation.
- Protocol-evaluated CVRP/warehouse postrun review now treats runtime feedback
  as review-ready only when raw runtime feedback exists and both fresh-runtime
  replay drain status and stage-transition drain status are present. Runtime
  budget diagnostics alone remain reportable, but they cannot support delegated
  conclusions without those drain signals.
- Postrun acceptance readiness now rejects stale problem-specific summary
  contracts: warehouse/CVRP summaries must use the current summary schema and a
  current delegated-review interpretation, and their `problem_family` must match
  the prepared contract, while remaining report-only, non-quality-judgment, and
  `DecisionFeatures`-excluded before `current_run_analysis_ready` can pass.
- Launch readiness now requires prepared-only analysis briefs to carry the
  matching warehouse/CVRP prepared summary with current schema and report-only,
  non-quality-judgment, `DecisionFeatures`-excluded boundary markers.
- Postrun acceptance readiness now validates every output declared by
  `postrun_acceptance/rebuild/rebuild_manifest.v1.json`, so stale report-family
  directory counts or replacement files cannot mask a missing manifest-declared
  artifact.
- Postrun acceptance readiness now also requires current-run
  `research_context_actionability_summary`, prompt block-family accounting, and
  prompt signal-density token accounting for warehouse/CVRP delegated review.
  This makes R4 context-transfer and signal-density gaps auditable without
  making research-context quality a Decision or promotion input.
- Postrun acceptance readiness now also requires current-run
  `failure_taxonomy_summary` evidence for warehouse/CVRP delegated review.
  Missing, stale, non-current, or empty failure taxonomy cannot be bypassed by a
  seemingly actionable problem-specific summary.
- Postrun acceptance readiness now also requires current-run protocol
  accounting, measurement-effect, runtime-feedback, and research-continuity
  summaries for warehouse/CVRP delegated review. A seemingly actionable
  problem-specific summary cannot bypass missing review-input summaries, and
  those summaries must preserve report-only, non-quality-judgment,
  `DecisionFeatures`-excluded boundary markers.
- Postrun acceptance readiness now also cross-checks the problem-specific
  summary's protocol, measurement, runtime, continuity, and quality-block
  evidence against those input summaries before declaring delegated current-run
  analysis ready.
- Warehouse plateau postrun review readiness now requires substantive
  research-continuity evidence, not only the presence of a continuity summary.
  Shallow one-off continuity evidence remains a report-only evidence gap.
- Launcher run scripts now call postrun readiness JSON generation with
  `--require-current-run-ready`, so `POSTRUN_READINESS_EXIT_STATUS` distinguishes
  ready current-run analysis from generated-but-not-ready report bundles.
- Launch readiness now also checks the prepared `run.sh` strict marker path via
  `run_script_strict_postrun_readiness`, so stale scripts that omit
  `--require-current-run-ready` cannot pass static readiness.
- Prepared runtime guard paths now include `scion/tools`, postrun/report
  package paths (`scion/scion/cli`, `scion/scion/core`,
  `scion/scion/lineage`), and the matching CVRP/warehouse problem runtime
  paths. Launch readiness requires that coverage before static readiness can
  pass, and runtime guard excludes that affect required paths now fail coverage.
- Launch readiness now also requires API-key-env-missing pre-campaign failures to
  call the same postrun report/readiness bundle before wrapper exit, exposed as
  `run_script_api_key_env_failure_reports=ok`.
- Current prepared roots:
  - CVRP:
    `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-large-twoopt-postsummary-aa54418-1r-gpt55-20260619T090714Z-claw`
  - Warehouse:
    `/home/xjy-ubuntu/research/scion-experiments/v04-warehouse-v2-followup-postsummary-aa54418-6r-gpt55-20260619T090659Z-claw`
- Both current prepared roots were prepared from WSL checkout `aa54418` after
  launch readiness began checking generated scripts for strict postrun readiness
  marker semantics, manifest-bound postrun readiness identity and
  declared-output coverage, research-context actionability and signal-density
  readiness, failure-taxonomy readiness, review-input summary readiness,
  review-input boundary readiness, problem-summary/input consistency, prepared
  and current-run problem-summary schema/boundary readiness, postrun/report
  package runtime guard coverage, problem runtime guard coverage,
  CVRP/warehouse code-constraint prompt bridge coverage, and run-script runtime
  guard enforcement, including the normal campaign-exit postrun report call,
  warehouse data-root failure report path, and API-key-env failure report path.
  Both are prepare-only roots with current prepared prompt/context,
  analysis-brief, problem-specific handoff coverage, brief contract identity,
  and declared `readiness` postrun family coverage; strict readiness expands
  that coverage as `prepared_analysis_brief_current=ok`,
  `problem_specific_prepared_handoff=ok`, `postrun_families_complete=ok`, and
  `run_script_strict_postrun_readiness=ok`.
  New prepared roots must also report
  `runtime_guard_paths_cover_launch_tools=ok` and
  `run_script_runtime_guard_enforced=ok`, proving the generated `run.sh`
  executes dirty/head-mismatch checks before `scion.cli.main run`, plus
  `run_script_postrun_reports_after_campaign=ok`, proving the campaign-exit path
  runs postrun reports/readiness before `exit "$STATUS"`, plus
  `run_script_data_root_failure_reports=ok`, proving warehouse
  data-root-missing failures remain postrun-reportable, plus
  `run_script_api_key_env_failure_reports=ok`, proving missing API-key-env
  configuration remains postrun-reportable.
  Older prepared roots before the postsummary roots above are no longer
  current because launch/readiness runtime paths changed after prepare time.
  Keep exact supersession provenance in
  launch/readiness evidence docs.
- Launch remains blocked by WSL `gpt-5.5` provider auth, not Scion code. With
  `SCION_API_KEY=pwd`, `/v1/models` lists `gpt-5.5` but real
  `/v1/chat/completions` preflight returns HTTP `401`,
  `classification=not_authenticated`, `code=invalid_api_key`, with auth pool
  `active=0`, `total=1`, and no launch-usable account. Require
  `scion/tools/check_launch_readiness.py <prepared-root> --require-launch-ready
  --format json` to report `launch_ready=true` before starting a prepared root.
  The readiness report must include
  `checks.prompt_context_readiness_complete.status=ok`, proving
  `ready_for_launch_prompt_audit=true`,
  `prepared_research_focus_prompt_bridge.available=true`, artifact identity for
  the current root/manifest, live bridge source/launch markers, and the
  family-specific active subject code-constraints prompt bridge
  (`cvrp_active_subject_code_constraints_prompt_bridge.available=true` for
  CVRP, `warehouse_active_subject_code_constraints_prompt_bridge.available=true`
  for warehouse). Launch
  markers must include the prepared manifest file, `launch.env` assignment, and
  `run.sh` export of
  `PREPARED_RUN_MANIFEST`. Static readiness also requires `run.sh` to carry
  `check_postrun_acceptance.py --require-current-run-ready` and
  `POSTRUN_READINESS_EXIT_STATUS`.
  WSL launches must set
  `PYTHONPATH=/home/xjy-ubuntu/research/or-autoresearch-agent/scion`.

## Current Task Split

1. Main thread owns v3 boundary alignment, task decomposition, acceptance
   criteria, status curation, git hygiene, and experiment launch decisions.
2. Restore the live `gpt-5.5` route before launching the next prepared
   campaign. Do not treat `/v1/models` or a tiny auth-only response as
   sufficient for a full agentic proposal run; require launch readiness with
   real completion preflight and a ready prepared prompt/context handoff.
3. Next warehouse slice: run a focused repeat from champion `v2` to test
   continuous follow-on improvement once LLM infrastructure is healthy, using
   the warehouse launcher so campaign resume and production config rewriting are
   reproducible.
4. Next CVRP slice: from a clean synchronized commit, verify construction-pivot
   lessons in live target-intent/hypothesis traces, then require a materially
   different problem-owned solver mechanism or a new causal path with CMT2/CMT4
   protection.
5. Subagents should receive bounded read/code/analysis scopes with the v3
   boundary requirement, exact files or artifacts, required tests, and concise
   evidence-backed deliverables.
6. Git management: keep commits sliced by logical track and do not mix
   unrelated CVRP actionability changes with generic framework repairs unless a
   test requires the combined behavior.

## Phase 2 Worker Brief Catalog

These briefs are execution slices for Phase 2 now that Phase 1 A/A is
complete. Every worker brief must begin by requiring the worker to read
`scion/design/scion-architecture-v3.md` and state how the change preserves the
v3 boundary.

### Worker A - Measurement Integration Closure (Completed)

Purpose: close integration gaps around problem-owned measurement declarations,
not reimplement the already-landed practical-delta path.

Completed state:

- `ProblemSpecV1.measurement` and `MeasurementSpec` already define
  `runtime_model`, `pairing_validity`, effect metric/unit, practical deltas,
  and calibration refs.
- `ProtocolConfig.with_problem_measurement()` already resolves problem-owned
  practical deltas and runtime model into protocol config.
- CVRP already declares `runtime_model: budget_exhausting`,
  `pairing_validity: trajectory_divergent`, `practical_delta_screen: 2.0`,
  and `practical_delta_validate: 1.0`.
- Formal CVRP and warehouse production assets now have regression coverage
  proving real problem declarations are consumed by real protocol configs.
- The `scion run` problem-v1 ingress path now has behavior coverage proving
  the resolved measurement-governed protocol config reaches `CampaignManager`,
  while `record-only` keeps readiness status without changing behavior fields.
- Warehouse production/package specs now use relative surrogate paths so local
  and WSL checkouts resolve the same calibration and canary assets.

Write scope:

- `scion/scion/config/protocol_config.py`
- `scion/scion/problem/spec.py`
- `scion/scion/problem/bridge.py`
- `scion/scion/cli/commands/init_run.py`
- tests only where needed under `scion/scion/tests/`

Acceptance status:

- Formal CVRP and warehouse production protocol loading consumes problem-owned
  measurement values, not synthetic-only fixtures.
- CLI campaign launch ingress consumes those values before constructing the
  campaign manager, with record-only governance covered as status-only.
- Measurement/calibration raw diagnostics do not enter `DecisionFeatures`.
- Focused verification:
  `pytest scion/scion/tests/test_config.py scion/scion/tests/test_problem_bridge.py scion/scion/tests/test_cvrp_formal_readiness.py scion/scion/tests/test_models.py`
  passed locally. Report:
  `scion/docs/experiments/v0.4/v04-measurement-integration-real-asset-coverage-20260618.md`.

### Worker B - Runtime Semantics Closure (Completed)

Purpose: finish evidence/feedback integration for comparative vs
budget-exhausting runtime semantics.

Completed state:

- Budget-exhausting runtime already suppresses fresh champion replay for cached
  runtime ties in the gate path.
- Runtime budget saturation already downgrades to info and clears actionable
  reason codes under `runtime_model: budget_exhausting`.
- V9 perf guard already treats budget-exhausting runtime as budget compliance.
- High aggregate `runtime_regression_rate` no longer blocks
  trajectory-divergent low-SNR screening expansion under
  `runtime_model: budget_exhausting`.
- Decision/lifecycle now treat aggregate `runtime_regression_rate` as
  report-only under `runtime_model: budget_exhausting`, so it does not
  soft-abandon branches or fragment repeated-signal signatures by itself.
- Prompt runtime feedback renders budget-exhausting aggregate regression as
  `not_applicable_budget_exhausting` and does not turn that summary into strong
  runtime actionability by itself.
- Proposal screening feedback preserves the raw aggregate regression rate for
  audit but does not classify zero-effect budget-exhausting screening as
  `runtime_regression`.
- The finalizer preserves lifecycle-selected low-signal `retain_head`
  workspaces for budget-exhausting solver evidence instead of discarding the
  branch head because of aggregate runtime saturation.

Write scope:

- `scion/scion/core/runtime_budget_diagnostics.py`
- `scion/scion/protocol/gates.py`
- `scion/scion/protocol/experiment/stages.py`
- `scion/scion/verification/perf_guard.py`
- `scion/scion/proposal/context_manager/runtime.py`
- `scion/scion/proposal/screening_feedback.py`
- `scion/scion/core/decision.py`
- `scion/scion/core/branch_lifecycle_policy.py`
- `scion/scion/core/decision_finalizer.py`

Acceptance status:

- Stage-level protocol results do not surface budget-exhausting saturation as
  actionable gate reason codes.
- Runtime feedback does not tell the agent to chase meaningless saturation on
  budget-exhausting anytime solvers.
- Quality-tie runtime speedup remains possible only from deterministic,
  non-regressive quality evidence and valid runtime evidence.
- Focused verification:
  `pytest scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/test_sprint_e2_context_runtime.py scion/scion/tests/unit/core/test_runtime_budget_diagnostics.py scion/scion/tests/test_verification_gate_integration.py`
  passed locally and in the WSL focused suite. Additional Decision/lifecycle
  verification:
  `pytest scion/scion/tests/test_decision_screening.py scion/scion/tests/unit/core/test_branch_lifecycle_policy.py scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/test_sprint_e2_context_runtime.py scion/scion/tests/unit/core/test_runtime_budget_diagnostics.py scion/scion/tests/test_decision_feature_extraction.py scion/scion/tests/test_models.py`
  passed locally and on WSL. Reports:
  `scion/docs/experiments/v0.4/v04-budget-exhausting-runtime-regression-semantics-repair-20260618.md`
  and
  `scion/docs/experiments/v0.4/v04-budget-exhausting-decision-lifecycle-runtime-semantics-repair-20260618.md`
  and
  `scion/docs/experiments/v0.4/v04-budget-exhausting-screening-feedback-runtime-tier-repair-20260618.md`
  and
  `scion/docs/experiments/v0.4/v04-budget-exhausting-finalizer-retain-head-repair-20260618.md`.

### Worker C - Low-SNR Expand And Lifecycle Depth (Completed)

Purpose: make low-SNR, trajectory-divergent research continue or expand when
evidence is inconclusive but not negative, while hard negatives still close.

Completed state:

- `pairing_validity` is resolved into `ProtocolConfig` from problem-owned
  measurement declarations.
- Screening gate and Decision can expand trajectory-divergent, tie-heavy or
  weak non-negative evidence below `0.5` aggregate win rate.
- Lifecycle thresholds are relaxed only for trajectory-divergent low-SNR
  research so same-mechanism follow-up can continue longer.
- Warehouse trajectory-stable behavior remains unchanged.

Completed write scope:

- `scion/scion/protocol/gates.py`
- `scion/scion/core/decision.py`
- `scion/scion/core/branch_lifecycle_policy.py`
- `scion/scion/core/scheduler.py` only if required

Acceptance status:

- For declared trajectory-divergent/low-SNR problems, high-tie or weak
  non-negative evidence can expand or continue without direct abandon.
- Negative median delta, loss-heavy evidence, candidate failure, infeasibility,
  timeout/crash, or runtime regression still fail close.
- Same-mechanism follow-up is possible for multiple low-SNR attempts so branch
  depth can grow beyond one-off candidates.
- Scheduler/result metadata now distinguishes the one allowed low-signal
  same-branch observation sample from true repair diagnostics by emitting
  `refine_active` for `same_branch_low_signal_observation_sample`.
- Proposal context now turns current active no-effect branch-local lessons into
  advisory `same_branch_refinement` requirements, preserving the lesson without
  hard-blocking code generation or leaking into Decision.
- Trajectory-divergent all-tie screening now follows the low-SNR expand path
  when objective/runtime evidence is non-regressive, closing the pure tie case
  that previously fell through to `SCREENING_FAIL_WIN_RATE`.
- Focused verification:
  `pytest scion/scion/tests/test_decision_screening.py scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/unit/core/test_branch_lifecycle_policy.py scion/scion/tests/unit/core/test_scheduler_runtime_evidence_pressure.py`
  passed as part of the integrated Phase 2 suite. Additional scheduler metadata
  verification:
  `pytest scion/scion/tests/unit/core/test_scheduler_runtime_evidence_pressure.py scion/scion/tests/unit/core/test_branch_step_runner_scheduler_metadata.py`
  passed locally. Report:
  `scion/docs/experiments/v0.4/v04-low-signal-same-branch-scheduler-slot-repair-20260618.md`.
  Additional lesson-usage verification:
  `pytest scion/scion/tests/unit/test_cross_branch_research.py scion/scion/tests/unit/core/test_branch_lesson_usage.py scion/scion/tests/unit/test_hypothesis_context_profiles.py`
  passed locally. Report:
  `scion/docs/experiments/v0.4/v04-low-signal-same-branch-lesson-usage-repair-20260618.md`.
  Additional all-tie low-SNR verification:
  `pytest scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/test_decision_screening.py scion/scion/tests/unit/core/test_branch_lifecycle_policy.py scion/scion/tests/unit/core/test_scheduler_runtime_evidence_pressure.py scion/scion/tests/unit/core/test_branch_step_runner_scheduler_metadata.py`
  passed locally. Report:
  `scion/docs/experiments/v0.4/v04-trajectory-divergent-all-tie-low-snr-expand-repair-20260618.md`.

### Worker D - Context Signal Density (Completed)

Purpose: improve proposal-visible research signal without moving diagnostics
into Decision.

Compressible material:

- Raw cross-branch audit/session metadata.
- Long `branch_lesson_usage_requirement` boilerplate.
- Repeated runtime saturation/low-confidence boilerplate.
- Generic governance rules, duplicate "Do Not Claim" text, and raw tool
  observation payloads when compact summaries exist.

Write scope:

- `scion/scion/proposal/context_manager/*`
- `scion/scion/proposal/engine/hypothesis_context_profiles.py`
- `scion/scion/proposal/engine/hypothesis_prompts.py`
- `scion/scion/proposal/prompt_manifest_accounting.py`

Acceptance status:

- Prompt manifests show improved problem-domain research-signal density.
- Postrun artifact inventories expose prompt signal-density evidence as a
  separate Phase 4 coverage requirement instead of bundling it under generic
  prompt/source visibility.
- Proposal context receives measurement/noise/opportunity diagnostics only as
  tainted problem-owned diagnostics excluded from `DecisionFeatures`.
- Validation/frozen per-case details remain hidden from proposal context.
- Focused verification:
  `pytest scion/scion/tests/unit/test_hypothesis_context_profiles.py scion/scion/tests/unit/test_prompt_manifest_accounting.py scion/scion/tests/unit/test_cross_branch_research.py`
  passed as part of the integrated Phase 2 suite.
  Inventory coverage verification:
  `pytest scion/scion/tests/test_postrun_artifact_inventory.py scion/scion/tests/test_postrun_analysis_brief.py scion/scion/tests/test_rebuild_postrun_acceptance.py`
  passed locally. Report:
  `scion/docs/experiments/v0.4/v04-prompt-signal-density-coverage-repair-20260618.md`.

### Worker E - Source Visibility Protection (Completed)

Purpose: make context compression phase-aware so it cannot remove the research
object code the agent must edit or understand.

Protected material:

- Hypothesis phase: champion/current branch research code, problem object,
  solver mechanics, active algorithm facts, research surface/interface.
- Code phase: approved target file source, required integration file source,
  API manifest, source digests, editable/frozen patterns, and grounding needed
  to write a valid patch.

Write scope:

- `scion/scion/proposal/engine/code_prompts.py`
- `scion/scion/proposal/target_intent_binding.py`
- `scion/scion/proposal/prompt_manifest_visibility.py`
- `scion/scion/proposal/agentic_session_budget_runtime.py`

Acceptance status:

- Code-phase prompt manifests explicitly prove target source and required
  integration source are present after compression.
- Compression may remove generic governance text, duplicated raw logs, and
  raw cross-branch payloads, but not source/code material needed to modify the
  champion or current branch.
- Focused verification:
  `pytest scion/scion/tests/unit/test_agentic_solver_design_prompt_payloads.py scion/scion/tests/unit/test_agentic_target_file_grounding.py scion/scion/tests/unit/test_agentic_session_tool_selection.py`
  passed as part of the integrated Phase 2 suite.

### Worker F - Calibration Tooling Evidence Closure (Completed)

Purpose: make A/A calibration artifacts replayable and diagnostic enough to
support Phase 1 conclusions and later measurement-aware governance.

Completed state:

- `tools/calibrate_aa_noise.py` can produce `scion.aa_noise_floor.v1` with MDE,
  false-pass rate, power curve, and per-case summaries.
- CVRP formal calibration exposed a tooling gap: the initial CLI did not reuse
  the declared problem data root wiring used by campaign launch, so the legacy
  uniform-60s run required a copied split with local `safe_data_roots`.
- The repaired CLI wires declared problem data roots and can use
  `--runtime-policy protocol_time_limits`.
- The repaired payload persists pair evidence, candidate seeds, elapsed
  runtime, selected case/seed lists, replicate count, seed offset, bootstrap
  samples, runtime policy, and case-resolution evidence.
- A/A pair evidence now records explicit champion/candidate runtime budget-hit
  ratios and flags, closing the earlier elapsed-runtime-only caveat.

Completed write scope:

- `scion/tools/calibrate_aa_noise.py`
- `scion/scion/measurement/aa_calibration.py`
- `scion/scion/cli/commands/data_roots.py` only if reuse helpers need a small
  public wrapper
- `scion/scion/config/protocol_config.py` only if calibration needs an existing
  runtime time-limit resolver exposed
- tests under `scion/scion/tests/unit/test_aa_calibration.py`,
  `scion/scion/tests/unit/test_cli_data_roots.py`, and
  `scion/scion/tests/test_cvrp_formal_readiness.py`

Acceptance status:

- CVRP formal A/A can resolve `cvrplib/...` cases through declared problem data
  roots without manually copying or editing the split manifest.
- The artifact records each pair's case, ledger seed,
  candidate seed, replicate, outcome, delta, raw delta, candidate/champion
  values, elapsed runtime, runtime budget-hit flags, and case resolution.
- The top-level payload records selected cases, selected seeds, replicate
  count, seed offset, bootstrap samples, selected surface, runtime policy, and
  safe data roots used.
- Calibration can optionally use protocol runtime time-limit rules so CVRP
  large-case screening budgets match campaign execution.
- The artifact remains `problem_owned_measurement_diagnostic` and explicitly
  excluded from `DecisionFeatures`.
- Focused verification:
  `pytest scion/scion/tests/unit/test_aa_calibration.py scion/scion/tests/unit/test_cli_data_roots.py scion/scion/tests/test_cvrp_formal_readiness.py`
- Final artifact:
  `/home/clawd/research/scion-experiments/v04-phase1-aa-cvrp-screening-modify-r3-protocoltime-20260611T191356Z-claw/aa_noise_floor.json`.
- Residual-caveat repair report:
  `scion/docs/experiments/v0.4/v04-aa-calibration-runtime-budget-hit-evidence-20260618.md`.

## Governing Constraints

- Preserve the v3 boundary: LLM output is tainted, and Decision reads only
  deterministic `DecisionFeatures`.
- Keep CVRP/VRP semantics problem-owned. BKS, gap, case hardness, runtime model,
  and measurement readiness may guide proposals and preflight, but must not
  become generic Decision inputs.
- Do not treat "no promotion" as a framework failure without the 2026-06-09
  audit checklist: copied configs, prompt visibility, pair-level metrics,
  Protocol evidence, safe features, and branch state must be reconciled.
- Runtime-improvement promotion remains a valid VRP research mode when quality
  is tied or non-regressive and runtime evidence is complete and materially
  better.

## V0.4 Repair Tracks

### R1 Measurement And Practical Delta

Goal: know what each problem/protocol can detect before spending long rounds.

- Add problem-owned measurement/readiness declarations for effect scale,
  practical deltas, pairing validity, runtime model, and calibration refs.
- Replace the hard-coded `ProtocolConfig.min_practical_delta == 0.001` with
  resolved problem/protocol values.
- Build A/A noise-floor calibration for CVRP and warehouse using champion
  replays, producing MDE and false-pass estimates.
- Surface measurement diagnostics in preflight/status/proposal context, not
  `DecisionFeatures`.

Exit criteria:

- CVRP and warehouse have full calibration reports. This criterion is now met
  for Phase 1; Phase 2 must consume those reports rather than rerun blind gate
  tuning.
- CVRP 6/10 screening failures are explained as below measured MDE, not only
  by aggregate win-rate labels.

### R2 Runtime Governance For Budget-Exhausting Solvers

Goal: make anytime ALNS/VNS runtime evidence meaningful.

- Add `runtime_model: comparative | budget_exhausting` semantics.
- In `budget_exhausting`, treat saturation as expected info unless there is a
  timeout/crash or budget violation.
- Disable meaningless `RUNTIME_TIE_FRESH_CHAMPION_REQUIRED` replay paths for
  budget-exhausting tie cases.
- Preserve quality-tie runtime speedup promotion when candidate/champion quality
  is non-regressive and runtime evidence is fresh enough for the declared model.
- Mark saturated runtime-regression-rate style fields as not-applicable when
  they compare millisecond jitter near the time limit.

Exit criteria:

- Focused tests cover both comparative and budget-exhausting runtime semantics,
  including V9 budget-compliance behavior, proposal feedback tiering, and
  finalizer workspace retention.
- CVRP reruns no longer spend effective rounds on runtime replay that cannot
  add decision evidence.

### R3 Branch Depth And Mechanism Continuity

Goal: Scion should pursue a research direction deeply enough to learn.

- Add branch-depth distribution and mechanism-family x effect summaries to
  campaign status/summary.
- Adjust low-signal lifecycle defaults so marginal/non-regressive evidence can
  drive same-mechanism follow-up rather than immediate churn to shallow sibling
  branches.
- Keep clear regressions and boundary/runtime failures fail-closed.
- Ensure next-hypothesis context shows branch-local failures, mechanism ids,
  and follow-up constraints as concise research guidance.

Exit criteria:

- Post-run analysis can reconstruct branch depth, mechanism continuity, and
  whether follow-up hypotheses used prior evidence.
- A focused campaign demonstrates at least one branch with meaningful depth
  beyond one-off attempts without violating v3 Decision boundaries.

### R4 Context Signal Density

Goal: the agent sees enough research signal without drowning in governance logs.

- Profile hypothesis/code prompts by block category: source, governance,
  tool observations, branch-local history, runtime/screening feedback, and
  cross-branch lessons.
- Compact cross-branch maps and runtime feedback into mechanism-level guidance.
- Keep full artifacts for audit, but avoid rendering duplicate raw logs as
  default model-visible context.
- Do not remove active solver facts, target source, branch state, or feedback
  merely to reduce token count.

Exit criteria:

- Prompt profile reports show improved research-signal ratio.
- Focused prompt tests confirm compact research signals are visible before
  broader rules, safety invariants remain present, and prompt manifests report
  research-signal/governance ratios. End-to-end campaign smoke remains a follow-up.

### R5 Focused VRP/Warehouse Validation

Goal: v0.4 must still show meaningful research on current problem families.

- After R1-R4, run focused CVRP/VRP campaigns that explicitly test:
  quality improvement, quality-tie runtime speedup, and branch-depth research.
- Revisit warehouse under the current framework: verify whether it can produce
  repeated promotions or whether stagnation has a measurement/context cause.
- Audit every run with the 2026-06-09 guide and pair-level metrics.

Exit criteria:

- Either VRP produces valid improvement/runtime evidence, or the report
  quantifies why the current protocol cannot detect it and what variant is
  needed.
- Warehouse has a reproducibility/stagnation analysis, not just a one-run
  promotion anecdote.

Current status:

- This plan file keeps the current execution snapshot, repair tracks, and
  acceptance criteria only. Detailed launch/postrun chronology is intentionally
  not maintained here.
- Warehouse postrun analysis briefs now distinguish prepared-only launch
  roots, quality-blocked proposal behavior, protocol-evaluated evidence with
  missing review inputs, and plateau-review readiness; the prepared champion-v2
  follow-up still needs a real launched run before the R5 stagnation conclusion
  can close.
- Research-continuity and measurement-effect briefs now expose branch-depth
  distribution, active shape, mechanism-family effect summaries, and
  branch-lesson semantic diagnostics, so R3/R4 review can distinguish deep
  follow-up from shallow branch scattering, family-level no-effect patterns,
  and failed prior-evidence transfer. Postrun briefs also join prompt
  block-family signal with continuity gaps through
  `research_context_actionability_summary`, so delegated review can audit
  whether missed transfer/follow-up aligns with missing research or cross-branch
  context instead of treating it as generic agent failure. The same summary now
  carries branch-lesson semantic failure/block reason mixes, separating
  metadata-only payloads, unrecognized linkage aliases, and true semantic
  mismatches for delegated review. Live hypothesis prompts now carry compact
  proposal-only research-shape diagnostics before broader rules, closing the
  loop from postrun branch-depth audit back into the next proposal context.
- Current operating truth lives in
  [`../../status/current-state.md`](../../status/current-state.md); the curated
  milestone index lives in
  [`../../status/v0.4-history.md`](../../status/v0.4-history.md).
- Detailed evidence remains in
  [`../../experiments/v0.4/`](../../experiments/v0.4/) and git history. Restore
  older chronology from those sources only when exact legacy provenance is
  needed.

## V0.5 Hand-Off

Only after v0.4 exits R1-R5 should v0.5 run the larger value-analysis program:

- governance ablation: full Scion vs naive vs mid-profile;
- reproduction matrices across warehouse, VRP/variant, and a third problem;
- prompt/context ablations;
- branch-depth and mechanism-trajectory studies;
- final kill/survive criteria for Scion's value claim.
