# Scion v0.4 Evidence Repair And Validation Plan

*Date: 2026-06-11*
*Branch: `codex/v04-evidence-repair-plan`*
*Status: Warehouse recovery checkpoint accepted; CVRP continuation plumbing repaired; demand-slack/post-pivot/construction CVRP pivots rejected; provider guidance updated; budget-exhausting runtime semantics repaired through Decision/lifecycle*

This plan translates the 2026-06-09 and 2026-06-11 audits into v0.4 work.
v0.5 should run broad controlled experiments; v0.4 must first make those
experiments scientifically interpretable and restore meaningful VRP/warehouse
research capability.

## Current Execution Status

This section is the current execution snapshot, not a chronological log.
Detailed launch/postrun facts live in
[`../../experiments/v0.4/`](../../experiments/v0.4/), and milestone history
lives in [`../../status/v0.4-history.md`](../../status/v0.4-history.md).

- Warehouse has an accepted v0.4 positive research-path checkpoint: champion
  `v2` promoted in the validation-transfer gate, and split-preserving
  cost-compression telemetry is now interpreted as effect-positive when cost or
  improving-move evidence is positive. The remaining warehouse question is
  continuous follow-on improvement.
- CVRP continuation, provider guidance, branch evidence transfer, follow-up
  case targeting, compact status projection, reduced measurement readiness, and
  research-efficiency reporting are repaired enough to support meaningful
  follow-up.
- Budget-exhausting runtime semantics are repaired enough for the next focused
  CVRP run: saturation remains audit/report information, cached-runtime tie
  replay does not drain rounds, high aggregate `runtime_regression_rate` does
  not block low-SNR trajectory-divergent expansion, does not drive lifecycle
  soft-abandon or repeated-signal noise, and prompt feedback marks that
  regression rate as not applicable for budget-exhausting solvers.
- CVRP live agentic checks now demonstrate useful framework behavior:
  target-intent/hypothesis guidance can avoid stale mechanisms, generate
  material solver changes, complete formal screening, preserve telemetry, and
  reject weak or negative mechanisms with evidence. They still have not produced
  a solver improvement or promotion.
- Current rejected/default-avoid CVRP directions are broad VNS removal, pure
  ALNS/no-polish, simple initial-VNS disablement, raw cadence-2,
  recent-best/stall gating, fixed early-8, tested share70 cap/rescue variants,
  unchanged route-merge absorption, unchanged demand-slack regret insertion,
  unchanged cross-route 2-opt reconnect, unchanged cluster-biased worst
  removal, and unchanged route-limit seed diversification.
- The next CVRP mechanism must be materially different or explain a new causal
  path with CMT2/CMT4 protection. Construction seed/portfolio revisits need
  direct objective-changing seed evidence, not fallback activation alone.
- The next live CVRP campaign is blocked by LLM quota/auth infrastructure, not
  Scion code. Restore a `gpt-5.5` route that passes real chat-completion
  preflight before launching, and use the prepared root
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-postpivot-resume-ready-1r-gpt55-measurementfix-1r-gpt55-20260618T105007Z-claw`.
  Warehouse follow-on is also launch-prepared at
  `/home/xjy-ubuntu/research/scion-experiments/v04-warehouse-v2-followup-ready-6r-gpt55-measurementfix-6r-gpt55-20260618T105007Z-claw`.
  Both roots use campaign copy/resume, completion preflight, runtime-source
  guards, and default postrun acceptance report generation. WSL launches must
  set `PYTHONPATH=/home/xjy-ubuntu/research/or-autoresearch-agent/scion`.

## Current Task Split

1. Main thread owns v3 boundary alignment, task decomposition, acceptance
   criteria, status curation, git hygiene, and experiment launch decisions.
2. Restore the live `gpt-5.5` route before launching the next CVRP campaign.
   Do not treat `/v1/models` or a tiny auth-only response as sufficient for a
   full agentic proposal run.
3. Next CVRP slice: from a clean synchronized commit, verify construction-pivot
   lessons in live target-intent/hypothesis traces, then require a materially
   different problem-owned solver mechanism or a new causal path with CMT2/CMT4
   protection.
4. Next warehouse slice: run a focused repeat from champion `v2` to test
   continuous follow-on improvement once LLM infrastructure is healthy, using
   the warehouse launcher so campaign resume and production config rewriting are
   reproducible.
5. Subagents should receive bounded read/code/analysis scopes with the v3
   boundary requirement, exact files or artifacts, required tests, and concise
   evidence-backed deliverables.
6. Git management: keep commits sliced by logical track and do not mix
   unrelated CVRP actionability changes with generic framework repairs unless a
   test requires the combined behavior.

## Phase 2 Worker Brief Catalog

These briefs are execution slices for Phase 2 now that Phase 1 A/A is
complete. Every worker brief must begin by requiring the worker to read
`scion/design/scion-architecture-v3.md` and state how the change preserves the
v3 boundary.

### Worker A - Measurement Integration Closure (Completed)

Purpose: close integration gaps around problem-owned measurement declarations,
not reimplement the already-landed practical-delta path.

Completed state:

- `ProblemSpecV1.measurement` and `MeasurementSpec` already define
  `runtime_model`, `pairing_validity`, effect metric/unit, practical deltas,
  and calibration refs.
- `ProtocolConfig.with_problem_measurement()` already resolves problem-owned
  practical deltas and runtime model into protocol config.
- CVRP already declares `runtime_model: budget_exhausting`,
  `pairing_validity: trajectory_divergent`, `practical_delta_screen: 2.0`,
  and `practical_delta_validate: 1.0`.
- Formal CVRP and warehouse production assets now have regression coverage
  proving real problem declarations are consumed by real protocol configs.
- Warehouse production/package specs now use relative surrogate paths so local
  and WSL checkouts resolve the same calibration and canary assets.

Write scope:

- `scion/scion/config/protocol_config.py`
- `scion/scion/problem/spec.py`
- `scion/scion/problem/bridge.py`
- `scion/scion/cli/commands/init_run.py`
- tests only where needed under `scion/scion/tests/`

Acceptance status:

- Formal CVRP and warehouse production protocol loading consumes problem-owned
  measurement values, not synthetic-only fixtures.
- Measurement/calibration raw diagnostics do not enter `DecisionFeatures`.
- Focused verification:
  `pytest scion/scion/tests/test_config.py scion/scion/tests/test_problem_bridge.py scion/scion/tests/test_cvrp_formal_readiness.py scion/scion/tests/test_models.py`
  passed locally. Report:
  `scion/docs/experiments/v0.4/v04-measurement-integration-real-asset-coverage-20260618.md`.

### Worker B - Runtime Semantics Closure (Completed)

Purpose: finish evidence/feedback integration for comparative vs
budget-exhausting runtime semantics.

Completed state:

- Budget-exhausting runtime already suppresses fresh champion replay for cached
  runtime ties in the gate path.
- Runtime budget saturation already downgrades to info and clears actionable
  reason codes under `runtime_model: budget_exhausting`.
- V9 perf guard already treats budget-exhausting runtime as budget compliance.
- High aggregate `runtime_regression_rate` no longer blocks
  trajectory-divergent low-SNR screening expansion under
  `runtime_model: budget_exhausting`.
- Decision/lifecycle now treat aggregate `runtime_regression_rate` as
  report-only under `runtime_model: budget_exhausting`, so it does not
  soft-abandon branches or fragment repeated-signal signatures by itself.
- Prompt runtime feedback renders budget-exhausting aggregate regression as
  `not_applicable_budget_exhausting` and does not turn that summary into strong
  runtime actionability by itself.

Write scope:

- `scion/scion/core/runtime_budget_diagnostics.py`
- `scion/scion/protocol/gates.py`
- `scion/scion/protocol/experiment/stages.py`
- `scion/scion/verification/perf_guard.py`
- `scion/scion/proposal/context_manager/runtime.py`
- `scion/scion/core/decision.py`
- `scion/scion/core/branch_lifecycle_policy.py`

Acceptance status:

- Stage-level protocol results do not surface budget-exhausting saturation as
  actionable gate reason codes.
- Runtime feedback does not tell the agent to chase meaningless saturation on
  budget-exhausting anytime solvers.
- Quality-tie runtime speedup remains possible only from deterministic,
  non-regressive quality evidence and valid runtime evidence.
- Focused verification:
  `pytest scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/test_sprint_e2_context_runtime.py scion/scion/tests/unit/core/test_runtime_budget_diagnostics.py scion/scion/tests/test_verification_gate_integration.py`
  passed locally and in the WSL focused suite. Additional Decision/lifecycle
  verification:
  `pytest scion/scion/tests/test_decision_screening.py scion/scion/tests/unit/core/test_branch_lifecycle_policy.py scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/test_sprint_e2_context_runtime.py scion/scion/tests/unit/core/test_runtime_budget_diagnostics.py scion/scion/tests/test_decision_feature_extraction.py scion/scion/tests/test_models.py`
  passed locally and on WSL. Reports:
  `scion/docs/experiments/v0.4/v04-budget-exhausting-runtime-regression-semantics-repair-20260618.md`
  and
  `scion/docs/experiments/v0.4/v04-budget-exhausting-decision-lifecycle-runtime-semantics-repair-20260618.md`.

### Worker C - Low-SNR Expand And Lifecycle Depth (Completed)

Purpose: make low-SNR, trajectory-divergent research continue or expand when
evidence is inconclusive but not negative, while hard negatives still close.

Completed state:

- `pairing_validity` is resolved into `ProtocolConfig` from problem-owned
  measurement declarations.
- Screening gate and Decision can expand trajectory-divergent, tie-heavy or
  weak non-negative evidence below `0.5` aggregate win rate.
- Lifecycle thresholds are relaxed only for trajectory-divergent low-SNR
  research so same-mechanism follow-up can continue longer.
- Warehouse trajectory-stable behavior remains unchanged.

Completed write scope:

- `scion/scion/protocol/gates.py`
- `scion/scion/core/decision.py`
- `scion/scion/core/branch_lifecycle_policy.py`
- `scion/scion/core/scheduler.py` only if required

Acceptance status:

- For declared trajectory-divergent/low-SNR problems, high-tie or weak
  non-negative evidence can expand or continue without direct abandon.
- Negative median delta, loss-heavy evidence, candidate failure, infeasibility,
  timeout/crash, or runtime regression still fail close.
- Same-mechanism follow-up is possible for multiple low-SNR attempts so branch
  depth can grow beyond one-off candidates.
- Focused verification:
  `pytest scion/scion/tests/test_decision_screening.py scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/unit/core/test_branch_lifecycle_policy.py scion/scion/tests/unit/core/test_scheduler_runtime_evidence_pressure.py`
  passed as part of the integrated Phase 2 suite.

### Worker D - Context Signal Density (Completed)

Purpose: improve proposal-visible research signal without moving diagnostics
into Decision.

Compressible material:

- Raw cross-branch audit/session metadata.
- Long `branch_lesson_usage_requirement` boilerplate.
- Repeated runtime saturation/low-confidence boilerplate.
- Generic governance rules, duplicate "Do Not Claim" text, and raw tool
  observation payloads when compact summaries exist.

Write scope:

- `scion/scion/proposal/context_manager/*`
- `scion/scion/proposal/engine/hypothesis_context_profiles.py`
- `scion/scion/proposal/engine/hypothesis_prompts.py`
- `scion/scion/proposal/prompt_manifest_accounting.py`

Acceptance status:

- Prompt manifests show improved problem-domain research-signal density.
- Proposal context receives measurement/noise/opportunity diagnostics only as
  tainted problem-owned diagnostics excluded from `DecisionFeatures`.
- Validation/frozen per-case details remain hidden from proposal context.
- Focused verification:
  `pytest scion/scion/tests/unit/test_hypothesis_context_profiles.py scion/scion/tests/unit/test_prompt_manifest_accounting.py scion/scion/tests/unit/test_cross_branch_research.py`
  passed as part of the integrated Phase 2 suite.

### Worker E - Source Visibility Protection (Completed)

Purpose: make context compression phase-aware so it cannot remove the research
object code the agent must edit or understand.

Protected material:

- Hypothesis phase: champion/current branch research code, problem object,
  solver mechanics, active algorithm facts, research surface/interface.
- Code phase: approved target file source, required integration file source,
  API manifest, source digests, editable/frozen patterns, and grounding needed
  to write a valid patch.

Write scope:

- `scion/scion/proposal/engine/code_prompts.py`
- `scion/scion/proposal/target_intent_binding.py`
- `scion/scion/proposal/prompt_manifest_visibility.py`
- `scion/scion/proposal/agentic_session_budget_runtime.py`

Acceptance status:

- Code-phase prompt manifests explicitly prove target source and required
  integration source are present after compression.
- Compression may remove generic governance text, duplicated raw logs, and
  raw cross-branch payloads, but not source/code material needed to modify the
  champion or current branch.
- Focused verification:
  `pytest scion/scion/tests/unit/test_agentic_solver_design_prompt_payloads.py scion/scion/tests/unit/test_agentic_target_file_grounding.py scion/scion/tests/unit/test_agentic_session_tool_selection.py`
  passed as part of the integrated Phase 2 suite.

### Worker F - Calibration Tooling Evidence Closure (Completed)

Purpose: make A/A calibration artifacts replayable and diagnostic enough to
support Phase 1 conclusions and later measurement-aware governance.

Completed state:

- `tools/calibrate_aa_noise.py` can produce `scion.aa_noise_floor.v1` with MDE,
  false-pass rate, power curve, and per-case summaries.
- CVRP formal calibration exposed a tooling gap: the initial CLI did not reuse
  the declared problem data root wiring used by campaign launch, so the legacy
  uniform-60s run required a copied split with local `safe_data_roots`.
- The repaired CLI wires declared problem data roots and can use
  `--runtime-policy protocol_time_limits`.
- The repaired payload persists pair evidence, candidate seeds, elapsed
  runtime, selected case/seed lists, replicate count, seed offset, bootstrap
  samples, runtime policy, and case-resolution evidence.
- A/A pair evidence now records explicit champion/candidate runtime budget-hit
  ratios and flags, closing the earlier elapsed-runtime-only caveat.

Completed write scope:

- `scion/tools/calibrate_aa_noise.py`
- `scion/scion/measurement/aa_calibration.py`
- `scion/scion/cli/commands/data_roots.py` only if reuse helpers need a small
  public wrapper
- `scion/scion/config/protocol_config.py` only if calibration needs an existing
  runtime time-limit resolver exposed
- tests under `scion/scion/tests/unit/test_aa_calibration.py`,
  `scion/scion/tests/unit/test_cli_data_roots.py`, and
  `scion/scion/tests/test_cvrp_formal_readiness.py`

Acceptance status:

- CVRP formal A/A can resolve `cvrplib/...` cases through declared problem data
  roots without manually copying or editing the split manifest.
- The artifact records each pair's case, ledger seed,
  candidate seed, replicate, outcome, delta, raw delta, candidate/champion
  values, elapsed runtime, runtime budget-hit flags, and case resolution.
- The top-level payload records selected cases, selected seeds, replicate
  count, seed offset, bootstrap samples, selected surface, runtime policy, and
  safe data roots used.
- Calibration can optionally use protocol runtime time-limit rules so CVRP
  large-case screening budgets match campaign execution.
- The artifact remains `problem_owned_measurement_diagnostic` and explicitly
  excluded from `DecisionFeatures`.
- Focused verification:
  `pytest scion/scion/tests/unit/test_aa_calibration.py scion/scion/tests/unit/test_cli_data_roots.py scion/scion/tests/test_cvrp_formal_readiness.py`
- Final artifact:
  `/home/clawd/research/scion-experiments/v04-phase1-aa-cvrp-screening-modify-r3-protocoltime-20260611T191356Z-claw/aa_noise_floor.json`.
- Residual-caveat repair report:
  `scion/docs/experiments/v0.4/v04-aa-calibration-runtime-budget-hit-evidence-20260618.md`.

## Governing Constraints

- Preserve the v3 boundary: LLM output is tainted, and Decision reads only
  deterministic `DecisionFeatures`.
- Keep CVRP/VRP semantics problem-owned. BKS, gap, case hardness, runtime model,
  and measurement readiness may guide proposals and preflight, but must not
  become generic Decision inputs.
- Do not treat "no promotion" as a framework failure without the 2026-06-09
  audit checklist: copied configs, prompt visibility, pair-level metrics,
  Protocol evidence, safe features, and branch state must be reconciled.
- Runtime-improvement promotion remains a valid VRP research mode when quality
  is tied or non-regressive and runtime evidence is complete and materially
  better.

## V0.4 Repair Tracks

### R1 Measurement And Practical Delta

Goal: know what each problem/protocol can detect before spending long rounds.

- Add problem-owned measurement/readiness declarations for effect scale,
  practical deltas, pairing validity, runtime model, and calibration refs.
- Replace the hard-coded `ProtocolConfig.min_practical_delta == 0.001` with
  resolved problem/protocol values.
- Build A/A noise-floor calibration for CVRP and warehouse using champion
  replays, producing MDE and false-pass estimates.
- Surface measurement diagnostics in preflight/status/proposal context, not
  `DecisionFeatures`.

Exit criteria:

- CVRP and warehouse have full calibration reports. This criterion is now met
  for Phase 1; Phase 2 must consume those reports rather than rerun blind gate
  tuning.
- CVRP 6/10 screening failures are explained as below measured MDE, not only
  by aggregate win-rate labels.

### R2 Runtime Governance For Budget-Exhausting Solvers

Goal: make anytime ALNS/VNS runtime evidence meaningful.

- Add `runtime_model: comparative | budget_exhausting` semantics.
- In `budget_exhausting`, treat saturation as expected info unless there is a
  timeout/crash or budget violation.
- Disable meaningless `RUNTIME_TIE_FRESH_CHAMPION_REQUIRED` replay paths for
  budget-exhausting tie cases.
- Preserve quality-tie runtime speedup promotion when candidate/champion quality
  is non-regressive and runtime evidence is fresh enough for the declared model.
- Mark saturated runtime-regression-rate style fields as not-applicable when
  they compare millisecond jitter near the time limit.

Exit criteria:

- Focused tests cover both comparative and budget-exhausting runtime semantics,
  including V9 budget-compliance behavior.
- CVRP reruns no longer spend effective rounds on runtime replay that cannot
  add decision evidence.

### R3 Branch Depth And Mechanism Continuity

Goal: Scion should pursue a research direction deeply enough to learn.

- Add branch-depth distribution and mechanism-family x effect summaries to
  campaign status/summary.
- Adjust low-signal lifecycle defaults so marginal/non-regressive evidence can
  drive same-mechanism follow-up rather than immediate churn to shallow sibling
  branches.
- Keep clear regressions and boundary/runtime failures fail-closed.
- Ensure next-hypothesis context shows branch-local failures, mechanism ids,
  and follow-up constraints as concise research guidance.

Exit criteria:

- Post-run analysis can reconstruct branch depth, mechanism continuity, and
  whether follow-up hypotheses used prior evidence.
- A focused campaign demonstrates at least one branch with meaningful depth
  beyond one-off attempts without violating v3 Decision boundaries.

### R4 Context Signal Density

Goal: the agent sees enough research signal without drowning in governance logs.

- Profile hypothesis/code prompts by block category: source, governance,
  tool observations, branch-local history, runtime/screening feedback, and
  cross-branch lessons.
- Compact cross-branch maps and runtime feedback into mechanism-level guidance.
- Keep full artifacts for audit, but avoid rendering duplicate raw logs as
  default model-visible context.
- Do not remove active solver facts, target source, branch state, or feedback
  merely to reduce token count.

Exit criteria:

- Prompt profile reports show improved research-signal ratio.
- Focused prompt tests confirm compact research signals are visible before
  broader rules, safety invariants remain present, and prompt manifests report
  research-signal/governance ratios. End-to-end campaign smoke remains a follow-up.

### R5 Focused VRP/Warehouse Validation

Goal: v0.4 must still show meaningful research on current problem families.

- After R1-R4, run focused CVRP/VRP campaigns that explicitly test:
  quality improvement, quality-tie runtime speedup, and branch-depth research.
- Revisit warehouse under the current framework: verify whether it can produce
  repeated promotions or whether stagnation has a measurement/context cause.
- Audit every run with the 2026-06-09 guide and pair-level metrics.

Exit criteria:

- Either VRP produces valid improvement/runtime evidence, or the report
  quantifies why the current protocol cannot detect it and what variant is
  needed.
- Warehouse has a reproducibility/stagnation analysis, not just a one-run
  promotion anecdote.

Current status:

- Warehouse proposal-context ablation has moved beyond the first shakedown. The
  formal 3x3x8 repeat from commit `171648c4204d` completed all 9 cells with
  valid exit status and no promotions. It is accepted as observational
  context-ablation evidence. Full context had the strongest evidence quality,
  including the only frozen row and no fresh-runtime replay rows.
  `no-measurement-diagnostics` cleanly removed measurement diagnostics while
  preserving broader research context and reached one validation row.
  `minimal-research-context` cut useful research signal too aggressively,
  reached no validation/frozen rows, and should not become the default context
  compression strategy.
- The next warehouse experiment should isolate measurement diagnostics while
  holding branch/cross-branch research context constant, and should add a
  trajectory-aware control or explicit control-pair keys before making a causal
  governance-value claim.
- Explicit proposal-trajectory control-pair keys are now implemented at the
  report layer. They improve postrun pairing discipline but do not make LLM
  trajectories deterministic; experiment reports must preserve that caveat and
  inspect trajectory divergence directly.
- The warehouse explicit-control-pair strong-control experiment from commit
  `bd8bfd8e020be2a51d1268070870c7ee2ff6b2ce` completed and is audited at
  [`../../experiments/v0.4/v04-phase5-warehouse-controlpair-full-vs-nomeas-4x2-20260613.md`](../../experiments/v0.4/v04-phase5-warehouse-controlpair-full-vs-nomeas-4x2-20260613.md).
  It compared `full` and `no-measurement-diagnostics` under fixed
  `measurement_governance=on`, warehouse production protocol/split/seeds,
  local `gpt-5.5`, four order-balanced repeats, 8 rounds per cell, and matched
  `control_pair_key=warehouse.full-vs-nomeas:<repeat>` postrun manifests. All
  8 cells exited `0` and were valid complete runs. Prompt isolation worked:
  measurement diagnostics were visible in 62/62 full hypothesis prompts and
  0/46 no-measurement prompts, while compact research, cross-branch maps,
  branch lessons, branch history, sibling branches, and champion research code
  remained visible in both arms. The no-measurement arm produced 2 promotions,
  3 validation rows, and 2 frozen rows; full produced none, despite deeper
  branch research (`max_depth=4`, mean `2.06`, vs `max_depth=3`, mean `1.53`).
  This supports moving measurement diagnostics to compact/on-demand prompt
  rendering for warehouse while keeping deterministic measurement governance
  ON.
- The promoted-only fixed-candidate replay follow-up is complete and audited at
  [`../../experiments/v0.4/v04-phase5-warehouse-promoted-fixed-replay-20260613.md`](../../experiments/v0.4/v04-phase5-warehouse-promoted-fixed-replay-20260613.md).
  The replay root is
  `/home/clawd/research/scion-experiments/v04-phase5-warehouse-promoted-only-fixed-replay-20260613T075754Z-claw`.
  The manifest builder now supports `--candidate-id` and `--hypothesis-id`
  filters; focused acceptance passed with `test_fixed_candidate_replay.py`
  (`7 passed`). The rep01 `merge_vehicles.py` promotion is robust at
  fixed-candidate screening replay (`expand`, case W/L/T `3/0/3`, median delta
  `875.0`, no ON/record-only difference). The rep04
  `split_safe_cost_repack.py` old artifact is not robust evidence: replay
  failed screening under both arms because `candidate.patch.json` recorded only
  the new operator file while the source workspace also changed `registry.yaml`
  to activate it.
- The formal-artifact completeness repair is implemented and validated:
  `FormalCandidatePatchArtifactRecorder` appends changed `registry.yaml` as an
  activation file, records `proposal_target_files` and `activation_files`, and
  includes activation files in the patch digest/replay identity. Acceptance
  passed with `test_fixed_candidate_replay.py` plus
  `test_decision_finalizer_lifecycle.py` (`21 passed`), py_compile, and
  `git diff --check`. The historical rep04 candidate was reconstructed from
  the archived source workspace into corrected candidate `2acf36e8e8709eb6`
  with `target_files=["operators/split_safe_cost_repack.py", "registry.yaml"]`
  and `activation_files=["registry.yaml"]`. The production-scope replay at
  `/home/clawd/research/scion-experiments/v04-phase5-warehouse-reconstructed-rep04-fixed-replay-20260613T081722Z-claw/replay_corrected_prod/fixed_candidate_replay_comparison.v1.json`
  completed with `row_count=2`, no errors, identical ON/`record_only`
  outcomes, and `SCREENING_EXPAND`: current case W/L/T `5/1/4`, pair W/L/T
  `12/2/6`, median delta `775.0`, CI `[0.0, 3025.0]`, and canary passed.
  Planning impact: rep04 reconstruction has validated the repaired artifact
  path. The next Phase 5 step can proceed to compact/on-demand measurement
  diagnostics or another pre-registered fixed-candidate control.
- Compact measurement diagnostics are implemented in commit `3c068c8`.
  `compact-measurement-diagnostics` is a proposal-only context mode that keeps
  protocol `measurement_governance=on`, preserves branch/cross-branch research
  memory and code-phase source visibility, and removes the standalone
  `## Problem Measurement Diagnostics` hypothesis prompt section. Measurement
  signal is visible only through bounded `Compact Research Signals` under
  `compact_problem_measurement_diagnostics`; prompt manifests record
  `measurement_diagnostics_visibility="compact"`. Acceptance passed with
  focused context/control tests (`20 passed`), source visibility check
  (`1 passed`), report/CLI/model suite (`61 passed`), py_compile, and
  `git diff --check`.
- Warehouse compact diagnostics shakedown is complete and audited at
  [`../../experiments/v0.4/v04-phase5-warehouse-compact-diagnostics-shakedown-20260613.md`](../../experiments/v0.4/v04-phase5-warehouse-compact-diagnostics-shakedown-20260613.md).
  It launched from commit `5159249` with one repeat, two rounds per arm,
  warehouse production protocol/split/seeds, local `gpt-5.5`,
  `measurement_governance=on`, and matched report-only
  `control_pair_key=warehouse.compactdiag-shakedown:rep01`. All three arms
  exited `0`. Prompt evidence passed: `full` showed standalone measurement
  diagnostics, `compact-measurement-diagnostics` removed the standalone section
  while preserving branch/cross-branch research context, and
  `no-measurement-diagnostics` suppressed measurement diagnostics entirely. The
  run is accepted as prompt/manifest shakedown only, not governance-value
  evidence.
- Follow-up accounting repair from the shakedown is accepted in commit
  `3835670`. Proposal trajectory joins now handle activation-complete duplicate
  formal-candidate rows, and code-phase source visibility accounting now treats
  `target_file_exists=false` or `source_status=new_file` as create-new evidence
  when action is absent. Rebuilt report-only manifests under
  `postrun_acceptance_rebuilt_after_joinfix` restore compact
  formal-candidate joins from `0` to `2` joined code sessions. Acceptance:
  trajectory/report/source prompt suite `84 passed`, py_compile, and
  `git diff --check`.
- Planning impact: run a longer compact diagnostics control, not another 2R
  shakedown. Keep deterministic measurement governance ON in every arm, use
  matched report-only control keys, and compare
  `compact-measurement-diagnostics` against `no-measurement-diagnostics` and
  optionally `full`. Pull-based on-demand diagnostics remain deferred unless
  the longer run shows the agent needs explicit detailed measurement access.
- Launched: warehouse compact diagnostics longer control from commit `0eca84f`
  at
  `/home/clawd/research/scion-experiments/v04-phase5-warehouse-compact-diagnostics-control-3x3-4r-20260613T092510Z-claw`.
  Design: 3 repeats x 3 prompt arms x 4 rounds, order-balanced arms,
  production warehouse protocol/split/seeds, local `gpt-5.5`, uniform 30s
  solver cap, `measurement_governance=on` in every arm, and matched report-only
  `control_pair_key=warehouse.compactdiag-control:<repeat>`. Purpose: longer
  prompt-context control, not final governance-value conclusion.
- Completed and audited: warehouse compact diagnostics longer control. Report:
  [`../../experiments/v0.4/v04-phase5-warehouse-compact-diagnostics-control-3x3-20260613.md`](../../experiments/v0.4/v04-phase5-warehouse-compact-diagnostics-control-3x3-20260613.md).
  All 9 cells exited `0` and produced complete postrun summaries, failure
  reports, trajectory manifests, and compares. Prompt visibility matched the
  pre-registered arms: `full` had standalone diagnostics in 24/24 hypothesis
  manifests; `compact-measurement-diagnostics` had 0/22 standalone sections
  and 22/22 bounded compact diagnostics; `no-measurement-diagnostics` had 0/22
  measurement diagnostic hits. No arm reached validation, frozen, or promotion,
  so this does not prove quality superiority or final governance value.
  Compact preserved branch follow-up behavior and is accepted as the next
  warehouse prompt baseline candidate; keep `no-measurement-diagnostics` as a
  strong ablation and `full` as a reference. Pull-based on-demand diagnostics
  remain deferred unless a targeted run shows compact lacks detailed
  measurement facts.
- Completed and audited: warehouse compact-baseline measurement-governance
  ON/OFF control from commit `e1e4c48` at
  `/home/clawd/research/scion-experiments/v04-phase5-warehouse-governance-compact-onoff-4x2-8r-20260613T115956Z-claw`.
  Report:
  [`../../experiments/v0.4/v04-phase5-warehouse-governance-compact-onoff-4x2-20260613.md`](../../experiments/v0.4/v04-phase5-warehouse-governance-compact-onoff-4x2-20260613.md).
  All 8 cells exited `0`. Matched report-only trajectory compares passed the
  v3 boundary checks and remain non-deterministic LLM trajectory evidence.
  Raw trajectory produced one record-only promotion and no ON promotions, but
  fixed-candidate replay for the promoted record-only candidate and the
  strongest ON candidate replayed identically under ON and record-only. Treat
  this as completed observational governance shakedown evidence, not as a
  causal governance-value conclusion.
- New repair prerequisites before another formal governance matrix: add a
  stable postrun accounting/research-efficiency report that separates effective
  budget, protocol rows, formal candidates, artifact rows, fresh-runtime drain,
  validation/frozen rows, quality blocks, verification-heavy failures, and
  code-generation failures; include non-fatal agentic/code/timeouts in failure
  taxonomy; repair modify-operation source identity after
  `old_string_not_found`/`stale_source` and 11/78 code manifests with
  `missing_required_source_paths`; and align fixed-candidate replay with the
  campaign problem-spec bridge or clearly require `ProblemSpecV1`.
- Accepted: that immediate governance-run repair gate is now implemented.
  `scion report research-efficiency` emits report-only JSON for budget,
  attempt, protocol, formal-candidate, artifact, stage-row, fresh-replay,
  proposal-quality, failure-taxonomy, and run-status accounting. Code prompt
  manifests now distinguish target source, required integration source, and
  activation source dependencies, so duplicate required paths already satisfied
  by visible target source no longer become false
  `missing_required_source_paths`. Fixed-candidate replay now fails early with
  a clear ProblemSpecV1 message when passed legacy `problem.yaml`. Acceptance:
  report/postmortem tests `19 passed`, replay/source/manifest tests
  `55 passed`, py_compile on touched modules, `git diff --check`, and real
  warehouse cell smoke reports generated under the compact governance run's
  `postrun_acceptance/research_efficiency` directory.
- CVRP remains blocked from being the first formal governance-value target by
  measurement power: the 8-seed A/A check still measured MDE `9.6` raw
  `total_distance`, well above `practical_delta_screen=2.0`.
- Queued after the immediate warehouse governance-run repair planning: design a CVRP/VRP
  ALNS+VNS versus ALNS-only baseline-strength contrast. The purpose is to test
  whether the current strong ALNS+VNS baseline leaves too little measurable
  headroom for agent research, causing repeated screening-level failures even
  when Scion produces real mechanisms. Keep the existing ALNS+VNS baseline as
  the control and create a separate problem-owned ALNS-only baseline variant by
  disabling VNS in solver configuration rather than deleting VNS code. First
  run no-LLM characterization/A-A checks for both baselines, then run matched
  Scion campaigns under the same split/seeds/model/budgets/governance settings.
  Interpret results as research productivity relative to each baseline's own
  MDE and headroom, not as a raw objective-quality comparison between unequal
  baselines. The pre-registered design is
  `scion/docs/planning/v0.4/v0.4-cvrp-baseline-strength-contrast-20260613.md`.
- Completed through repair implementation: CVRP Phase B matched Scion campaigns
  are audited, and the staged diagnostic-validation gate repair is implemented
  under v3 boundaries. The repair lets CVRP protocols explicitly allow
  expand-exhausted, measurable pair-level screening signal to enter diagnostic
  validation while preserving strict validation/frozen promotion criteria.
  Deterministic pair policy now includes minimum pair sample size, pair wins,
  pair win/loss margin, all-pair win rate, non-tie pair win rate, and maximum
  pair loss rate. Acceptance so far: `test_config.py`,
  `test_decision_screening.py`, and `test_protocol_stats_gates.py` passed with
  `85 passed`; wider gate-adjacent regression passed with `190 passed`;
  `py_compile` and `git diff --check` passed.
- Implemented under acceptance before the 12/16-round follow-up: bounded
  post-budget `stage_transition_drain`. This is separate from the gate repair:
  the Phase B `rep02/alns_only` signal queued validation on the final row, but
  ordinary validation did not run because `max_rounds` was exhausted. The new
  drain executes already-produced validation/frozen work only, does not
  generate new hypotheses, does not increment effective proposal rounds, and
  records separate status/accounting fields. Targeted loop/campaign tests passed
  with `65 passed`; accounting/status tests passed with `73 passed`; touched
  modules `py_compile` passed. Combined gate/campaign/accounting regression
  passed with `328 passed`; `git diff --check` passed.
- Operational update: future long CVRP cells may be split across server and the
  user's WSL host. The authoritative handoff/status channel is
  `/home/clawd/research/scion-experiments/v04-cvrp-phaseB-wsl-handoff-20260614T095900Z/status/wsl_status.md`.
  When the reverse tunnel is active, server login is
  `ssh -i /home/clawd/.ssh/id_ed25519_codex_wsl -p 2222 xjy-ubuntu@127.0.0.1`.
  Keep code synchronization via git and experiment synchronization via rsync.
- Completed and audited: CVRP Phase C long-run follow-up. Report:
  [`../../experiments/v0.4/v04-cvrp-baseline-strength-phaseC-postrun-20260615.md`](../../experiments/v0.4/v04-cvrp-baseline-strength-phaseC-postrun-20260615.md).
  The corrected WSL-only six-cell matrix completed `16/16` in every cell and
  passed strict postrun acceptance with recorded formal artifacts and CVRPLIB
  metrics. There were no promotions: every accepted cell retained champion
  version `1` and `promote_decisions_total=0`. ALNS-only reached validation in
  all repeats and frozen in two repeats, with 6 rows at or above its Phase A
  MDE `4.65`, but the strongest validation signals collapsed at frozen.
  Canonical ALNS+VNS reached validation in two repeats, reached no frozen rows,
  and never exceeded its Phase A MDE `9.6`.
- Planning impact: Phase C validates the repaired reach path but does not close
  the CVRP effective-research gate. The narrow report-only lesson-use accounting
  gap is now closed enough to avoid a false diagnosis: accepted Phase C sessions
  mostly contain structured branch-lesson usage, so the main blocker is not
  generic observability. The next R5 work should be targeted VRP research
  analysis, not a larger campaign by default: inspect ALNS-only
  validation-to-frozen collapses by size/family/timeout bucket, determine why
  branch lessons did not become stronger mechanisms, and reduce
  hypothesis-context crowding while preserving full code-phase source visibility
  and the v3 `DecisionFeatures` boundary.
- Runtime-size update: the ALNS-only validation positives were complete on
  `30/45/60s` buckets and improved median BKS gap, but the frozen X501+
  bucket (`X-n573-k30`, `X-n641-k35`, `X-n1001-k43`) produced `0/18` valid
  paired comparisons in the two ALNS-only frozen rows because every attempt
  timed out. A no-LLM ALNS-only champion smoke at
  `/home/clawd/research/scion-experiments/v04-cvrp-runtime-budget-smoke-20260615`
  showed direct solver calls returning after Phase C runner kill points for
  `X-n401@90s` and `X-n573@120s`, but 2x budget still produced no distance
  improvement or best update. Next R5 work should therefore start with a small
  current/2x/4x runtime curve plus best-update trace/effect attribution for
  large-X frozen cases. This remains problem-owned postrun diagnostics and must
  stay outside `DecisionFeatures`.
- Runtime tooling update: CVRP solver-design runtime now has optional
  problem-owned best-update trace/summary telemetry, and
  `scion/tools/cvrp_runtime_curve.py` can run no-LLM direct-solver current/2x/4x
  curves with JSON/CSV summaries, BKS-gap calculation, resume support, and
  configurable parallelism. A dry-run over `X-n401`, `X-n573`, `X-n641`,
  `X-n1001`, seeds `61/67/89`, and multipliers `1/2/4` yields `36` jobs. The
  full curve execution is still pending and should be treated as the next
  experiment artifact, not as another framework-observability repair.
- Execution update: the full 36-job curve completed on WSL from commit
  `72491c0` under
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-largeX-runtime-curve-20260615T150454Z`,
  with final server sync at
  `/home/clawd/research/scion-experiments/v04-cvrp-largeX-runtime-curve-20260615T150454Z`.
  Report:
  [`../../experiments/v0.4/v04-cvrp-largeX-runtime-curve-20260615.md`](../../experiments/v0.4/v04-cvrp-largeX-runtime-curve-20260615.md).
  All `36` planned rows are represented (`34` completed, `2`
  `timeout_expired` on `X-n1001 seed=61` at `120s/240s`). Completed rows were
  objective/gap-stable across `1/2/4` multipliers and had
  `best_update_count=0`. R5 interpretation: runner grace must be fixed for
  valid large-X paired evidence, but simply extending solver time is not enough
  to create large-X search leverage.
- Candidate replay result: completed the no-LLM direct-solver replay for the two
  Phase C ALNS-only validation-positive candidates. Report:
  [`../../experiments/v0.4/v04-cvrp-candidate-largeX-replay-launch-20260615.md`](../../experiments/v0.4/v04-cvrp-candidate-largeX-replay-launch-20260615.md).
  Postrun:
  [`../../experiments/v0.4/v04-cvrp-candidate-largeX-replay-postrun-20260615.md`](../../experiments/v0.4/v04-cvrp-candidate-largeX-replay-postrun-20260615.md).
  Server root:
  `/home/clawd/research/scion-experiments/v04-cvrp-candidate-largeX-replay-20260615T164410Z`.
  Design: `4` large-X cases, seeds `61/89`, budget multipliers `1/4`,
  parallelism `2`, selected surface `solver_design`, and no LLM calls. It
  planned `32` candidate/champion pairs and produced `29` completed pairs.
  Overall completed-pair W/L/T was `2/0/27`, mean candidate-minus-champion
  delta `-8.6897`, median delta `0.0`, and route-count comparison was all ties.
  Missing rows were concentrated on `X-n1001-k43 seed61`, so runner grace
  remains a real large-X evidence-completeness issue. But the mechanism finding
  is negative: both candidates had `0` candidate best updates on completed rows
  and did not show broad large-X search leverage. R5 order now becomes:
  finish/evaluate the size-gated two-opt large-X diagnostic replay, and before
  any further CVRP LLM campaign require no-LLM evidence of nonzero best-update
  density plus objective movement on large-X.
- Warehouse longrun update: attempted and aborted invalid a Phase 4 single-arm
  longrun intended to test continuous warehouse improvement/promotion potential
  under the compact default. Report:
  [`../../experiments/v0.4/v04-phase4-warehouse-longrun-compact-on-launch-20260615.md`](../../experiments/v0.4/v04-phase4-warehouse-longrun-compact-on-launch-20260615.md).
  Clean WSL root:
  `/home/xjy-ubuntu/research/scion-experiments/v04-phase4-warehouse-longrun-compact-on-3x24r-20260615T163700Z`.
  Design: `3` repeats x `24` rounds, warehouse production protocol/split/seeds,
  `measurement_governance=on`, `compact-measurement-diagnostics`,
  `time_limit_sec=30`, disabled early stop, WSL-local `problem.yaml`,
  `problem-v1.yaml`, and split path copies, and max two concurrent cells with a
  600s stagger. Earlier roots ending `T162506Z` and `T163050Z` were stopped
  before useful evidence because of WSL path and missing-ProblemSpecV1-sibling
  launch issues. The clean root entered the adapter-backed production path but
  was stopped at `2026-06-15T16:52:52Z` after rep01/rep02 produced `0` protocol
  metric rows and early patch-edit/`verification_light` loops
  (`rep01 verification_failure_consumed_candidates=7`, rep02 `=2`). Treat this
  as agent behavior/debug evidence, not warehouse promotion evidence.
- Behavior-debug update: a read-only subagent is preparing a minimal single-round
  debug run and artifact checklist to inspect prompt context, branch lesson
  visibility, source visibility, proposal quality, patch artifact fidelity, and
  protocol outcomes. Do not start that new LLM campaign until the design is
  reviewed.
- Independent-baseline control update: launched a separate VRP-only Codex
  researcher as an intentional non-Scion control. It is forbidden from reading
  Scion design/docs/reports/core, may only use standalone `vrp/`, and must keep
  scratch changes plus a research log and benchmark output under
  `/home/clawd/research/scion-experiments/independent-vrp-baseline-research-20260615T165347Z`.
  This provides a process-quality control for comparing plain Codex VRP
  research with Scion-guided research.
- Independent-baseline control result: completed and documented at
  [`../../experiments/v0.4/v04-independent-vrp-baseline-control-20260615.md`](../../experiments/v0.4/v04-independent-vrp-baseline-control-20260615.md).
  The non-Scion researcher found a lightweight intra-route 2-opt polish
  scheduling change for medium instances where full VNS is skipped. The scratch
  benchmark had `60/60` successful subprocesses, mean gap `8.7653% -> 8.1732%`,
  paired outcomes `17` improved / `13` tied / `0` regressed, and X-subset
  outcomes `15` improved / `3` tied / `0` regressed. A quick read-only probe
  shows Scion CVRP has analogous VNS-threshold gating and existing
  `_two_opt_intra`, so this is a portable scheduling/gating hypothesis that
  needs Scion direct-solver replay and formal protocol validation.
- Second independent VRP-only control result: completed and documented at
  [`../../experiments/v0.4/v04-independent-vrp-research-agent-20260615.md`](../../experiments/v0.4/v04-independent-vrp-research-agent-20260615.md).
  The control again read only `vrp/`. On the complete standalone `A/B/P/E/X`
  EUC_2D set with seed `0`, the final candidate left small-instance full VNS
  unchanged and added cheap `two_opt_intra` polish above the full-VNS
  threshold. Paired benchmark-feasible rows produced ALL W/L/T `39/1/74`, mean
  gap `3.3243% -> 3.1658`; X W/L/T `34/0/13`, mean gap
  `5.9733% -> 5.6062`, and median X cost delta `-101`. Risks: one B instance
  lost benchmark feasibility, one paired B row regressed, and single-seed
  wall-clock evidence needs 3-seed/repeated replay. Treat it as a Scion
  hypothesis seed, not a merge-ready patch.
- Warehouse abort behavior result: completed and documented at
  [`../../experiments/v0.4/v04-warehouse-abort-behavior-analysis-20260615.md`](../../experiments/v0.4/v04-warehouse-abort-behavior-analysis-20260615.md).
  The aborted warehouse root failed before Protocol because the WSL verification
  environment lacked `pytest`; fix-stage `wrong_owner` handling then lacked a
  legal no-op/abort path and produced invalid empty or whole-file
  `exact_replace` edits. Required order before another warehouse longrun:
  verifier preflight, fix-stage no-patch/abort handling, a one-candidate
  lifecycle debug that reaches `protocol_metric_results>0`, then a short 3-5R
  compact debug.
- Warehouse abort repair update: verifier preflight and fix-stage no-patch
  handling are now implemented. Campaign startup calls
  `VerificationGate.run_preflight()` after problem runtime preflight, and the
  gate fails before proposal work when configured V3/V4 pytest checks exist but
  `pytest` is unavailable in the active interpreter. `CreativeLayer.fix_code`
  now treats explicit `premise_check=wrong_owner` plus a concrete reason as a
  legal no-patch exit, so environmental or boundary-owned failures do not
  degenerate into invalid empty/whole-file `exact_replace` attempts. Focused
  acceptance:
  `PYTHONPATH=/home/clawd/research/or-autoresearch-agent/scion pytest -q scion/scion/tests/test_verification_runner_checks.py scion/scion/tests/unit/core/test_campaign_control_preflight_contract.py scion/scion/tests/unit/test_agentic_session_core_flow.py -q`
  passed with `39 passed`. This is not warehouse recovery evidence; the next
  empirical gate remains a one-candidate lifecycle debug with
  `protocol_metric_results>0`.
- Behavior-debug design update: read-only subagent Ampere recommends a CVRP
  single-candidate/one-round debug before warehouse because CVRP already has
  valid Phase C protocol artifacts, large-X runtime diagnostics, branch/context
  evidence, and the independent VRP control signal. The minimal run should use
  WSL, `gpt-5.5`, `measurement_governance=on`,
  `compact-measurement-diagnostics`, `--stage-transition-drain-limit 0`, one
  cell only, foreground `timeout 2h`, and then audit prompt manifests, source
  visibility, branch/cross-branch lesson use, hypothesis quality, patch
  fidelity, Contract/Verification/Protocol rows, and context composition. If
  `protocol_metric_results=0`, classify the result as framework/environment
  until stronger evidence proves agent research quality is the cause.
- Behavior-debug launch update: prepared and launched the CVRP 1R debug on WSL
  from commit `17fdeb8`. WSL root:
  `/home/xjy-ubuntu/research/scion-experiments/v04-single-round-debug-cvrp-compact-1r-gpt55-20260615T172112Z-claw`.
  It runs in tmux session `scion_cvrp_1r_debug_172112` using `timeout 2h bash
  run.sh`, one cell, `rounds=1`, `gpt-5.5`, `measurement_governance=on`,
  `compact-measurement-diagnostics`, `time_limit_sec=30`, and
  `SCION_STAGE_TRANSITION_DRAIN_LIMIT=0`. First check confirmed
  `run_status.json`, `status.json`, and `scion.db` creation.
- Behavior-debug postrun update: the WSL run was stopped and synced as invalid
  pre-repair evidence. Report:
  [`../../experiments/v0.4/v04-cvrp-1r-debug-pre-repair-postrun-20260615.md`](../../experiments/v0.4/v04-cvrp-1r-debug-pre-repair-postrun-20260615.md).
  It has `run_validity.reason=invalid_no_effective_rounds`,
  `protocol_metric_results=0`, `screening_protocol_results=0`,
  `agentic_sessions=10`, and repeated `algorithm_smoke_failure` before formal
  candidate evaluation. Read-only artifact audit found source visibility was
  present, but context remains large and diluted by rules/diagnostics. The
  repeated hard runtime failure is a CVRP problem-owned source-interface bug,
  not a Protocol negative result:
  ALNS/VNS scheduler best-update instrumentation passed internal `_Solution`
  objects to `record_best_update()`, and runtime audit coercion could not
  convert internal `_Route` objects to public `CvrpSolution`.
- CVRP runtime-boundary repair update: the local worktree now fixes that
  pre-Protocol blocker by passing `best.routes_as_tuples()` from the scheduler
  and allowing CVRP-owned solution coercion to accept bare routes-like
  iterables. Focused acceptance:
  `PYTHONPATH=/home/clawd/research/or-autoresearch-agent/scion pytest -q scion/scion/tests/test_cvrp_solver_algorithm_runtime.py scion/scion/tests/test_cvrp_solver_vrp_smoke.py scion/scion/tests/test_cvrp_protocol_smoke.py`
  passed with `21 passed`; scoped `py_compile` and `git diff --check` passed.
  Next gate is to rerun a small one-round debug from the repaired commit before
  any long CVRP campaign.
- Repaired behavior-debug launch update: WSL now runs the repaired CVRP 1R
  debug from commit `96ba571` at
  `/home/xjy-ubuntu/research/scion-experiments/v04-single-round-debug-cvrp-compact-1r-repaired-gpt55-20260615T175742Z-claw`
  in tmux session `scion_cvrp_1r_repaired_20260615T175742Z`. It uses the same
  one-cell `gpt-5.5`, `measurement_governance=on`,
  `compact-measurement-diagnostics`, `time_limit_sec=30`,
  `SCION_STAGE_TRANSITION_DRAIN_LIMIT=0`, and `timeout 2h` shape as the
  pre-repair debug. First launch check confirmed `run_status.json`,
  `status.json`, `scion.db`, and `run.log` creation.
- Third independent VRP-only control result: completed and documented at
  [`../../experiments/v0.4/v04-independent-vrp-research-agent-20260615b.md`](../../experiments/v0.4/v04-independent-vrp-research-agent-20260615b.md).
  This control was deliberately isolated from Scion artifacts. Because real
  `cvrplib/` data was absent from the allowed roots, it generated synthetic
  CVRPLIB-format fixtures and found a weaker VNS intra-route relocate
  hypothesis seed: synthetic paired W/L/T `18/5/15`, mean cost delta `-4.8421`,
  median delta `0.0`, and no feasibility or route-count regressions. This is
  useful as a process-control artifact but lower priority than the earlier
  real-CVRPLIB two-opt scheduling signal.
- Two-opt direct replay launch update: a no-LLM direct replay for the stronger
  independent-control mechanism is active on WSL. Report:
  [`../../experiments/v0.4/v04-cvrp-twoopt-polish-direct-replay-launch-20260615.md`](../../experiments/v0.4/v04-cvrp-twoopt-polish-direct-replay-launch-20260615.md).
  It creates experiment-owned copied workspaces for `USE_VNS=False` baseline
  and `USE_VNS=False + _two_opt_intra` polish candidate, then runs a smoke on
  `B-n45-k6`, `B-n66-k9`, `A-n80-k10`, and `M-n200-k17` across seeds
  `11/29/43` in tmux session `scion_cvrp_twoopt_smoke_20260615T1820Z`.
  Large-X replay remains gated on smoke acceptance.
- Repaired behavior-debug completion update: the repaired CVRP 1R debug
  completed valid with wrapper exit `0` and was synced to
  `/home/clawd/research/scion-experiments/v04-single-round-debug-cvrp-compact-1r-repaired-gpt55-20260615T175742Z-claw`.
  Report:
  [`../../experiments/v0.4/v04-cvrp-1r-debug-repaired-postrun-20260615.md`](../../experiments/v0.4/v04-cvrp-1r-debug-repaired-postrun-20260615.md).
  It produced `effective_protocol_rounds=1`, `formal_screened_candidates=1`,
  `protocol_metric_results=1`, `screening_protocol_results=1`,
  `validation_protocol_results=0`, `frozen_protocol_results=0`,
  `agentic_sessions=2`, and `quality_block_ledger=0`. This closes the
  immediate path-health question after the runtime-boundary repair: the next
  judgment must come from artifact analysis of the candidate, source visibility,
  prompt/context density, and branch signal, not from the earlier pre-Protocol
  failure. Subagent `Carson` completed that read-only artifact analysis and
  accepted the run for path health only: the candidate was
  `split_route_ejection_merge`, Protocol returned case W/L/T `2/1/5`, pair
  W/L/T `6/5/21`, `win_rate=0.25`, `median_delta=0.0`, CI `[0.0, 5.25]`, and
  `SCREENING_EXPAND_LOW_SNR_TRAJECTORY_DIVERGENT`. The hypothesis prompt was
  still about `120,089` characters and truncated `compact_research_signals`.
- Two-opt direct replay smoke completion update: the no-LLM smoke completed and
  is recorded in
  [`../../experiments/v0.4/v04-cvrp-twoopt-polish-direct-replay-launch-20260615.md`](../../experiments/v0.4/v04-cvrp-twoopt-polish-direct-replay-launch-20260615.md).
  The candidate produced positive aggregate smoke evidence (`9/3/0`, mean delta
  `-3.4167`, median delta `-3.5`, route/fleet regressions `0`, nonzero two-opt
  activation), but failed the pre-registered large-X gate because B-family rows
  had repeated objective regressions (`B-n45-k6` two losses, `B-n66-k9` one
  loss). Do not spend large-X replay budget on this mechanism as-is; either
  design a narrower gated candidate or analyze why B-family behavior regressed.
- Two-opt follow-up launch update: the narrower no-LLM smoke is launched on WSL
  and recorded in
  [`../../experiments/v0.4/v04-cvrp-twoopt-polish-followup-smoke-launch-20260615.md`](../../experiments/v0.4/v04-cvrp-twoopt-polish-followup-smoke-launch-20260615.md).
  It tests `initial_only` and diagnostic `size70` variants against the same
  four-case, three-seed smoke before any large-X replay. This keeps the
  investigation problem-owned and avoids changing generic core or
  `DecisionFeatures`.
- Two-opt follow-up postrun update: the smoke completed and is recorded in
  [`../../experiments/v0.4/v04-cvrp-twoopt-polish-followup-smoke-postrun-20260615.md`](../../experiments/v0.4/v04-cvrp-twoopt-polish-followup-smoke-postrun-20260615.md).
  `initial_only` failed the large-X gate because `B-n45-k6` still had two
  losses. `size70` passed with W/L/T `6/0/6`, mean delta `-3.8333`, median
  delta `-1.5`, route/fleet regressions `0`, B rows all tied with zero two-opt
  activation, and A/M rows all won. The size70 large-X diagnostic replay was
  launched and recorded in
  [`../../experiments/v0.4/v04-cvrp-twoopt-size70-largeX-launch-20260615.md`](../../experiments/v0.4/v04-cvrp-twoopt-size70-largeX-launch-20260615.md).
- Two-opt size70 large-X postrun result: the replay completed and is recorded
  in
  [`../../experiments/v0.4/v04-cvrp-twoopt-size70-largeX-postrun-20260615.md`](../../experiments/v0.4/v04-cvrp-twoopt-size70-largeX-postrun-20260615.md).
  It planned `24` candidate rows (`4` large-X cases x `3` seeds x multipliers
  `{1,4}`), produced `24` candidate rows, and completed `23`
  candidate/champion pairs. The only planned noncompleted pair was
  `X-n1001-k43 seed61 m1`, where both baseline and candidate timed out.
  Completed pairs were W/L/T `23/0/0`, mean candidate-minus-champion delta
  `-295.5652`, median `-192.0`, route/fleet regressions `0`, initial two-opt
  accepts `23`, embedded accepts `72`, and `candidate_best_update_count=0`.
  This satisfies the no-LLM diagnostic gate for large-X objective movement and
  makes size70 two-opt the leading CVRP mechanism seed. It is not promotion
  evidence: it is direct replay, runtime pressure is heavy, `m=2` was
  intentionally not run, and the gain comes through construction/two-opt polish
  rather than ALNS incumbent-update trace.
- Fourth independent VRP-only external control launch update: subagent
  `Anscombe` (`019ecc8e-d45e-7983-b346-3621f90d38f4`) is running as a fresh,
  non-forked plain Codex researcher:
  [`../../experiments/v0.4/v04-independent-vrp-research-agent-20260615c.md`](../../experiments/v0.4/v04-independent-vrp-research-agent-20260615c.md).
  This control is intentionally outside the usual v3-first Scion subagent rule:
  it may only study standalone `vrp/`, must not read Scion artifacts, and can
  only produce external-control hypothesis seeds.
- Fourth independent VRP-only external control result: `Anscombe` completed and
  retained no `candidate.patch`. It ran real standalone CVRPLIB `A` subset rows
  (`27` cases, seed `0`) and tested six small candidates across VNS gating,
  periodic VNS, VNS operator reordering, accepted-candidate polish, and ALNS
  destroy-ratio changes. All six had worse mean objective than baseline. The
  best-looking C3 operator-reordering attempt measured W/L/T `4/2/21`, mean
  objective delta `+0.741` where positive is worse, median delta `0.0`, and no
  feasibility or route-count regressions. This is negative process-control
  evidence, not a Scion improvement hypothesis.
- Fifth independent VRP-only external control result: subagent `Feynman`
  (`019eccb2-2fc0-7ff1-8032-94358c217c8a`) completed as a fresh, non-forked
  plain Codex researcher. Report:
  [`../../experiments/v0.4/v04-independent-vrp-research-agent-20260615d.md`](../../experiments/v0.4/v04-independent-vrp-research-agent-20260615d.md).
  Artifact root:
  `/home/clawd/research/scion-experiments/v04-independent-vrp-research-agent-20260615d`.
  This control was intentionally isolated from Scion. It ran `5` baseline
  solver runs and `16` candidate solver runs. The selected H3
  `route_elimination` candidate retained a `candidate.patch`, with route-count
  and objective/route improvements on the small smoke matrix: `B-n78-k10 seed0`
  objective tied with routes `11 -> 10`, `B-n78-k10 seed1` improved
  `1251 -> 1250` with routes `11 -> 10`, `X-n200-k36 seed0` improved
  `61796 -> 61420` with routes `38 -> 37`, and `A-n32-k5` / `X-n101-k25` were
  neutral. This is a positive external-control hypothesis seed requiring
  broader no-LLM replay, not Scion Protocol evidence.
- Sixth independent VRP-only external control launch update: subagent
  `Lovelace` (`019ecce4-4806-7581-b033-d33911f8b276`) is running as a fresh,
  non-forked plain Codex researcher. Launch report:
  [`../../experiments/v0.4/v04-independent-vrp-research-agent-20260615e.md`](../../experiments/v0.4/v04-independent-vrp-research-agent-20260615e.md).
  Artifact root:
  `/home/clawd/research/scion-experiments/v04-independent-vrp-baseline-research-longrun-20260615`.
  It is forbidden from reading Scion artifacts and may only study standalone
  `vrp/`, write process logs, run bounded baseline/candidate experiments, and
  retain patches under the artifact root. This is external-control process
  evidence only; any positive mechanism must go through separate no-LLM Scion
  replay before adoption.
- Warehouse lifecycle-debug design result: the warehouse gate was specified
  in
  [`../../experiments/v0.4/v04-warehouse-1candidate-lifecycle-debug-design-20260615.md`](../../experiments/v0.4/v04-warehouse-1candidate-lifecycle-debug-design-20260615.md).
  It should be one WSL production cell, `rounds=1`, local `gpt-5.5`,
  `measurement_governance=on`, `compact-measurement-diagnostics`,
  `time_limit_sec=30`, disabled early stop, foreground `timeout 2h`, and
  experiment-local WSL copies of `problem.yaml`, sibling `problem-v1.yaml`, and
  `split_manifest_prod.yaml`. WSL preflight is repaired: `pytest` is installed
  in `/home/xjy-ubuntu/miniconda3/envs/scion`, the WSL repo is at `9204315`,
  and the focused preflight/fix-stage suite passed on WSL with `39 passed`.
  The CVRP size70 large-X solver load finished before launch. The gate was
  later accepted only after `protocol_metric_results > 0` and screening
  Protocol rows existed.
- Warehouse lifecycle-debug postrun result: the gate completed and is recorded
  in
  [`../../experiments/v0.4/v04-warehouse-1candidate-lifecycle-debug-postrun-20260615.md`](../../experiments/v0.4/v04-warehouse-1candidate-lifecycle-debug-postrun-20260615.md).
  First root
  `/home/clawd/research/scion-experiments/v04-warehouse-1candidate-lifecycle-debug-20260615T1950Z`
  is invalid launch/config evidence because the copied split manifest resolved
  `safe_data_roots` to the wrong WSL data root; it had
  `protocol_metric_results=0`. Accepted rerun root:
  `/home/clawd/research/scion-experiments/v04-warehouse-1candidate-lifecycle-debug-20260615T2000Z`.
  The rerun used an absolute WSL safe root, exited `0`, and passed the gate
  with `run_validity.status=valid`, `effective_rounds_completed=1`,
  `protocol_metric_results=1`, `protocol_metric_stage_counts.screening=1`,
  `formal_screened_candidates=1`, Contract/Verification/canary all passed, and
  no postrun failures. The candidate reached screening and Decision
  `continue_explore`; case W/L/T was `1/2/7`, pair W/L/T was `5/8/7`, and
  median delta was `0.0`. Next warehouse step: run a short `3-5R` compact
  debug before relaunching any full `3 x 24R` longrun.
- Warehouse short-debug launch update: a `3R` compact ON-arm debug is active
  and recorded in
  [`../../experiments/v0.4/v04-warehouse-short-debug-3r-launch-20260615.md`](../../experiments/v0.4/v04-warehouse-short-debug-3r-launch-20260615.md).
  WSL root:
  `/home/xjy-ubuntu/research/scion-experiments/v04-warehouse-short-debug-3r-20260615T201259Z`.
  Server root:
  `/home/clawd/research/scion-experiments/v04-warehouse-short-debug-3r-20260615T201259Z`.
  Tmux session: `scion_warehouse_short3r_201259`. It started at
  `2026-06-15T20:14:17Z` from commit `dabfcee` with `rounds=3`,
  `measurement_governance=on`, `compact-measurement-diagnostics`,
  `time_limit_sec=30`, local `gpt-5.5`, disabled early stop, and corrected WSL
  safe roots. Acceptance requires postrun analysis of Protocol rows, branch
  continuity, prompt composition, and marginal-evidence use before any full
  warehouse longrun rerun.
- Warehouse short-debug postrun result: the run completed and is recorded in
  [`../../experiments/v0.4/v04-warehouse-short-debug-3r-postrun-20260615.md`](../../experiments/v0.4/v04-warehouse-short-debug-3r-postrun-20260615.md).
  It exited `0` and is valid: `requested_rounds=3`,
  `effective_rounds_completed=3`, `protocol_metric_results=2`,
  `formal_screened_candidates=2`, and one verification-heavy failure consumed
  the third round before Protocol. The first `subcategory_pack_upgrade.py`
  candidate was marginal; the second same-branch modify became no-effect; the
  third clean-fork `swap_orders.py` candidate failed V9 runtime verification.
  Branch lesson usage appeared in all `6` sessions, but read-only semantic
  audit judged only the same-branch modify as clearly satisfying the lesson
  intent. Prompt payloads remain large and tool-selection/general heavy. Do not
  relaunch full warehouse `3 x 24R` yet; do targeted prompt/guidance repair and
  another short `3-6R` debug first.
- Independent VRP control E result: `Lovelace` completed and the report is
  updated at
  [`../../experiments/v0.4/v04-independent-vrp-research-agent-20260615e.md`](../../experiments/v0.4/v04-independent-vrp-research-agent-20260615e.md).
  The retained `construction_portfolio` patch is an external-control backlog
  seed only. It had small net positive evidence but also `+1` regressions, and
  the observed baseline included the pre-existing dirty standalone
  `vrp/src/solver.py`.
- CVRP size70 next-rung design result: fixed-candidate validation design is
  recorded at
  [`../../planning/v0.4/v04-cvrp-size70-fixed-candidate-validation-design-20260615.md`](../../planning/v0.4/v04-cvrp-size70-fixed-candidate-validation-design-20260615.md).
  Next order is fixed-candidate validation-grade replay first, then a short
  seeded Scion CVRP run only if mechanism validity passes. Large-X completion
  replay must include `m=2`; minimal tooling gap is stage-aware fixed-candidate
  replay for validation/frozen and external full-file candidate artifacts.
- Latest CVRP size70 execution update: the Tier 1 Large-X diagnostic is
  complete and accepted, and the formal fixed-candidate validation replay has
  been relaunched on WSL with the experiment-local WSL `safe_data_roots` fix and
  a protocol override for all `12` validation cases x `4` seeds. Active WSL
  root:
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-size70-fixed-validation-full-20260615T225148Z`.
  Server sync root:
  `/home/clawd/research/scion-experiments/v04-cvrp-size70-fixed-validation-full-20260615T225148Z`.
  Tmux session: `scion_cvrp_size70_validation_full_225148`. Latest synced raw
  metrics showed the replay still active at `9/48` attempted/valid pairs,
  `0` failed pairs, W/T/L `0/8/1`, mean delta `-0.222`, and median delta `0.0`.
  This is progress telemetry only; the fixed-candidate mechanism gate remains
  open until all per-arm pairs and top-level comparison outputs are present. A
  later sync clarified that the `48` planned pairs are per replay arm: the `on`
  arm completed `48/48` with `0` failed pairs, then the run began
  `record_only`. Do not accept the validation gate until `record_only`
  completes, `exit_code.txt` exists, and
  `fixed_candidate_replay_comparison.v1.json` is present.
- CVRP size70 fixed-candidate validation completion: the full replay completed
  with wrapper exit `0`, comparison `row_count=2`, both `on` and `record_only`
  rows `completed`, and both raw validation arms at `48/48` valid pairs with
  `0` failed pairs. Postrun:
  [`../../experiments/v0.4/v04-cvrp-size70-fixed-validation-postrun-20260615.md`](../../experiments/v0.4/v04-cvrp-size70-fixed-validation-postrun-20260615.md).
  ON W/T/L was `26/13/9`, median delta `4.0`; `record_only` W/T/L was
  `27/13/8`, median delta `6.0`. Both arms failed validation with
  `VALIDATION_FAIL_NO_HIERARCHICAL_GAIN`. The size70 two-opt mechanism should
  not proceed to frozen or seeded Scion CVRP; the next gate should use a
  separate pre-registered hypothesis or problem, not extend this candidate.
- Latest independent-control update: `Helmholtz`
  (`019ecd83-3324-7820-912e-2c7d94517e7e`) completed as a fresh, non-forked
  VRP-only external research subject. Result report:
  [`../../experiments/v0.4/v04-independent-vrp-research-agent-20260615k-result.md`](../../experiments/v0.4/v04-independent-vrp-research-agent-20260615k-result.md).
  Artifact root:
  `/home/clawd/research/vrp-independent-codex-research/phase-k-20260615`.
  It is deliberately outside the Scion subagent rules and must not read Scion
  artifacts. It retained a `regret4_repair` external hypothesis seed with W/T/L
  `8/5/2`, mean delta `-32.333`, median delta `-11.0`, and failures `0` on a
  small standalone smoke grid. The next gate is broader no-LLM validation
  across A/B/P/E plus the sampled X cases and seeds `0..9`; this is not yet
  Scion evidence or a default solver change.
- Latest independent-control update: `Newton`
  (`019ecded-8a24-7a03-b64b-8b1929c9af49`) completed as a fresh, non-forked
  VRP-only external research subject. Result report:
  [`../../experiments/v0.4/v04-independent-vrp-research-agent-20260616l-result.md`](../../experiments/v0.4/v04-independent-vrp-research-agent-20260616l-result.md).
  Artifact root:
  `/home/clawd/research/vrp-independent-codex-research/phase-l-20260616`.
  It retained no `candidate.patch`. The implemented intra-route Or-opt VNS
  candidate was rejected after `30` paired real CVRPLIB rows: combined W/T/L
  `7/12/11`, mean delta `+1.6`, median delta `0.0`, failures `0`, and no
  candidate route-count regressions. The outside-case sanity slice was
  negative (`2/1/6`, mean delta `+5.666667`). This closes phase L as negative
  external-control process evidence and does not change the next CVRP gate:
  the `Helmholtz` `regret4_repair` seed still needs broader no-LLM validation
  before any Scion replay or default solver change.
- Latest warehouse action: launched a WSL `3 x 24R` warehouse longrun
  regression check against the v0.3 promotion reference. Launch report:
  [`../../experiments/v0.4/v04-warehouse-longrun-regression-3x24r-launch-20260616.md`](../../experiments/v0.4/v04-warehouse-longrun-regression-3x24r-launch-20260616.md).
  This is a single intended-default arm from commit `f384884` with
  `measurement_governance=on`, `compact-measurement-diagnostics`, production
  protocol/split/seeds, `30s` solver cap, disabled early stop, and max WSL
  parallelism `2`. Purpose: check whether v0.4 warehouse has regressed against
  v0.3's production promotion evidence (`3/3` Sonnet promoted after fixes) and
  continuous-improvement evidence, while also evaluating final champion
  quality/gap and whether any no-promotion result is real plateau or framework
  failure. WSL tmux session `scion_wh_longrun_reg_071323` is running; initial
  health check showed commit `f384884`, `status=running`, and `rep01` started.
  Monitor/postrun worker `Sartre`
  (`019ecf4a-359e-7870-8764-ae389aafb106`) owns sparse polling and postrun
  report generation.
- Warehouse longrun result: complete and valid. Postrun:
  [`../../experiments/v0.4/v04-warehouse-longrun-regression-3x24r-postrun-20260616.md`](../../experiments/v0.4/v04-warehouse-longrun-regression-3x24r-postrun-20260616.md).
  All three cells exited `0` and reached Protocol, validation, and frozen.
  Promotions were `rep01=1`, `rep02=0`, `rep03=1`; final champions were `v2`,
  `v1`, and `v2`. This proves v0.4 warehouse still promotes and has no
  catastrophic launch/preflight regression, but it does not recover v0.3
  cadence (`3/3` production Sonnet promotions after fixes; strongest synthetic
  `4`-promotion chain). Next repair focus is proposal/context/code-edit quality
  and fresh-runtime replay closure, not another blind longrun.
- Latest CVRP action: delegated the `Helmholtz` `regret4_repair` broader
  no-LLM validation to worker `Aquinas`
  (`019ecf46-fbac-7a42-b360-b1bd4aecf6a0`). Artifact root:
  `/home/clawd/research/scion-experiments/v04-vrp-regret4-broader-validation-20260616`.
  The worker must use clean archive-based baseline/candidate workspaces and
  report whether the candidate can proceed to Scion fixed replay.
- CVRP broader validation result: complete and rejected as-is. Postrun:
  [`../../experiments/v0.4/v04-vrp-regret4-broader-validation-postrun-20260616.md`](../../experiments/v0.4/v04-vrp-regret4-broader-validation-postrun-20260616.md).
  All `80/80` rows completed, but W/T/L was `21/31/28`, W-L `-7`, median
  delta `0.0`, and repeated regression families were `E`, `M`, and `P`.
  Feasibility and route-count constraints were preserved. Do not proceed to
  Scion fixed replay with the broad `regret4_repair` patch.
- Latest process synthesis: complete and accepted. Main synthesis:
  [`../../experiments/v0.4/v04-warehouse-vrp-process-synthesis-20260616.md`](../../experiments/v0.4/v04-warehouse-vrp-process-synthesis-20260616.md).
  Warehouse branch audits show v0.4 can still promote but has not recovered
  v0.3-style continuous promotion. The next warehouse rung should be targeted
  research-loop repair before another full longrun: deterministic
  `branch_lesson_usage` canonicalization, explicit proposal modes,
  `semantic_linkage_valid` reporting, robust same-file code patching, cheap
  operator invariants, replay candidate materialization/closure, and earlier
  all-tie branch stop/diagnosis. VRP independent audit shows BKS gap is
  targeting information, not Decision evidence; next VRP work should diagnose
  family/slice/runtime-phase mechanisms before proposing another broad
  candidate.
- Accepted W4/P2 repair: prompt signal-density rendering. Acceptance plan:
  [`v04-prompt-signal-density-repair-acceptance-20260616.md`](v04-prompt-signal-density-repair-acceptance-20260616.md).
  Worker `Lovelace` implemented compact mechanism-level rendering for critical
  research sections (`compact_research_signals`,
  `branch_lesson_usage_context`, and `cross_branch_research_map`) so they are
  not provider-visible truncated under a realistic stress context. Full
  artifacts and v3 Decision boundaries stay intact; champion/source code
  visibility is explicitly covered by the stress test. Main-session acceptance:
  focused prompt/validation/branch-lesson suite `79 passed`, proposal
  pipeline/artifact subset `45 passed`, `py_compile`, and `git diff --check`.
  The next live campaign still needs a prompt-manifest postrun check before
  treating the repair as field-proven.
- Follow-up accepted after user review: commit `fd185cf`
  (`fix: remove prompt research signal hard caps`) removes the remaining
  research-stage hard character/list caps, array slicing, and ellipsis markers
  from projected prompt research signals while preserving raw/noise filtering
  and v3 Decision boundaries. This aligns the current phase with the principle
  that effective research context should be present first; budget control can
  be evaluated later as an explicit governance/ablation layer.
- Latest warehouse evidence gate: the pair-signal diagnostic rerun from commit
  `3ca26a0` is complete and valid, but failed research-quality acceptance.
  Postrun:
  [`../../experiments/v0.4/v04-warehouse-pair-signal-diagnostic-rerun6r-postrun-20260616.md`](../../experiments/v0.4/v04-warehouse-pair-signal-diagnostic-rerun6r-postrun-20260616.md).
  It completed `6/6` effective rounds with no proposal quality blocks,
  verification failures, or fresh-runtime replay drain, but all protocol rows
  remained screening-only. The pair-signal protocol repair is accepted as
  fail-closed: no loss-heavy or negative-median candidate was routed to
  validation. Warehouse research quality is still rejected because the deep
  `move_order.py` branch repeated the same `1/2/3` case W/L/T, `3/4/5` pair
  W/L/T, and median `0.0` signal four times.
- Accepted local repair after that gate: `BranchLifecyclePolicy` now parks
  loss-dominated marginal/no-effect follow-ups when case losses exceed wins,
  pair losses are at least pair wins, and median delta is non-positive. The
  rule is generic lifecycle governance over deterministic `DecisionFeatures`
  numbers and generic evidence tiers; it adds no warehouse/CVRP semantics to
  core and preserves pair-positive marginal branch depth. Acceptance passed
  `181` branch lifecycle/Decision/finalizer/scheduler tests, `51`
  orchestrator/scheduler/hygiene tests, Python compile, and `git diff --check`.
  The next warehouse field gate should be a short production rerun from this
  repair commit. Acceptance should require no repeated loss-dominated same-
  branch formal screening loop; validation/frozen/promotion still require
  ordinary protocol evidence or the conservative pair-level diagnostic path.
- Lifecycle repair field gate result: the short rerun from commit `6e3988c`
  completed validly but remained screening-only. Postrun:
  [`../../experiments/v0.4/v04-warehouse-lossheavy-lifecycle-rerun6r-postrun-20260616.md`](../../experiments/v0.4/v04-warehouse-lossheavy-lifecycle-rerun6r-postrun-20260616.md).
  The target loss-heavy marginal loop did not recur, so this is a no-regression
  field check for the lifecycle brake rather than direct trigger proof. The
  run did preserve positive marginal branch depth. The next blocker is now more
  specific: expanded-exhausted positive low-SNR evidence such as `3/1/10` case
  W/L/T, `13/6/9` pair W/L/T, median `300`, and CI low `0` still remains in
  screening. Before another longrun, the warehouse-owned diagnostic validation
  route should admit this non-regressive positive-CI shape while keeping
  negative-median and loss-heavy evidence fail-closed.
- Warehouse-owned diagnostic admission repair: implemented locally and accepted
  deterministically. Report:
  [`../../experiments/v0.4/v04-warehouse-positive-diagnostic-protocol-repair-20260616.md`](../../experiments/v0.4/v04-warehouse-positive-diagnostic-protocol-repair-20260616.md).
  `protocol_prod.yaml` now uses `pair_win_rate_min=0.46` and
  `pair_non_tie_win_rate_min=0.68` for expanded-borderline pair-level
  diagnostic validation. Generic Decision code is unchanged. Direct replay
  admits the field-positive `3/1/10` case, `13/6/9` pair, median `300`, CI low
  `0` shape, while keeping negative-median and loss-dominated shapes below
  validation. Next step remains a short production warehouse rerun from the
  repair commit before any warehouse efficacy claim.
- Positive-diagnostic field gate result: complete with split acceptance.
  Postrun:
  [`../../experiments/v0.4/v04-warehouse-positive-diagnostic-rerun6r-postrun-20260616.md`](../../experiments/v0.4/v04-warehouse-positive-diagnostic-rerun6r-postrun-20260616.md).
  The run was valid, completed `6/6`, and produced `5` screening rows plus `1`
  validation row. This proves repaired v0.4 can reach validation again in the
  warehouse short-run path and directly field-accepts the loss-heavy lifecycle
  brake. It does not prove warehouse research quality: the validation candidate
  failed `VALIDATION_FAIL_NO_HIERARCHICAL_GAIN`, and no frozen row or promotion
  occurred. Do not loosen validation/frozen gates. The next repair should target
  warehouse proposal quality and problem-owned operator diagnostics so
  screening-positive ideas explain their validation-case transfer risk before
  another short field gate.
- Warehouse validation-transfer proposal-quality repair: implemented locally
  and accepted as code, not efficacy evidence. Report:
  [`../../experiments/v0.4/v04-warehouse-validation-transfer-quality-repair-20260616.md`](../../experiments/v0.4/v04-warehouse-validation-transfer-quality-repair-20260616.md).
  A generic optional adapter hook now runs before code generation in
  non-agentic and agentic proposal paths, but the transfer-risk rule remains
  problem-owned in `WarehouseDeliveryAdapter` and `problem-v1.yaml`. Warehouse
  operator hypotheses must state validation-case transfer risk,
  proposal-level activation/effect diagnostics, and a screening-only no-effect
  guard. The guidance explicitly prevents inventing undeclared
  `expected_telemetry` keys for those counters. No `DecisionFeatures`, Protocol
  thresholds, validation/frozen/promotion gates, or holdout exposure changed.
  Acceptance passed warehouse tests (`12 passed`), context/agentic tests
  (`90 passed`), proposal pipeline tests (`64 passed`), Python compile, and
  `git diff --check`. Next step remains a short production warehouse rerun
  before any warehouse efficacy or v0.3-continuity claim.
- Warehouse validation-transfer rerun early-stop: the short production rerun
  from commit `88e31d7` was intentionally stopped after it exposed a narrower
  remaining quality gap. Early-stop report:
  [`../../experiments/v0.4/v04-warehouse-validation-transfer-quality-rerun6r-early-stop-20260616.md`](../../experiments/v0.4/v04-warehouse-validation-transfer-quality-rerun6r-early-stop-20260616.md).
  The hypothesis-stage gate worked: `proposal_quality_blocks=2`, both recorded
  as `agent_quality_blocked` for missing transfer/diagnostic claims. However,
  a non-blocked candidate reached screening with `activation_status=not_declared`
  and `effect_status=not_declared`, even though its hypothesis promised
  activation/effect counters. The next repair is therefore a problem-owned
  patch/code quality hook: core should provide only the generic hook and
  lifecycle routing, while `WarehouseDeliveryAdapter` owns the warehouse
  semantics and blocks patches that do not make promised diagnostics executable
  or observable before Protocol.
- Warehouse validation-transfer patch-quality repair: implemented locally and
  accepted as code/framework behavior. Report:
  [`../../experiments/v0.4/v04-warehouse-validation-transfer-patch-quality-repair-20260616.md`](../../experiments/v0.4/v04-warehouse-validation-transfer-patch-quality-repair-20260616.md).
  The early-stop field trace patches for `merge_vehicles.py` and
  `move_order.py` are now both blocked before Protocol with
  `agent_quality_blocked:warehouse_validation_transfer_patch_quality_missing`.
  Generic core adds only an optional patch-quality hook and lifecycle routing;
  the warehouse semantics stay problem-owned. No Decision or Protocol threshold
  changed. A feedback-format follow-up in commit `b7627fc` removed a duplicate
  `agent_quality_blocked` prefix from structured quality-block details. The
  first `b7627fc` field-gate launch proved the patch-quality gate can fire, but
  was stopped as shakedown because step records inherited a stale partial
  hypothesis `proposal_session_ref` on the code-stage block path. Fresh field
  gate launch/debug report:
  [`../../experiments/v0.4/v04-warehouse-validation-transfer-patch-quality-rerun6r-launch-20260616.md`](../../experiments/v0.4/v04-warehouse-validation-transfer-patch-quality-rerun6r-launch-20260616.md).
  The follow-up cache-coherency fix invalidates per-branch proposal-session-ref
  cache entries when newer agentic session refs are recorded. Relaunch the
  formal field gate only after that fix is committed and pushed.
- Later copied-config data-root fallback evidence reached validation and
  field-accepted the path-safety/config repair, but not warehouse research
  quality. The focused audit/prep report is
  [`../../experiments/v0.4/v04-warehouse-swaporders-cvrp-focused-diagnostic-prep-20260617.md`](../../experiments/v0.4/v04-warehouse-swaporders-cvrp-focused-diagnostic-prep-20260617.md).
  The `swap_orders` validation failure was classified as real mechanism
  insufficiency: W/T/L `8/1/6`, median delta `-200`,
  `VALIDATION_FAIL_NO_HIERARCHICAL_GAIN`, `split_delta_sum=0`, and about
  `1.025` median runtime ratio. The accepted repair is problem-owned context
  feedback only; no generic Decision, Protocol threshold, or
  `DecisionFeatures` behavior changed.
- CVRP follow-up should now use exact-case no-LLM diagnostics before any long
  LLM campaign. `scion/tools/cvrp_mechanism_matrix.py` supports repeatable
  `--case-id`, allowing targeted synchronized WSL runs on P-family local wins
  and high-gap canonical cases without a full 16-case rerun.
- The focused exact-case WSL matrix from commit `70dfc53` completed `60/60`
  rows and is recorded at
  [`../../experiments/v0.4/v04-cvrp-focused5-mechanism-wsl-70dfc53-postrun-20260617.md`](../../experiments/v0.4/v04-cvrp-focused5-mechanism-wsl-70dfc53-postrun-20260617.md).
  It rejected broad `alns_only` or `size70_two_opt_candidate` replacement
  (`4/15/1` overall vs canonical, median delta `7.0`). The only replicated
  local signal is `P-n76-k4` (`3/4` wins, median delta `-4.0`, no route/fleet
  regression). Before any CVRP LLM campaign, deepen `P-n76-k4` fixed-candidate
  seed diagnostics and compare phase telemetry against high-gap `CMT2` /
  `M-n151-k12` loss shapes.
- The deeper `P-n76-k4` 20-seed WSL diagnostic from commit `14c2a34` completed
  `60/60` rows and is recorded at
  [`../../experiments/v0.4/v04-cvrp-p76-deepseed-wsl-14c2a34-postrun-20260617.md`](../../experiments/v0.4/v04-cvrp-p76-deepseed-wsl-14c2a34-postrun-20260617.md).
  It rejects the earlier `P-n76-k4` local-win pocket as unstable:
  `alns_only` and `size70_two_opt_candidate` were both `6/14/0` versus
  canonical with median delta `+8.0`. Do not launch a CVRP LLM campaign from
  this pocket. The next CVRP work should separate initial-VNS, embedded-VNS,
  size70/two-opt fallback, and pure ALNS/no-polish variants, with explicit VNS
  before/after objective telemetry.
- Warehouse validation-transfer acceptance-contract guidance repair is locally
  accepted and recorded at
  [`../../experiments/v0.4/v04-warehouse-validation-transfer-acceptance-contract-repair-20260617.md`](../../experiments/v0.4/v04-warehouse-validation-transfer-acceptance-contract-repair-20260617.md).
  It is problem-owned context/code-constraint guidance only: split-positive
  moves are preferred; split-preserving cost-only moves require computed
  `split_delta == 0`, `cost_delta > 0`, exported deltas, and a no-op fallback.
  No generic Decision, Protocol threshold, validation/frozen gate, or
  `DecisionFeatures` behavior changed. Next warehouse proof is one short
  agentic `6R` behavior check, not a broad matrix.
- CVRP VNS variant/telemetry diagnostic repair is locally accepted and recorded
  at
  [`../../experiments/v0.4/v04-cvrp-vns-variant-telemetry-repair-20260617.md`](../../experiments/v0.4/v04-cvrp-vns-variant-telemetry-repair-20260617.md).
  Default matrices remain the existing three mechanisms; explicit
  `--mechanism` selection now supports initial-VNS disabled, embedded-VNS
  disabled, and pure ALNS/no-polish probes, with VNS objective before/after
  telemetry. Next CVRP proof is a WSL no-LLM variant matrix before any CVRP LLM
  campaign.
- The WSL CVRP VNS variant matrix from commit `6d742c6` completed `80/80` rows
  and is recorded at
  [`../../experiments/v0.4/v04-cvrp-vns-variant-matrix-wsl-6d742c6-postrun-20260617.md`](../../experiments/v0.4/v04-cvrp-vns-variant-matrix-wsl-6d742c6-postrun-20260617.md).
  It rejects broad VNS removal again but identifies conditional initial-VNS
  scheduling as the first promising CVRP mechanism clue. Before an agentic CVRP
  campaign, run a deeper canonical-vs-`initial_vns_disabled` seed matrix and
  derive a case-general trigger from objective probes rather than case ids.
- The deeper canonical-vs-`initial_vns_disabled` WSL matrix from commit
  `6d742c6` completed `160/160` rows and is recorded at
  [`../../experiments/v0.4/v04-cvrp-initial-vns-deepseed-wsl-6d742c6-postrun-20260617.md`](../../experiments/v0.4/v04-cvrp-initial-vns-deepseed-wsl-6d742c6-postrun-20260617.md).
  It rejects disabling initial VNS as a stable standalone mechanism. The next
  CVRP plan should use objective probes to form an early-ALNS starvation or
  budget-pressure scheduling hypothesis; do not launch an LLM campaign from a
  simple initial-VNS disable rule.
- CVRP scheduler-local budget/iteration instrumentation is locally accepted and
  recorded at
  [`../../experiments/v0.4/v04-cvrp-scheduler-iteration-telemetry-repair-20260617.md`](../../experiments/v0.4/v04-cvrp-scheduler-iteration-telemetry-repair-20260617.md).
  The compact WSL validation matrix from commit `875dc83` is complete and
  recorded at
  [`../../experiments/v0.4/v04-cvrp-scheduler-instrumentation-compact-wsl-875dc83-postrun-20260617.md`](../../experiments/v0.4/v04-cvrp-scheduler-instrumentation-compact-wsl-875dc83-postrun-20260617.md).
  It completed `60/60` no-LLM rows and accepted the instrumentation path:
  `alns_iteration_trace`, `alns_core`, `vns_initial`, and `vns_embedded`
  fields are preserved with expected mechanism coverage. Mechanism result:
  broad embedded-VNS removal is rejected. It increases mean ALNS iterations
  from `4.0` to `22.4` but remains worse/tied overall (`2/8/10`, mean delta
  `+17.3`); pure ALNS/no-polish is worse (`2/18/0`, mean delta `+35.6`). The
  next CVRP slice should test adaptive embedded-VNS scheduling/triggering and
  expose that problem-owned opportunity summary to proposal context before any
  agentic CVRP campaign.
- The adaptive embedded-VNS scheduling probe is locally accepted and recorded
  at
  [`../../experiments/v0.4/v04-cvrp-adaptive-embedded-vns-probe-repair-20260617.md`](../../experiments/v0.4/v04-cvrp-adaptive-embedded-vns-probe-repair-20260617.md).
  It adds `adaptive_embedded_vns_cadence4`: initial VNS remains enabled,
  embedded VNS runs every fourth ALNS iteration, and embedded VNS still runs
  when the repaired candidate already improves current/best before polish.
  Canonical behavior is unchanged by default. Local validation passed
  py_compile, focused runtime/matrix tests (`20 passed`), broader related
  tests (`35 passed`), and a one-second P-n76 smoke (`2/2`) showing the probe
  can reduce embedded-VNS runtime at equal distance.
- The compact WSL canonical-vs-adaptive matrix from commit `dd5b17a` is
  complete and recorded at
  [`../../experiments/v0.4/v04-cvrp-adaptive-embedded-vns-compact-wsl-dd5b17a-postrun-20260617.md`](../../experiments/v0.4/v04-cvrp-adaptive-embedded-vns-compact-wsl-dd5b17a-postrun-20260617.md).
  It completed `40/40` no-LLM rows and is accepted as mechanism evidence but
  rejected as a production candidate. `adaptive_embedded_vns_cadence4` reduces
  whole-matrix embedded-VNS runtime share from `0.651` to `0.361` and raises
  mean ALNS iterations from `4.0` to `8.35`, but paired quality is still worse
  overall (`4/7/9`, mean delta `+6.95`). Next CVRP gate is adaptive-trigger
  tuning, not an agentic campaign from cadence-4 as-is.
- The local adaptive-trigger variant repair is accepted and recorded at
  [`../../experiments/v0.4/v04-cvrp-adaptive-trigger-variants-repair-20260617.md`](../../experiments/v0.4/v04-cvrp-adaptive-trigger-variants-repair-20260617.md).
  It adds `adaptive_embedded_vns_cadence2` and
  `adaptive_embedded_vns_improve_only` without changing canonical defaults,
  generic Decision, Protocol, lifecycle, promotion, or `DecisionFeatures`.
  Local validation passed py_compile, focused runtime/matrix tests
  (`20 passed`), broader related tests (`35 passed`), `git diff --check`, and
  a one-second P-n76 smoke (`3/3`). Next gate is the compact WSL no-LLM
  adaptive-trigger matrix.
- The compact WSL adaptive-trigger matrix from commit `eddaf8c` is complete and
  recorded at
  [`../../experiments/v0.4/v04-cvrp-adaptive-trigger-compact-wsl-eddaf8c-postrun-20260617.md`](../../experiments/v0.4/v04-cvrp-adaptive-trigger-compact-wsl-eddaf8c-postrun-20260617.md).
  It completed `80/80` no-LLM rows. `adaptive_embedded_vns_cadence2` is
  accepted as a problem-owned proposal-context opportunity source, not as a
  production default: embedded-VNS share dropped from `0.653` to `0.528`, mean
  ALNS iterations rose from `4.0` to `6.0`, median paired delta stayed `0.0`,
  and mean delta was `+1.8` with no route/fleet regressions. Next CVRP gate is
  proposal-context wiring plus a short agentic refinement campaign; do not
  launch broad VNS removal or default-solver cadence changes from this evidence.
- The CVRP adaptive-trigger proposal-context repair is locally accepted and
  recorded at
  [`../../experiments/v0.4/v04-cvrp-adaptive-trigger-proposal-context-repair-20260617.md`](../../experiments/v0.4/v04-cvrp-adaptive-trigger-proposal-context-repair-20260617.md).
  Hypothesis prompts now receive the cadence-2 opportunity as problem-owned,
  proposal-only guidance, and hypothesis context now consumes the
  solver-design prompt provider as code context already did. No Decision,
  Protocol, lifecycle, promotion, `DecisionFeatures`, or canonical default
  behavior changed. Next gate is a short CVRP agentic refinement campaign.
- The WSL CVRP agentic `4R` campaign from commit `7104928` completed and is
  recorded at
  [`../../experiments/v0.4/v04-cvrp-adaptive-trigger-agentic-4r-7104928-postrun-20260617.md`](../../experiments/v0.4/v04-cvrp-adaptive-trigger-agentic-4r-7104928-postrun-20260617.md).
  It is positive framework behavior evidence: wrapper `exit_code=0`, `4/4`
  effective screening rounds, `4` Protocol rows, `3` formal candidate
  artifacts, `19` `gpt-5.5` traces, a depth-3
  `route_limit_aware_regret_repair` branch, same-mechanism weak-signal
  continuation, and a loss-heavy clean fork that was abandoned. It is not
  cadence-2 trigger-refinement evidence because the live hypothesis prompts
  did not include the cadence-2 opportunity text. A local prompt-provider-ref
  resolver repair now restores problem-owned provider guidance after agentic
  context sanitization, and the CVRP provider wording now treats cadence-2 as
  the explicit exception to the older warning against blind scheduler tweaks;
  focused provider/context/prompt tests pass. Next gate: rerun a short targeted
  CVRP campaign and inspect live traces before interpreting candidate
  mechanisms.
- The warehouse validation-transfer acceptance-contract short behavior gate
  from commit `ce5d884` completed on WSL. Launch:
  [`../../experiments/v0.4/v04-warehouse-validation-transfer-contract-rerun6r-launch-20260617.md`](../../experiments/v0.4/v04-warehouse-validation-transfer-contract-rerun6r-launch-20260617.md).
  Postrun:
  [`../../experiments/v0.4/v04-warehouse-validation-transfer-contract-rerun6r-postrun-20260617.md`](../../experiments/v0.4/v04-warehouse-validation-transfer-contract-rerun6r-postrun-20260617.md).
  It used one warehouse production `6R` cell with full proposal context,
  measurement governance `on`, disabled early stop, WSL-local `gpt-5.5`, and
  no stage-drain/retry/session-timeout override. The result is valid and
  complete: wrapper `exit_code=0`, `6/6` effective rounds, `8` Protocol rows,
  stage counts `screening=5`, `validation=2`, `frozen=1`, `6` proposal
  quality blocks, `0` verification-consumed failures, and champion v2
  promoted. It field-accepts the warehouse acceptance-contract behavior and is
  positive v0.4 warehouse recovery evidence, while preserving the caveat that
  this single `6R` cell does not prove continuous long-run promotion. The
  promoted `pack_compatible_vehicles` operator is a split-preserving
  cost-compression mechanism, so the next measurement-declaration cleanup
  should distinguish useful `cost_delta_sum` effects from zero
  `split_delta_sum` diagnostics.

## V0.5 Hand-Off

Only after v0.4 exits R1-R5 should v0.5 run the larger value-analysis program:

- governance ablation: full Scion vs naive vs mid-profile;
- reproduction matrices across warehouse, VRP/variant, and a third problem;
- prompt/context ablations;
- branch-depth and mechanism-trajectory studies;
- final kill/survive criteria for Scion's value claim.
