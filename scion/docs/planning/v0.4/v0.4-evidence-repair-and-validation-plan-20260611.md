# Scion v0.4 Evidence Repair And Validation Plan

*Date: 2026-06-11*
*Branch: `codex/v04-evidence-repair-plan`*
*Status: v0.4 framework repairs are accepted enough for focused follow-up. The
current repair slice is design-first framework work rather than CVRP-specific
tuning: Designs A-K are implemented and focused-tested, CVRP/warehouse
launchers call problem-owned guidance providers while retaining legacy
prepared-manifest compatibility, and generic scheduler/target-intent authority
blockers have accepted current-run validation roots. Designs L/M are now
implemented on the synchronized local/WSL worktrees: budget-exhausting runtime
aggregates stay raw-evidence only for proposal-visible slowdown semantics, and
budget-exhausting low/cached/insufficient runtime evidence no longer creates
clean-fork pressure or stale fresh-runtime branch-card follow-up. Local conda
`claw` and WSL conda `scion` focused validation both pass (`108 passed`), and
local/WSL sync commits are recorded in git. Current short diagnostics should
use the server conda `claw` environment until WSL is available again. The
local gpt-5.5 proxy is authenticated after the 2026-06-28 Codex relogin;
`/v1/models` lists `gpt-5.5`, and a `gpt-5.5` chat completion returns HTTP
200. No additional proxy restart was required in the latest check. Generic CVRP/warehouse launcher resume handling now
quarantines copied terminal artifacts under `run_root/resume_snapshot/`,
exposes `resume_snapshot_ref`, lets prepared prompt-context readiness read
copied status/summary from the declared snapshot as historical resume context,
preserves that snapshot reference in running/preflight-failure status, and
reports explicit in-flight Protocol running state without inflating
completed-only counters. Warehouse has shown
fresh effective research movement from champion `v2` to `v3`; the clean
post-repair v2 positive-control rerun is now plateau-review-ready rather than
continuous-promotion evidence, and its plateau postrun report is recorded in
`scion/docs/experiments/v0.4/v04-warehouse-v2-positive-plateau-postrun-20260623.md`.
Postrun acceptance recheck stability is repaired: the checker now uses the
stored inventory artifact declared by the rebuild manifest before falling back
to live inventory rebuild, so normal checkout advancement no longer creates a
false historical prepared-contract git mismatch.
CVRP rank-gap, route-pressure,
forced-local, required-intra-two-opt, missing-primary, scheduler-status,
target-intent authority, solver-depth follow-up, and current-sync large-twoopt
roots are current-run-ready rejection or repair evidence, not solver
improvements. The current-sync root from WSL commit `d3efc3cb` is clean
framework research evidence: valid/complete, postrun-ready, 7 screening
Protocol metric rows, 0 active-slot blocks, branch depth 5, same-mechanism
follow-up 8/8, and budget-exhausting runtime saturation kept observational. It
is also solver-negative: champion stayed `v1`, all rows were below MDE, and
direct large-twoopt evidence was not ready. CVRP still lacks promotion or
positive-at-MDE solver progress.
The active design/repair boundary remains
`scion/design/v0.4-effective-research-repair-design.md`; keep CVRP and
warehouse semantics problem-owned and out of generic scheduling/projection
code. Next design work should split oversized postrun/readiness helpers into
named ports and add problem-owned CVRP/VRP opportunity providers rather than
more CVRP-specific generic gates. Design basis:
`scion/design/v0.4-postrun-readiness-and-opportunity-ports.md`. See
`scion/docs/status/current-state.md`. Design N skeleton is implemented in
`scion.postrun`, and `check_postrun_acceptance.py` has a compatibility adapter
for typed readiness summaries. Design O initial slice is implemented as
`scion.measurement.MeasurementConsumerView`, with `ProtocolConfig` consuming
the typed view, and proposal-context measurement diagnostics now consume it as
well. Prepared CVRP/warehouse measurement handoff builders also consume the
typed view, and postrun research-efficiency artifact fallback uses it for
copied calibration artifacts; local/WSL postrun/measurement focused tests pass
(`54 passed` each). Individual check migration and problem-owned
review/opportunity providers remain open. Design P proposal-context wiring is
now present as `scion.opportunity` plus
`scion.problems.cvrp.opportunity.CvrpOpportunityProvider`: the typed summary is
collected through adapter/context-manager hooks, rendered as bounded proposal
context, classified as prompt-manifest `research_signal`, and aggregated in
postrun proposal-visibility reports as report-only section visibility. CVRP
postrun review now adds a problem-owned opportunity usage summary over
structured proposal fingerprints, keeping use/contrast/ignore interpretation
out of generic core and out of DecisionFeatures. Designs Q/R are now
live-validated on a fresh no-resume CVRP root:
`/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-designqr-codeprompt-fresh-23f24bca-data-2r-gpt55-20260625T140430Z-claw`.
The code prompt rendered `Opportunity Evidence Commitment`, postrun visibility
reported the code section visible with zero summary-without-section drops, and
postrun acceptance was current-run-ready. The run is solver-negative and
checklist-unproven, so the next CVRP/VRP work is problem-owned evidence quality
and required-checklist follow-through, not generic core gates. The
seed-family review alignment is now implemented locally: recomputing the
proof-status mirror recognizes
`large_instance_intra_route_two_opt_seed` activation/objective/phase telemetry,
keeps positive-at-MDE and CMT protection separate, and changes the required
evidence proof from `not_ready` to `unproven`.*
*The CMT follow-through repair also projects the remaining
`missing_cmt_case_protection_evidence` into the CVRP code-phase opportunity
commitment with explicit CMT2/CMT4 case-level `total_distance` delta
requirements.*
*Current local head `0c0afd9b` repairs prepared successor focus propagation,
default-avoid target-intent rejection inheritance into formal hypothesis
prompts, and over-broad target-file token matching without adding CVRP-specific
generic logic. The focused prepared-successor/proposal-pipeline suite,
target-intent/schema/opportunity suite, and rejected-binding/authority suite
pass locally (`58`, `91`, and `21` tests). The first local bounded
local-search successor root is valid/postrun-ready but solver-negative, so the
current local repair adds problem-owned successor evidence/review rather than
generic gate growth: `cvrp_successor_summary` reviews non-two-opt successor
families from measurement-effect top rows and continuity signals, and
`cvrp_opportunity_usage_summary.v2` carries per-family required-evidence
proofs. The current guidance slice makes the first bounded successor explicit
prepared guidance: `bounded_2node_cross_exchange` is now a reviewed successor
id and exact default-avoid entry, while `bounded_local_search_variant` remains
available only for materially new bounded-local-search causal paths. The
successor2 probe then exposed a generic proposal-loop false block: rejected
target intents were still treated as binding, and broad `variants`
default-avoid matching could reject a target on one generic token. The local
repair keeps rejected target intents out of formal binding and narrows
single-token broad matches to specific tokens. The repaired successor2
verification root from commit `19811b02` then reached formal Protocol
screening and finished postrun-ready, so the proposal-loop repair is accepted
for this slice. The candidate `intra_route_or_opt_reinsert` is solver-negative
but evidence-clean: win-rate `0.25`, median delta `-0.75`, CI high below MDE,
CMT2/CMT4 both `1` win and `3` losses, and no promotion. CVRP successor review
now maps that exact mechanism to `bounded_local_search_variant`, marks its
checklist `proven`, and records outcome `measured_no_positive_at_mde` without
blocking the whole family. The successor3 construction-seed verification root
from commit `b430c646` then selected
`rotated_sweep_seed_tournament` in `construction.py`, finished
valid/complete and postrun-ready with 1 effective screening row and 32/32
valid pairs, but remained solver-negative and checklist-unproven:
`SCREENING_TELEMETRY_FAILED` due missing activation under the declared
mechanism id, champion `v1`, promotions `0`, rows at or above MDE `0`. The
current CVRP-owned repair makes `construction_seed_portfolio` a first-class
successor family and maps rotated sweep aliases in successor and opportunity
review. The successor4 destroy/repair verification root from commit
`6a50fcba` then selected `angular_sector_removal` in `destroy_repair.py`,
finished valid/complete and postrun-ready with 2 effective screening rows, 2
formal candidate artifacts, no proposal quality blocks, and no active-slot
blocks. It proved the `destroy_repair_selection` evidence checklist but
remained solver-negative: champion `v1`, promotions `0`, rows at or above MDE
`0`, win rate `0.25` in both rows, median deltas `-3.25` and `0.0`, both CI
highs below MDE. The next slot should not repeat unchanged
`angular_sector_removal`; it should use a materially different destroy/repair
causal path or construction seed work with same-run seed-baseline/accepted-delta
evidence. Successor5 from commit `7c6e3ea5` then verified this guidance can
avoid the reviewed angular-sector path: `radial_string_removal` was abandoned
as solver-negative, and `bounded_intra_route_3opt` produced weak-positive
low-SNR evidence below MDE and remains the active follow-up branch after a
CVRP-owned postrun alias repair.*

This plan translates the 2026-06-09 and 2026-06-11 audits into v0.4 work.
v0.5 should run broad controlled experiments; v0.4 must first make those
experiments scientifically interpretable and restore meaningful VRP/warehouse
research capability.

## Current Execution Status

This section is a short execution snapshot, not a repair ledger. Current
operational truth lives in
[`../../status/current-state.md`](../../status/current-state.md). Detailed
launch/readiness/postrun evidence lives in
[`../../experiments/v0.4/`](../../experiments/v0.4/), and curated milestone
history lives in [`../../status/v0.4-history.md`](../../status/v0.4-history.md).

- v0.4 framework/readiness/launcher repairs are accepted enough for focused
  warehouse and CVRP follow-up, but v0.4 is not closed until Scion demonstrates
  effective research behavior.
- Designs L/M have been synchronized to WSL and focused-tested on both local
  and WSL conda environments (`108 passed` each). The clean current-sync CVRP
  root below accepts their runtime-semantics behavior for continuation/rejection
  evidence, but not solver improvement.
- Design K is WSL-validated through follow-up commit `650d9c65`. The old-checkout
  CVRP solver-depth root from WSL commit `65115459` finished current-run-ready
  and valid/complete with 6 of 6 effective Protocol rows, but showed
  `wide_shallow` continuity, max branch depth 1, no promotions, all rows below
  MDE, and missing direct large-two-opt signal. Treat it as branch-local
  mechanism integration evidence, not proof that the solver idea itself is
  weak.
- The follow-up CVRP solver-depth root
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-solverdepth-mechfollowup-readyfix-6r-gpt55-20260623T115013Z-claw`
  finished valid/complete and postrun-ready from old launch commit `9b1db176`,
  and was mirrored to
  `/home/clawd/research/scion-experiments/v04-cvrp-solverdepth-mechfollowup-readyfix-6r-gpt55-20260623T115013Z-claw`.
  It shows materially better research continuity than the previous solver-depth
  root: 6 of 6 effective Protocol rows, 0 proposal quality blocks, 0 active-slot
  blocks, max branch depth 4, 4 of 4 observed same-mechanism follow-ups
  selected, and no research-context actionability gaps. It remains
  solver-negative and caveated: champion stayed `v1`, there were 0 promotions,
  all rows were below MDE, postrun interpretation is
  `protocol_evaluated_without_large_twoopt_signal`, and launch/readiness reports
  the checkout changed while the process was live. Use it as live-run research
  evidence, not clean current-sync acceptance.
- The clean current-sync CVRP continuation
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-current-sync-d3efc3cb-postsolverdepth-6r-gpt55-20260623T182433Z-claw`
  finished valid/complete and postrun-ready from WSL commit `d3efc3cb`, and was
  mirrored to
  `/home/clawd/research/scion-experiments/v04-cvrp-current-sync-d3efc3cb-postsolverdepth-6r-gpt55-20260623T182433Z-claw`.
  It completed 6 of 6 effective-budget rounds and 7 screening Protocol metric
  rows, with 2 proposal quality blocks and 0 active-slot blocks. It accepts the
  repaired CVRP continuation/rejection loop for this slice: max branch depth 5,
  same-mechanism follow-up 8/8, no research-context actionability gaps, no
  missing required prompt/source target visibility, and runtime saturation
  remained observational under `budget_exhausting`. It is not solver progress:
  champion stayed `v1`, promotions were `0`, every effect row was below MDE,
  direct large-twoopt evidence was not ready, and postrun interpretation is
  `protocol_evaluated_without_large_twoopt_signal`. Detailed report:
  [`../../experiments/v0.4/v04-cvrp-current-sync-large-twoopt-postrun-20260624.md`](../../experiments/v0.4/v04-cvrp-current-sync-large-twoopt-postrun-20260624.md).
  Current decision: stop adding generic gates for this failure shape. The next
  v0.4 step is design-first cleanup of postrun/readiness ports plus a
  problem-owned CVRP/VRP solver-opportunity provider. Design basis:
  [`../../../design/v0.4-postrun-readiness-and-opportunity-ports.md`](../../../design/v0.4-postrun-readiness-and-opportunity-ports.md).
  The Design N skeleton now exists as `scion.postrun`, and the Design O
  measurement consumer view now feeds protocol config plus proposal-context
  measurement diagnostics plus prepared measurement handoff builders; postrun
  research-efficiency fallback also uses the typed view. Current
  postrun/readiness checks still need migration behind typed ports/views.
  The Design P schema/provider skeleton now exists, and the typed opportunity
  summary is wired into proposal-context rendering, prompt-manifest visibility,
  postrun section-visibility aggregation, and CVRP-owned postrun usage
  interpretation over structured proposal fingerprints.
- The fresh no-resume CVRP Design Q/R root
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-designqr-codeprompt-fresh-23f24bca-data-2r-gpt55-20260625T140430Z-claw`
  finished valid/complete and postrun-ready from isolated WSL worktree commit
  `23f24bca`, and was mirrored to
  `/home/clawd/research/scion-experiments/v04-cvrp-designqr-codeprompt-fresh-23f24bca-data-2r-gpt55-20260625T140430Z-claw`.
  It validates Designs Q/R for live code-prompt relay and postrun visibility:
  the code prompt rendered `Opportunity Evidence Commitment` for
  `large_instance_intra_route_two_opt_seed`, postrun visibility reported
  `code_section_visible_trace_count=1`,
  `commitment_summary_trace_count=1`, and both summary-without-section counts
  `0`. It remains solver-negative and checklist-unproven: 2 of 2 effective
  rounds, 2 screening Protocol rows, 0 quality blocks, max branch depth 2,
  same-mechanism follow-up 1/1, champion `v1`, 0 promotions, 0 positive rows,
  0 rows at or above MDE, and opportunity usage
  `checklist_unproven`. Detailed report:
  [`../../experiments/v0.4/v04-cvrp-designqr-codeprompt-postrun-20260625.md`](../../experiments/v0.4/v04-cvrp-designqr-codeprompt-postrun-20260625.md).
  Current decision: the relay works; the next CVRP/VRP repair should improve
  problem-owned opportunity evidence and required-checklist follow-through,
  not add CVRP-specific generic gates.
- The first local required-checklist follow-through slice is implemented:
  CVRP large-twoopt postrun summaries emit problem-owned
  `evidence_requirement_statuses`, CVRP opportunity usage consumes them as
  `required_evidence_proof`, and prompt-facing provider status no longer
  treats missing positive-at-MDE outcome evidence as missing checklist proof.
  Focused local conda `claw` validation passes (`15 passed`), with
  `py_compile` and `git diff --check` clean.
- The WSL proof-status follow-up root
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-proofstatus-followup-05ade2e0-2r-gpt55-20260625T155106Z-claw`
  finished valid/complete and postrun-ready from WSL runtime commit
  `05ade2e0`. It validates the live proof-status carrier but remains
  solver-negative and checklist-unproven: 2 screening rows, champion `v1`,
  promotions `0`, positive rows `0`, rows at or above MDE `0`,
  same-mechanism follow-up `2/2`, and opportunity usage
  `checklist_unproven`. The seed-family review alignment repair is now
  implemented locally and documented in
  [`../../experiments/v0.4/v04-cvrp-seed-family-review-alignment-20260626.md`](../../experiments/v0.4/v04-cvrp-seed-family-review-alignment-20260626.md):
  recomputing the local mirror recognizes the prepared seed family and
  activation/objective/phase telemetry, changes required evidence proof from
  `not_ready` to `unproven`, and leaves CMT protection plus positive-at-MDE
  solver outcome as the remaining evidence gaps.
- The CMT commitment follow-through repair is implemented locally and
  documented in
  [`../../experiments/v0.4/v04-cvrp-cmt-commitment-followthrough-20260628.md`](../../experiments/v0.4/v04-cvrp-cmt-commitment-followthrough-20260628.md):
  the next code-phase opportunity commitment for
  `large_instance_intra_route_two_opt_seed` should carry
  `missing_cmt_case_protection_evidence` plus explicit CMT2/CMT4 case-level
  `total_distance` delta requirements.
- The first local CVRP bounded local-search successor run
  `/home/clawd/research/scion-experiments/v04-cvrp-successor-7a4590a7-local-1r-gpt55-20260628T111147Z-claw`
  finished wrapper-valid/complete and postrun-ready from runtime commit
  `7a4590a7`. It validates repaired successor-focus propagation and clean
  successor branch routing: target intent selected
  `bounded_2node_cross_exchange` in `bounded_local_search_variant`, not the
  reviewed large-twoopt seed or acceptance variants. It is solver-negative:
  1 of 1 effective Protocol row, 32 of 32 valid screening pairs, 10 wins,
  14 losses, 8 ties, median delta `0.0`, champion `v1`, promotions `0`, rows
  at or above CVRP MDE `0`, CI high below MDE `1`, and max effect/MDE
  `-0.10101`. Current decision: the framework can route materially different
  successor attempts; the current local repair now reviews that bounded
  successor through `cvrp_successor_summary` as checklist `proven` but
  `measured_no_positive_at_mde`. The next prepared attempt should use this
  problem-owned evidence to test a materially different destroy/repair or
  construction successor, or a stronger bounded-local-search causal path
  explicitly distinct from `bounded_2node_cross_exchange`, not add generic core
  gates. The follow-up successor2 root prepared cleanly but failed before
  Protocol rows with a circuit breaker after three rejected-target binding
  mismatches; treat it as proposal-loop repair evidence, not solver evidence.
- The repaired local successor2 verification
  `/home/clawd/research/scion-experiments/v04-cvrp-successor2-19811b02-local-1r-gpt55-20260628T123904Z-claw`
  finished valid/complete and postrun-ready from commit `19811b02`. It
  validates the rejected-target/default-avoid proposal-loop repair with one
  effective Protocol screening row and no quality blocks or readiness
  failures. The candidate `intra_route_or_opt_reinsert` is solver-negative
  but evidence-clean: win-rate `0.25`, median delta `-0.75`, CI high `7.5`
  below MDE, CMT2/CMT4 both `1` win and `3` losses, champion stayed `v1`, and
  promotions `0`. Current CVRP successor review maps this exact mechanism to
  `bounded_local_search_variant`, marks required evidence `proven`, records
  outcome `measured_no_positive_at_mde`, and adds the exact mechanism id to
  reviewed/default-avoid prepared guidance.
- The clean local successor3 construction-seed verification
  `/home/clawd/research/scion-experiments/v04-cvrp-successor3-b430c646-local-1r-gpt55-20260628T133031Z-claw`
  finished valid/complete and postrun-ready from commit `b430c646`. It
  selected `rotated_sweep_seed_tournament` in `construction.py`, produced one
  effective screening row with 32 of 32 valid pairs, and had no proposal
  quality blocks. It is solver-negative and checklist-unproven: champion
  stayed `v1`, promotions `0`, rows at or above MDE `0`, top effect row
  `win_rate=0.0`, `median_delta=0.0`, `ci_high=0.0`, and branch decision
  `SCREENING_TELEMETRY_FAILED` because the declared mechanism activation was
  not observed. Current CVRP review now maps this construction seed path to
  `construction_seed_portfolio` and opportunity usage reports it as
  `opportunity_evidence_checklist_unproven`, not `no_structured_match`.
  Detailed report:
  `scion/docs/experiments/v0.4/v04-cvrp-construction-successor-review-20260628.md`.
- The clean local successor4 destroy/repair verification
  `/home/clawd/research/scion-experiments/v04-cvrp-successor4-6a50fcba-local-2r-gpt55-20260628T142639Z-claw-2r-gpt55-20260628T142639Z-claw`
  finished valid/complete and postrun-ready from commit `6a50fcba`. It
  selected `angular_sector_removal` in `destroy_repair.py`, produced two
  effective screening rows and two formal candidate artifacts, and had no
  proposal quality blocks or active-slot blocks. It is solver-negative but
  evidence-clean: `cvrp_successor_summary` marks `destroy_repair_selection`
  checklist `proven`, outcome `measured_no_positive_at_mde`, with direct
  activation/objective/phase and CMT2/CMT4 evidence observed; champion stayed
  `v1`, promotions `0`, rows at or above MDE `0`, both rows had win rate
  `0.25`, median deltas `-3.25` and `0.0`, and both CI highs were below MDE.
  Detailed report:
  `scion/docs/experiments/v0.4/v04-cvrp-destroy-repair-successor-review-20260628.md`.
- The clean local successor5 verification
  `/home/clawd/research/scion-experiments/v04-cvrp-successor5-7c6e3ea5-local-2r-gpt55-20260628T155743Z-claw-2r-gpt55-20260628T155743Z-claw`
  finished valid/complete and postrun-ready from commit `7c6e3ea5`, with 2/2
  effective rows, 2 formal candidates, no proposal quality blocks, and no
  active-slot blocks. It avoided unchanged `angular_sector_removal`: round 1
  `radial_string_removal` was abandoned as solver-negative, while round 2
  `bounded_intra_route_3opt` produced weak-positive low-SNR evidence below MDE
  and remains active for a CMT2-protected follow-up. A CVRP-owned alias repair
  maps `bounded_intra_route_3opt` to `bounded_local_search_variant` in postrun
  successor/opportunity review. Detailed report:
  `scion/docs/experiments/v0.4/v04-cvrp-successor5-3opt-followup-review-20260628.md`.
- Warehouse has a historical positive research-path checkpoint:
  `/home/xjy-ubuntu/research/scion-experiments/v04-wh-v2-33f0e976-transfer-6r-gpt55-20260621T183412Z-claw`
  completed 6 effective rounds and promoted from champion `v2` to `v3`. The run
  also exposed prompt/source visibility audit gaps, so it is superseded for
  current acceptance by the clean v2 positive-control root below.
- The clean warehouse v2 positive-control rerun
  `/home/xjy-ubuntu/research/scion-experiments/v04-wh-v2-positive-2f8e9f21-current-8r-gpt55-20260623T161630Z-claw`
  finished valid/complete and postrun-ready from WSL status/runtime commit
  `2f8e9f21`, and was mirrored to
  `/home/clawd/research/scion-experiments/v04-wh-v2-positive-2f8e9f21-current-8r-gpt55-20260623T161630Z-claw`.
  It is current clean effective-research evidence and a plateau-review
  candidate, not continuous-promotion evidence: starting/current champion stayed
  `v2`/`v2`, champion version gain was `0`, 8 of 8 effective Protocol rows
  completed, every effect row was below MDE, postrun readiness had no failures,
  and warehouse interpretation is `protocol_evaluated_plateau_review_ready`.
  Research behavior is strong: max branch depth 8, active shape `deep_focused`,
  same-mechanism follow-up 11/11, and no research-context actionability gaps.
  Plateau postrun report:
  [`../../experiments/v0.4/v04-warehouse-v2-positive-plateau-postrun-20260623.md`](../../experiments/v0.4/v04-warehouse-v2-positive-plateau-postrun-20260623.md).
  Current decision: this is enough for restored warehouse effective-research
  and plateau-review readiness in v0.4 framework evidence. Do not launch
  another warehouse root by default; one narrow repeat is optional only for
  independent solver-level plateau confirmation.
- Postrun acceptance stored-inventory recheck repair:
  [`../../experiments/v0.4/v04-postrun-acceptance-stored-inventory-recheck-20260623.md`](../../experiments/v0.4/v04-postrun-acceptance-stored-inventory-recheck-20260623.md).
  Local and WSL postrun acceptance focused tests pass (`86 passed` each), and
  the warehouse positive-control root rechecks with
  `current_run_analysis_ready=true` from `stored_postrun_inventory`.
- Post prompt/source-visibility warehouse probe
  `/home/xjy-ubuntu/research/scion-experiments/v04-wh-v2-306fc271-postrepair-6r-gpt55-20260622T005300Z-claw`
  passed strict launch readiness and live prompt/source evidence under the
  patched checker, but was manually stopped after 5 effective rounds and 313
  quality blocks because alternating proposal-quality failures bypassed the old
  consecutive-only repeat guard. The guard is fixed at local commit `11ba7898`
  / WSL commit `7bd1a42c`; rerun warehouse from that commit or later before
  accepting a plateau conclusion.
- Quality-loop guard verification root
  `/home/xjy-ubuntu/research/scion-experiments/v04-wh-v2-84a6d0d0-qloopfix-6r-gpt55-20260622T013700Z-claw`
  stopped after 3 repeated quality blocks with
  `last_stop_reason=repeated_quality_block_signature`, proving the guard no
  longer runs away. It also exposed stale partial-hypothesis recovery after
  quality feedback; local commit `621b9604` / WSL commit `43ac9935` now forces
  a fresh APS hypothesis when prior quality blocks are present.
- Warehouse APS retry root
  `/home/xjy-ubuntu/research/scion-experiments/v04-wh-v2-60029d30-apsretry-6r-gpt55-20260622T014615Z-claw`
  finished naturally from WSL commit `60029d30` with wrapper exit `0`,
  postrun readiness exit `0`, and postrun acceptance `ready`. It produced 3
  effective rounds, 3 protocol-evaluated candidates, 5 screening rows, 5
  quality blocks, 0 promotions, and `champion_version=2`, then stopped
  fail-closed on `repeated_quality_block_signature`. This verifies the repaired
  live prompt/source, repeated quality-block, and APS quality-feedback paths,
  but is partial plateau/quality-guidance evidence rather than a warehouse
  promotion result. Rebuilding it with the current actionability classifier
  reports empty actionability gaps, so its old same-mechanism gap is not a live
  framework blocker.
- CVRP has current-run-ready complete post-repair evidence:
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-twoopt-2e1bc5ae-postrepair-4r-gpt55-20260622T021910Z-claw`
  launched from WSL commit `2e1bc5ae`, finished with wrapper/postrun exit `0`,
  validity `valid`, completeness `complete`, 4 effective rounds, 4 screening
  rows, 0 quality blocks, 0 promotions, and champion `v1`. It demonstrated
  same-mechanism `rank_gap_annealing_acceptance` follow-up to branch depth 4,
  source visibility, expanded screening, and MDE-aware fail-closed rejection.
  It still lacks solver improvement or promotion: both aggregate-positive
  32-pair screens reversed or weakened under 48-pair expansion, all rows were
  below MDE, and final protected-case behavior was negative on CMT2/CMT3.
- CVRP route-pressure follow-up evidence:
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-nextmech-1aae436c-postrankgap-4r-gpt55-20260622T041502Z-claw`
  launched from WSL commit `1aae436c`, resumed from the rank-gap root, and
  finished current-run-ready with wrapper/postrun exit `0`, validity `valid`,
  completeness `complete`, 4 effective rounds, 4 screening rows, 0 quality
  blocks, 0 promotions, and champion `v1`. It did not follow the bounded
  large-instance two-opt opportunity: all current rows were
  `route_pressure_acceptance`, expanded 48-pair rows had only `+8` and `+5`
  net raw delta, protected CMT cases were neutral, all rows were below MDE, and
  postrun analysis reported `missing_large_twoopt_mechanism_signal`. This is
  framework-valid rejection evidence, not a solver improvement.
- CVRP default-avoid guard probe:
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-nonaccept-443b1a51-postroutepressure-4r-gpt55-20260622T073501Z-claw`
  passed strict launch readiness from WSL commit `443b1a51`, but the first
  proposal still targeted `policies/baseline_modules/acceptance.py` with
  `distance_scaled_sa_reheat`. The run was stopped before Protocol rows. This
  proves prompt-only `default_avoid_directions` are not sufficient; the local
  worktree now enforces prepared default-avoid directions in
  `proposal.schema_preview` through proposal-only `launch_research_focus`.
- First guarded CVRP relaunch:
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-nonaccept-guard-24b609de-postroutepressure-4r-gpt55-20260622T075205Z-claw`
  passed strict launch readiness from WSL commit `24b609de`, then failed
  closed before Protocol rows with `last_stop_reason=circuit_breaker`, 3
  proposal quality blocks, and `invalid_no_effective_rounds`. This verifies the
  pre-Protocol default-avoid blocker, but also exposed over-broad phrase
  matching over narrative route/seed/VNS words. The follow-up local patch now
  requires multi-token avoid phrases to hit candidate identity fields.
- Tightened-guard CVRP relaunch:
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-nonaccept-tightguard-93a3b3c8-postroutepressure-4r-gpt55-20260622T080005Z-claw`
  passed strict launch readiness from WSL commit `93a3b3c8`, then failed
  closed before Protocol rows with
  `last_stop_reason=repeated_quality_block_signature`, 3 proposal quality
  blocks, and `invalid_no_effective_rounds`. This verifies the tightened
  pre-Protocol default-avoid blocker, but also proves that unconstrained prompt
  guidance still does not make the agent choose bounded local search.
- CVRP scheduler-status and target-intent authority validation are now accepted
  as generic framework evidence, not solver progress. The clean
  scheduler-status root completed 4 of 4 effective rounds with 0 active-slot
  blocks. The target-intent authority validation root
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-authority-542d1f99-postweakpressure-4r-gpt55-20260623T055230Z-claw`
  finished current-run-ready with wrapper/postrun exit `0`, postrun acceptance
  `ready`, 4 effective Protocol rows, 0 proposal quality blocks, 0 active-slot
  blocks, and no promotion. It proves branch-local weak-positive follow-up can
  cross target intent, formal hypothesis/code, and Protocol instead of cycling
  between target-intent binding and same-mechanism guards. The remaining CVRP
  gap is solver direction quality and effective research behavior, not a need
  for VRP-specific core exceptions. The accepted root's postrun
  actionability gaps were report-classifier false positives: accepted clean-fork
  policy choices now stay diagnostic rather than missed same-mechanism
  opportunities, and voluntary branch-lesson usage no longer creates semantic
  gaps without an active requirement or explicit semantic failure/block.
- Current operational roots are listed in
  [`../../status/current-state.md`](../../status/current-state.md). Older
  prepared/live roots are superseded trajectory evidence only; do not use them
  as current v0.4 acceptance unless current-state names them as current.
- Latest prompt/source visibility repair is local commit `774c981d` / WSL
  commit `a9a537c4`: active-subject code constraints are no longer
  prompt-truncated, cross-branch/branch-lesson sections are accounted as
  cross-branch signal, and target-source visibility is required only when a
  target-intent/source requirement exists. These fields remain proposal/postrun
  audit material and excluded from `DecisionFeatures`.
- Low nonzero proposal/APS caps now fail launch readiness; exact `0` remains
  the explicit disabled state for focused v0.4 roots. Fresh-runtime replay
  drain also uses explicit `0` to disable and must be present in launch
  artifacts rather than hidden behind the core legacy default. Stage-transition
  drain is explicit positive `4` in the current roots.
- Prompt-context readiness now summarizes prepared renderer evidence in launch
  readiness detail, including CVRP CMT2/CMT4/resume-continuity/required-evidence,
  warehouse champion-v2/required-evidence, and active code-constraint checks. It
  explicitly reports `raw_provider_prompt_rendered=false`, so first live
  provider prompts remain postlaunch evidence.
- Prepared measurement handoff keeps `measurement_readiness` reduced and
  summary-only, while the sibling `calibration` block carries source artifact
  `sha256` and compact `calibration_run` provenance for auditability. Prepared
  prompt-context readiness now proves that this provenance reaches the
  proposal-only research-focus bridge.
- Nominal no-effect/tie runtime summaries stay report-only in proposal
  context; generic bounded/top-k runtime guidance now requires actual
  comparative slowdown, runtime failure, or runtime budget saturation.
- Active no-effect branch-card guidance stays aligned with same-mechanism
  follow-up: ordinary no-effect/tie evidence does not emit runtime-saturated
  diversity or clean-fork redirect. Cross-branch repeated-signature pressure
  preserves current active no-effect diagnostic follow-up, and portfolio
  no-effect plateau lessons expose the same current-branch diagnostic allowance
  while still blocking unchanged sibling copies; true runtime
  regression/saturation still can emit runtime diversity guidance.
- Scheduler-depth repair keeps ordinary active no-effect/marginal low-signal
  branches schedulable for same-mechanism follow-up, and avoids
  scheduler-origin parked-lineage blocks unless the branch is truly ineligible,
  quality-regressive, or already parked by Decision.
- Search memory uses hard-failure counts for global AVOID; repeated ordinary
  no-effect/tie screening memory remains diagnostic near-field context rather
  than an exhausted-family directive.
- Current preserved guarantees are summarized in
  [`../../status/current-state.md`](../../status/current-state.md); detailed
  repair evidence belongs in [`../../experiments/v0.4/`](../../experiments/v0.4/).
  Keep this section short when future roots supersede the current pair.
- Completed CVRP forced-local root:
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-forced-local-eb2627e5-postroutepressure-4r-gpt55-20260622T081704Z-claw`.
  It launched from WSL commit `eb2627e5` with forced
  `solver_design` / `modify` /
  `policies/baseline_modules/local_search.py`, finished current-run-ready, and
  produced complete negative local-search evidence: 4 effective screening rows,
  0 quality blocks, 0 promotions, champion still `v1`, all rows below MDE, the
  refined `bounded_interroute_2opt_bridge` regressed, and
  `cmt_slack_aware_segment_swap` was abandoned. The launcher prepared focus now
  treats those failed local-search mechanisms as proposal-visible
  default-avoid directions. Detailed report:
  [`../../experiments/v0.4/v04-cvrp-forced-local-postroutepressure-postrun-20260622.md`](../../experiments/v0.4/v04-cvrp-forced-local-postroutepressure-postrun-20260622.md).
  The immediate next-local relaunch from WSL commit `6f40ebcb` verified the
  guard but failed closed before Protocol rows after default-avoid hits for
  `pure ALNS/no-polish`, `cross-route 2-opt reconnect`, and unchanged
  `bounded_interroute_2opt_bridge`. The current launcher focus repair narrows
  the next required prepared direction to
  `large_instance_intra_route_two_opt_seed` as a deadline-aware bounded
  local-search mechanism. Detailed loop report:
  [`../../experiments/v0.4/v04-cvrp-nextlocal-default-avoid-loop-20260622.md`](../../experiments/v0.4/v04-cvrp-nextlocal-default-avoid-loop-20260622.md).
  A follow-up WSL root from commit `4b7e78b7`
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-intratwoopt-4b7e78b7-postavoidloop-4r-gpt55-20260622T123924Z-claw`
  verified that prompt-visible `next_required_direction` alone still did not
  move the agent: it failed closed before Protocol rows after default-avoid
  repeats. The current repair adds
  `required_mechanism_ids=["large_instance_intra_route_two_opt_seed"]` plus a
  schema-preview guard requiring that id in `mechanism_changes`. Detailed
  report:
  [`../../experiments/v0.4/v04-cvrp-intratwoopt-required-direction-loop-20260622.md`](../../experiments/v0.4/v04-cvrp-intratwoopt-required-direction-loop-20260622.md).
  The next WSL root from commit `1e4c2dde`
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-requiredmech-1e4c2dde-postintratwoopt-4r-gpt55-20260622T124949Z-claw`
  verified the structured schema guard itself: all three attempts failed
  closed before Protocol rows on
  `launch_research_focus_required_mechanism`. This is not solver evidence; it
  exposed that launch-focus guard payloads were not yet converted into full
  in-session retry feedback. The follow-up repair added that feedback path and
  prompt projection before the next relaunch.
  The completed warehouse APS retry root is sufficient to verify repaired live
  prompt/source, quality-loop, and APS recovery behavior for the v0.4 baseline.
- Current warehouse positive-control root
  `/home/xjy-ubuntu/research/scion-experiments/v04-wh-v2-positive-65115459-current-8r-gpt55-20260623T084049Z-claw`
  finished wrapper/postrun-ready but stopped after 1 effective round with
  5 proposal quality blocks. Treat it as evidence for the Design H
  guidance-binding repair, not as plateau or promotion evidence.
- CVRP solver-depth old-checkout root
  `/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-solverdepth-65115459-postauthority-6r-gpt55-20260623T084213Z-claw`
  finished from WSL commit `65115459`, passed WSL postrun acceptance, and was
  mirrored locally. It is Design K trigger evidence, not solver progress.

## Current Task Split

1. Main thread owns v3 boundary alignment, task decomposition, acceptance
   criteria, status curation, git hygiene, and experiment launch decisions.
2. Keep WSL and local commits synchronized before launch; use conda Python
   `/home/xjy-ubuntu/miniconda3/envs/scion/bin/python` for readiness, tests, and
   live runs.
3. Current framework slice is committed locally and synced to WSL: Design A
   scheduling-status model, Design B research-guidance contract migration,
   Design H binding semantics, Design I launcher running status, Design J
   launcher lifecycle contract, and Design K mechanism-evidence/follow-up
   contract. Scheduler,
   active-slot inventory, and branch cards now share one problem-neutral
   interpretation. Prepared manifests now carry typed
   `ResearchGuidanceContract` payloads, and generic projection/readiness checks
   schema, proposal-only visibility, rendered-path coverage, and explicit
   hard-binding semantics rather than CVRP or warehouse strings. The generated
   `run.sh` outer wrapper is rendered from a problem-neutral lifecycle plan;
   CVRP/warehouse command construction and problem guards remain problem-owned.
4. Post-commit validation slice is complete through WSL follow-up code commit
   `650d9c65`: focused Design K/core, branch/card/telemetry,
   screening/protocol, launcher/guidance, launch/postrun tool, direct launcher
   entry, py-compile, and diff checks pass. Dirty
   worktree readiness failures before commit are expected and must not be
   bypassed for acceptance.
5. CVRP follow-up after commit: rerun a controlled validation launch from the
   intended clean baseline to prove the active-slot stop no longer recurs for
   generic resumed weak-positive/inactive branch shapes. Preserve forced or
   otherwise audited non-acceptance target control until unaided target
   selection is reliable, and keep CMT/protected case details problem-owned.
6. Warehouse follow-up should rerun the champion-`v2` positive-control path from
   the current clean WSL runner after the tested follow-up commit `650d9c65`.
   Do not interpret the latest quality-blocked positive-control root as a real
   plateau.
7. Subagents should receive bounded read/code/analysis scopes with the v3
   boundary requirement, exact files or artifacts, required tests, and concise
   evidence-backed deliverables.
8. Git management: keep commits sliced by logical track and do not mix
   unrelated CVRP actionability changes with generic framework repairs unless a
   test requires the combined behavior.

## Phase 2 Worker Brief Catalog

These briefs are execution slices for Phase 2 now that Phase 1 A/A is
complete. Every worker brief must begin by requiring the worker to read
`scion/design/scion-architecture-v3.md` and state how the change preserves the
v3 boundary.

### Worker A - Measurement Integration Closure (Completed)

Purpose: close integration gaps around problem-owned measurement declarations,
not reimplement the already-landed practical-delta path.

Completed state:

- `ProblemSpecV1.measurement` and `MeasurementSpec` already define
  `runtime_model`, `pairing_validity`, effect metric/unit, practical deltas,
  and calibration refs.
- `ProtocolConfig.with_problem_measurement()` already resolves problem-owned
  practical deltas and runtime model into protocol config.
- CVRP already declares `runtime_model: budget_exhausting`,
  `pairing_validity: trajectory_divergent`, `practical_delta_screen: 2.0`,
  and `practical_delta_validate: 1.0`.
- Formal CVRP and warehouse production assets now have regression coverage
  proving real problem declarations are consumed by real protocol configs.
- The `scion run` problem-v1 ingress path now has behavior coverage proving
  the resolved measurement-governed protocol config reaches `CampaignManager`,
  while `record-only` keeps readiness status without changing behavior fields.
- Warehouse production/package specs now use relative surrogate paths so local
  and WSL checkouts resolve the same calibration and canary assets.

Write scope:

- `scion/scion/config/protocol_config.py`
- `scion/scion/problem/spec.py`
- `scion/scion/problem/bridge.py`
- `scion/scion/cli/commands/init_run.py`
- tests only where needed under `scion/scion/tests/`

Acceptance status:

- Formal CVRP and warehouse production protocol loading consumes problem-owned
  measurement values, not synthetic-only fixtures.
- CLI campaign launch ingress consumes those values before constructing the
  campaign manager, with record-only governance covered as status-only.
- Measurement/calibration raw diagnostics do not enter `DecisionFeatures`.
- Focused verification:
  `pytest scion/scion/tests/test_config.py scion/scion/tests/test_problem_bridge.py scion/scion/tests/test_cvrp_formal_readiness.py scion/scion/tests/test_models.py`
  passed locally. Report:
  `scion/docs/experiments/v0.4/v04-measurement-integration-real-asset-coverage-20260618.md`.

### Worker B - Runtime Semantics Closure (Completed)

Purpose: finish evidence/feedback integration for comparative vs
budget-exhausting runtime semantics.

Completed state:

- Budget-exhausting runtime already suppresses fresh champion replay for cached
  runtime ties in the gate path.
- Runtime budget saturation already downgrades to info and clears actionable
  reason codes under `runtime_model: budget_exhausting`.
- V9 perf guard already treats budget-exhausting runtime as budget compliance.
- High aggregate `runtime_regression_rate` no longer blocks
  trajectory-divergent low-SNR screening expansion under
  `runtime_model: budget_exhausting`.
- Decision/lifecycle now treat aggregate `runtime_regression_rate` as
  report-only under `runtime_model: budget_exhausting`, so it does not
  soft-abandon branches or fragment repeated-signal signatures by itself.
- Prompt runtime feedback renders budget-exhausting aggregate regression as
  `not_applicable_budget_exhausting` and does not turn that summary into strong
  runtime actionability by itself.
- Prompt runtime feedback also keeps nominal no-effect/tie runtime summaries
  report-only, so the generic bounded/top-k hint is reserved for actual
  comparative slowdown, runtime failure, or runtime budget saturation.
- Proposal screening feedback preserves the raw aggregate regression rate for
  audit but does not classify zero-effect budget-exhausting screening as
  `runtime_regression`.
- The finalizer preserves lifecycle-selected low-signal `retain_head`
  workspaces for budget-exhausting solver evidence instead of discarding the
  branch head because of aggregate runtime saturation.

Write scope:

- `scion/scion/core/runtime_budget_diagnostics.py`
- `scion/scion/protocol/gates.py`
- `scion/scion/protocol/experiment/stages.py`
- `scion/scion/verification/perf_guard.py`
- `scion/scion/proposal/context_manager/runtime.py`
- `scion/scion/proposal/screening_feedback.py`
- `scion/scion/core/decision.py`
- `scion/scion/core/branch_lifecycle_policy.py`
- `scion/scion/core/decision_finalizer.py`

Acceptance status:

- Stage-level protocol results do not surface budget-exhausting saturation as
  actionable gate reason codes.
- Runtime feedback does not tell the agent to chase meaningless saturation on
  budget-exhausting anytime solvers.
- Quality-tie runtime speedup remains possible only from deterministic,
  non-regressive quality evidence and valid runtime evidence.
- Focused verification:
  `pytest scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/test_sprint_e2_context_runtime.py scion/scion/tests/unit/core/test_runtime_budget_diagnostics.py scion/scion/tests/test_verification_gate_integration.py`
  passed locally and in the WSL focused suite. Additional Decision/lifecycle
  verification:
  `pytest scion/scion/tests/test_decision_screening.py scion/scion/tests/unit/core/test_branch_lifecycle_policy.py scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/test_sprint_e2_context_runtime.py scion/scion/tests/unit/core/test_runtime_budget_diagnostics.py scion/scion/tests/test_decision_feature_extraction.py scion/scion/tests/test_models.py`
  passed locally and on WSL. Reports:
  `scion/docs/experiments/v0.4/v04-budget-exhausting-runtime-regression-semantics-repair-20260618.md`
  and
  `scion/docs/experiments/v0.4/v04-budget-exhausting-decision-lifecycle-runtime-semantics-repair-20260618.md`
  and
  `scion/docs/experiments/v0.4/v04-budget-exhausting-screening-feedback-runtime-tier-repair-20260618.md`
  and
  `scion/docs/experiments/v0.4/v04-budget-exhausting-finalizer-retain-head-repair-20260618.md`.

### Worker C - Low-SNR Expand And Lifecycle Depth (Completed)

Purpose: make low-SNR, trajectory-divergent research continue or expand when
evidence is inconclusive but not negative, while hard negatives still close.

Completed state:

- `pairing_validity` is resolved into `ProtocolConfig` from problem-owned
  measurement declarations.
- Screening gate and Decision can expand trajectory-divergent, tie-heavy or
  weak non-negative evidence below `0.5` aggregate win rate.
- Lifecycle policy is now open-ended only for measurement-governed
  `pairing_validity: trajectory_divergent` non-regressive low-SNR screening
  research, so fixed no-effect, repeated-signature, rollback-budget, and
  zero-win lifecycle counters do not park the branch before same-mechanism
  follow-up can happen.
- Branch cards and sibling projections preserve that same-mechanism guidance
  for ordinary no-effect branches instead of also emitting a runtime
  diversity/clean-fork redirect.
- Negative median delta, loss-heavy evidence, candidate runtime failure,
  verification failure, canary failure, timeout, and true runtime regression
  still fail closed. Default/trajectory-stable lifecycle behavior remains
  unchanged.

Completed write scope:

- `scion/scion/protocol/gates.py`
- `scion/scion/core/decision.py`
- `scion/scion/core/branch_lifecycle_policy.py`
- `scion/scion/core/scheduler.py` only if required

Acceptance status:

- For declared trajectory-divergent/low-SNR problems, high-tie or weak
  non-negative evidence can expand or continue without direct abandon.
- Negative median delta, loss-heavy evidence, candidate failure, infeasibility,
  timeout/crash, or runtime regression still fail close.
- Same-mechanism follow-up is possible for multiple low-SNR attempts so branch
  depth can grow beyond one-off candidates.
- Fixed low-signal lifecycle counters no longer truncate declared
  trajectory-divergent, non-regressive follow-up within the requested campaign
  rounds.
- Scheduler/result metadata now distinguishes the one allowed low-signal
  same-branch observation sample from true repair diagnostics by emitting
  `refine_active` for `same_branch_low_signal_observation_sample`.
- Proposal context now turns current active no-effect branch-local lessons into
  advisory `same_branch_refinement` requirements, preserving the lesson without
  hard-blocking code generation or leaking into Decision.
- Trajectory-divergent all-tie screening now follows the low-SNR expand path
  when objective/runtime evidence is non-regressive, closing the pure tie case
  that previously fell through to `SCREENING_FAIL_WIN_RATE`.
- Focused verification:
  `pytest scion/scion/tests/test_decision_screening.py scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/unit/core/test_branch_lifecycle_policy.py scion/scion/tests/unit/core/test_scheduler_runtime_evidence_pressure.py`
  passed as part of the integrated Phase 2 suite. Additional scheduler metadata
  verification:
  `pytest scion/scion/tests/unit/core/test_scheduler_runtime_evidence_pressure.py scion/scion/tests/unit/core/test_branch_step_runner_scheduler_metadata.py`
  passed locally. Report:
  `scion/docs/experiments/v0.4/v04-low-signal-same-branch-scheduler-slot-repair-20260618.md`.
  Additional lesson-usage verification:
  `pytest scion/scion/tests/unit/test_cross_branch_research.py scion/scion/tests/unit/core/test_branch_lesson_usage.py scion/scion/tests/unit/test_hypothesis_context_profiles.py`
  passed locally. Report:
  `scion/docs/experiments/v0.4/v04-low-signal-same-branch-lesson-usage-repair-20260618.md`.
  Additional all-tie low-SNR verification:
  `pytest scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/test_decision_screening.py scion/scion/tests/unit/core/test_branch_lifecycle_policy.py scion/scion/tests/unit/core/test_scheduler_runtime_evidence_pressure.py scion/scion/tests/unit/core/test_branch_step_runner_scheduler_metadata.py`
  passed locally. Report:
  `scion/docs/experiments/v0.4/v04-trajectory-divergent-all-tie-low-snr-expand-repair-20260618.md`.
  Additional open low-SNR lifecycle verification:
  `pytest scion/scion/tests/test_decision_screening.py scion/scion/tests/unit/core/test_branch_lifecycle_policy.py scion/scion/tests/test_decision.py scion/scion/tests/test_decision_validation_frozen.py scion/scion/tests/unit/core/test_decision_finalizer_park_lifecycle.py scion/scion/tests/test_scheduler.py scion/scion/tests/unit/core/test_branch_step_runner_scheduler_metadata.py scion/scion/tests/test_config.py scion/scion/tests/test_problem_bridge.py scion/scion/tests/unit/test_problem_measurement_artifacts.py scion/scion/tests/unit/test_measurement_readiness.py scion/scion/tests/test_protocol_stats_gates.py scion/scion/tests/unit/core/test_runtime_budget_diagnostics.py scion/scion/tests/unit/test_agentic_feedback_screening.py scion/scion/tests/unit/test_screening_feedback_tiers_memory.py scion/scion/tests/unit/core/test_campaign_control_lifecycle_budget_runtime.py scion/scion/tests/unit/core/test_retry_round_accounting.py scion/scion/tests/unit/core/test_proposal_pipeline_hypothesis.py scion/scion/tests/unit/core/test_decision_finalizer_lifecycle.py scion/scion/tests/unit/core/test_evaluation_orchestrator_telemetry.py scion/scion/tests/unit/core/test_scheduler_runtime_evidence_pressure.py scion/scion/tests/unit/test_branch_followup_policy.py scion/scion/tests/unit/test_branch_prompt_projection.py`
  passed locally and on WSL. Report:
  `scion/docs/experiments/v0.4/v04-trajectory-divergent-open-low-signal-lifecycle-20260619.md`.

### Worker D - Context Signal Density (Completed)

Purpose: improve proposal-visible research signal without moving diagnostics
into Decision.

Compressible material:

- Raw cross-branch audit/session metadata.
- Long `branch_lesson_usage_requirement` boilerplate.
- Repeated runtime saturation/low-confidence boilerplate.
- Generic governance rules, duplicate "Do Not Claim" text, and raw tool
  observation payloads when compact summaries exist.

Write scope:

- `scion/scion/proposal/context_manager/*`
- `scion/scion/proposal/engine/hypothesis_context_profiles.py`
- `scion/scion/proposal/engine/hypothesis_prompts.py`
- `scion/scion/proposal/prompt_manifest_accounting.py`

Acceptance status:

- Prompt manifests show improved problem-domain research-signal density.
- Hypothesis prompt cross-branch maps and branch-lesson context render
  mechanism-level distilled signals with explicit `omitted_*`/digest audit
  markers, not raw long lesson prose, raw rows, or large branch/case
  enumerations.
- Hypothesis prompt runtime feedback renders bounded screening/verification
  proposal guidance with explicit omitted-line and omitted-char digest markers,
  not raw long runtime or telemetry-like strings.
- Postrun artifact inventories expose prompt signal-density evidence as a
  separate Phase 4 coverage requirement instead of bundling it under generic
  prompt/source visibility.
- Proposal context receives measurement/noise/opportunity diagnostics only as
  tainted problem-owned diagnostics excluded from `DecisionFeatures`.
- Validation/frozen per-case details remain hidden from proposal context.
- Focused verification:
  `pytest scion/scion/tests/unit/test_hypothesis_context_profiles.py scion/scion/tests/unit/test_prompt_manifest_accounting.py scion/scion/tests/unit/test_cross_branch_research.py`
  passed as part of the integrated Phase 2 suite.
  Inventory coverage verification:
  `pytest scion/scion/tests/test_postrun_artifact_inventory.py scion/scion/tests/test_postrun_analysis_brief.py scion/scion/tests/test_rebuild_postrun_acceptance.py`
  passed locally. Report:
  `scion/docs/experiments/v0.4/v04-prompt-signal-density-coverage-repair-20260618.md`.

### Worker E - Source Visibility Protection (Completed)

Purpose: make context compression phase-aware so it cannot remove the research
object code the agent must edit or understand.

Protected material:

- Hypothesis phase: champion/current branch research code, problem object,
  solver mechanics, active algorithm facts, research surface/interface.
- Code phase: approved target file source, required integration file source,
  API manifest, source digests, editable/frozen patterns, and grounding needed
  to write a valid patch.

Write scope:

- `scion/scion/proposal/engine/code_prompts.py`
- `scion/scion/proposal/target_intent_binding.py`
- `scion/scion/proposal/prompt_manifest_visibility.py`
- `scion/scion/proposal/agentic_session_budget_runtime.py`
- `scion/scion/proposal/agentic_session_code_tools.py`

Acceptance status:

- Code-phase prompt manifests explicitly prove target source and required
  integration source are present after compression.
- Prepared prompt-context readiness explicitly proves active problem-owned code
  constraints reach the rendered code prompt with provider item counts,
  version/subject identity, problem-specific guard markers, and
  `DecisionFeatures` exclusion evidence.
- Code-phase required target/surface source tool reads are not skipped by final
  preview slot reservation while hard tool-loop capacity remains.
- Focused launchers pass explicit code-phase APS tool-call headroom, and launch
  readiness verifies it across `launch.env`, manifest execution, manifest
  command, and `run.sh`.
- Compression may remove generic governance text, duplicated raw logs, and
  raw cross-branch payloads, but not source/code material needed to modify the
  champion or current branch.
- Focused verification:
  `pytest scion/scion/tests/unit/test_agentic_solver_design_prompt_payloads.py scion/scion/tests/unit/test_agentic_target_file_grounding.py scion/scion/tests/unit/test_agentic_session_tool_selection.py`
  passed as part of the integrated Phase 2 suite.
  Additional slot-reserve regression:
  `pytest scion/scion/tests/unit/test_agentic_session_tool_selection.py scion/scion/tests/unit/test_agentic_session_model_planner.py scion/scion/tests/unit/test_agentic_target_file_grounding.py`
  passed locally on 2026-06-19.

### Worker F - Calibration Tooling Evidence Closure (Completed)

Purpose: make A/A calibration artifacts replayable and diagnostic enough to
support Phase 1 conclusions and later measurement-aware governance.

Completed state:

- `tools/calibrate_aa_noise.py` can produce `scion.aa_noise_floor.v1` with MDE,
  false-pass rate, power curve, and per-case summaries.
- CVRP formal calibration exposed a tooling gap: the initial CLI did not reuse
  the declared problem data root wiring used by campaign launch, so the legacy
  uniform-60s run required a copied split with local `safe_data_roots`.
- The repaired CLI wires declared problem data roots and can use
  `--runtime-policy protocol_time_limits`.
- The repaired payload persists pair evidence, candidate seeds, elapsed
  runtime, selected case/seed lists, replicate count, seed offset, bootstrap
  samples, runtime policy, and case-resolution evidence.
- A/A pair evidence now records explicit champion/candidate runtime budget-hit
  ratios and flags, closing the earlier elapsed-runtime-only caveat.

Completed write scope:

- `scion/tools/calibrate_aa_noise.py`
- `scion/scion/measurement/aa_calibration.py`
- `scion/scion/cli/commands/data_roots.py` only if reuse helpers need a small
  public wrapper
- `scion/scion/config/protocol_config.py` only if calibration needs an existing
  runtime time-limit resolver exposed
- tests under `scion/scion/tests/unit/test_aa_calibration.py`,
  `scion/scion/tests/unit/test_cli_data_roots.py`, and
  `scion/scion/tests/test_cvrp_formal_readiness.py`

Acceptance status:

- CVRP formal A/A can resolve `cvrplib/...` cases through declared problem data
  roots without manually copying or editing the split manifest.
- The artifact records each pair's case, ledger seed,
  candidate seed, replicate, outcome, delta, raw delta, candidate/champion
  values, elapsed runtime, runtime budget-hit flags, and case resolution.
- The top-level payload records selected cases, selected seeds, replicate
  count, seed offset, bootstrap samples, selected surface, runtime policy, and
  safe data roots used.
- Calibration can optionally use protocol runtime time-limit rules so CVRP
  large-case screening budgets match campaign execution.
- The artifact remains `problem_owned_measurement_diagnostic` and explicitly
  excluded from `DecisionFeatures`.
- Focused verification:
  `pytest scion/scion/tests/unit/test_aa_calibration.py scion/scion/tests/unit/test_cli_data_roots.py scion/scion/tests/test_cvrp_formal_readiness.py`
- Final artifact:
  `/home/clawd/research/scion-experiments/v04-phase1-aa-cvrp-screening-modify-r3-protocoltime-20260611T191356Z-claw/aa_noise_floor.json`.
- Residual-caveat repair report:
  `scion/docs/experiments/v0.4/v04-aa-calibration-runtime-budget-hit-evidence-20260618.md`.

## Governing Constraints

- Preserve the v3 boundary: LLM output is tainted, and Decision reads only
  deterministic `DecisionFeatures`.
- Keep CVRP/VRP semantics problem-owned. BKS, gap, case hardness, runtime model,
  and measurement readiness may guide proposals and preflight, but must not
  become generic Decision inputs.
- Do not treat "no promotion" as a framework failure without the 2026-06-09
  audit checklist: copied configs, prompt visibility, pair-level metrics,
  Protocol evidence, safe features, and branch state must be reconciled.
- Runtime-improvement promotion remains a valid VRP research mode when quality
  is tied or non-regressive and runtime evidence is complete and materially
  better.

## V0.4 Repair Tracks

### R1 Measurement And Practical Delta

Goal: know what each problem/protocol can detect before spending long rounds.

- Add problem-owned measurement/readiness declarations for effect scale,
  practical deltas, pairing validity, runtime model, and calibration refs.
- Replace the hard-coded `ProtocolConfig.min_practical_delta == 0.001` with
  resolved problem/protocol values.
- Build A/A noise-floor calibration for CVRP and warehouse using champion
  replays, producing MDE and false-pass estimates.
- Surface measurement diagnostics in preflight/status/proposal context, not
  `DecisionFeatures`.

Exit criteria:

- CVRP and warehouse have full calibration reports. This criterion is now met
  for Phase 1; Phase 2 must consume those reports rather than rerun blind gate
  tuning.
- CVRP 6/10 screening failures are explained as below measured MDE, not only
  by aggregate win-rate labels.

### R2 Runtime Governance For Budget-Exhausting Solvers

Goal: make anytime ALNS/VNS runtime evidence meaningful.

- Add `runtime_model: comparative | budget_exhausting` semantics.
- In `budget_exhausting`, treat saturation as expected info unless there is a
  timeout/crash or budget violation.
- Disable meaningless `RUNTIME_TIE_FRESH_CHAMPION_REQUIRED` replay paths for
  budget-exhausting tie cases, including stale persisted marker/pressure paths.
- Do not let comparative runtime-ratio slowdown checks block
  trajectory-divergent low-SNR expansion/follow-up under a budget-exhausting
  runtime model.
- Preserve quality-tie runtime speedup promotion when candidate/champion quality
  is non-regressive and runtime evidence is fresh enough for the declared model.
- Mark saturated runtime-regression-rate style fields as not-applicable when
  they compare millisecond jitter near the time limit.

Exit criteria:

- Focused tests cover both comparative and budget-exhausting runtime semantics,
  including V9 budget-compliance behavior, proposal feedback tiering, finalizer
  workspace retention, stale fresh-runtime replay marker suppression, and
  budget-exhausting low-SNR runtime-ratio suppression.
- CVRP reruns no longer spend effective rounds on runtime replay that cannot
  add decision evidence.

### R3 Branch Depth And Mechanism Continuity

Goal: Scion should pursue a research direction deeply enough to learn.

- Add branch-depth distribution and mechanism-family x effect summaries to
  campaign status/summary.
- Adjust low-signal lifecycle defaults so marginal/non-regressive evidence can
  drive same-mechanism follow-up rather than immediate churn to shallow sibling
  branches.
- Keep clear regressions and boundary/runtime failures fail-closed.
- Ensure next-hypothesis context shows branch-local failures, mechanism ids,
  and follow-up constraints as concise research guidance.

Exit criteria:

- Post-run analysis can reconstruct branch depth, mechanism continuity, and
  whether follow-up hypotheses used prior evidence.
- A focused campaign demonstrates at least one branch with meaningful depth
  beyond one-off attempts without violating v3 Decision boundaries.

### R4 Context Signal Density

Goal: the agent sees enough research signal without drowning in governance logs.

- Profile hypothesis/code prompts by block category: source, governance,
  tool observations, branch-local history, runtime/screening feedback, and
  cross-branch lessons.
- Compact cross-branch maps and runtime feedback into mechanism-level guidance.
- Render branch lessons and cross-branch guidance as compact mechanism-level
  signals with explicit omitted-detail counts/digests instead of raw long
  lesson prose or large branch/case enumerations.
- Render runtime feedback as compact screening/verification proposal guidance
  with explicit omitted-detail counts/digests instead of raw long runtime or
  telemetry-like strings.
- Keep full artifacts for audit, but avoid rendering duplicate raw logs as
  default model-visible context.
- Do not remove active solver facts, target source, branch state, or feedback
  merely to reduce token count.

Exit criteria:

- Prompt profile reports show improved research-signal ratio.
- Focused prompt tests confirm compact research signals are visible before
  broader rules, safety invariants remain present, and prompt manifests report
  research-signal/governance ratios. End-to-end campaign smoke remains a follow-up.

### R5 Focused VRP/Warehouse Validation

Goal: v0.4 must still show meaningful research on current problem families.

- After R1-R4, run focused CVRP/VRP campaigns that explicitly test:
  quality improvement, quality-tie runtime speedup, and branch-depth research.
- Revisit warehouse under the current framework: verify whether it can produce
  repeated promotions or whether stagnation has a measurement/context cause.
- Audit every run with the 2026-06-09 guide and pair-level metrics.

Exit criteria:

- Either VRP produces valid improvement/runtime evidence, or the report
  quantifies why the current protocol cannot detect it and what variant is
  needed.
- Warehouse has a reproducibility/stagnation analysis, not just a one-run
  promotion anecdote.

Current status:

- This plan file keeps the current execution snapshot, repair tracks, and
  acceptance criteria only. Detailed launch/postrun chronology is intentionally
  not maintained here.
- Warehouse postrun analysis briefs now distinguish prepared-only launch
  roots, quality-blocked proposal behavior, protocol-evaluated evidence with
  missing review inputs, positive at-or-above-MDE follow-up opportunities, and
  plateau-review readiness; the prepared champion-v2 follow-up still needs a
  real launched run before the R5 stagnation conclusion can close. Postrun
  readiness keeps that distinction interpretation-aware: taxonomy-backed
  quality-blocked no-protocol conclusions can be current-run-analysis-ready,
  but protocol-evaluated plateau conclusions still require
  plateau-consistent measurement-effect with CI high below MDE for all protocol
  effect rows, review-ready runtime feedback, and substantive continuity
  evidence without fully missed same-mechanism follow-up opportunities.
- CVRP postrun analysis briefs now expose quality-blocked proposal evidence for
  taxonomy-backed no-protocol two-opt conclusions, with readiness rejecting the
  conclusion when failure-taxonomy quality-block evidence is missing.
- Quality-blocked no-protocol readiness now also rejects warehouse/CVRP summaries
  when the problem summary or protocol-accounting review input reports any
  protocol-evaluated candidate.
- Problem-summary readiness now recomputes the expected warehouse/CVRP
  `interpretation`, `evidence_gaps`, `review_axes_actionability`, and
  launch-required flags from current review-input summaries before delegated
  review. Stale positive, plateau, two-opt, or gap-pruned conclusions no longer
  pass by shape alone.
- Launcher wrappers now turn strict postrun rebuild/readiness failure into an
  effective wrapper failure and record the postrun acceptance outcome in
  top-level `run_status.json`. Postrun readiness also rejects missing or
  nonzero root/campaign wrapper status and top-level postrun acceptance failure
  markers before delegated review, including status-writer failure markers that
  would otherwise leave stale clean status JSON behind.
- Research-continuity and measurement-effect briefs now expose branch-depth
  distribution, active shape, mechanism-family effect summaries, and
  branch-lesson semantic diagnostics, so R3/R4 review can distinguish deep
  follow-up from shallow branch scattering, family-level no-effect patterns,
  and failed prior-evidence transfer. Postrun briefs also join prompt
  block-family signal with continuity gaps through
  `research_context_actionability_summary`, so delegated review can audit
  whether missed transfer/follow-up aligns with missing research or cross-branch
  context instead of treating it as generic agent failure. The same summary now
  carries branch-lesson semantic failure/block reason mixes, separating
  metadata-only payloads, unrecognized linkage aliases, and true semantic
  mismatches for delegated review. Live hypothesis prompts now carry compact
  proposal-only research-shape diagnostics before broader rules, closing the
  loop from postrun branch-depth audit back into the next proposal context.
- Resume prepared handoffs now expose quarantined source-campaign branch cards
  as bounded `resume_snapshot.top_branches` in the report-only analysis brief
  and inventory. The successor6 prepared handoff rebuild verifies the active
  weak-positive 3-opt follow-up branch is visible from the copied snapshot
  without treating copied campaign artifacts as current-run evidence.
- Current CVRP successor runs show restored effective-research execution but
  not promotion-grade solver progress. Successor15-18b found, refined, and then
  parked the `granular_savings_seed_portfolio` construction branch; the best
  successor18b granular row remained below MDE (median delta `5.0`, effect/MDE
  `0.505`, no positive-at-MDE row), and the follow-up
  `exact_short_route_polish` was loss-heavy quality-regression evidence. Champion
  is still `v1`. The generic prepared successor-focus scheduler repair now has
  two layers: reviewed branch-local mechanism ids are suppressed for prepared
  successor clean-fork selection, and problem-provided
  `suppressed_mechanism_ids` can also exclude twice-inactive mechanisms such as
  `seed_post_optimization_selector` without labeling them as reviewed
  no-positive evidence or adding CVRP semantics to core/DecisionFeatures. The
  suppression is mechanism-granular: an entire branch is superseded only when
  all visible branch mechanism ids are reviewed/suppressed; mixed branches with
  a non-excluded mechanism remain schedulable under proposal authority guards.
  CVRP problem guidance now treats `granular_savings_seed_portfolio` and
  `exact_short_route_polish` as reviewed/default-avoid evidence, while
  `seed_post_optimization_selector` remains only a suppressed activation-repair
  target.
- Current operating truth lives in
  [`../../status/current-state.md`](../../status/current-state.md); the curated
  milestone index lives in
  [`../../status/v0.4-history.md`](../../status/v0.4-history.md).
- Detailed evidence remains in
  [`../../experiments/v0.4/`](../../experiments/v0.4/) and git history. Restore
  older chronology from those sources only when exact legacy provenance is
  needed.

## V0.5 Hand-Off

Only after v0.4 exits R1-R5 should v0.5 run the larger value-analysis program:

- governance ablation: full Scion vs naive vs mid-profile;
- reproduction matrices across warehouse, VRP/variant, and a third problem;
- prompt/context ablations;
- branch-depth and mechanism-trajectory studies;
- final kill/survive criteria for Scion's value claim.
