# Scion v0.4 W3 H11 official test-authority canonicalization acceptance

*Date: 2026-07-22*
*Status: H11 official canonicalization accepted; acceptance commit `7623077f`
is integrated into `origin/v0.4-dev` by exact fast-forward. No solver experiment
or privileged launch is authorized by this acceptance.*

## Accepted fixed inputs

| responsibility | SHA-256 | commit |
| --- | --- | --- |
| canonicalization design | `a6b86d71c53e2ed220f9b4ee283d59311e2a1f93263746a21940bb7e4763d2ce` | `c5ce6bffef8a7442914c9c47abc6a5121179c1d5` |
| production installer | `c048f3cf943173db76307029c3ed98faf54edf2d9db53d3ee30430b70c9b1043` | `87fae4c79c9f81f7613d04c9a4cba734f216fa7c` |
| implementation oracle | `15bd5ff9b0203944e9f7be106e7ef8c10a52e3a6c1d2ed69737194cf049292e1` | `46ca056e428afb077801ddb9a742f38d1570e935` |
| official fixture tests | `641067ca1406b6e89d1c57e6928dfcf74a3552a90ef6857d112df6124d17e5d0` | `96ebb938356151355a4436c5b692929a5674c7cc` |

The design manifest contains exactly 177 unique old parameter nodes and the
implemented responsibility split is exact:

```text
P  91
TL  8
OF 33
OR 16
D  29
```

All 99 `P/TL` node IDs remain collected. None of the 78 `OF/OR/D` old node
IDs remain collected. C2d contains exactly the 21 retained `P:SAME` nodes; its
12 oracle-owned and three obsolete-private nodes are absent. No removed
Session/Closure/Permit type, staging plan, private Flow field, compatibility
wrapper, retry, timeout, polling, sleep, `O_NONBLOCK`, skip or xfail was added.

## Executed evidence

Every authoritative run used `-W error`, a fresh basetemp and no pytest cache:

- Python 3.12 implementation oracle: exact `456 passed`;
- Python 3.13 implementation oracle: exact `456 passed`;
- Python 3.12 public C2a surface: exact `74 passed`;
- Python 3.13 public C2a surface: exact `74 passed`;
- Python 3.12 full official collection: exact `863`;
- Python 3.12 full official execution: exact
  `862 passed / 1 skipped`;
- Stage 1 C1+C2b checkpoint: exact
  `893 collected / 820 passed / 72 failed / 1 skipped`;
- Stage 2 C2c checkpoint: exact
  `878 collected / 841 passed / 36 failed / 1 skipped`;
- Stage 3 C2d checkpoint: exact
  `863 collected / 862 passed / 1 skipped`;
- `py_compile` and `git diff --check`: pass.

The oracle also freezes the exact reachable scanner total `2088`, with
`1039` internal, `899` pure-value and `150` sensitive edges; unresolved,
multi-target and unclassified totals remain zero and both fixed ledgers are
unchanged. Production retains the exact two live transaction tuples, `2/3`
global loads and the five transaction observation boundaries. Writer open,
`fstat` and proof `OSError` values have the exact `InstallerError` wrapper and
original cause; non-`OSError` values preserve identity.

## Review closure

- the fixed design and its three specialty reviews close at
  `P0=0 / P1=0 / P2=0 / P3=0`;
- production and oracle fixed-hash reviews close at
  `P0=0 / P1=0 / P2=0 / P3=0`;
- the first official fixed-hash review rejected SHA `316d9702...e81192ea`
  with three P1 evidence gaps: one private Flow observation, incomplete cause
  assertions and incomplete artifact-position assertions;
- those gaps were corrected without changing collection count, production or
  oracle, and the final fixed-hash review of
  `641067ca...d17e5d0` closes at
  `P0=0 / P1=0 / P2=0 / P3=0`;
- the independent main acceptance pass reproduced the exact node manifest,
  prohibition scans and all final test counts.

The accepted official tests observe public `H11RootAuthorizationFlow` state,
returned or raised values, exact artifacts and frames, and real physical file
descriptor generations. They do not claim removed private topology. C2d
freezes both endpoint schedules, one write and EOF, the positive plus seven-bit
transaction matrix, exact prewrite/write-ambiguous/postwrite states, primary
identity/cause/note ABI, artifact position and close-once evidence.

## Scope after acceptance

This closes H11 official canonicalization. It does not authorize a Warehouse
W3 or CVRP F1 launch. Those experiments remain locked by their separately
frozen external authority, hash-to-exec, guardian, source and dry-root gates.
The accepted feature head was pushed cleanly and acceptance commit `7623077f`
entered `origin/v0.4-dev` only by exact fast-forward.
