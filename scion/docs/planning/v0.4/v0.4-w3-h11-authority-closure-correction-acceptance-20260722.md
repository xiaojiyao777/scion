# Scion v0.4 W3 H11 authority-closure correction acceptance

*Date: 2026-07-22*
*Status: correction accepted; full H11 official canonicalization and branch
merge remain open; no privileged launch or solver experiment is authorized.*

## Accepted fixed inputs

| responsibility | SHA-256 | commit |
| --- | --- | --- |
| correction design | `bd2e2aaa1ac709822bd8d533804ee369ce5ac732fd3afeeb6193b706a2830541` | `fb81e116dd021c0b9acf18b7c1313adf7354d5d4` |
| production installer | `1d7ded027fbe84db73de41d3606cd06dd4c75cf42680edfd281b5601f29e9a0c` | `adb407dbac146dd6ae90524473ad3b6fdaba5044` |
| implementation oracle | `c7c535b5438f2805218d973561341b0f33caff35b3e9941e202022b596be9835` | `f64ecdf874c1e542a638c53df1cdc99f3e5b0d48` |
| official fixture tests | `12cbaef56a3c11781e353fbdb5875d7e0d3fbf977fc49f4069ab884bc37155ca` | `d5ce7a8a068470af8696dbc126f225e476ee8b33` |

The design closes exactly the harness root projections, present-role tuple,
canonical TREE source layout, complete retained-authority identity closure,
late present-output alias gate, and five transaction-phase non-`ENOENT`
exception boundaries. It adds no compatibility owner, generic stat wrapper,
retry, timeout, budget, truncation, skip or xfail.

## Executed evidence

- Python 3.12 implementation oracle: exact `456 passed`;
- Python 3.13 implementation oracle: exact `456 passed`;
- Python 3.12 public C2a surface: exact `74 passed`;
- Python 3.13 public C2a surface: exact `74 passed`;
- scoped official correction surface: exact `11 passed`;
- Python 3.12 full official diagnostic: exact
  `763 passed / 177 failed / 1 skipped`;
- full official collection: exact `941` tests;
- `git diff --check`: pass.

The oracle retains exact 71 domain axes, the 27-field AuthorityView and 71
global constructor stores. Its dynamic transaction matrix is exactly five
observation sites by seven roles. Every cell proves the exact
`InstallerError`, phase/role label, original `OSError` cause identity,
`FAILED_PREWRITE`, one sweep and zero permit commit frame. The existing raw
initial-source-open `OSError` boundary remains unchanged.

The official slice migrates only the ten confirmed legacy gaps and adds one
present-to-indirect-regular-source alias case. All eleven cases use the real
public `H11RootAuthorizationFlow`, real READY peer and `require_root=False`;
they do not inspect removed Session/Closure objects or private phase data.

## Review closure

- the correction design received two independent fixed-hash reviews with
  `P0=0 / P1=0 / P2=0 / P3=0`;
- production fixed-hash review closed at
  `P0=0 / P1=0 / P2=0 / P3=0`;
- the first oracle final review found one P1 source-shape mutant gap; the
  existing two tests were strengthened without changing collection count, and
  the second fixed-hash review closed at
  `P0=0 / P1=0 / P2=0 / P3=0`;
- official fixture fixed-hash review closed at
  `P0=0 / P1=0 / P2=0 / P3=0`.

The accepted correction therefore satisfies its scoped acceptance contract.
It does not accept or waive the remaining 177 official failures.

## Remaining gate

The remaining exact groups are:

```text
C1a   8
C1b1 17
C1b2 22
C1c  20
C2b  38
C2c  36
C2d  36
```

They require one separately frozen test-authority canonicalization that
classifies every case as retained public behavior, oracle-owned coverage, or
obsolete private topology, then rewrites, retains or deletes by class. Do not
repair the 177 one by one and do not restore compatibility wrappers. Full H11
official acceptance, feature-branch merge, Warehouse W3 launch authority and
CVRP F1 launch authority remain locked until their existing gates close.
