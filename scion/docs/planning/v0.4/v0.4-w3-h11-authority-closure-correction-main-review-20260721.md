# Scion v0.4 W3 H11 authority-closure correction main review

> **SUPERSEDED — historical engineering record only.**
>
> V3 and the active `scion/TASK.md` retire every identity, owner-token,
> capability, lease, issuance/registration, receipt/hash-authority,
> activation/reopen and repeated closure mechanism described below. Its old
> `active`, `accepted`, `frozen`, `authorized` or `pending` wording has no
> current force and authorizes no implementation, test, build, launch or
> experiment. Preserve any domain or scientific observation as history; do not
> restore its proof lifecycle.


*Review date: 2026-07-21*
*Outcome: P0=0, P1=0, P2=0, P3=0 for the fixed candidate below.*
*Authority: review evidence only; this review does not authorize privileged
launch, solver execution, or implementation before the separately required
second independent fixed-hash review.*

## Fixed input

Reviewed design:
`v0.4-w3-h11-authority-closure-correction-design-20260720.md`

Raw SHA-256:
`bd2e2aaa1ac709822bd8d533804ee369ce5ac732fd3afeeb6193b706a2830541`

Implementation basis:
`e2e74742b8f96983ce6574cb3fecc8befe667e7e` on
`codex/h11-c2e-implementation`.

The five fixed source/design hashes in the candidate were recomputed and match
exactly. The feature worktree was clean except for the reviewed candidate.

## Reproduced baseline

All commands used `require_root=False`; no systemd manager, cgroup, privileged
installer, Warehouse/CVRP experiment, or formal W3 root was invoked.

- Python 3.12 C2e implementation oracle: exact `416 passed` with `-W error`.
- Python 3.13 C2e implementation oracle: exact `416 passed` with `-W error`.
- Python 3.12 public C2a surface: exact `74 passed` with `-W error`.
- Python 3.13 public C2a surface: exact `74 passed` with `-W error`.
- Python 3.12 full official fixture diagnostic: exact
  `752 passed / 187 failed / 1 skipped`.

## Independent gap reproduction

Each reproduction used a fresh temporary public C2a fixture and the real
`H11RootAuthorizationFlow`.

1. A coherently rebound harness `input_root` reached `COMPLETE`, returned a
   `permit-committed` receipt, and emitted one permit frame.
2. A coherently reversed `present_prerequisite_roles` tuple in both harness and
   READY authority reached the same incorrect successful outcome.
3. A coherently rebound TREE source outside the authority root reached the same
   incorrect successful outcome.
4. A present `h0` hardlinked to the retained harness-manifest source, with all
   current references refreshed, reached the same incorrect successful
   outcome.
5. A real `PermissionError(EACCES)` injected at the third
   `AUTHORIZE-RELEASE.json` observation while the Flow was
   `AUTHORITIES_RETAINED` escaped as that same raw object. The Flow failed
   prewrite and emitted no permit frame, confirming the exception-ABI gap.

The first attempted role reproduction changed only the harness copy and was
correctly rejected by existing harness/READY cross-binding. It was discarded
as an invalid reproduction; the accepted evidence above uses the required
coherent producer-chain mutation.

## Boundary review

- Harness root and present-role checks remain in the existing direct-binding
  owner and consume already-retained values.
- TREE layout consumes the already-validated TREE source reference and adds no
  read, open, descriptor, decoder, or producer replay.
- The ordered identity closure is a transport of nine already-validated direct
  source identities, three direct-only directory identities, and the existing
  indirect specs. Existing direct, indirect, and cross-domain alias owners
  remain responsible for uniqueness.
- The late present gate is correctly placed after both present sources are
  pinned and revalidated and before transaction evidence or READY phase-data
  construction.
- The five by seven non-`ENOENT` matrix preserves `FileNotFoundError` as the
  absence observation while converting other `OSError` instances to exact
  `InstallerError` labels with cause identity. It adds no retry or fallback.
- The proposed oracle and public-test count deltas are arithmetically closed:
  `416 + 3 + 2 + 35 = 456`; eleven public passing cases replace ten existing
  failures, yielding `763 passed / 177 failed / 1 skipped`.
- No compatibility owner, generic callback/helper surface, timeout, deadline,
  retry, budget, cap, truncation, cleanup policy, production hook, or formal
  launch authority is introduced.

## Disposition

No P0-P3 finding remains in the fixed design candidate. This is one independent
fixed-hash review. The candidate must remain unimplemented until a second
independent review of the same raw SHA also closes with no open P0 or P1, as
required by its implementation order.
