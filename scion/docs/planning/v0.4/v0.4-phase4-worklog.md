# Scion v0.4 Phase 4 Worklog

*Date: 2026-05-01*
*Branch: v0.4-dev*

## Summary

Phase 4 has started with the runtime input gap between Phase 3 manifests and
the Scion CVRP adapter.

Phase 3 can select fixed CVRP cases from `vrp/results` CSV artifacts. Those
case paths are CVRPLIB-style `.vrp` paths, while the Phase 2 CVRP runtime only
loaded tiny JSON fixtures. P4-01 closes that adapter boundary.

## P4 Research Surface Follow-Up

Added after the dual-Sonnet audit:

- `ProblemSpecV1.research_surfaces` declares problem-owned optimization
  surfaces.
- Operator design remains a first-class optimization surface; this is an
  expansion from operator-only search to heuristic-algorithm-surface search,
  not a replacement of the v0.3 operator work.
- CVRP adds `policies/search_policy.py` as a bounded policy surface for
  baseline/operator budget scheduling.
- ContractGate now enforces surface action permissions, target files, operator
  class signatures for declared operator surfaces, and required module-level
  functions for declared policy surfaces.

## P4-01 CVRPLIB Input Support

Added:

```text
scion/scion/problems/cvrp/cvrplib.py
scion/scion/tests/test_cvrp_cvrplib_adapter.py
```

Updated:

```text
scion/scion/problems/cvrp/adapter.py
```

### Semantics

- `CvrpAdapter.load_instance()` now dispatches:
  - `.json` to existing `CvrpInstance.from_json`;
  - `.vrp` to Scion's owned CVRPLIB parser.
- Parser supports EUC_2D only.
- Parser reads:
  - `NAME`;
  - `DIMENSION`;
  - `CAPACITY`;
  - `EDGE_WEIGHT_TYPE`;
  - `NODE_COORD_SECTION`;
  - `DEMAND_SECTION`;
  - `DEPOT_SECTION`.
- CVRPLIB 1-based ids are mapped into Scion zero-based ids with depot `0`.
- Sibling `.sol` files populate `bks` and `bks_routes` when present.
- Missing `.sol` leaves reference fields unknown.
- Unsupported `EDGE_WEIGHT_TYPE` fails closed.
- Integer-distance mode is used for integer coordinates; decimal coordinates
  keep continuous Euclidean distance.

Tests use temporary synthetic `.vrp/.sol` files only. They do not read raw
`vrp/cvrplib/**` files.

### Tests

Targeted command:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/tests/test_cvrp_cvrplib_adapter.py \
  scion/tests/test_cvrp_adapter.py \
  -q
```

Result after main-session review tightening:

```text
20 passed in 0.27s
```

Default Scion suite:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest -q
```

Result:

```text
1108 passed, 1 skipped in 20.75s
```

## P4-02 CVRP Solver Wrapper For `.vrp`

Updated:

```text
scion/scion/problems/cvrp/solver.py
scion/scion/tests/test_cvrp_solver_vrp_smoke.py
```

### Semantics

- The CVRP solver CLI now loads instances through `CvrpAdapter.load_instance()`
  instead of calling `CvrpInstance.from_json()` directly.
- Existing tiny JSON fixture behavior is unchanged.
- Synthetic `.vrp` inputs can be executed by `LocalSubprocessRunner`.
- Solver output remains the same JSON contract:
  - `routes`;
  - `feasible`;
  - `objective.fleet_violation`;
  - `objective.total_distance`;
  - `objective.routes`;
  - `runtime`.
- `.sol` references populate `bks` and `bks_routes` on the instance through
  adapter loading; solver output still reports promotion objectives only.

Tests use temporary synthetic `.vrp/.sol` files and existing tiny JSON fixtures
only. They do not read raw `vrp/cvrplib/**` files.

### Tests

Targeted command:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/tests/test_cvrp_solver_vrp_smoke.py \
  scion/tests/test_cvrp_protocol_smoke.py \
  scion/tests/test_cvrp_cvrplib_adapter.py \
  -q
```

Result:

```text
15 passed in 7.56s
```

Default Scion suite:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest -q
```

Result:

```text
1111 passed, 1 skipped in 26.20s
```

## P4-03a Controlled Synthetic Manifest Fixtures

Added:

```text
scion/scion/problems/cvrp/controlled/README.md
scion/scion/problems/cvrp/controlled/budgets.json
scion/scion/problems/cvrp/controlled/{screening,validation,frozen,final}.csv
scion/scion/problems/cvrp/controlled/manifests/{screening,validation,frozen,final}.json
scion/scion/problems/cvrp/controlled/data/synthetic_*_{5,6}.vrp
scion/scion/problems/cvrp/controlled/data/synthetic_*_{5,6}.sol
scion/scion/tests/test_cvrp_controlled_manifests.py
```

### Semantics

- Added four checked-in controlled stages:
  - `screening`;
  - `validation`;
  - `frozen`;
  - `final`.
- Each stage has two tiny synthetic CVRPLIB-style `.vrp/.sol` cases.
- Case ids are disjoint across stages.
- Manifest paths are relative to `scion/problems/cvrp`.
- CSV rows are importable through the Phase 3 CVRP baseline import path.
- Checked-in manifest JSON uses `CvrpCaseManifest` payload shape.
- `budgets.json` declares manifest path, CSV path, max cases, seeds, and
  runtime budget for every stage.
- The final manifest can build a `CvrpFinalEvaluationConfig`.

These fixtures are local controlled-run inputs only. They are not benchmark
claims and do not read or depend on raw `vrp/cvrplib/**` files.

### Tests

Targeted command:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/tests/test_cvrp_controlled_manifests.py \
  scion/tests/test_cvrp_solver_vrp_smoke.py \
  -q
```

Result after main-session review:

```text
11 passed in 0.95s
```

Default Scion suite:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest -q
```

Result:

```text
1119 passed, 1 skipped in 21.43s
```

## P4-04a Controlled Campaign Smoke

Added:

```text
scion/scion/problems/cvrp/controlled/protocol.yaml
scion/scion/problems/cvrp/controlled/split_manifest.yaml
scion/scion/problems/cvrp/controlled/seed_ledger.yaml
scion/scion/problems/cvrp/controlled/data/synthetic_controlled_canary_5.vrp
scion/scion/problems/cvrp/controlled/data/synthetic_controlled_canary_5.sol
scion/scion/tests/test_cvrp_controlled_campaign.py
```

Updated:

```text
scion/scion/problems/cvrp/controlled/README.md
scion/scion/evidence/cvrp_case_manifest.py
scion/scion/evidence/cvrp_manifest_evaluation.py
scion/scion/evidence/__init__.py
scion/scion/tests/test_cvrp_controlled_manifests.py
scion/scion/tests/unit/evidence/test_cvrp_manifest_evaluation.py
```

### Semantics

- Added a short controlled campaign configuration using standard Scion loaders:
  - `ProtocolConfig`;
  - `SplitManifest`;
  - `SeedLedgerConfig`.
- Screening uses the synthetic controlled `.vrp` cases from P4-03a.
- Canary uses a separate synthetic controlled `.vrp/.sol` fixture.
- Added `load_cvrp_case_manifest(path)` for checked-in/final manifest reuse.
- Manifest-driven final evaluation now resolves relative case paths for adapter
  loading against `baseline_workspace`.
- Runner calls still receive the original manifest case path, so each
  workspace evaluates its own copied fixture tree.
- Added a mock-LLM controlled `CampaignManager.run_one_step()` smoke that
  reaches screening with adapter-backed runtime checks.
- Added a post-campaign final evidence smoke that manually writes the final
  evidence package and attaches refs through `attach_final_evidence_package`.
- `campaign_summary.json` receives top-level `final_evidence_refs`; step schema
  remains unchanged.

This keeps final quality evaluation outside the campaign loop. BKS/gap remains
final evidence only and is not used for promotion.

### Tests

Targeted command:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/tests/test_cvrp_controlled_campaign.py \
  scion/tests/test_cvrp_controlled_manifests.py \
  scion/tests/unit/evidence/test_cvrp_manifest_evaluation.py \
  -q
```

Result after main-session review:

```text
18 passed in 6.00s
```

Default Scion suite:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest -q
```

Result:

```text
1123 passed, 1 skipped in 29.29s
```

## P4-04b CVRP Operator Runtime Boundary

Added:

```text
scion/scion/problems/cvrp/registry.yaml
scion/scion/tests/test_cvrp_solver_operator_runtime.py
```

Updated:

```text
scion/scion/problems/cvrp/solver.py
```

### Semantics

- CVRP solver now reads `--registry` when the file exists.
- Missing or empty registry preserves the original nearest-neighbor baseline.
- Operators are loaded from workspace-relative `file_path` plus `class_name`.
- Operator paths that escape the workspace are skipped and audited.
- Operator loading/runtime errors are counted in solver `runtime` metadata.
- Operators run in a deterministic bounded loop:
  - sorted by descending weight and registry order;
  - capped by a fixed round count and the solver time budget.
- Operators must return `CvrpSolution`.
- Returned solutions must pass adapter consistency and feasibility checks.
- Acceptance uses only the promotion objective:
  - first `fleet_violation`;
  - then `total_distance`.
- BKS/gap is not used in runtime acceptance.
- Solver output keeps the existing JSON contract:
  - `routes`;
  - `feasible`;
  - `objective`;
  - `runtime`.
- Runtime metadata now includes operator audit counters/events.
- Added root `registry.yaml` with an empty operator list so branch workspaces can
  register generated `create_new` operators.

This closes the main runtime gap found after P4-04a: generated CVRP operators
can now affect evaluated solver output.

### Tests

Targeted command:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/tests/test_cvrp_solver_operator_runtime.py \
  scion/tests/test_cvrp_solver_vrp_smoke.py \
  scion/tests/test_cvrp_controlled_campaign.py \
  -q
```

Result after main-session review:

```text
11 passed in 9.35s
```

Default Scion suite:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest -q
```

Result:

```text
1128 passed, 1 skipped in 29.16s
```

## P4-04c Controlled Multi-Step Promotion Smoke

Updated:

```text
scion/scion/problems/cvrp/controlled/data/synthetic_{screening,validation,frozen,final}_{micro_5,split_6}.vrp
scion/scion/problems/cvrp/controlled/data/synthetic_{screening,validation,frozen,final}_{micro_5,split_6}.sol
scion/scion/problems/cvrp/controlled/{screening,validation,frozen,final}.csv
scion/scion/problems/cvrp/controlled/manifests/{screening,validation,frozen,final}.json
scion/scion/tests/test_cvrp_controlled_campaign.py
```

### Semantics

- Adjusted controlled synthetic fixtures so the baseline nearest-neighbor route
  is deliberately improvable.
- Micro cases:
  - baseline nearest-neighbor cost: `13`;
  - controlled reference route cost: `11`;
  - `bks_routes=1`.
- Split cases:
  - baseline nearest-neighbor cost: `19`;
  - controlled reference route cost: `15`;
  - `bks_routes=1`.
- The generated smoke operator is produced through `MockLLMClient` and passes
  normal contract/verification gates.
- The smoke operator does not import problem modules; it returns
  `solution.__class__(routes=...)` for recognized controlled customer sets and
  no-ops otherwise.
- Controlled campaign now exercises:
  - screening -> `QUEUE_VALIDATE`;
  - validation -> `QUEUE_FROZEN`;
  - frozen -> `PROMOTE`.
- Promotion creates champion `v2` with the generated operator and registry
  entry in the champion snapshot.
- Post-campaign final evidence compares:
  - baseline workspace: original CVRP package;
  - candidate workspace: promoted champion snapshot.
- Final evidence uses explicit baseline/candidate registry paths.
- Final evidence on controlled final cases reports candidate better on all four
  case/seed observations.
- `campaign_summary.json` keeps final evidence refs at top level and does not
  alter step schema.

This is a synthetic control harness only. It proves the branch-governed CVRP
runtime path is wired end to end; it is not a benchmark result.

### Tests

Targeted command:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/tests/test_cvrp_controlled_campaign.py \
  scion/tests/test_cvrp_controlled_manifests.py \
  scion/tests/test_cvrp_solver_operator_runtime.py \
  -q
```

Result after main-session review:

```text
17 passed in 14.14s
```

Default Scion suite:

```bash
cd scion
/home/clawd/miniconda3/envs/claw/bin/python -m pytest -q
```

Result:

```text
1129 passed, 1 skipped in 35.73s
```

## Next Slice

P4-05 should be a matrix-readiness pass, not yet the formal matrix run:
document exact commands, budgets, outputs, and residual risks for the first
real CVRP campaigns.

## 2026-05-04 Post-Audit Formal-Readiness Fix

### Trigger

A real CVRP Sonnet run completed with all screening pairs tied and no promotion.
Detailed audit showed it was not valid quality evidence:

- hypothesis/code prompts still described the old high-invocation VNS pool;
- the CVRP wrapper was using a small post-baseline operator loop;
- generated operators commonly returned workspace-local `models.CvrpSolution`,
  which failed package-class `isinstance` checks;
- the formal run did not use the intended repo `vrp/src` ALNS+VNS baseline.

### Framework Boundary Repair

- Removed research-object mechanics from framework prompt text.
- Added generic `solver_mechanics` plumbing from adapter to proposal prompts.
- Kept CVRP-specific mechanics only in `scion.problems.cvrp.adapter`.
- Removed CVRP-specific env-var naming from framework subprocess env handling;
  the runner now passes generic `SCION_*` variables.
- Removed warehouse-object reconstruction from framework feasibility/objective
  verification fallback. New problems must use `ProblemAdapter`; non-adapter
  legacy problems need generic oracle hooks.

### CVRP Runtime Repair

- CVRP `.vrp` runs use repo `vrp/src` ALNS+VNS baseline when
  `SCION_PROBLEM_DATA_ROOT` points at the repo `vrp` directory.
- Synthetic `.vrp` fixtures do not implicitly trigger the full baseline.
- Required-baseline fallback is a runtime audit failure.
- Workspace-local `CvrpSolution` return values are accepted structurally when
  route data is valid.
- Malformed, infeasible, or exception-raising operators fail closed through
  runtime audit instead of becoming ties.
- `vrp/src` internal node ids are mapped back to Scion's depot-first CVRP ids.

### Governance Repair

- Runtime audit failures now include operator invalid outputs and required
  baseline failures.
- Protocol raw metrics include candidate/champion runtime summaries.
- Validation/frozen exposed summaries remain aggregate-only and no longer expose
  raw metrics paths.
- Missing canary configuration fails closed in the evaluation pipeline.
- `V9_perf_guard` uses the configured runtime ratio threshold.
- `ProblemSpecV1` can be loaded as the authoritative source and bridged to the
  legacy runtime shape.

### Validation

Full Scion suite:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/tests -q
1190 passed, 1 skipped in 37.27s
```

Synthetic CVRP baseline smoke with explicit `SCION_PROBLEM_DATA_ROOT`:

```text
success True
objective {'fleet_violation': 0, 'total_distance': 20.0, 'routes': 2}
baseline_mode vrp_alns_vns
baseline_required True
baseline_error None
```

### Active Post-Fix Runs

Two detached Sonnet campaigns were started for post-fix validation:

```text
run_root = /home/clawd/research/scion-experiments/v04-dual-sonnet-20260504T091234Z

CVRP:
  pid      = 1922419
  rounds   = 50
  campaign = .../cvrp/campaign
  log      = .../cvrp/run.log
  protocol = scion/scion/problems/cvrp/formal/protocol.yaml

Warehouse delivery:
  pid      = 1922420
  rounds   = 50
  campaign = .../warehouse/campaign
  log      = .../warehouse/run.log
  protocol = scion/problems/warehouse_delivery/protocol.yaml
```

Status and interpretation are tracked in
`scion/docs/status/current-state.md`.

## 2026-05-04 Deferred Audit Backlog

A read-only architecture/code audit against the v3 blueprint and v0.4 design
found several issues that should be optimized after the active dual Sonnet
experiments finish and are audited round by round.

Recorded for the post-experiment repair pass:

- CVRP formal `.vrp` baseline selection must fail closed when the intended
  `vrp/src` baseline cannot be used.
- Frozen holdout use limits must be enforced, not only configured.
- Final evidence attachment must become a formal close/readiness condition.
- Warehouse-shaped legacy framework fallbacks should be moved behind adapters
  or isolated as legacy-only behavior.
- Programmatic `CampaignManager` construction should preserve runtime
  verification checks by default.
- `campaign.py` still needs another elegance/decomposition pass after the
  experiment audit.
- Formal CVRP result interpretation should distinguish the active 5 second
  readiness budget from the stage-specific budgets in P4-05.

The active runs have since completed and were audited round by round in
`scion/docs/experiments/v0.4/v0.4-dual-sonnet-postrun-analysis-20260504.md`.

## 2026-05-04 Post-Run Experiment-Validity Repair

Implemented after combining the architecture audit with the dual-Sonnet
experiment audit:

- CVRP generated registry operators now stop after a full round with zero
  accepted moves. Runtime audit records `operator_no_improvement_rounds`,
  `operator_rounds_with_acceptance`, and `operator_stop_reason`.
- Formal data-root-relative `.vrp` cases now set `baseline_required=true`
  independently of whether `vrp/src` is available. Missing or failing external
  baselines become `baseline_runtime_error` evidence through runtime audit.
- Screening proposal context now distinguishes objective no-signal,
  candidate-side runtime failures, champion-side runtime failures, operator
  attempts/accepted moves, operator errors, invalid outputs, and contract
  failures.
- CLI diagnostic mode adds `scion run --disable-early-stop` so fixed-round
  experiments can avoid idle/stagnation early termination.
- Round-1 LLM proposal/schema failures now write `StepRecord` rows with
  `failure_stage=proposal`, closing the summary-row gap observed in the
  warehouse run.

Validation:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/tests/test_cvrp_solver_operator_runtime.py scion/tests/test_cvrp_solver_vrp_smoke.py scion/tests/test_sprint_e2.py scion/tests/test_cli.py scion/tests/unit/core/test_campaign_control_boundaries.py -q
76 passed in 4.02s

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/tests -q
1194 passed, 1 skipped in 37.58s
```

At that point, the following governance/design items were not closed by the
experiment-validity repair pass. The governance hardening pass below closes the
frozen-budget, formal-readiness, strict-runtime, complexity-guard, and
route-taxonomy items.

- frozen holdout use budget enforcement;
- formal close/readiness validation for final evidence refs;
- strict runtime verification defaults for non-CLI programmatic campaigns;
- broader static complexity guard;
- route-native taxonomy/legacy fallback isolation;
- further `campaign.py` composition cleanup;
- CVRP research-surface decision beyond post-baseline operators.

## 2026-05-04 Governance Hardening Repair

Implemented before restarting post-repair Sonnet validation:

- Added a persisted campaign-level frozen budget ledger. Frozen attempts
  consume `FrozenConfig.max_uses_per_campaign` before protocol evaluation; once
  exhausted, the branch is abandoned with `frozen_budget_exhausted` and no
  frozen protocol run is launched.
- Added formal readiness validation for top-level final evidence refs. Campaign
  summaries now include `formal_readiness.formal_ready` and missing fields
  without changing step schema.
- Made programmatic adapter-backed campaigns strict by default for runtime
  verification. Adapter campaigns without a protocol runner fail closed unless
  compatibility mode is explicitly requested.
- Broadened contract complexity governance for generated operators:
  high-order combinations, permutations, risky product over problem-scale
  iterables, uncapped while loops, and three-level problem-scale nested loops
  fail the static gate.
- Threaded `ProblemSpecV1.family_taxonomy` into search memory, and added CVRP
  route-native families: `route_local`, `route_pair`, and `ruin_recreate`.

Validation:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/tests/test_contract.py scion/tests/unit/core/test_campaign_control_boundaries.py scion/tests/unit/evidence/test_formal_readiness.py scion/tests/unit/evidence/test_final_evidence_refs.py scion/tests/test_problem_bridge.py scion/tests/unit/test_sprint_j1_search_memory.py -q
119 passed in 0.70s

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/tests -q
1210 passed, 1 skipped in 39.28s
```

An attempted detached validation run under
`/home/clawd/research/scion-experiments/v04-postrepair-sonnet-20260504T150448Z`
was marked aborted because these governance repairs were still open. It should
not be treated as evidence.

Remaining design items after this pass:

- further `campaign.py` composition cleanup;
- broader legacy fallback isolation beyond the route-native search-memory fix;
- CVRP research-surface decision beyond post-baseline operators;
- optional CLI/readiness command for final evidence closeout.

## 2026-05-04 Prompt Taxonomy Boundary Repair

The post-repair prompt/context validation evidence was marked invalid. The
framework taxonomy helper still contained problem-specific labels and keyword
maps, so CVRP prompts could inherit framework-owned route semantics or
warehouse-shaped mechanism labels instead of reading aliases exclusively from
the active problem spec.

Repair:

- Extended `ProblemSpecV1.family_taxonomy` with problem-defined `aliases`.
- Replaced framework mechanism classification with generic normalization,
  family-label exact matching, and problem-provided alias matching only.
- Moved warehouse and CVRP mechanism aliases into their `problem-v1.yaml` files.
- Threaded the taxonomy object, not only the `families` list, through classifier,
  context/search memory, stagnation, and evidence summary paths.
- Made legacy protocol objective fallback and saturation case-feature fallback
  problem-agnostic.
- Added proposal feedback for no accepted operator moves, tie-dominated
  screening evidence, and `operator_stop_reason=no_improvement_round` as
  weak/no-op operator-quality signals.
- Moved constructor service wiring from `campaign.py` to
  `core/campaign_composition.py` and extracted `CircuitBreaker`.
- Runtime slow-case proposal feedback now follows
  `protocol.runtime.max_runtime_ratio`.

Validation:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/tests/test_sprint_o0.py scion/tests/test_sprint_e2.py scion/tests/unit/test_sprint_j_patch.py scion/tests/unit/test_sprint_j1_search_memory.py scion/tests/test_problem_bridge.py scion/tests/unit/test_sprint_j5_classifier.py scion/tests/test_sprint_o1.py scion/tests/test_mini_validation_b.py scion/tests/unit/test_sprint_k.py scion/tests/test_campaign_control_boundaries.py scion/tests/test_contract.py -q
250 passed in 0.82s

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/tests -q
1227 passed, 1 skipped in 38.17s
```

## 2026-05-05 Short Boundary Experiment and Risk Repair

A four-round detached CVRP Sonnet diagnostic was run after the taxonomy
boundary repair:

```text
run root = /home/clawd/research/scion-experiments/v04-short-boundary-sonnet-20260505T005635Z
campaign = /home/clawd/research/scion-experiments/v04-short-boundary-sonnet-20260505T005635Z/cvrp/campaign
model    = claude-sonnet-4-6
rounds   = 4
budget   = 5 seconds per solver run
```

Result:

- all 4 rounds reached screening and failed closed with
  `SCREENING_FAIL_WIN_RATE`;
- champion remained v1, with no validation/frozen/promotion;
- contract, verification, canary, and runtime evidence were complete;
- prompt traces no longer contained warehouse taxonomy labels;
- generated CVRP operators were safe but ineffective: 96 operator attempts,
  0 accepted moves, and `operator_stop_reason=no_improvement_round`.

Experiment-derived repairs:

- Mechanism extraction now accepts an optional step `change_locus` preference,
  so a hypothesis that says "unlike the failed intra-route 2-opt" is not
  misclassified as the historical family. This fixes search memory,
  stagnation, and `campaign_summary.family_coverage`.
- Strategy guidance no longer suggests `modify` or `remove` when the current
  champion has no targetable operator file.
- CVRP route-pair aliases were narrowed/extended to reduce generic
  `route_local` false positives while preserving inter-route phrasing.
- Campaign composition now persists the initial v1 champion in
  `ChampionStore`; when safe, it also creates a read-only initial champion
  snapshot under the campaign directory.
- Campaign research journal now reports `v1, 0 promotions` for a base-only
  campaign instead of deriving both values from `COUNT(champions)`.
- CLI problem-v1 runs now pass the problem objective policy into
  `ExperimentProtocol`, so future weighted/single-objective problems do not
  fall back to lexicographic assumptions.

Interpretation:

The short run is valid as framework/control evidence, not as CVRP quality
evidence. The remaining CVRP research risk is the search surface: generated
registry operators currently run as post-baseline polish after the strong
`vrp/src` ALNS+VNS baseline. If formal CVRP campaigns continue to produce
all-no-op operators, v0.4 should expose problem-owned levers such as baseline
neighborhood mix, destroy/repair policy, and budget allocation rather than only
post-baseline operator files.

Validation after this repair:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/tests -q
1235 passed, 1 skipped in 41.02s
```

## 2026-05-07 V2/V5 Gate Modernization

Closed the P0 gate-modernization slice after the selected-surface protocol
audit pass:

- Extracted/used the shared AST-only research-surface interface validator for
  Verification V2 as well as ContractGate C7.
- Forwarded `problem_spec` and explicit or hypothesis-derived
  `selected_surface` through `VerificationGate.run()` into V2.
- Added V2 acceptance coverage for missing required policy functions,
  declared function signatures, static return constraints, valid module-level
  policy files with no class, and selected-surface target mismatch.
- Added generic adapter metadata to bridged legacy `ProblemSpec` objects:
  `spec_version`, `adapter_import_path`, and
  `requires_adapter_for_runtime`.
- Made adapter-required problem-v1 specs fail closed at
  `V5_solution_consistency` when the adapter is missing, before the legacy
  assignment/vehicles fallback can accept the solver output.
- Preserved explicit legacy/no-adapter compatibility and avoided
  CVRP/warehouse semantics in core.

Validation:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/scion/tests/test_verification.py scion/scion/tests/unit/test_research_surfaces.py scion/scion/tests/test_problem_bridge.py -q
131 passed in 3.09s

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/scion/tests/test_verification.py scion/scion/tests/unit/test_research_surfaces.py scion/scion/tests/unit/test_agentic_proposal_tools.py scion/scion/tests/test_cvrp_adapter.py scion/scion/tests/test_cvrp_solver_operator_runtime.py scion/scion/tests/test_protocol.py scion/scion/tests/unit/core/test_evaluation_pipeline.py scion/scion/tests/unit/core/test_campaign_control_boundaries.py scion/scion/tests/test_cvrp_protocol_smoke.py scion/scion/tests/test_cvrp_controlled_campaign.py scion/scion/tests/test_sprint_n1.py -q
305 passed in 32.75s

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/scion/tests -q
1422 passed, 1 skipped in 46.95s
```

## 2026-05-07 V6/V7/V8 Gate Modernization

Closed the next gate-modernization slice after V2/V5:

- Added a shared generic verification helper for adapter-required runtime
  metadata, declared objective metric names, and selected-surface target files.
- V6 feasibility now disables legacy oracle fallback for adapter-required
  specs when the adapter is missing, while preserving legacy/no-adapter oracle
  compatibility.
- V7 objective now disables legacy oracle fallback for adapter-required specs,
  and adapter-backed V7 requires declared objective metrics in both reported
  solver objective and adapter recomputation. Auxiliary recomputed metrics are
  still compared only when both sides report them.
- V8 nondeterminism now accepts an optional adapter. Adapter-required specs
  without an adapter fail closed before legacy objective-only comparison.
- Adapter-backed V8 compares a stable canonical solver artifact signature
  derived from normalized solution, adapter-filtered objective, and feasible
  flag. A dynamic adapter fingerprint hook can override that signature without
  broadening the required `ProblemAdapter` protocol.
- V8 failure detail records the selected surface and comparison mode, and
  failure archives prefer selected-surface target files from
  `problem_spec.research_surfaces`, falling back to `operators/`.
- No CVRP or warehouse semantics were added to framework core.

Validation:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/scion/tests/test_verification.py scion/scion/tests/test_state_leak.py scion/scion/tests/test_problem_bridge.py -q
98 passed in 2.65s

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/scion/tests/test_verification.py scion/scion/tests/unit/test_research_surfaces.py scion/scion/tests/test_protocol.py scion/scion/tests/unit/core/test_evaluation_pipeline.py scion/scion/tests/test_cvrp_adapter.py scion/scion/tests/test_cvrp_solver_operator_runtime.py scion/scion/tests/test_cvrp_protocol_smoke.py scion/scion/tests/test_cvrp_controlled_campaign.py -q
225 passed in 30.06s

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/scion/tests -q
1435 passed, 1 skipped in 44.87s
```

## 2026-05-07 Post-Gate Sonnet CVRP Smoke Launch

Launched a detached real Sonnet CVRP formal-path smoke to exercise the
post-modernization framework path:

```text
run_root=/home/clawd/research/scion-experiments/v04-v678-sonnet-vrp-20260507T035528Z
scion_commit=a80bd60
model=claude-sonnet-4-6
rounds=5
agentic_proposal=true
disable_early_stop=true
cvrp_time_limit_sec=10
data_root=/home/clawd/research/or-autoresearch-agent/vrp
protocol=scion/scion/problems/cvrp/formal/protocol.yaml
split=scion/scion/problems/cvrp/formal/split_manifest.yaml
seeds=scion/scion/problems/cvrp/formal/seed_ledger.yaml
wrapper_pid=2160932
scion_pid=2160933
status=running
```

The run is intended as framework/control evidence for APS, selected-surface
runtime audit, and adapter-backed V2-V8 verification after the modernization
slice. Raw logs and metrics are reserved for delegated post-run analysis.
