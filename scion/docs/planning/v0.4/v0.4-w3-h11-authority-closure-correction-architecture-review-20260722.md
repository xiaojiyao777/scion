# Scion v0.4 W3 H11 authority-closure architecture review

*Review date: 2026-07-22*
*Reviewer: independent subagent `h11_arch_review_r2`*
*Outcome: P0=0, P1=0, P2=0, P3=0.*
*Authority: fixed-input design review only; no privileged launch or experiment
was reviewed or authorized.*

## Fixed input

- design:
  `v0.4-w3-h11-authority-closure-correction-design-20260720.md`;
- raw SHA-256:
  `bd2e2aaa1ac709822bd8d533804ee369ce5ac732fd3afeeb6193b706a2830541`;
- basis commit:
  `e2e74742b8f96983ce6574cb3fecc8befe667e7e`.

The reviewer independently recomputed the design hash and all five source,
oracle, official-fixture, atomic-design, and public-migration hashes in the
fixed-input table. Every value matched the reviewed checkout.

## Findings

No P0-P3 finding was reported.

The review confirmed:

- the correction strengthens deterministic fail-closed authority validation
  without moving protocol, scheduling, promotion, or problem semantics into an
  untrusted input surface;
- direct roots and present-role authority remain owned by
  `_prove_h11_direct_authority_bindings`;
- TREE compares one already-validated source path with the canonical authority
  leaf and adds no open, read, decoder, or producer replay;
- the ordered identity closure transports nine proved direct-source identities,
  three direct-only directory identities, and the existing ordered indirect
  specs without duplicating uniqueness or filesystem proof;
- the late present alias gate executes after pin, reread, current-name proof,
  and view revalidation and before transaction observation, partition
  construction, or READY phase-data publication;
- each of the five existing transaction loops retains inline seven-role
  observation and adds only the specified `OSError -> InstallerError from exc`
  branch after `FileNotFoundError` absence handling;
- no second lifecycle owner, generic stat wrapper, compatibility surface,
  retry, timeout, fallback, privileged launch, or experiment is introduced.

The reviewer did not read the main-session review before reaching this
conclusion, modified no file, and invoked no systemd, cgroup, privileged, or
experimental path. Together with the independent main-session fixed-hash
review, this satisfies the design's two-review precondition for the scoped
production implementation slices.
