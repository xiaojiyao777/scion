# W3 Generic SpawnBackend/cgroup/systemd Design Acceptance

*Date: 2026-07-18*
*Status: design accepted; implementation authorized; all later layers locked*

## Accepted authority

- design:
  `v0.4-w3-generic-spawn-backend-systemd-contract-20260718.md`;
- exact raw SHA256:
  `d40e75a33a6582680632b3fcd3c53ea7b1720ce949f2e29daf5cc8d0a5911f50`;
- parent W3 correction SHA256:
  `8e2a610eeec15ca1bb118d7affa855b753b320be1ae055b2e517613731d10945`;
- accepted native contract SHA256:
  `afaa0b7e60b820e168d1300ecdf8a0f2085e5dad7461e7f7bbc1edbf88524f27`;
- accepted native external record SHA256:
  `51948ccda6b9a24811c05e4fd3795ddefcf1b62ac2e1604297e70ede91700de7`.

The design file above is frozen. This acceptance record does not modify or
replace it.

## Review result

Two independent full reviews of the exact accepted SHA report:

```text
P0=0
P1=0
P2=0
DESIGN_ACCEPT
```

A third independent systemd-255 specialist review of the same SHA reports:

```text
P0=0
P1=0
P2=0
SYSTEMD_DESIGN_ACCEPT
```

The reviews independently confirmed:

- one native/backend/cgroup owner with exact move/consume alias invalidation;
- closed backend-open, pre-handle and contained-failure schemas;
- positive observation only after original-pidfd reap, complete stream EOF,
  empty cgroup, stable identity and authority closure;
- retained empty-cgroup cleanup authority until a durable one-shot permit;
- complete binary stdout/stderr with no timeout, cap, truncation, retry or
  fallback spawn;
- exact `Delegate=pids`, `DelegateSubgroup=supervisor`,
  `CollectMode=inactive`, OnSuccess/OnFailure plus closer `After=` handoff;
- authoritative system-manager fixtures using pre-hashed static templates and
  exactly one `StartUnit(unit, "fail")`;
- clean immutable pushed-source acceptance that excludes the dirty-worktree
  rejected Warehouse lifecycle without modifying or staging those files.

## Scope unlocked

Only the following implementation is now authorized:

```text
scion/runtime/execution/
  __init__.py
  model.py
  cgroup_v2.py
  spawn_backend.py
  systemd255.py

focused pure/delegated/systemd-255 acceptance fixtures and tests
```

The implementation must follow the accepted package boundary and executed
matrix exactly. It may not reuse or alter ordinary `LocalSubprocessRunner`, the
launcher, or the rejected Warehouse `Popen/killpg` lifecycle. Focused tests and
source receipts must be produced from an exact clean immutable source, not the
dirty developer worktree.

## Still locked

- production cleanup-permit issuer and guardian durable state;
- external authority, materialization, installation and formal StartUnit;
- UNIT_DRAINED, UNIT_FINAL, COMPLETE and CLOSED publication;
- Warehouse schedule, output/Oracle/row validation, analysis and replay;
- a W3 dry root or solver/formal experiment;
- CVRP F1 launch authority.

Implementation acceptance requires the complete frozen matrix, exact source and
artifact hashes, clean fixture cleanup, and two final independent P0/P1 reviews.
It unlocks only the next guardian/terminal design, not a formal experiment.
