# CVRP R3 Ordinary-Lineage R45 Diagnosis R3 Driver Replacement — 2026-08-10

**Status:** `VALID_COMPLETE_PROVIDER_FREE_DIAGNOSIS / DESCRIPTIVE_COHORT_ONLY / NO_BROAD_IMMEDIATE_BASE_ADVANCE`

## Post-run terminal note

R3 completed with ordinary-user launcher exit `0`. Its output root records
`complete=true`, all 37 of 37 accepted atomic blocks and exactly 1,056 unique
fresh pair rows; the last block is `2x`, candidate ordinal `5`. A terminal
`analysis.json` exists, and no R1 or R2 pair was copied, combined or reused.

The independent B0 A/A result gives `MDE@80%=2.0` only for the frozen `12x8`,
`1x`, homogeneous-additive estimand. Across the six complete `12x8`
immediate-base comparisons, candidate median signs are `0 positive / 5 zero /
1 negative`. Case/seed and budget contrasts remain descriptive; case-by-seed
interaction and whole-budget-arm machine drift remain `UNIDENTIFIABLE`.

R3 is sealed
`VALID_COMPLETE_PROVIDER_FREE_DIAGNOSIS / DESCRIPTIVE_COHORT_ONLY /
NO_BROAD_IMMEDIATE_BASE_ADVANCE`. It does not select, recover, reject or promote
a candidate and changes no Protocol, Safe Feature or Decision. Full results and
claim boundaries are in the
[postrun](v0.4-cvrp-r3-ordinary-lineage-r45-diagnosis-r3-postrun-20260811.md).
The remainder of this document is the frozen preregistration and launch history.

## Launch history

After explicit confirmation, R3 consumed its one allowed launch at
`2026-08-10 15:21:30 UTC`. An independent post-launch check found driver PID
`65082`, tmux pane `dead=0`, exit pending, experiment status zero of 37 accepted
blocks with `last_block=null`, and the first fresh MDE raw at 1/96 attempted and
valid pairs with zero candidate, champion or total failures.

Until terminal, monitoring reads only process health and structural
attempted/valid/failure/accepted counts. It does not read or report W/L/T,
effect, gate, ranking or objective values.

## Replacement boundary

R2 ended
`RUN_INVALID_EXPERIMENT_DRIVER / ZERO_ACCEPTED_BLOCKS / NO_ADMISSIBLE_ANALYSIS`.
Its first raw block completed 96/96 attempted and valid pairs with zero subject
failures, but the one-off driver rejected every otherwise valid A/B side because
CVRP included the auxiliary `routes` field beside its two declared metrics.
The rejection happened before atomic block acceptance and before analysis.

This replacement decision uses only process status, structural counts, field
names/types and the checked-in CVRP recomputation contract. No R2 W/L/T,
effect, gate, ranking or objective value was read. R2 remains sealed and no R2
raw pair, workspace result, status or analysis input is copied, appended,
completed or reused.

## Sole correction

R3 uses a fresh experiment-owned input bundle copied from the R2 bundle. The
only scientific-driver code change replaces this false requirement:

```python
set(objective) == set(declared_metrics)
```

with the actual boundary:

```python
set(declared_metrics).issubset(objective)
```

Every declared metric must still be present, non-boolean, numeric and finite.
The driver still requires exact declared `metric_deltas`, recomputes each delta
from the declared A/B values, checks the problem-declared tie tolerances, and
requires success, feasibility, zero exit, the exact time limit, finite elapsed
time, fresh/cache-off execution, complete Cartesian populations and exact
AB/BA order. Auxiliary objective fields are diagnostic only and never enter
the descriptive analysis or any Decision.

The focused fake Protocol record now includes real-shaped `routes` telemetry
and still proves that removing a declared metric fails closed. All 12 focused
tests, Ruff import/style checks, formatting checks and launcher syntax checks
pass. No Scion production file or gate changes.

## Frozen science

Everything else remains the design frozen by the
[R1 preregistration](v0.4-cvrp-r3-ordinary-lineage-r45-diagnosis-preregistration-20260810.md)
and the
[R2 replacement](v0.4-cvrp-r3-ordinary-lineage-r45-diagnosis-r2-replacement-preregistration-20260810.md):

- clean `c32f5b8a` Scion runtime plus the experiment-owned driver/test;
- the same exact six-candidate ordinary-lineage cohort and immediate bases;
- the same B0 A/A MDE, `Q00`, `Q01`, `Q10`, `Q11`, `.5x` and `2x` blocks;
- exactly 37 accepted blocks and 1,056 unique pair rows;
- the same cases, seeds, dimension budgets, cache-off fresh subjects and
  deterministic AB/BA parity;
- the same MDE and terminal descriptive analysis;
- provider-free, Decision-excluded interpretation under the V3 authority
  `Contract -> Verification -> Protocol -> Safe Features -> Decision`.

## Fresh roots and launch

- Prepared R3 input bundle:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r45-diagnosis-20260810-r3-input`
- R3 output root:
  `/home/clawd/research/scion-experiments/v04-cvrp-r3-ordinary-lineage-r45-diagnosis-20260810-r3-claw`
- R3 process-observation control root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r45-diagnosis-20260810-r3-control`
- Explicit tmux socket: `/tmp/scion-r45-r3.tmux.sock`
- Launcher: `launch-r3.sh` in the prepared input bundle

The output, control and socket paths must all be absent at launch. R3 composes
fresh workspaces and begins again at `MDE` 0/96; it does not import the complete
R2 MDE block. The ordinary `clawd` launcher retains the corrected independent
tmux process seam, bounded PID/log/exit observations and fail-if-present
once-only behavior. Those process observations are not gates or Decision
inputs.

R3 may launch once only after explicit confirmation and a final outcome-blind
launch-readiness check. If R3 is interrupted or rejected, seal it and do not
automatically create R4. Until terminal, monitoring reads only process health
and structural attempted/valid/failure/accepted counts. No deployment,
distribution, build, root/systemd, Trust/Hash, identity, signing, lease,
receipt or self-proof work is introduced.
