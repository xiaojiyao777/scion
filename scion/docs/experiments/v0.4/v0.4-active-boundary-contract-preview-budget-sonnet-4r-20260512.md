# v0.4 Active Boundary Contract-Preview Budget Sonnet 4R Diagnostic

*Date: 2026-05-12*
*Status: completed; active boundary and Contract-preview budget repair validated*

## Run

```text
run_root=/home/clawd/research/scion-experiments/v04-active-boundary-contract-preview-budget-sonnet-4r-20260512T003103Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=4
rounds_completed=4
screened_experiments=2
time_limit_sec=30
agentic_proposal=true
agentic_session_timeout_sec=600
force_surface=none
SCION_PROBLEM_DATA_ROOT=/home/clawd/research/or-autoresearch-agent/vrp
scion_commit_at_launch=4e88a2dff14da22800c15ceb1d03e53ca6494398
worktree_dirty_at_launch=false
exit_code=0
```

Raw artifacts checked: `exit.txt`, `run.log`, `campaign/status.json`,
`campaign/campaign_summary.json`, `campaign/scion.db`,
`agentic_session_index.json`, APS `output.json` files, and selected LLM traces.

## Completion Status

```text
status.total_rounds=4
status.n_steps=4
status.n_experiments=2
status.n_active_branches=0
circuit_breaker_tripped=false
stopped=null
stop_reason=null
exit_code=0
agentic_sessions=7
agentic_statuses={completed: 3, partial_hypothesis_only: 3, failed: 1}
```

## Results

| Order | Surface | Target | Outcome |
|---:|---|---|---|
| 1 | `solver_design` | `policies/main_search_strategy.py` | Contract passed; Verification passed; screening failed with `win_rate=0.0`, `median_delta=0.0`; hypothesis rejected |
| 2 | `solver_design` | `policies/main_search_strategy.py` | Contract passed; Verification failed heavy `V5_solution_consistency`; hypothesis rejected |
| 3 | `solver_design` | `policies/main_search_strategy.py` | Contract passed; Verification passed; screening failed with `win_rate=0.0`, `median_delta=0.0`; hypothesis rejected |
| 4 | `proposal` failure row | none | Hypothesis generation failed closed before approval: `schema or target preview did not pass` |

Persisted hypotheses:

```text
05c58c2e-f785-4a15-81a7-cacec5f437ce  rejected  solver_design  modify  policies/main_search_strategy.py
d9c4fa50-2d29-4cc9-bf19-ed8e58d7d194  rejected  solver_design  modify  policies/main_search_strategy.py
8186cd15-4d19-43d6-aede-64fa13ce01ef  rejected  solver_design  modify  policies/main_search_strategy.py
```

APS self-check summary:

```text
15b6d2b0-3748-4938-b925-017e5122c447  completed  schema_valid=true  contract_preview_passed=true  observation_chars=50119/64000
4ef82ff0-8837-4d21-85f6-32dd963c48ae  completed  schema_valid=true  contract_preview_passed=true  observation_chars=29013/64000
95d79ce2-c144-47b1-ac1a-339740b2bb85  completed  schema_valid=true  contract_preview_passed=true  observation_chars=51832/64000
7ba56294-64ad-4f42-a861-4a9a3d050a91  failed     schema_valid=false contract_preview_passed=null  observation_chars=45968/64000
```

No APS `output.json` in this run contained `result_too_large`.

## Interpretation

The active problem-object boundary held in a free-surface run. All completed
and partial APS hypotheses stayed on `solver_design` with target
`policies/main_search_strategy.py`; no component-policy fallback occurred
after the heavy Verification failure or the two zero-movement screening
failures.

The Contract-preview budget repair is live-validated. Three code sessions
completed with real self-check evidence, including Contract-preview pass
summaries at roughly `50.1k/64k`, `29.0k/64k`, and `51.8k/64k` observation
characters. The previous `result_too_large` terminal-preview failure did not
reappear.

The last failed APS session was not a budget failure. Target permission preview
passed, but schema preview failed before approval because the generated
hypothesis supplied `novelty_signature.deep_components_selected=[]`. For
`solver_design`, `deep_components_selected` is a required semantic-signature
identity field. This fail-closed behavior is correct, and it is also useful
signal: APS is still proposing shallow lifecycle variations that can omit the
declared deep solver-design identity.

Solver efficacy remains unproven. Two candidates reached screening and both
failed with `win_rate=0.0` and `median_delta=0.0`; one candidate failed heavy
Verification. The next optimization should target solver-design candidate
quality and problem adaptation, not more boundary enforcement.

## Follow-Up

1. Keep `solver_design` as the top-level research object.
2. Improve model-facing problem adaptation and feedback so solver-design
   hypotheses must describe a complete lifecycle and non-empty deep component
   identity when deep components are part of the surface contract.
3. Stop increasing rounds for the same shallow pattern until a short diagnostic
   shows nonzero phase-best movement.
4. Treat larger runtime/API budgets as secondary: the current blocker is
   candidate quality, not Contract-preview retention or component drift.
