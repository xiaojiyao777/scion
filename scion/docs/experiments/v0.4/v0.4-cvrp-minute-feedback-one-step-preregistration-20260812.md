# CVRP Minute Feedback One-Step Exploratory Screen Preregistration — 2026-08-12

**Preparation status:** `PREPARED_NOT_STARTED`

**Authorization state:** `AWAITING_FRESH_SOURCE_SEND_AUTHORIZATION`

No provider or solver call has started. This document freezes a second short
direct-V3 research step conditioned on the completed R53 evidence. It does not
authorize launch, validation, frozen testing, promotion or a longer campaign.
The future output root, process-control root and tmux socket were absent when
the completed input bundle was checked against this document.

## Question and V3 authority

The H request will ask exactly:

> Under the same fixed CVRP deadlines, what one CVRP-owned algorithm change can
> convert search opportunity into more final-best total_distance
> improvement—not merely more ALNS iterations—while preserving feasibility,
> fleet semantics, and useful local-search polishing? Use the visible fresh-B0
> source and the R53 negative evidence to choose a causal mechanism materially
> different from the tested global embedded-VNS runtime-share cap, and state
> the observable trajectory that would falsify it.

The immediate question is whether a second source-grounded mechanism can use
the opportunity exposed by R53 more effectively. The experiment starts from
fresh B0, not from R53's provisional `0.35` branch. It keeps the complete R53
measurement design fixed and changes only the scientific H question/prior to
include the terminal R53 observation.

[`design/scion-architecture-v3.md`](../../../design/scion-architecture-v3.md)
is the sole architecture authority. The direct-runtime addendum may describe
the lightweight implementation but cannot replace V3 ownership. The active
chain remains:

```text
problem-owned facts + complete editable source + prior safe evidence
  -> at most one structured H
  -> Hypothesis Contract
  -> at most one approved-H-bound C
  -> Patch Contract
  -> isolated Workspace
  -> executable Verification
  -> canary + problem-owned paired SCREENING Protocol
  -> Safe Features
  -> deterministic Decision
```

The phrase “materially different” is part of the research question, not a new
novelty classifier, candidate gate or host-side scoring rule. If H repeats the
tested cap, the ordinary Contract/Verification/Protocol chain records it
without a host novelty veto or retry. A typed H/C rejection or negative
candidate likewise terminalizes normally without a provider repair loop. LLM
output remains tainted and cannot select cases, seeds, Protocol results,
Decision actions or promotion.

## Fresh source-send authorization boundary

The H call would send source-derived context from the frozen runtime, problem
facts, prior safe evidence and the R53 screening feedback through the local
Codex proxy to `gpt-5.6-terra`. If H passes Contract, C would receive that exact
approved H plus the ordinary complete editable-source context. This is a new
provider experiment; authorization for R53 does not carry forward.

Launch therefore requires a new explicit user authorization for this R54 H/C
source-derived send. Until then, only offline bundle checks and fake/static
tests may run. Those checks may not call the proxy, provider or solver.

## Frozen source, model and call envelope

| Input | Frozen value |
|---|---|
| Campaign label | `v04-cvrp-minute-feedback-one-step-20260812` |
| Input bundle | `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-minute-feedback-one-step-20260812-input` |
| Runtime | bundle-owned ordinary-user clean archive under `runtime/scion` |
| Exact source reference | `e4b6b98d63eac7e088d9a9be960a6b5df08bca9d` |
| Problem/solver source | `runtime/scion/scion/problems/cvrp` |
| External case root | `/home/clawd/research/or-autoresearch-agent/vrp` |
| Champion | original CVRP B0, version `1` |
| Provider | local Codex proxy at `http://127.0.0.1:8080` |
| Model | `gpt-5.6-terra` |
| H calls | at most one; 90-second provider timeout |
| C calls | at most one, only after approved H; 180-second provider timeout |
| Provider retries | zero |
| Campaign-manager calls | `run_one_step()` exactly once; `run()` never |

The exact commit is ordinary scientific source selection, not an identity,
receipt, authority or Trust/Hash object. No source acceptance, registry, lease,
signature, hash chain or self-proof closure is introduced.

The provider surface is the unchanged direct-runtime structured H and typed C
surface. H receives complete visible fresh-B0 source through the normal
problem-owned projection. C receives exactly the approved H plus the ordinary
editable-source context. No provider shell, network, execution, file navigation
or same-call edit/repair loop is added.

## Frozen R53 feedback

H receives the following experiment-owned neutral evidence item exactly once:

> R53 began from B0 and changed only EMBEDDED_VNS_MAX_RUNTIME_SHARE from 0.0 to
> 0.35 while retaining repair-improvement rescue. Its four fresh seed-11 pairs
> completed with no execution, feasibility, fleet, or route-count failure:
> P-n65 finished 16 distance units worse and E-n101, X-n120, and X-n233 tied.
> Across the roster, embedded-VNS runtime share moved from 0.4199 to 0.2878 and
> ALNS iterations from 44 to 151, but ALNS best updates moved from 7 to 4 and
> VNS improvement count from 192 to 90. X-n120 and X-n233 each completed only
> one candidate ALNS iteration while initial VNS consumed large deadline shares.
> This shows that the tested cap activated and freed iterations without
> improving final quality. It does not prescribe acceptance, destroy/repair,
> initial VNS, local search, scheduling, any parameter, or any file.

This is feedback about an observed failed mechanism, not an instruction to
edit a particular module. It cannot enter Decision or relax Contract,
Verification, Protocol, feasibility or fleet semantics.
Its typed guidance-block id is `cvrp_r53_negative_feedback_prior`.

## Frozen measurement

R54 deliberately reuses R53's exact screening measurement so the only planned
experimental change is the feedback-conditioned second mechanism. Verification
uses a two-second runtime limit. Protocol canary uses
`controlled/data/synthetic_controlled_canary_5.vrp`, seed `101`, with a
two-second per-subject limit. Canary remains veto-only and is not improvement
evidence.

If Contract, Verification and canary permit formal evaluation, the only
reachable stage is one initial, non-expanded `SCREENING` call over seed `11`:

| Ordinal | Case | Per-subject limit | Scheduled order |
|---:|---|---:|---|
| 0 | `cvrplib/P/P-n65-k10.vrp` | 8 s | `AB` |
| 1 | `cvrplib/E/E-n101-k14.vrp` | 12 s | `BA` |
| 2 | `cvrplib/X/X-n120-k6.vrp` | 12 s | `AB` |
| 3 | `cvrplib/X/X-n233-k16.vrp` | 15 s | `BA` |

`A` is fresh B0/v1 and `B` is the fresh R54 candidate. Candidate ordinal and
block ordinal are zero; case ordinals are `0..3`, seed ordinal is zero, and
actual order must equal scheduled order. Champion caching is disabled and the
declared subjects execute fresh. The maximum declared screening solver time is
94 subject-seconds. The frozen paired block id is
`v04-cvrp-minute-feedback-one-step-20260812-screening`.

The problem-owned estimator remains paired-effect median with zero equivalence
band. `total_distance` is the effect metric and `fleet_violation` is protected.
The practical screening delta remains `2`; the ordinary case-quality rule is
unchanged. `require_expanded_for_pass=false` because this wrapper executes only
one exploratory step and cannot drain another stage.

Schema-valid disjoint placeholders remain unreachable: validation uses
`cvrplib/B/B-n67-k10.vrp`, seed `47`; frozen uses
`cvrplib/P/P-n76-k4.vrp`, seed `61`. The wrapper rejects expanded screening,
validation and frozen execution, invokes `CampaignManager.run_one_step()` once,
and immediately calls
`finalize_requested_stop("minute_feedback_one_step_complete")` after the
returned step. A queued Decision is recorded but not executed by this run.

## Adaptive same-roster claim boundary

R53's seed-11 result directly shaped this H question and prior. Reusing the
same seed, cases, limits and AB/BA schedule is intentional for a controlled
mechanism comparison, but it is adaptive evidence on an already observed
roster. Even a positive complete R54 screen cannot establish independent
replication, generalization, validation, frozen retention, promotion or an
improvement against B0 beyond this feedback-conditioned screen.

If the ordinary Protocol result is positive, R54 still stops. The only next
admissible evidence would first be a separately preregistered seed-29
replication of the exact fixed R54 candidate. This document neither designs,
authorizes nor launches that replication, and a positive R54 Decision cannot
bypass it to validation or promotion.

## Execution and process envelope

- Local solver subprocess limits are 45 seconds, 2,048 MiB and 256 file
  descriptors. The driver hard wall is 720 seconds, delivered through
  foreground `SIGINT` with a bounded kill-after fallback.
- Launch is ordinary-user `clawd` only. It uses tmux socket
  `/tmp/scion-minute-feedback.tmux.sock` and session
  `scion-minute-feedback`, with remain-on-exit and bounded process observation.
  No root, systemd, deployment, distribution, packaging or build step exists.
- The inner process starts from a minimal `env -i`. The non-`SCION_*`
  ephemeral `CODEX_PROXY_CLIENT_KEY` is removed by the driver before any
  candidate/champion subprocess and passed explicitly only to the provider
  client. It is not persisted or printed.
- Proxy authentication/model acquisition is bounded. Output, control and tmux
  socket must be fresh. The driver creates the output once, writes terminal
  status atomically and has no resume or retry path.

Frozen future roots are:

- output:
  `/home/clawd/research/scion-experiments/v04-cvrp-minute-feedback-one-step-20260812-claw`;
- process control:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-minute-feedback-one-step-20260812-control`;
- input:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-minute-feedback-one-step-20260812-input`.

## Completion and scientific classification

The hot-path driver does not turn raw metrics or candidate quality into a new
acceptance gate. A normal return from the single `run_one_step()` followed by
successful finalization is `COMPLETED_ONE_STEP_WRAPPER`, regardless of an H/C
rejection, canary veto, subject failure or positive/negative Protocol result.
The driver records bounded calls, returned typed summary and any raw reference.
Only a lifecycle exception or violation of the declared one-step envelope makes
the wrapper `RUN_INVALID`.

Scientific classification is a separate read-only postrun operation. A
complete successful four-pair result may be called one valid exploratory
screen; typed research or subject rejection may be retained only within its
exact boundary; partial, interrupted, resource, configuration, malformed or
untyped evidence is invalid. None of these postrun labels may change Decision,
resume the root or invoke another provider or formal stage.

Champion must remain B0/v1. No `PROMOTE` is admissible, and no validation,
frozen holdout, final replay or independent retention executes. Any
`campaign_summary.run_complete` value is interpreted only within the one-step
wrapper scope.

## Launch rule

The sole launch is blocked on fresh explicit source-send authorization. Once
authorization exists, the prepared launcher may run once without changing the
source, question, feedback prior, model, call/time limits, case roster, seed,
order, cache policy, Protocol or claim boundary. Failure does not authorize a
retry.

After terminalization, independent read-only postrun analysis must separate H
research quality, C fidelity, applied source, Contract, Verification, canary,
subject execution, Protocol measurement and deterministic Decision. The result
cannot launch seed-29 replication or any promotion campaign by itself.
