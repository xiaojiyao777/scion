# Warehouse prod-1.2 Independent Held-Out Replay Preregistration — 2026-08-09

**Status:** `COMPLETED_RETAINED_PRODUCTION_IMPROVEMENT`

The fixed replay is reported in the
[postrun](v0.4-warehouse-prod12-independent-heldout-v1-postrun-20260809.md).

## Question

Does the sole champion promoted by the completed Warehouse prod-1.2 campaign
retain its improvement over its immediate parent on production-style cases
that were fixed before launch and never used by the campaign?

This is a no-LLM, evaluation-only comparison. It neither reopens the campaign
nor asks an agent to change code.

## Completed source campaign

- Campaign root:
  `/home/clawd/research/scion-experiments/v04-warehouse-v3-production-transfer-prod12-24stage-r1-gpt56terra-20260809T044055Z-claw/campaign`.
- The wrapper finished with exit status 0 and no signal.
- All 24 requested typed Protocol stages completed: 22 screening, one
  validation and one frozen stage.
- The final champion is v2, so the only directed comparison is the persisted
  `champion_v2` snapshot against its immediate parent `champion_v1`.
- Runtime source for this replay is commit
  `b9730c35512e1d377cfea4b868cea45f761c2d0d`. Its only executable difference
  from the campaign source is the tested reporting compatibility fix that
  recognizes the current Protocol success reason `FROZEN_PASS`; it does not
  change solver snapshots, cases, seeds, measurement, or gate semantics.

## Fixed population and measurement

The frozen population is exactly:

1. `instance_prod_fro_x03.json`
2. `instance_prod_fro_xx01.json`
3. `instance_prod_fro_xx02.json`
4. production `instance_prod_day1.json`

The seeds are exactly `256`, `512`, and `1024`, giving 12 formal pairs. Both
sides also run the fixed production canaries `instance_prod_can_s01.json` and
`instance_prod_can_s02.json` with seed `999` before formal evaluation. The
per-execution limit is 30 seconds.

The comparison uses the unchanged Warehouse problem declaration,
`protocol_prod.yaml`, case aggregation, lexicographic objective
(`subcategory_splits`, then `total_cost`), problem-owned `total_cost` effect
statistics, 10,000-sample frozen bootstrap, and current `FROZEN_PASS`
semantics. Runtime remains diagnostic only.

The tracked inputs are:

- `warehouse-prod12-independent-heldout-v1/heldout_split.yaml`;
- `warehouse-prod12-independent-heldout-v1/comparison_manifest.v1.json`;
- the existing production seed ledger, whose frozen and canary seeds are used
  without modification.

The held-out cases were selected in the prod-1.2 preregistration, before the
campaign produced a candidate. A terminal read-only check of every formal
event, metric file, provider trace and status artifact found none of the four
case names. Any later match invalidates this replay before execution.

## Fixed outcome classification

- `RETAINED_PRODUCTION_IMPROVEMENT`: canaries pass, all 12 pairs are valid,
  neither side fails, and the unchanged frozen Protocol returns
  `FROZEN_PASS` for v2 versus v1.
- `COMPLETE_NOT_RETAINED`: all declared evidence is complete but the frozen
  Protocol does not pass.
- `REPLAY_INVALID_EXECUTION`: a canary or formal execution fails, a declared
  pair is absent, or the replay is interrupted.

An in-campaign promotion remains only
`PRODUCTION_PROMOTION_OBSERVED_REPLAY_PENDING` until this comparison completes.
This replay cannot establish a second Warehouse promotion, CVRP improvement,
or overall `TASK.md` completion.

## Frozen command

Run once from the absent output directory:

```bash
set -euo pipefail

SOURCE=/home/clawd/research/or-autoresearch-agent-v3-hotpath/scion
MANIFEST="$SOURCE/docs/experiments/v0.4/warehouse-prod12-independent-heldout-v1/comparison_manifest.v1.json"
OUTPUT=/home/clawd/research/scion-experiments/v04-warehouse-prod12-independent-heldout-v1-20260809T072000Z-claw/run

test ! -e "$OUTPUT"

env -i \
  PATH=/home/clawd/miniconda3/envs/claw/bin:/usr/bin:/bin \
  LANG=C.UTF-8 \
  LC_ALL=C.UTF-8 \
  PYTHONPATH="$SOURCE" \
  /home/clawd/miniconda3/envs/claw/bin/python -m scion.cli.main report \
  champion-heldout-comparison \
  --manifest "$MANIFEST" \
  --output-dir "$OUTPUT"
```

No Trust/Hash acceptance, identity, lease, issuance, registration, deployment,
distribution, packaging, root, or systemd step is part of this replay.
