# v0.4 Solver-Design Semantic-Identity Guidance Sonnet 4R Diagnostic

*Date: 2026-05-12*
*Status: completed; semantic-identity and active-boundary guidance repair validated; solver quality still blocked*

## Run

```text
run_root=/home/clawd/research/scion-experiments/v04-solver-design-semantic-identity-guidance-sonnet-4r-20260512T020020Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=4
rounds_completed=4
screened_experiments=3
time_limit_sec=30
agentic_proposal=true
agentic_session_timeout_sec=600
force_surface=none
SCION_PROBLEM_DATA_ROOT=/home/clawd/research/or-autoresearch-agent/vrp
scion_commit_at_launch=8618917c84100fd87daac03413aa0446a0e4f0ee
worktree_dirty_at_launch=false
exit_code=0
stopped_reason=max_rounds_exhausted
```

Raw artifacts checked: `exit.txt`, `launch.env`, `campaign/status.json`,
`campaign/campaign_summary.json`, `campaign/scion.db`, APS `output.json`
files, `agentic_session_index.json`, metrics JSON files, and selected LLM
traces.

## Completion Status

```text
status.total_rounds=4
status.n_steps=4
status.n_experiments=3
status.n_active_branches=0
circuit_breaker_tripped=false
stopped=null
agentic_sessions=8
agentic_statuses={completed: 4, partial_hypothesis_only: 4}
action_locus_coverage={modify/solver_design: 4}
verification_failure_breakdown={V5_solution_consistency: 1}
```

## Results

| Order | Surface | Target | Outcome |
|---:|---|---|---|
| 1 | `solver_design` | `policies/main_search_strategy.py` | Contract passed; Verification passed; screening failed with `win_rate=0.0`, `median_delta=0.0`; abandoned |
| 2 | `solver_design` | `policies/main_search_strategy.py` | Contract passed; Verification passed; screening improved slightly to `win_rate=0.125`, `median_delta=0.0`; abandoned |
| 3 | `solver_design` | `policies/main_search_strategy.py` | Contract passed; Verification passed; screening failed with `win_rate=0.0`, `median_delta=0.0`; abandoned |
| 4 | `solver_design` | `policies/main_search_strategy.py` | Contract passed; heavy Verification failed with `V5_solution_consistency`; candidate rejected |

Persisted hypotheses:

```text
3dfe44f1-8a2d-47ba-9b86-15ddf5e71ad8  rejected  solver_design  baseline_75pct_improvement_4rounds_topk32
1aa2cd96-93f9-49f5-81e2-27c943037853  rejected  solver_design  baseline_90pct_improvement_1round_topk64
1fcf58a8-16b1-408d-b7d2-591861344dee  rejected  solver_design  baseline_95pct_improvement_1round_topk16_destroy_only
c249d90e-b348-4b03-8b6d-f4544980aa43  rejected  solver_design  baseline_50pct_improvement_8rounds_topk128
```

Every persisted hypothesis had non-empty
`novelty_signature.selected_components` and
`novelty_signature.deep_components_selected`.

APS self-check summary:

```text
completed sessions=4
partial_hypothesis_only sessions=4
schema_valid=true for all 8 sessions
contract_preview_passed=true for all 4 completed sessions
observation budget range=24104/64000 to 51872/64000
```

No APS artifact contained `result_too_large`. No valid-run trace contained the
old active-boundary bug pattern (`forced_surface_rule` or
`allowed_surface_ids=[null]`). Traces contained the repaired
`active_problem_boundary_rule` with `allowed_surface_ids=["solver_design"]`.

## Runtime Interpretation

The control-plane repair worked. Across all 8 APS sessions and all 4 persisted
hypotheses, the proposal path stayed on `solver_design` with target
`policies/main_search_strategy.py`. The run did not fall back to isolated
component-policy surfaces after screening failures.

The semantic-identity repair also worked. The model supplied non-empty
component arrays for every accepted `solver_design` hypothesis, and the
completed sessions reached Contract preview instead of dying from shallow or
empty semantic signatures.

Candidate diversity improved compared with the previous shallow loop. The four
solver-design plans changed baseline budget, selected components, restart and
perturbation patterns, and top-k/round budgets rather than repeating one
policy knob:

```text
round 1: baseline 0.75, all main-search components, restart/after-no-improvement perturbation
round 2: baseline 0.90, intra_route_2opt + route_pair_swap, no restart/perturbation
round 3: baseline 0.95, bounded_destroy_repair only, before-first-round perturbation
round 4: baseline 0.50, intra_route_2opt + inter_route_relocate, 8 rounds/topk128
```

Solver efficacy remains blocked. The only screening improvement was small
(`win_rate=0.125`, `median_delta=0.0`) and came with runtime regression
(`runtime_ratio_median=1.124`, `runtime_delta_median_ms=3032.5`). The
main-search phase still did not produce phase-best movement:
`main_search_component_phase_delta_sum`,
`main_search_component_phase_best_delta`, and
`main_search_component_phase_improvement_counts` were zero for all screened
candidates. The first candidate accepted some current/recovery moves, but
those did not refresh phase best or case-level median quality.

The fourth candidate failed Verification because selected-surface evidence was
empty for `main_search_deep_components_selected`. That fail-closed behavior is
correct: the hypothesis declared deep components, but runtime evidence did not
show non-empty selected deep components.

## Invalid Pre-Run

An earlier validation attempt was intentionally terminated and should not count
as validation:

```text
run_root=/home/clawd/research/scion-experiments/v04-solver-design-semantic-identity-sonnet-4r-20260512T014032Z
scion_commit_at_launch=3f950e83638d4b3de95017556b0f06dc2a1b7af3
exit.txt=terminated_for_prompt_guidance_bug
```

That run did show non-empty semantic signatures and stayed on `solver_design`,
but traces exposed a prompt-guidance bug: active-boundary sessions rendered
`forced_surface_rule` and `allowed_surface_ids=[null]`. Commit `8618917`
fixed that bug, and the valid run above confirmed the repaired guidance.

## Follow-Up

1. Keep `solver_design` as the default top-level CVRP research object.
2. Do not return to forced singleton component-policy diagnostics as the main
   optimization path.
3. Do not run long CVRP solver-quality validation yet. The short diagnostic
   still lacks nonzero main-search phase-best movement.
4. Next repair should target solver lifecycle quality: why component-local
   accepted/recovery moves fail to refresh phase best, why bounded destroy
   repair still exhausts repair budget, and why extra baseline budget can
   create isolated wins without median movement.
