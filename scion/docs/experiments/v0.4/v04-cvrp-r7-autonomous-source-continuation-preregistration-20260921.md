# CVRP R7: autonomous complete-source continuation

State: completed normally at `2026-09-22T03:23:30.659934+00:00` with
`requested_rounds_completed`: 12 evaluated screening stages, one Contract
rejection, no promotion or held-out evaluation. The final positive initial
screen requests expansion at the round-count stop; terminal R7 must not resume.
See the [postrun](v04-cvrp-r7-autonomous-source-continuation-postrun-20260922.md).
Launched once at `2026-09-21T23:21:02Z` from clean `ffde7f66` in
`scion-r7-source-continuation-20260921` (driver PID 86636). The prospective inputs
below remain frozen; this administrative update does not change them.

At launch, status was running, version 1 / weight revision 0, one explore branch,
zero evaluated rounds. The first three provider traces report successful
`gpt-5.6-sol` H turns: read two source files and one historical record. The
actual H index offers all 3 prior observations plus 65 scientific history rows;
index availability does not imply every record has been read. The new
`champions/champion_v1` snapshot is byte-equal to all 100 files of the selected
source. No scientific result or promotion is inferred from this startup evidence.

The user requested experiment analysis followed by optimization on 2026-09-21.
The [R6 postrun](v04-cvrp-r6-minus-2for1-b0-postrun-20260921.md) closes its
negative fixed comparison and discloses overlapping server cleanup. R7 is a
fresh autonomous development campaign, not an R6 retry or a confirmation claim.

## Source, question and boundaries

Runtime is the current main checkout on `v0.4-dev`, with implementation
unchanged from `e405bfd2`. New files are this prospective record, ordinary
scientific/research inputs, a focused input test and status/postrun documents.
They were committed as `ffde7f66` before launch; source and inputs remain frozen
thereafter. Any later launch-status edit is documentation only.

The starting source is the complete, read-only R6
[input snapshot](/home/clawd/research/scion-experiments/v04-cvrp-r6-minus-2for1-b0-20260921/input_snapshots/candidate):
v2-minus-2-for-1, byte-equal to its 100-file declared input tree. Selecting this
value preserves existing algorithm research without replaying patches. It does
not assert superiority over original B0. The CLI starts version 1, weight
revision 0, fresh branches and fresh provider counters, and copies the whole
tree into its normal champion snapshot. It restores no mutable campaign state.

Question: can the autonomous H/C agent improve this complete starting algorithm
through successive verified source changes and paired development evidence,
while protecting feasibility and fleet violation? H chooses the mechanism and
C implements it. There is no forced target, patch, action or host-selected
search schedule; no generic-core or problem-owned solver edit precedes launch.
Algorithm time allocation is researchable source, not a host-imposed new gate.

## Prior evidence and exposure

The [ordinary research input](inputs/v04-cvrp-r7-autonomous-source-continuation-research-input.json)
contains exactly three chronological observations: R4 full-v2 versus B0,
R5 diagnostic full-v2 versus minus-2-for-1, and R6 minus-2-for-1 versus B0.
They record distinct contrasts, complete screening counts and negative results;
R5 has no Decision or promotion (`not_applicable` in the existing input
transport), and R6 explicitly records possible operator resource contention.
R6 additionally exposes all six case medians/sign patterns and the observed
zero-ALNS large-case phase behavior, without prescribing a repair. No held-out
outcome, source path, target filename or synthetic H/C is inserted.

Existing ordinary history files are loaded in the exact order R3, R3b, R3c,
R3d, R3e, R3f, R3g, R3h, R3i; exact paths appear below. File line counts are
`[21,1,2,1,1,22,2,15,23] = 88`, totaling 2,824,607 bytes. The unchanged
production projection exposes 65 scientific records and excludes 23 typed
operational rows from H; original files are untouched. There is no new
ranking, selection, truncation or summary policy. These are H-only facts,
not restored branch state or proof that old patches fit the current source.

## Prospective science

- [Protocol](inputs/v04-cvrp-r7-autonomous-source-continuation-protocol.yaml)
- [Split](inputs/v04-cvrp-r7-autonomous-source-continuation-split.yaml)
- [Seeds](inputs/v04-cvrp-r7-autonomous-source-continuation-seeds.yaml)

Reuse R6's exact 6 screening / 6 validation / 12 frozen case partitions and
dimension-based 30/45/60/90/120-second subject limits. Screening is now explicitly
outcome-known adaptive development. R6 never executed its validation or frozen
populations; they remain held out from H/C in R7. The 12-case final retained
block is not in this campaign and remains unopened.

Seeds are the first nine primes above 40,000, not selected by outcomes:
screening 40009/40013/40031/40037; validation 40039/40063; frozen 40087/40093;
canary 40099. They are distinct from the R3–R6 declared ledgers. Canary remains
the familiar synthetic safety case, with a 10-second limit and no effect claim.

The ordinary autonomous funnel starts at 3 cases x 2 seeds; a qualifying
candidate must reach expanded screening 6 x 4, validation 6 x 2 and frozen
12 x 2 with exact source reuse. Complete pairs, feasibility, fleet protection,
case-median aggregation, practical deltas, uncertainty and case-quality gates
are identical to R6. No change to `SCREENING_FAIL_CASE_QUALITY`. An initial
negative screen may retain a verified provisional branch source for deeper
research, as the existing deterministic Decision specifies.

Target: 12 formally evaluated stages, not 12 provider requests or guaranteed
promotions. K=1 and the existing maximum-three-branch scheduler remain unchanged.
The same R3i proposal-local research limits allow 12 turns, 8 reads, 8 searches,
4 tests and no total transcript-character ceiling. Local rejection remains
attempt-local. Parameter search remains explicitly disabled.

Provider: `gpt-5.6-sol`, reasoning `high`, via the existing local proxy.
H/default timeout 180 s; C turn/finalize 300 s. The finite invocation safety
envelope is 600 physical dispatches, up to two charged redispatches per frozen
typed-transient request, and a 172,800-second outer guard. These are resource
limits, not algorithm quality gates or a project-wide campaign lifetime.
A models-list check confirms catalog presence only, not successful generation.

A R7 promotion supports a held-out comparison with this campaign's selected
starting source or later local champion. It does not prove retained improvement
over original B0. CVRP/v0.4 remain open pending a separately declared exact-candidate
B0 comparison. Do not pool R4/R5/R6 or R7's different stage populations into a
single effect estimate.

## Prelaunch checks and operations

- R6 raw pair medians, all 48 formal arm outcomes, fleet equality, AB/BA order
  and complete source snapshots were checked read-only.
- Existing focused source-continuation, source CLI, prior-input/history,
  observation and fixed-funnel tests: 143 passed. New prospective-input tests:
  2 passed, including lossless ordered H-context projection and unchanged gates.
  Runtime was not modified, so the existing 2397-pass full-suite evidence still
  describes its implementation; no extra Warehouse run is required.
- Read-only preparation parses all 24 declared cases, validates source/fresh
  output, normal CLI expansion configuration, disabled parameter search and
  the existing research limits. It creates no campaign or solver/provider call.
- Freeze all inputs before launch, verify no active solver/tests/cleanup and a
  clean worktree, and start once. No maintenance, large tests or parallel solver
  experiment should overlap wall-clock-budgeted measurement.
- Output: `/home/clawd/research/scion-experiments/v04-cvrp-r7-autonomous-source-continuation-20260921`.
  tmux: `scion-r7-source-continuation-20260921`. Both must be absent at launch.
  tmux is only a carrier. Preserve its exit evidence and never resume/restart
  the resulting terminal campaign.

## Frozen launch command

Run the following once in that new tmux pane from the checked-out source. The
credential is fetched at runtime, never printed or written into artifacts.
There is no legacy launcher, prepared-root workflow, identity or receipt.

```bash
set -Eeuo pipefail
cd /home/clawd/research/or-autoresearch-agent
test -z "$(git status --porcelain)"
test ! -e /home/clawd/research/scion-experiments/v04-cvrp-r7-autonomous-source-continuation-20260921
proxy_key_value=$(curl -fsS --connect-timeout 5 --max-time 15 http://127.0.0.1:8080/auth/status | jq -er '.proxy_api_key | select(type == "string" and length > 0)')
curl -fsS --connect-timeout 5 --max-time 15 -H "Authorization: Bearer $proxy_key_value" http://127.0.0.1:8080/v1/models | jq -e 'any(.data[]?; .id == "gpt-5.6-sol")' >/dev/null
exec env \
  PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1 \
  PYTHONPATH=/home/clawd/research/or-autoresearch-agent/scion \
  OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 \
  SCION_PROBLEM_DATA_ROOT=/home/clawd/research/scion-experiment-inputs/v04-cvrp-r6-minus-2for1-b0-20260921/data \
  SCION_MODEL=gpt-5.6-sol SCION_REASONING_EFFORT=high \
  SCION_BASE_URL=http://127.0.0.1:8080 SCION_API_KEY="$proxy_key_value" \
  SCION_LLM_TIMEOUT_SEC=180 SCION_LLM_HYPOTHESIS_RESEARCH_TURN_TIMEOUT_SEC=180 \
  SCION_LLM_CODE_RESEARCH_TURN_TIMEOUT_SEC=300 SCION_LLM_CODE_RESEARCH_FINALIZE_TIMEOUT_SEC=300 \
  /home/clawd/miniconda3/envs/claw/bin/python -B -m scion.cli.main run \
    --problem /home/clawd/research/or-autoresearch-agent/scion/scion/problems/cvrp/problem-v1.yaml \
    --source-tree /home/clawd/research/scion-experiments/v04-cvrp-r6-minus-2for1-b0-20260921/input_snapshots/candidate \
    --research-input /home/clawd/research/or-autoresearch-agent/scion/docs/experiments/v0.4/inputs/v04-cvrp-r7-autonomous-source-continuation-research-input.json \
    --research-history /home/clawd/research/scion-experiments/v04-cvrp-r3-normal-k1-sol-20260828-r1/research_history.jsonl \
    --research-history /home/clawd/research/scion-experiments/v04-cvrp-r3b-normal-k1-sol-20260829-r1/research_history.jsonl \
    --research-history /home/clawd/research/scion-experiments/v04-cvrp-r3c-normal-k1-sol-20260830-r1/research_history.jsonl \
    --research-history /home/clawd/research/scion-experiments/v04-cvrp-r3d-normal-k1-sol-20260830-r1/research_history.jsonl \
    --research-history /home/clawd/research/scion-experiments/v04-cvrp-r3e-normal-k1-sol-20260830-r1/research_history.jsonl \
    --research-history /home/clawd/research/scion-experiments/v04-cvrp-r3f-normal-k1-sol-20260831-r1/research_history.jsonl \
    --research-history /home/clawd/research/scion-experiments/v04-cvrp-r3g-normal-k1-sol-20260901-r1/research_history.jsonl \
    --research-history /home/clawd/research/scion-experiments/v04-cvrp-r3h-normal-k1-sol-20260902-r1/research_history.jsonl \
    --research-history /home/clawd/research/scion-experiments/v04-cvrp-r3i-normal-k1-sol-20260903-r1/research_history.jsonl \
    --code-research-limits /home/clawd/research/or-autoresearch-agent/scion/docs/experiments/v0.4/inputs/v04-cvrp-r3i-long-run-code-research-limits.json \
    --protocol /home/clawd/research/or-autoresearch-agent/scion/docs/experiments/v0.4/inputs/v04-cvrp-r7-autonomous-source-continuation-protocol.yaml \
    --split /home/clawd/research/or-autoresearch-agent/scion/docs/experiments/v0.4/inputs/v04-cvrp-r7-autonomous-source-continuation-split.yaml \
    --seeds /home/clawd/research/or-autoresearch-agent/scion/docs/experiments/v0.4/inputs/v04-cvrp-r7-autonomous-source-continuation-seeds.yaml \
    --time-limit-sec 30 --rounds 12 --provider-call-cap 600 \
    --provider-transient-retries 2 --outer-hardwall-sec 172800 \
    --campaign-dir /home/clawd/research/scion-experiments/v04-cvrp-r7-autonomous-source-continuation-20260921
```

After launch, use current CLI inspect/report and ordinary status/summary,
H/C traces, history and metric references. Do not open the original SQLite
database. Observe actual H grounding, faithful C, verified source continuation,
paired results and Decision; a healthy process or successful provider turn is
not an algorithm improvement.
