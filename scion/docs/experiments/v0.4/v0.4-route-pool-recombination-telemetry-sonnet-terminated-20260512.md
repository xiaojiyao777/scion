# v0.4 Route-Pool Recombination Telemetry Repair

Date: 2026-05-12

Run root:
`/home/clawd/research/scion-experiments/v04-route-pool-recombination-telemetry-sonnet-8r-20260512T121501Z`

## Setup

- Model: `claude-sonnet-4-6`
- Problem/protocol: CVRP formal
- Surface: free `solver_design`
- Rounds requested: 8
- Time limit: 45 seconds
- Agentic proposal timeout: 900 seconds
- Agentic proposal: enabled
- Stop: manually terminated after the first complete screening because the
  run validated route-pool execution/telemetry but showed zero route-pool
  recombination and zero route-pool phase-best movement.

## Result

The run produced one complete screening experiment:

- valid pairs: 16/16
- decision: abandon
- reason: `T4: win_rate < 0.3`
- wins/losses/ties: 1/3/12
- deltas: one `+4.0`, three losses `-1.0`, `-1.0`, `-4.0`, and twelve ties
- median delta: 0.0
- runtime ratio median: 1.083
- runtime regression rate: 1.0

Runtime evidence:

- `main_search_strategy_loaded=true`, `main_search_strategy_active=true`, and
  `main_search_strategy_errors=0` on all 16 pairs.
- Runtime selected and attempted
  `route_pool_recombination`, `route_pair_swap`, and
  `bounded_destroy_repair` on all pairs.
- Route-pool telemetry was preserved in screening metrics on all 16 pairs:
  `main_search_route_pool_source_solutions` present with values 6-8,
  `main_search_route_pool_size` present with values 16-64, and
  `main_search_route_pool_branch_calls` summed to 580.
- `main_search_route_pool_recombined_routes=0` on all 16 pairs.
- `main_search_component_phase_delta_sum.route_pool_recombination=0.0` on all
  16 pairs.
- Only one pair had nonzero main-search improvement-loop phase movement, from
  `bounded_destroy_repair=1.0`; route-pool contributed none.

## Interpretation

The repair moved the system past the earlier exposure failure. Even when the
candidate novelty text still described the old route-pair plus bounded
destroy/repair pattern, solver normalization auto-added
`route_pool_recombination`, execution attempted it, and the formal screening
metrics exposed its source-solution, pool-size, branch-call, and recombined
route telemetry.

The remaining failure is solver quality. The current route-pool primitive
builds pools and searches them, but the bounded recombination search did not
find or accept any route-set recombination on the screening cases. More rounds,
budget, or prompt pressure on the same candidate shape is not justified until
the route-pool candidate-generation/recombination logic itself is improved.

## Follow-Up

Do not start another long CVRP validation from this state. The next useful
slice is a deeper route-pool quality repair: inspect why sampled complete
solutions produce route pools but no accepted recombined route sets, then test
that logic locally and with a short formal diagnostic.
