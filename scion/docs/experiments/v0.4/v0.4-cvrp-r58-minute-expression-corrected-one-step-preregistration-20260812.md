# CVRP R58 Expression-Corrected Seed-29 Minute One-Step Preregistration — 2026-08-12

**Preparation status:** `PREPARED_NOT_STARTED`

**Authorization state:** `AWAITING_FRESH_SOURCE_SEND_AUTHORIZATION`

No R58 provider, canary or formal Protocol call has started. The clean input
bundle exists, while the future output and process-control roots remain absent.
The user's generic assent authorizes preparation only; it does not identify or
authorize the exact source-derived send below. R58 is a new short fresh-B0
experiment, not an R56 repair, retry, resume, replication or promotion run.

## Question, purpose and V3 authority

The H request will ask exactly:

> Starting from fresh B0 under the fixed CVRP deadlines, what one CVRP-owned
> algorithm change is most likely to improve final-best total_distance while
> preserving feasibility, fleet semantics, and useful search within the
> available budget? Use the visible source and the supplied R53–R56 algorithm
> evidence to choose one causal mechanism. You may repeat, refine, or avoid a
> prior proposal; state the observable trajectory that would falsify it.

The immediate purpose is to observe one real algorithm-research step after the
provider-free R57 C-expression correction, using seed `29` instead of the
twice-adaptive seed `11`. The algorithm question remains open: it does not
require repair or use of elapsed-budget simulated annealing, a novel mechanism,
a particular file, parameter, edit intent or implementation shape. Repeating,
refining or avoiding a prior proposal is not a host-side acceptance condition.

[`design/scion-architecture-v3.md`](../../../design/scion-architecture-v3.md)
is the sole architecture authority. The direct-runtime addendum may describe
the lightweight implementation but cannot replace V3 ownership. The active
chain remains:

```text
problem-owned facts + complete fresh-B0 source + safe algorithm evidence
  -> at most one structured H
  -> Hypothesis Contract
  -> at most one approved-H-bound C
  -> Patch Contract
  -> isolated Workspace
  -> executable Verification
  -> canary + problem-owned paired SCREENING Protocol
  -> Safe Features
  -> deterministic Decision
```

No novelty, implementation-style, activation, telemetry-quality or
host-authored algorithm gate is added. LLM output remains tainted and cannot
choose cases, seeds, Protocol results, Decision actions or promotion.

## R57 expression-correction boundary

The clean R58 source contains the provider-free R57 correction at `f537fed5`:
the C schema offers optional source-bound `exact_line_replace` alongside the
existing exact and full-file edit forms. It can express the same complete
logical-line transformation across different outer indentation levels. It adds
no provider source-search, file-navigation, execution, network or edit/repair
loop.

R57 is a C tool fact, not algorithm evidence. It is absent from the H neutral
prior and does not require C to select that edit intent. Exact replacement and
full-file edits remain available, and ordinary Contract, source binding and
Verification decide whether any returned proposal is executable. There is no
required-use, edit-style, match-count, implementation-quality or novelty gate.
The provider-free R56 counterfactual replay is not an R56 algorithm result and
cannot enter Protocol or Decision.

The same clean runtime also contains the already-completed R55 candidate-failure
persistence and problem-owned V3/V4 test corrections. They remain host/runtime
facts rather than algorithm evidence. Before source-send authorization, only
provider-free static bundle checks and the configured subsecond B0 V3/V4
microtests may run. They must not call the proxy, provider, canary or formal
Protocol and cannot be reported as an R58 algorithm result.

## Fresh source-send authorization boundary

The H request would send problem-owned source-derived context from the frozen
runtime, problem facts, the exact question above and the exact R53–R56 neutral
algorithm prior below through the local Codex proxy to `gpt-5.6-terra`. If H
passes Contract, C would receive that exact approved H plus the ordinary
complete editable-source context and the frozen typed C tool schema.

This is a new provider experiment. R53, R54 and R56 authorizations do not carry
forward. The user's generic `同意` authorizes preparation of this preregistration
and bundle only. Launch requires a new explicit authorization that identifies:

- clean source `42535efc` and its fresh-B0 CVRP problem-owned source-derived
  context;
- the R53–R56 neutral algorithm prior and the R58 question;
- local Codex proxy transmission to `gpt-5.6-terra`;
- at most one H and conditional one C, with zero retries; and
- exactly one R58 seed-29 minute one-step screen with fresh subject executions.

Until that exact authorization is recorded, no proxy, provider, canary, formal
Protocol or solver screen may start.

## Frozen source, model and call envelope

| Input | Frozen value |
|---|---|
| Campaign label | `v04-cvrp-r58-minute-expression-corrected-one-step-20260812` |
| Schema | `scion.cvrp_r58_minute_expression_corrected_one_step.v1` |
| Input bundle | `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r58-minute-expression-corrected-one-step-20260812-input` |
| Runtime | one bundle-owned clean `git archive`; no dirty-worktree overlay |
| Exact source reference | `42535efc88582fdd7ab3ece13b349d3c3085813b` |
| Problem/solver source | `runtime/scion/scion/problems/cvrp` |
| External case root | `/home/clawd/research/or-autoresearch-agent/vrp` |
| Champion | original CVRP B0, version `1` |
| Provider | local Codex proxy at `http://127.0.0.1:8080` |
| Model | `gpt-5.6-terra` |
| H calls | at most one; 90-second provider timeout |
| C calls | at most one, only after approved H; 180-second provider timeout |
| Provider retries | zero |
| Campaign-manager calls | `run_one_step()` exactly once; `run()` never |

Relative to the R56 source `acdc80ba`, the R58 executable addition is the
optional R57 `exact_line_replace` C-expression form. CVRP B0 solver policy
source is unchanged. The exact commit is an ordinary scientific source
selection, not an identity, receipt, signature, registry, Trust/Hash or
authority object.

The provider surface remains one structured H call followed conditionally by
one typed C call. Complete visible problem-owned source is supplied through the
ordinary context. No provider shell, execution, network, file-navigation or
same-call edit/repair loop is added.

## Frozen provider-visible R53–R56 prior

H receives one additional neutral guidance block with id
`cvrp_r53_r56_algorithm_evidence_prior` and title
`R53–R56 algorithm evidence`. The following text appears exactly once and R57
tool evidence does not appear in it:

> R53 started from B0 and changed only the global embedded-VNS runtime-share cap
> from 0.0 to 0.35. On four fresh seed-11 pairs it completed without execution,
> feasibility, or fleet failure: P-n65 was 16 distance units worse and E-n101,
> X-n120, and X-n233 tied. The cap activated and ALNS iterations increased from
> 44 to 151, but best updates fell from 7 to 4 and final quality did not improve.
> This is negative evidence only for that tested cap on that roster, not for
> phase allocation or another mechanism. R54 and R56 each again started from
> B0, and each H proposed elapsed-budget-normalized simulated annealing after
> considering the R53 result. Neither produced an admissible algorithm-quality
> result: R54's applied source was mechanically incomplete and produced one
> typed candidate runtime failure plus three valid ties, while R56's applied
> source was mechanically incomplete and Verification rejected it before canary
> or formal Protocol. The quality of elapsed-budget-normalized simulated
> annealing is therefore unidentifiable rather than positive or negative. These
> facts do not prescribe repairing, repeating, or avoiding it, or selecting any
> mechanism, parameter, file, edit intent, or implementation shape.

The failed R54 row remains excluded from delta and W/L/T, and R56 has zero
Protocol evidence. The prior therefore neither promotes nor rejects
elapsed-budget simulated annealing. It cannot enter Decision or relax Contract,
Verification, feasibility, fleet or Protocol semantics.

## Verification, canary and frozen screening population

Verification uses a two-second runtime limit and the configured problem-owned
V3/V4 tests. Protocol canary uses
`controlled/data/synthetic_controlled_canary_5.vrp`, seed `101`, with a
two-second per-subject limit. Canary is veto-only and is not improvement
evidence.

If Contract and Verification pass and canary does not veto, the only reachable
formal stage is one initial, non-expanded `SCREENING` call. The experiment-owned
complete screening seed roster is `[29]`; its only seed has ordinal zero:

| Ordinal | Case | Seed | Per-subject limit | Scheduled order |
|---:|---|---:|---:|---|
| 0 | `cvrplib/P/P-n65-k10.vrp` | 29 | 8 s | `AB` |
| 1 | `cvrplib/E/E-n101-k14.vrp` | 29 | 12 s | `BA` |
| 2 | `cvrplib/X/X-n120-k6.vrp` | 29 | 12 s | `AB` |
| 3 | `cvrplib/X/X-n233-k16.vrp` | 29 | 15 s | `BA` |

`A` is fresh B0/v1 and `B` is the fresh R58 candidate. Candidate and block
ordinals are zero; case ordinals are `0..3`; seed ordinal is zero. The existing
parity rule therefore yields `AB/BA/AB/BA`, and actual order must equal
scheduled order. Champion caching is disabled, so both subjects in every pair
execute fresh. The paired block id is
`v04-cvrp-r58-minute-expression-corrected-one-step-20260812-screening`; maximum
declared formal solver time is `94` subject-seconds.

The estimator remains paired-effect median with zero equivalence band.
`total_distance` is lower-better and `fleet_violation` is protected. The
practical screening delta remains `2`; the unchanged screening rule retains
case win rate `0.60`, bootstrap-CI lower bound `0`, minimum net case score
`0.25`, and maximum case loss rate `0.20`.
`require_expanded_for_pass=false` because this is a one-step observation, not
an expansion campaign.

Schema-valid disjoint placeholders remain unreachable: validation uses
`cvrplib/B/B-n67-k10.vrp`, seed `47`; frozen uses
`cvrplib/P/P-n76-k4.vrp`, seed `61`. The wrapper rejects expanded screening,
validation and frozen execution. It calls `finalize_requested_stop()`
immediately after the single returned step; any queued follow-up is retained as
evidence but is not drained.

## Why seed 29 changes the claim but does not create a holdout

R53 observed seed `11`; that result shaped R54, and the R53–R54 results then
shaped R56. The R56 preregistration therefore correctly described seed `11` as
twice adaptive. Reusing it again would add another adaptive observation without
testing a new seed coordinate. R58 prospectively moves to seed `29`, the next
ordered screening seed, while keeping the cases, deadlines, cache policy,
subject roles and order schedule fixed.

Seed `29` differs from the immediate R53–R56 minute-series seed `11`, but it is
not globally unseen, out of prior evidence or a protected holdout. R3 already
used ordered seed `29` on these exact P65/E101/X120/X233 cases as part of its
larger formal population. Only the R58 subject executions are fresh. The four
cases and seed coordinate have prior exposure, and R58 generates a new H/C
candidate rather than fixing the exact R54 or R56 candidate. Consequently:

- R58 is not an exact-candidate replication of R54 or R56;
- its result must not be pooled with seed-11 outcomes from different candidates;
- one seed per case cannot establish seed robustness;
- the already observed four-case roster cannot establish case generalization;
  and
- even a positive result cannot establish validation, frozen retention,
  promotion, retained B0 improvement or broad CVRP improvement.

This seed change is a prospective measurement coordinate, not a new Decision
gate. No result-dependent seed or case selection is permitted after launch.

## Execution and process envelope

- Local solver subprocess limits are 45 seconds, 2,048 MiB and 256 file
  descriptors. The driver hard wall is 720 seconds, delivered through
  foreground `SIGINT` with a bounded kill-after fallback.
- Launch is ordinary-user `clawd` only through the user-owned tmux socket
  `/tmp/scion-r58-minute-expression-corrected.tmux.sock` and session
  `scion-r58-minute-expression-corrected`, with remain-on-exit and bounded
  acquisition observation. No root, systemd, deployment, distribution,
  packaging or build step is involved.
- This two-core host runs no concurrent solver campaign, pytest, benchmark or
  other intentionally CPU-heavy task during R58. The pre-launch conflict check
  is load hygiene, not a candidate, research or quality gate.
- The inner process starts from minimal `env -i`. The non-`SCION_*` ephemeral
  `CODEX_PROXY_CLIENT_KEY` is removed before candidate/champion subprocesses and
  passed explicitly only to the provider client. It is not an argv, log or
  scientific artifact field.
- Proxy authentication and model acquisition are bounded. Output, control and
  socket roots must all be fresh. The driver creates output once, writes
  terminal status atomically and has no resume or retry path.

Frozen future roots are:

- output:
  `/home/clawd/research/scion-experiments/v04-cvrp-r58-minute-expression-corrected-one-step-20260812-claw`;
- process control:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r58-minute-expression-corrected-one-step-20260812-control`;
- input:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r58-minute-expression-corrected-one-step-20260812-input`.

Neither future output nor process-control root may be created during
preparation.

## Completion, terminal classification and claim boundary

A normal return from the single `run_one_step()` followed by successful
finalization is `COMPLETED_ONE_STEP_WRAPPER`, regardless of H/C rejection,
canary veto, candidate failure or Protocol quality result. The hot-path driver
does not parse raw metrics into another gate. Only an exception or violation of
the declared one-step lifecycle makes the wrapper invalid. The normal terminal
stop reason is `r58_minute_expression_corrected_one_step_complete`.

Independent read-only postrun analysis may apply only these bounded
interpretations:

| Terminal evidence | Postrun boundary |
|---|---|
| Four complete successful seed-29 pairs with both subjects executed fresh, plus a typed Decision | One valid new-H/C different-seed exploratory screen on an already exposed case/seed coordinate |
| Typed candidate canary veto | Bounded candidate-canary negative; no screen comparison |
| Complete formal attempt containing candidate-side failure | Terminal candidate-execution negative; failed objectives excluded and four-case W/L/T undefined |
| Complete formal attempt containing champion-side failure | Partial champion evidence; candidate comparison unavailable |
| Typed terminal H/C rejection, including a malformed/schema-invalid terminal provider response, or Contract/Verification rejection before Protocol | Valid terminal `RESEARCH_REJECTED`/research-process observation; zero solver-quality evidence |
| Partial, interrupted, resource or configuration failure; missing or invalid required local artifact; missing typed execution outcome; provider call without a terminal response; or framework exception | `RUN_INVALID_*` at the exact failed layer; no scientific comparison |

None of these labels may alter Decision, request another provider call, resume
the root or enter another formal stage. A complete clean run is one post-R57
expression-corrected seed-29 lifecycle observation, not proof of stability or
authorization for a long campaign.

Champion must remain B0/v1. No `PROMOTE` is admissible; no expanded screening,
validation, frozen holdout, final replay or independent retention runs. A
positive screening action may remain queued for evidence purposes, but this
wrapper cannot drain it. `campaign_summary.run_complete`, if present, refers
only to the bounded wrapper lifecycle.

## Launch and postrun rule

Launch remains blocked on fresh exact R58 source-send authorization. Once that
authorization exists, the frozen launcher may run once without changing source,
question, prior, model, tool schema, call/time limits, tests, cases, seed,
ordinals, order, cache policy, Protocol or claims. Acquisition, provider,
candidate, champion or host failure does not authorize a retry.

After terminalization, read-only analysis must separate H algorithm reasoning,
C expression fidelity, applied source, Contract, each Verification check,
canary, raw pair evidence, Protocol, Decision and wrapper/process stability.
The result may motivate another separately preregistered short step. It cannot
launch that step, replicate another candidate, validate, freeze, promote or
start a long campaign itself.
