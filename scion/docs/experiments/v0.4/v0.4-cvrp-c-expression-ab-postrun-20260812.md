# v0.4 CVRP C-expression matched A/B postrun

**Status:** `VALID_COMPLETE_C_EXPRESSION_DIAGNOSTIC / STRICT_DIFF_NOT_ADOPTED / EXACT_RETAINED`

## Boundary

This was the frozen proposal-expression diagnostic from the
[preregistration](v0.4-cvrp-c-expression-ab-preregistration-20260810.md), not a
solver experiment. It compared the current typed exact/full-file terminal tool
with a strict source-bound unified-diff terminal tool on four matched approved-H
and source fixtures. It ran no solver, Protocol or Decision, and its result
cannot promote, rank or reinterpret an algorithm.

The experiment output is
`/home/clawd/research/scion-experiments/v04-cvrp-c-expression-ab-20260811-claw`.
The ordinary-user launcher acquired the driver at `2026-08-12T08:54:12Z`; by
about `09:00:07Z` (about 5m55s) the driver had exited `0` and terminalized all
8/8 planned provider cells without retry or provider failure.

## Terminal cells

All eight responses ended with one JSON-valid `generate_patch` tool call. The
four exact cells normalized, applied and passed the unchanged Patch Contract.
Each of the four diff cells returned a provider-terminal proposal, but strict
application rejected it with `context or removal does not match source`. As
preregistered, each rejection is a complete treatment-negative cell with fixed
H-obligation/source-anchor scores `0/0`, source
`fixed_no_normalized_patch`, and no blind packet. No cell was retried or
resumed. Terminal review found hunk-position mismatches of at least `+2`,
`+12/+21`, `+37/+14` and `+3` lines across the four diff cells. The frozen
no-offset/no-fuzzy parser therefore rejected them as designed; this was not a
parser relaxation trigger.

## Blind review and adoption result

The four exact normalized patches produced four blind packets. Two independent
reviewers agreed on every packet. In frozen fixture order, both reviewers
assigned H-obligation/source-anchor scores `2/2`, `1/1`, `1/1`, and `1/1`.
Thus each reviewer's exact-arm totals are H-obligation `5` and source-anchor
`5`; the diff arm's fixed totals are `0/0`.

| Blind packet | Mechanical arm after review | Blind reviewer 1 H/anchor | Blind reviewer 2 H/anchor |
|---|---|---:|---:|
| `00-response-pine.json` | exact | `2/2` | `2/2` |
| `01-response-lake.json` | exact | `1/1` | `1/1` |
| `02-response-lake.json` | exact | `1/1` | `1/1` |
| `03-response-pine.json` | exact | `1/1` | `1/1` |

Each reviewer was restricted to these four packets and the four frozen fixture
checklists and declared that no arm mapping, cell record, historical response,
or solver/Protocol result was accessed during scoring. Diff cells had no
normalized patch or review packet and therefore retained their preregistered
mechanical `0/0` scores.

All four adoption conditions fail: zero of four diff cells parsed, applied and
passed Contract; both successful controls regress to `0/0`; both omission
fixtures regress rather than improve; and diff totals are not strictly higher
than exact totals. The preregistered decision is therefore to retain the
current exact/full-file expression and close this strict-diff direction. There
is no production normalizer change, repair, retry or new gate.

This result is bounded to the frozen strict parser, tool definition, prompts
and fixtures. It does not establish that exact edits are universally superior;
it establishes that this strict unified-diff treatment is not a viable next
Scion intervention. Subsequent CVRP work remains focused on lightweight
research context and real algorithm experiments.

## Earlier acquisition observation

Before the scientific run, one control-only attempt failed before driver
acquisition and made zero scientific/model-generation or solver calls. Its
unchanged observation is archived at
`campaign_out/v04-cvrp-c-expression-ab-20260811-control-pre-driver-acquisition-failure-20260812T084111Z`.
It consumed no scientific cell and is not combined with the terminal run.
