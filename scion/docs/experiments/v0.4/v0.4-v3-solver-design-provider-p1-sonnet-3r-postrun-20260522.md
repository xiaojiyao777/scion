# v0.4 v3 solver-design provider P1 Sonnet 3R postrun 分析 - 2026-05-22

实验路径：
`/home/clawd/research/scion-experiments/v04-v3-solver-design-provider-p1-sonnet-3r-20260522T161325Z`

代码仓库：
`/home/clawd/research/or-autoresearch-agent`

验证 commit：
`2371701`

本报告只分析实验产物并新增本独立文档，不修改 Scion 源码。分析依据包括
`command.txt`、`exit.txt`、`launch.env`、`run.log`、`campaign/status.json`、
`campaign/campaign_summary.json`、`campaign/scion.db`、`campaign/metrics/*.json`、
`campaign/llm_traces/*.json`、`campaign/agentic_sessions/*/{output,transcript}.json`，
以及三个 screened 分支的 workspace/archive 代码。

## 0. 结论摘要

实验正常结束：`exit.txt` 为 `EXIT_CODE:0`，命令请求 `--rounds 3`，最终完成 3 个
effective screening，`stopped_reason=max_rounds_exhausted`。没有 champion 晋升，最终
`champion_version=1`、`champion_weight_revision=0`、`n_active_branches=0`。

本轮最关键的修复验证是正向的：`proposal_quality_blocks_consumed=0/6`，没有 campaign
级 proposal-quality block。首轮 `spatial_cluster_removal` 明确使用 geographic/spatial
cluster 语言，也对比了 random/Shaw/worst/route removal；它没有再被 random removal 或
Shaw/geographic cluster novelty matcher 误伤，而是完成 code、contract、smoke 和 screening。
因此本轮针对 random/geographic cluster 的误伤在 campaign 层面已经消失。

agent 确实看到了与 gate 同源的 active algorithm facts。每个 hypothesis/code prompt
manifest 都包含 `agentic_active_algorithm_facts`，且带 fact packet digest；LLM prompt
正文中的 facts 标记 `used_by_gate=true` 和 `used_by_prompt=true`。tool observations
经常被压缩或省略，但 active facts 没有被省略。

三个 screened 分支都是真正的算法主体修改，不是 adapter 后处理或 protocol 层改写：

- Round 1 修改 `policies/baseline_modules/destroy_repair.py`，并通过
  `policies/baseline_modules/scheduler.py` 把 `spatial_cluster_removal` 注册到 ALNS
  `destroy_ops`。
- Round 2 修改 `policies/baseline_modules/acceptance.py`，并通过 scheduler 在 ALNS loop
  中调用 `gap_adaptive_update`。
- Round 3 修改 `policies/baseline_modules/local_search.py`，把 `_double_bridge_perturbation`
  加入 `_default_vns_operators()`。

质量 gate 链路整体闭合：三轮 contract/verification 都通过，algorithm smoke 都最终通过，
screening telemetry guard 都通过；screening gate 均因 `SCREENING_FAIL_WIN_RATE` 失败。
decision 没有误晋升：Round 1 因有 case-level loss 被 `abandon`，Round 2/3 因中性弱信号
`continue_explore`，随后 max rounds 用尽。

剩余问题主要不在 v3 边界控制，而在算法语义一致性和 telemetry attribution：

- **P1-1：code smoke 能通过一个与 hypothesis 语义不一致的实现。** Round 3 的 hypothesis
  声称加入跨最多 4 条路线的 non-sequential 4-edge / double-bridge perturbation；实际代码
  只在单条 route 内做 `A-B-C-D -> A-C-B-D` 重排，且用 Python `hash(...)` 而不是 `rng`
  抽 split points。这仍是算法主体修改，但不是获批 hypothesis 描述的跨路线机制。
- **P1-2：mechanism-specific effect telemetry 仍可被非因果或膨胀记录满足。** Round 1 在
  destroy 阶段移除 customers 后立刻用 partial solution cost 记录
  `spatial_cluster_removal` 的 positive delta，尚未经过 repair/feasibility/acceptance；
  Round 2 把几乎整个 ALNS loop elapsed time 记到 `gap_adaptive_sa_temperature` phase，
  并把普通 ALNS best-improved delta 也记到 gap-adaptive mechanism。telemetry guard
  因而能看到 activation/effect，但不能保证这些 effect 是机制本身导致的。
- **P1-3：regret repair 文案/语义比旧版本好，但仍未完全干净。** active fact 本身正确地说明
  `_regret2_insertion` 和 `_regret3_insertion` 已在 `repair_ops` 中；provider reason 也已加入
  “mischaracterizes active regret repair semantics”。但实现上仍用
  `_claims_missing_regret_insertion_repair` 这条 missing predicate 同时承载“缺失”和“语义误述”，
  reason 把 absent 与 semantics 混在同一句，且 evidence fact 没有显式描述 regret score
  如何选择客户。此 run 没有 regret proposal block，因此没有 campaign 级损害，但 provider
  的诊断语义仍有残留债务。

P0：未发现。没有 runtime/import/contract 崩溃，没有越界修改，没有错误晋升，没有
proposal-quality loop 停止。

## 1. 实验总体状态

启动命令：

```text
/home/clawd/miniconda3/envs/claw/bin/python -m scion.cli.main run
  --problem scion/problems/cvrp/problem.yaml
  --protocol scion/problems/cvrp/formal/protocol.yaml
  --split scion/problems/cvrp/formal/split_manifest.yaml
  --seeds scion/problems/cvrp/formal/seed_ledger.yaml
  --campaign-dir /home/clawd/research/scion-experiments/v04-v3-solver-design-provider-p1-sonnet-3r-20260522T161325Z/campaign
  --rounds 3
  --time-limit-sec 10
  --disable-early-stop
  --agentic-proposal
```

关键运行参数：

| 项 | 值 |
|---|---|
| model | `claude-sonnet-4-6` |
| data root | `/home/clawd/research/or-autoresearch-agent/vrp` |
| requested rounds | 3 |
| time limit | 10s |
| early stop | disabled |
| agentic proposal | enabled |
| exit | `EXIT_CODE:0`, `EXIT_REASON:command_returned` |

`status.json` / `campaign_summary.json` 核心状态：

| 指标 | 值 | 判断 |
|---|---:|---|
| `proposal_attempts` | 3 | 3 次 attempt，全都进入 effective screening |
| `screened_experiments` | 3 | 与 requested rounds 一致 |
| `campaign_loop.effective_rounds_completed` | 3 | 达成 |
| `proposal_quality_blocks_consumed` | 0 / 6 | 重点修复通过验证 |
| `proposal_quality_blocks_remaining` | 6 | 没有 qblock 消耗 |
| `telemetry_failed_experiments` | 0 | 三个 screening 的 telemetry guard 都通过 |
| `verification_failure_breakdown` | `{}` | 无 verification failure |
| `balance_exhausted` | false | 未耗尽 |
| `circuit_breaker_tripped` | false | 未触发 |
| `budget_utilization` | 0.003 | 预算未接近耗尽 |
| `frozen_budget` | used 0 / limit 2 | 未进入 frozen |
| `action_locus_coverage` | `modify/solver_design: 3` | 全在 v3 solver_design 边界内 |
| `family_coverage` | `solver_design: 3` | 无 legacy/component-policy 偏离 |
| `formal_readiness.status` | `non_formal_final_evidence_closed` | 正常结束但无 formal final evidence package |

LLM trace 数量：

| 类型 | 数量 |
|---|---:|
| `tool_selection` | 53 |
| `hypothesis` | 7 |
| `code` | 7 |

## 2. Campaign 时间线

| Round | Branch | Hypothesis session | Code session | 机制 / target | Gate / decision |
|---:|---|---|---|---|---|
| 1 | `2c0f6b5f...` | `746eba01...` | `3143d3c6...` | `spatial_cluster_removal` / `destroy_repair.py` + scheduler | screening fail, `abandon` |
| 2 | `8266cdd6...` | `006684a3...` | `e444f3d4...` | `gap_adaptive_sa_temperature` / `acceptance.py` + scheduler | screening fail, `continue_explore` |
| 3 | `64d1917c...` | `dbd816b0...` | `e7e057af...` | `double_bridge_perturbation` / `local_search.py` | screening fail, `continue_explore`; then max rounds |

DB `branches` 里 3 个 branch 最终均为 `abandoned`。Round 2/3 的 `failure_codes` 中有
`MAX_ROUNDS_EXHAUSTED`，这是 campaign 收尾清理，不是该 branch 的 contract/runtime failure。

## 3. Round 1 - `spatial_cluster_removal`

### Hypothesis 阶段

Session：`agentic_sessions/746eba01-fa9d-4f93-8722-31787877f32e`

工具调用和输出：

| 步骤 | 工具 | 输出摘要 |
|---|---|---|
| required preface | `context.list_surfaces` | 返回 1 个 declared surface：`solver_design` |
| required preface | `context.read_problem` | 返回 CVRP adapter/spec summary |
| required preface | `context.list_algorithm_files` | 返回 allowlisted solver_design files |
| required preface | `context.read_active_solver_design` | 返回 entrypoint、call graph、mechanisms、provenance、legacy exclusions |
| required preface | `context.read_solver_call_graph` | 返回 active solver_design call graph |
| planner | `memory.query` | 返回 tainted safe memory |
| planner | `feedback.query_screening` | 返回 0/0 screening feedback rows |
| planner | `context.read_algorithm_file` x5 | 读取 `local_search.py`、`destroy_repair.py`、`acceptance.py`、`construction.py`、`config.py` |
| fallback | `feedback.query_runtime` | 无 safe screening-derived runtime feedback |
| self-check | `context.read_surface` | 返回 solver_design interface |
| self-check | `proposal.schema_preview` | 第一次失败，第二次通过 |
| self-check | `proposal.target_permission_preview` | 两次均通过 |

LLM calls：

| 调用 | trace | 结果 |
|---|---|---|
| hypothesis #1 | `20260522T161351293822_hypothesis_a7da0c487c_6372a0de.json` | 提出 `proximity_cluster_removal`，文本承认 `_random_removal`、`_shaw_removal`、`_route_removal`、`_worst_removal` 已存在；schema preview 因 C11 telemetry activation 写法不合规拒绝 |
| hypothesis #2 | `20260522T161416350201_hypothesis_e9635145bc_1603f921.json` | 提出 `spatial_cluster_removal`，`expected_telemetry` 改为 mechanism-specific activation/effect paths，schema/target preview 通过 |

上下文判断：

- prompt manifest `api_visible_prompt_manifest_0002_hypothesis_preview_retry.json` 包含
  `agentic_active_algorithm_facts`，`fact_packet_digest=2b2613c4...`，status 为 included。
- `agentic_tool_observations` 因预算被 omitted/truncated，但 active facts 没有被省略。
- Active facts 明确包含 `_shaw_removal` 是 seed-based related/proximity destroy、
  `_random_removal` 是 scheduler `"random"` operator、`_regret2/_regret3` 已 wired。

结论：首轮 hypothesis 明确触碰了 random/geographic/Shaw contrast 语义，但没有触发
random/Shaw novelty false positive。第一次 rejection 是 C11 telemetry schema，不是
mechanism novelty block。

### Code 阶段

Session：`agentic_sessions/3143d3c6-93d7-4095-b32d-baed03898220`

工具调用和 repair：

| 阶段 | 结果 |
|---|---|
| code context | 重新读取 required preface 和相关 algorithm files，随后读取 `context.read_surface` |
| code #1 | `20260522T161512956697_code_1d8e27e5cd_51c93199.json`; contract preview 通过；algorithm smoke 要求 `context.record_move('spatial_cluster_removal', ..., delta=..., best_improved=...)` |
| code #2 | `20260522T161614566400_code_111f786a4b_06bbd2e2.json`; contract preview 通过；algorithm smoke 未观察到 `solver_algorithm_context_records.spatial_cluster_removal_iterations` |
| code #3 | `20260522T161724305957_code_5ed28de455_18d5ce7e.json`; contract preview 通过；algorithm smoke 通过 |

最终代码修改：

- 在 `destroy_repair.py` 新增 `_spatial_cluster_removal(solution, q, rng, context=None)`。
- 在 `scheduler.py` import 新函数，包装为 `_spatial_cluster_op`，加入
  `destroy_ops = [..., ("spatial_cluster", _spatial_cluster_op)]`。
- 在 spatial destroy 执行时记录 `spatial_cluster_removal` phase、iteration、move telemetry。

这是算法主体修改，且进入 scheduler active path，不是后处理。

### Screening / Decision

Raw metrics：`metrics/ea36210b-ba0a-4aee-9d74-458af792b9ab.json`

| 指标 | 值 |
|---|---:|
| contract / verification | true / true |
| telemetry guard | passed |
| case wins/losses/ties | 0 / 1 / 7 |
| pair wins/losses/ties | 2 / 3 / 11 |
| median delta / CI | 0.0 / [-0.75, 0.75] |
| runtime ratio median | 0.99765 |
| runtime regression rate | 0.375 |
| gate outcome | fail |
| decision | `abandon` |
| reason codes | `SCREENING_FAIL_WIN_RATE`, `SCREENING_SOFT_ABANDON_LOSS_WITHOUT_WIN` |

Telemetry guard 看到 `spatial_cluster_removal` activation/runtime/effect：
`spatial_cluster_removal_iterations` candidate positive 13/16，phase runtime positive 13/16，
phase improvement/best-delta positive 13/16。

质量判断：decision 合理，没有晋升。注意 telemetry 的 effect 语义有缺陷：候选在 destroy
移除 customers 后、repair 之前用 partial solution cost 记录 positive delta，因此 positive
effect 更像“移除客户导致路线成本下降”的结构性副作用，不等于最终 feasible candidate 被该
operator 改善。screening objective 没有被该 telemetry 欺骗成晋升，但 smoke/telemetry guard
没有识别 attribution 问题。

## 4. Round 2 - `gap_adaptive_sa_temperature`

### Hypothesis 阶段

Session：`agentic_sessions/006684a3-2c65-4caf-ac23-477a61f5f29c`

工具调用和输出：

| 步骤 | 工具 | 输出摘要 |
|---|---|---|
| required preface | `context.list_surfaces` / `read_problem` / `list_algorithm_files` / `read_active_solver_design` / `read_solver_call_graph` | solver_design context 完整 |
| planner | `memory.query` | 返回 tainted memory |
| planner | `feedback.query_screening` | 返回 1/1 screening feedback row，即 Round 1 |
| planner | `feedback.query_runtime` | 返回 screening-derived runtime feedback |
| planner | `context.read_algorithm_file` x5 | 读取 `local_search.py`、`destroy_repair.py`、`acceptance.py`、`config.py`、`construction.py` |
| self-check | `context.read_surface` | 返回 solver_design interface |
| self-check | `proposal.schema_preview` | 第一次失败，最终通过 |
| self-check | `proposal.target_permission_preview` | 通过 |

LLM calls：

| 调用 | trace | 结果 |
|---|---|---|
| hypothesis #1 | `20260522T162730492085_hypothesis_be9b400527_c6f38d1a.json` | `nn_accelerated_two_opt_star`；C11 拒绝 activation 引用 aggregate/effect fields |
| hypothesis #2 | `20260522T162758920771_hypothesis_4079c3ea2c_04a9a935.json` | `lk3opt_cross_route`；mechanism novelty gate semantic retry，prompt 中加入 `or_opt_1_relocation` negative fact |
| hypothesis #3 | `20260522T162827924816_hypothesis_498ab077f6_576cf296.json` | `gap_adaptive_sa_temperature`；schema/target preview 通过 |

上下文判断：

- prompt manifest 的 active facts digest 为 `fdc3cf5d...`，`agentic_active_algorithm_facts`
  included。
- 该轮 agent 看到了 Round 1 的 screening/runtime feedback，并据此引用
  `alns=267 vs vns=96796` 一类 runtime pattern。
- semantic retry 没有消耗 campaign qblock，但 `lk3opt_cross_route` 被 `or_opt_1` fact 推走。
  这个非 fatal rejection 仍有可疑点：文本承认 Or-opt 已存在，真正声称缺的是 3-opt* route
  reconnection。它没有影响本轮完成 screening，但说明 local-search novelty matcher 仍可能
  对“列出现有 operator 后说缺 filter/更高阶 move”的语句敏感。

### Code 阶段

Session：`agentic_sessions/e444f3d4-6585-4d79-9297-bfefaacc991a`

工具调用和 repair：

| 阶段 | 结果 |
|---|---|
| code context | 读取 screening feedback、memory、核心 algorithm files、`acceptance.py`、surface、branch state |
| code #1 | `20260522T162926840881_code_6a321645b6_69aec977.json`; contract 通过；smoke 要求 effect `record_move` |
| code #2 | `20260522T163019147884_code_ac04a59fae_f58209fd.json`; contract 通过；smoke 未观察到 activation `gap_adaptive_sa_temperature_iterations` |
| code #3 | `20260522T163114754102_code_a581271de1_5e110e61.json`; contract 通过；smoke 通过 |

最终代码修改：

- 在 `acceptance.py` 的 `_SimulatedAnnealing` 增加 `best_cost`、`_gap_iterations` 和
  `gap_adaptive_update(...)`。
- 在 `scheduler.py` ALNS loop 中调用 `annealing.gap_adaptive_update(...)`，每次 iteration
  记录 `gap_adaptive_sa_temperature` move telemetry，loop 后记录 phase runtime 和 iterations。

这是 active ALNS path 内部修改，不是 postprocessing。

### Screening / Decision

Raw metrics：`metrics/8a160376-f9bc-434a-a176-68d8fff770d3.json`

| 指标 | 值 |
|---|---:|
| contract / verification | true / true |
| telemetry guard | passed |
| case wins/losses/ties | 0 / 0 / 8 |
| pair wins/losses/ties | 1 / 1 / 14 |
| median delta / CI | 0.0 / [-0.5, 0.5] |
| runtime ratio median | 1.00295 |
| runtime regression rate | 0.4375 |
| gate outcome | fail |
| decision | `continue_explore` |
| reason codes | `SCREENING_FAIL_WIN_RATE`, `SCREENING_NEUTRAL_SIGNAL_CONTINUE` |

Telemetry guard 看到 `gap_adaptive_sa_temperature` activation/runtime/effect：
iterations positive 16/16，phase runtime positive 16/16，effect positive 14/16。

质量判断：screening/decision 合理，没有晋升。telemetry attribution 不干净：phase runtime
几乎等于整段 ALNS loop（样例 7993ms、7988ms、8347ms），并非 O(1) gap update 自身的成本；
`record_move('gap_adaptive_sa_temperature', delta=...)` 使用的是 ALNS best-improved delta，
不能证明 temperature update 是改进因果来源。gate 只能证明代码运行并记录了字段，不能证明
机制级 effect 是严格归因的。

## 5. Round 3 - `double_bridge_perturbation`

### Hypothesis 阶段

Session：`agentic_sessions/dbd816b0-49a4-4784-8680-28c77bf41c56`

工具调用和输出：

| 步骤 | 工具 | 输出摘要 |
|---|---|---|
| required preface | `context.list_surfaces` / `read_problem` / `list_algorithm_files` / `read_active_solver_design` / `read_solver_call_graph` | solver_design context 完整 |
| planner | `feedback.query_runtime` | 返回 screening-derived runtime feedback |
| planner | `memory.query` | 返回 tainted memory |
| planner | `context.read_algorithm_file` x5 | 读取 `local_search.py`、`destroy_repair.py`、`acceptance.py`、`construction.py`、`scheduler.py` |
| fallback | `feedback.query_screening` | 返回 2/2 screening feedback rows |
| self-check | `context.read_surface` | 返回 solver_design interface |
| self-check | `proposal.schema_preview` | 第一次失败，第二次通过 |
| self-check | `proposal.target_permission_preview` | 通过 |

LLM calls：

| 调用 | trace | 结果 |
|---|---|---|
| hypothesis #1 | `20260522T164105134779_hypothesis_771b1e74ba_f057d1b5.json` | `double_bridge_perturbation` 初稿；C11 拒绝 activation 使用 aggregate/effect fields |
| hypothesis #2 | `20260522T164140329914_hypothesis_522790948f_0b6573c0.json` | 修正 expected telemetry，schema/target preview 通过 |

上下文判断：

- prompt manifest active facts digest 为 `cf84d470...`，active facts included。
- agent 看到了前两轮 screening/runtime feedback，继续引用 VNS dominate improvement /
  ALNS low contribution pattern。

### Code 阶段

Session：`agentic_sessions/e7e057af-b777-4af0-b77b-277ea355157e`

工具调用和输出：

| 阶段 | 结果 |
|---|---|
| code context | 读取 required preface、2 条 screening feedback、runtime feedback、memory、核心 algorithm files、surface、branch state |
| code #1 | `20260522T164238832492_code_8428f07965_18479658.json`; contract preview 通过；algorithm smoke 一次通过 |

最终代码修改：

- 在 `local_search.py` 的 `_default_vns_operators()` 后追加 `_double_bridge_perturbation`。
- 新函数在单条 route 内挑 3 个 cut points，将 `A-B-C-D` 重排为 `A-C-B-D`，只接受
  route cost 改善，并记录 `double_bridge_perturbation` telemetry。

这是 active VNS path 内的算法主体修改，不是 postprocessing。

### Screening / Decision

Raw metrics：`metrics/568c643d-fa84-4191-9cb8-27cf60ebbe8e.json`

| 指标 | 值 |
|---|---:|
| contract / verification | true / true |
| telemetry guard | passed |
| case wins/losses/ties | 0 / 0 / 8 |
| pair wins/losses/ties | 0 / 0 / 16 |
| median delta / CI | 0.0 / [0.0, 0.0] |
| runtime ratio median | 0.99922 |
| runtime regression rate | 0.375 |
| gate outcome | fail |
| decision | `continue_explore` |
| reason codes | `SCREENING_FAIL_WIN_RATE`, `SCREENING_NEUTRAL_SIGNAL_CONTINUE` |

Telemetry guard 看到 `double_bridge_perturbation` activation/runtime/effect：
iterations positive 16/16，phase runtime positive 15/16，effect positive 6/16。

质量判断：screening/decision 合理，没有质量收益。但 code 与 hypothesis 语义明显漂移：
hypothesis 描述“cuts four route edges and reconnects segments across up to 4 routes
simultaneously”，no-op 条件也是“fewer than 4 non-empty routes”。实际实现要求的是单条 route
至少 4 个 customers，并且只在单条 route 内重排。contract/smoke 没有发现这种 approved
mechanism 与 generated code 的语义不一致。

## 6. Active facts 同源性

本 run 中 active facts 是 prompt 和 gate 共享的一等上下文：

| Session / manifest | Digest | 状态 |
|---|---|---|
| Round 1 hypothesis/code | `2b2613c4...` | `agentic_active_algorithm_facts` included |
| Round 2 hypothesis/code | `fdc3cf5d...` | `agentic_active_algorithm_facts` included |
| Round 3 hypothesis/code | `cf84d470...` | `agentic_active_algorithm_facts` included |

digest 随 branch/provenance 变化，但内容类型一致：active facts 中每条 fact 都有
`used_by_gate=true` 和 `used_by_prompt=true`。示例 facts 包括：

- `cvrp.destroy_repair.random_removal_destroy`：`_random_removal` 已是 scheduler `"random"`
  destroy operator。
- `cvrp.destroy_repair.shaw_related_removal`：`_shaw_removal` 已是 seed-based
  distance/demand/original-route relatedness destroy。
- `cvrp.destroy_repair.regret_insertion_repair`：`_regret2_insertion` 和
  `_regret3_insertion` 已 wired through `repair_ops`。
- `cvrp.local_search.or_opt_1_relocation` / `cross_route_or_opt_2_3` /
  `cross_route_tail_exchange`：VNS 现有 local-search operator facts。

Round 2 semantic retry 后，最终 hypothesis prompt 的 negative fact block 明确包含
`fact_id=cvrp.local_search.or_opt_1_relocation` 和同一个 `fact_packet_digest=fdc3cf5d...`。
这说明不是 agent 没看到 gate 使用的 facts，而是 provider/matcher 如何解释文本的问题。

## 7. 本轮重点修复验证

### random/geographic cluster 误伤

结论：已在本 run 消失。

证据：

- Round 1 LLM 第一次输出 `proximity_cluster_removal`，第二次输出
  `spatial_cluster_removal`。
- 文本包含 random、Shaw、geographic/spatial cluster、pure distance proximity 等触发词。
- 该 hypothesis 未被 `random_removal_destroy` 或 `shaw_related_removal` novelty gate 拦截。
- 它完成 code、contract preview、algorithm smoke、screening。
- `proposal_quality_blocks_consumed=0`。

保留观察：最终文本说“lacks a targeted destroy operator that removes customers forming a
geographic cluster”，这句话如果脱离下文会和 `_shaw_removal` 的 proximity fact 接近；但下文
明确区分了 Shaw 的 distance+demand+route relatedness 与新机制的 pure distance proximity。
本轮 gate 允许该 variant 是合理的。

### regret repair 文案/语义

结论：没有在本 run 中直接触发 qblock，但源码层仍有残留问题。

事实层是正确的：active code 确实包含 `_regret2_insertion`、`_regret3_insertion`，
`scheduler.py` 的 `repair_ops` 也确实注册 `("regret2", ...)` 和 `("regret3", ...)`。
provider fact 说“Repair portfolio already includes regret-2 and regret-3 insertion
heuristics wired through scheduler repair_ops.” 这句话不再是错事实。

残留问题：

- provider 的 rejection reason 同时写“claims or implies regret insertion repair is absent”
  和 “mischaracterizes the active regret repair semantics”，但它仍由
  `_claims_missing_regret_insertion_repair` 触发，名字和 span 语义仍偏 missing。
- 对 “Regret insertion repair operators always insert customers at globally cheapest
  positions without considering regret score.” 这类语义误述，当前 matcher 依赖 `without`
  触发 missing-style regex，而不是一个专门的 semantics predicate。
- evidence fact 只说明 regret-2/3 已 wired，没有把 `_regret_insertion` 中
  `score = sum(move_delta - delta ...)` 和 tie-break `delta` 的真实语义写进 fact。因此
  reason 说“mischaracterizes semantics”时，证据链仍偏薄。

这不是本 run 的 P0，也没有造成 campaign 停止；但作为 provider 诊断质量，仍建议列为 P1
残留债务。

### qblocks

结论：为 0。

`campaign_loop`：

```json
{
  "proposal_quality_blocks_consumed": 0,
  "proposal_quality_blocks_remaining": 6,
  "proposal_quality_limit": 6
}
```

注意：三个 hypothesis-only session 的 `failure_ledger` 中都有
`contract_boundary_failure: hypothesis awaits ContractGate approval`。这是 APS 分阶段暂停
等待批准的正常边界，不是 proposal-quality qblock，也没有计入 consumed。

### 三个 screened 分支是否为算法主体修改

结论：是。

| Round | 代码差异 | Active path |
|---|---|---|
| 1 | 新增 `_spatial_cluster_removal`，scheduler import 并加入 `destroy_ops` | ALNS 每轮 destroy operator 选择可调用 |
| 2 | 新增 `_SimulatedAnnealing.gap_adaptive_update`，scheduler loop 调用 | ALNS 接受/冷却流程内调用 |
| 3 | 新增 `_double_bridge_perturbation`，加入 `_default_vns_operators` | VNS operator loop 可调用 |

没有修改 adapter、protocol、split、seed、objective recomputation、benchmark data 或 solver runtime
外壳。所有 `action_locus` 都是 `modify/solver_design`。

## 8. v3 框架边界符合性

符合项：

- proposal tools 全部是 exposure-controlled tools：`context.*`、`feedback.*`、
  `memory.query`、`proposal.schema_preview`、`proposal.target_permission_preview`、
  `proposal.contract_preview`、`proposal.algorithm_smoke`。
- 修改文件均在 declared solver_design allowlist 内：`policies/baseline_algorithm.py` 或
  `policies/baseline_modules/*.py` 范围；实际没有改 entrypoint。
- tainted proposal feedback 只用于 hypothesis/code reasoning。screening decision 来自
  protocol metrics 和 Decision engine reason codes。
- contract/verification/smoke/screening/decision 链路都可追溯到 session transcript、
  DB `experiment_events` 和 `campaign_summary.steps`。
- no promotion under failed screening，符合 v3 promotion integrity。

边界外未发现：

- 未见 legacy component-policy、operators registry、lifecycle config 目标。
- 未见 adapter/objective/protocol/data root/seed/split 改写。
- 未见 postprocessing 包装 candidate output 以绕过 solver body。

## 9. 质量拦截评价

合理拦截：

- C11 telemetry schema preview 多次拒绝 aggregate/effect fields 被放入 activation；这些
  rejection 都发生在 hypothesis retry 内，没有消耗 qblock。它们防止了无效 telemetry 合同
  进入 code/screening。
- Code-phase algorithm smoke 对 Round 1/2 的缺失 effect telemetry 和 activation telemetry
  给出明确 repair guidance，最终 code generator 修复到 smoke pass。
- Screening gate 对三个候选都没有晋升；结合 case-level win rate 为 0，这是合理的。

不充分拦截：

- Smoke 只验证 mechanism id 的 activation/effect 字段可观察，不验证 effect 是否在最终
  accepted feasible candidate 上因果成立。
- Contract/smoke 不验证 code 是否保持 approved hypothesis 的关键语义，例如 Round 3 的
  cross-route double-bridge 被实现成 single-route segment reorder。
- Phase runtime attribution 没有强约束，Round 2 把整段 ALNS loop 记给
  `gap_adaptive_sa_temperature` 仍可通过。

## 10. P0/P1 发现

### P0

无。

### P1

1. **P1-1 code/hypothesis semantic drift 未被 contract/smoke 拦截。** Round 3 通过了
   contract 和 smoke，但实现不是 hypothesis 中描述的 cross-route / up-to-4-routes
   double-bridge perturbation，而是单 route 内 `A-C-B-D` reorder。这会让 screening 评价的
   机制与获批机制不一致。

2. **P1-2 mechanism telemetry attribution 仍可被非因果记录满足。** Round 1 的
   `spatial_cluster_removal` effect 来自 destroy 后 partial solution cost，Round 2 的
   `gap_adaptive_sa_temperature` phase runtime/effect 被整段 ALNS 和普通 ALNS improvement
   污染。telemetry guard passed 不等于机制 effect 可信。

3. **P1-3 regret repair semantic diagnostics 仍有残留债务。** fact 本身正确，但 provider
   仍用 missing-style predicate 和混合 reason 处理 semantic mischaracterization；证据 fact
   没有显式呈现 regret score 语义。本 run 未触发该问题，但源码/测试路径显示问题仍存在。

