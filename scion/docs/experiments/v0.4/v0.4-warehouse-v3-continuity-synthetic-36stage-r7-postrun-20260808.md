# Warehouse V3 Continuity Synthetic 36-Stage R7 Postrun — 2026-08-08

## Disposition

R7 is `RUN_INVALID_INFRA` with valid partial scientific evidence. It completed
four of 36 formal stages, including one complete validation negative, and then
stopped when the third Code provider call ended upstream without a terminal
response. It produced no promotion and does not answer the continuous-
optimization endpoint.

- campaign root:
  `/home/clawd/research/scion-experiments/v04-warehouse-v3-continuity-synth-36r-r7-gpt56terra-20260808T193710Z-claw/campaign`;
- requested/completed formal stages: `36 / 4`;
- formal stages: screening/validation/frozen = `3 / 1 / 0`;
- scheduled calls: `5`;
- provider calls: `3 H + 3 C`;
- typed outcomes: evaluated/blocked-infra = `4 / 1`;
- validity/completeness: `valid_but_incomplete / incomplete`;
- stop reason: `execution_blocked_infra`;
- champion: v1, no promotion.

The 4 evaluated stages are bounded partial science. The blocked provider call
is excluded from algorithm conclusions and cannot be reconstructed into a C.

## Evaluated funnel

All 62 declared pairs in the four completed stages were valid. Candidate and
champion failed-pair counts were zero.

| H | Stage | Decision | Pairs | Case W/L/T | Pair W/L/T | Median primary delta | Confidence interval | Median runtime ratio |
|---|---|---|---:|---|---|---:|---|---:|
| `578703a4...` | screening | `EXPAND_SCREENING` | 12/12 | 3/1/2 | 7/3/2 | `+0.75` | `[-1.25, 2.5]` | `1.067` |
| `578703a4...` | expanded screening | `CONTINUE_EXPLORE` | 20/20 | 2/3/5 | 9/11/0 | `-0.5` | `[-2.0, 0.75]` | `1.072` |
| `da909347...` | screening | `QUEUE_VALIDATE` | 12/12 | 4/1/1 | 8/2/2 | `+2.25` | `[-2.5, 5.75]` | `2.606` |
| `da909347...` | validation | `ABANDON` | 18/18 | 1/5/0 | 3/15/0 | `-15.0` | `[-24.0, -0.5]` | `2.205` |

The first candidate showed weak initial improvement but failed the expanded
screening population. The second candidate passed screening, then regressed
decisively in validation and was abandoned. Both are scientific negatives;
neither source may be promoted or presented as retained improvement.

The exact terminal reason codes were
`SCREENING_EXPAND_CASE_LEVEL_UNCERTAIN`, `SCREENING_FAIL_WIN_RATE`,
`SCREENING_PASS`, and `VALIDATION_FAIL_HIERARCHICAL_NEGATIVE` respectively.

## Terminal provider failure

The third H completed. The third C began at
`2026-08-08T20:04:18.226092Z` and ended about 29.8 seconds later with HTTP 504:
`Upstream closed stream without a terminal event`.

The corresponding trace
`llm_traces/20260808T200418226058_code_3d4115de.json` records:

- request kind `code`, model `gpt-5.6-terra`;
- `ok=false` and no response payload;
- exact H and branch call context;
- provider error `codex_api_error` / server error 504.

This is distinct from the R6 proposal-content rejection. R6 had a complete C
that failed source-bound normalization; R7 has no terminal C at all. The
runtime correctly classified R7 as `BLOCKED_INFRA` and stopped rather than
inventing or retrying a response. Immediately after terminal exit, the local
proxy still reported authenticated state and one active account with no
quota/rate/refresh condition. The evidence therefore supports an intermittent
upstream stream termination, not an authentication or persistent proxy-state
failure.

R7 is sealed. The failed C will not be retried, resumed, repaired or copied to
another campaign. A later matched run must use a fresh root and new H/C calls.

## Claim boundary

R7 supports these bounded claims:

- the post-R6 runtime completed ordinary screening and validation scheduling;
- one candidate expanded then failed screening, and another passed screening
  then failed validation on complete pair populations;
- Protocol negatives returned to research without framework blockage;
- the final stop was a correctly classified provider-without-terminal-response
  infrastructure event.

R7 does not support:

- completion of the 36-stage horizon;
- any Warehouse promotion or retained solver improvement;
- champion v2/v3 or continuous optimization;
- runtime exercise of proposal `RESEARCH_REJECTED` scheduler-forward;
- independent replay, production transfer or cross-problem generalization.

The concise result is: **four valid formal stages and two interpretable
candidate negatives, followed by an intermittent terminal-less provider C;
useful partial science, invalid as the declared 36-stage continuity run.**
