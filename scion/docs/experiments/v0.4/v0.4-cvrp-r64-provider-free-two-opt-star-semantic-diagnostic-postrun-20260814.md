# CVRP R64 Provider-Free Two-Opt-Star Semantic Diagnostic Postrun — 2026-08-14

**Terminal record:** `TERMINAL_DIAGNOSTIC`

```text
complete:                    true
semantic_endpoints_claimed: true
classification:              null
aggregate_semantic_correctness: null
aggregate_algorithm_success:    null
```

## Scope and authority

R64 is the unique durable formal publication of the separately
[preregistered](v0.4-cvrp-r64-provider-free-two-opt-star-semantic-diagnostic-preregistration-20260814.md)
provider-free diagnostic over the two sealed R63 materializations. Candidate
authority remains subject commit
`51f9bbd77f1e93777bbe9c401b8ba05c09a3e819`, approved-H trace
`20260809T082821069862_hypothesis_c6409fa4`, frozen C-input trace
`20260809T082849758576_code_21402fe4`, and the ordered 11-source values. Clean
`47fe81ee6e17c04bc805197e2b2ad34e0fff4d14` is runtime authority only.

R64 neither rewrites R63 nor compares or ranks CONTROL and REVIEW. It keeps its
three semantic endpoints separate, records one descriptive call count per arm,
and fixes classification and both aggregate fields to `null`.

## Durable lifecycle and artifacts

- Input bundle:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r64-provider-free-two-opt-star-semantic-diagnostic-20260814-input`
- Unique formal output root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r64-provider-free-two-opt-star-semantic-diagnostic-20260814-output`
- Formal root creation time: `2026-08-14T09:41:56Z`
- Root mode: `0700`
- Durable files: `result.json` and `status.json`, each mode `0600`

The final root shape is consistent with the manifest's
`fresh_output_only=true` and `resume=false` policy and contains no stage or
partial artifact. A final filesystem snapshot cannot independently prove that
the root was absent immediately before publication or observe the rename
instant; no second formal root or attempt is recorded.

## Preformal calibration and formal role

Before the formal root existed, bundle pytest temporary roots had already run
the exact sealed alpha and beta candidates and observed the same per-arm vector
reported below. The durable record therefore has the frozen role:

```text
preformal_calibration_status: EXACT_CANDIDATE_TEMPORARY_REPRODUCTION_OBSERVED
formal_role:                  DURABLE_REPRODUCTION_OF_EXACT_CANDIDATE_CALIBRATED_PROVIDER_FREE_DIAGNOSTIC
outcome_blind:                false
independent_confirmation:     false
claim_boundary:               preformal_exact_candidate_outcomes_seen_no_independent_confirmation
```

The nested preparation snapshot retains
`preparation_calibration.formal_output_created=false`. That field describes the
earlier temporary-root calibration phase. It does not say that the current
formal root is absent and must not be used to contradict the durable root and
terminal files listed above.

Thus the formal result is a durable reproduction of an exact-candidate-
calibrated provider-free diagnostic, not prospective outcome discovery,
outcome-blind evidence, or independent confirmation.

## Separate per-arm endpoint record

The reveal maps alpha to `control_original_message` and beta to
`treatment_review_suffix`. Both formal cells report the same vector:

| Endpoint | CONTROL / alpha | REVIEW / beta |
|---|---:|---:|
| `cut_delta_equivalence` | `TRUE` | `TRUE` |
| `near_eps_decision_equivalence` | `TRUE` | `TRUE` |
| `first_improvement_state_equivalence` | `TRUE` | `TRUE` |
| `candidate_route_distance_evaluation_calls` | `0` | `0` |

The cut endpoint evaluated 27 frozen symmetric/directed orientation and
`0`/end/interior cases per arm. Every bound candidate delta/comparison value
equaled its full-distance reference value, with no candidate exception.

The near-EPS endpoint evaluated three below/equal/above-`-EPS` decisions per
arm. Every decision equaled its reference decision, with no candidate
exception.

The state endpoint executed each candidate through termination on the three
frozen state fixtures. Candidate and reference accepted-move counts were:

| State fixture | CONTROL candidate/reference | REVIEW candidate/reference |
|---|---:|---:|
| Over-capacity rejection | `0 / 0` | `0 / 0` |
| Empty-route cleanup | `1 / 1` | `1 / 1` |
| Multi-accept termination | `5 / 5` | `5 / 5` |

For both arms, complete accepted-move traces, move indices/cuts, pre-cleanup
trace, route partition, loads, per-route costs, total cost, empty-route cleanup,
and final state equaled the independent reference. All frozen invariants held
after every accept, and no candidate exception occurred. The descriptive call
counter observed zero calls through the local-search module's route-distance
binding during candidate `_two_opt_star` execution.

## Null classification and aggregates

The three `TRUE` values are separate finite-fixture endpoint observations. R64
does not combine them into a semantic-success bit, winner, treatment estimate,
or algorithm result. The durable record therefore intentionally keeps:

```text
classification:                    null
aggregate_semantic_correctness:    null
aggregate_algorithm_success:       null
```

Neither equality across arms nor a zero call count establishes that the review
suffix was ineffective, that both implementations are generally semantically
equivalent, or that either is algorithmically better.

## Zero-call and non-mutation record

```text
provider calls:                 0
H or C calls:                   0
canary calls:                   0
Contract calls:                 0
V3 calls:                       0
V4 calls:                       0
formal solver/Protocol calls:   0
algorithm-quality calls:        0
Decision calls:                 0
benchmark or BKS inputs:        0
production source changes:      0
production gate changes:        0
R63 artifact changes:           0
```

No objective, runtime-quality, feasibility-generalization, W/L/T, effect, or
promotion evidence was produced. B0/v1 remains champion.

## Claim boundary and stop

R64 shows only that both exact sealed candidates agree with the independent
reference on this finite deterministic fixture roster and that the formal root
durably reproduces the already observed temporary-root vector. It does not
prove correctness beyond the roster, establish algorithm quality or
generalization, independently confirm the candidates, or estimate an effect of
the review suffix.

The diagnostic remains non-gating. It authorizes no adoption, rollback, repair,
provider call, solver-quality experiment, Protocol, Decision, or promotion.
Provider algorithm experiments remain paused. R64 stops at this terminal
record; no R65 experiment is preregistered, authorized, or launched.
