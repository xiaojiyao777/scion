# CVRP R66 H-Only Mechanism-Frontier Probe Preregistration — 2026-08-14

**Preparation status:** `PREPARED_AUTHORIZED_NOT_STARTED`

**Design disposition:** `H_ONLY_DISCOVERY / NO_CANDIDATE /
NO_AUTOMATIC_R67`

No R66 provider call or downstream action has started. At design freeze, the
input, output, process-control and socket roots were absent. Bundle preparation
and provider-free static/fake checks may proceed, but they are not an H
observation. The user's exact authorization below permits one immediate H-only
call after the frozen bundle passes its preflight; it permits no C call, patch,
materialization or solver action.

## Exact discovery question

The exact one-line `research_question.current_question` is:

```text
Given the exact fresh-B0 CVRP source and the neutral mechanism-exclusion ledger below, propose one bounded, source-grounded algorithm mechanism that could improve final-best total_distance without worsening fleet_violation and is not a repair, parameter tuning, longer-budget rerun, re-expression, or direct extension of an excluded direction. Identify the current source weakness, exact primary source locus, causal mechanism, measurable expected effect, and a result that would falsify the idea. Do not write code, choose cases, seeds, or budgets, or recommend changing Contract, Verification, Protocol, Decision, tooling, prompts, telemetry gates, or infrastructure.
```

R66 records one forced-proposal H observation. It does not test whether the
model can satisfy a novelty gate, create or approve a candidate, select an
evaluation population, estimate algorithm quality, or close S6. The ledger is
prior-informed by the historical CVRP record, so the observation is explicitly
non-blind and not independent discovery evidence.

## Frozen identity and roots

- Label: `v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814`
- Manifest schema:
  `scion.cvrp_r66_h_only_mechanism_frontier_probe.manifest.v1`
- Result schema:
  `scion.cvrp_r66_h_only_mechanism_frontier_probe.result.v1`
- Status schema:
  `scion.cvrp_r66_h_only_mechanism_frontier_probe.status.v1`
- Input root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-input`
- Output root:
  `/home/clawd/research/scion-experiments/v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-claw`
- Process-control root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-control`
- Tmux socket: `/tmp/scion-r66-h-only-mechanism-frontier-probe.tmux.sock`
- Tmux session: `scion-r66-h-only-frontier`

Before launch, the input root must exist as a frozen non-symlink directory and
every frozen member must be regular and non-symlink. The output root,
process-control root and socket must remain absent.
The launcher refuses reuse, resume or overwrite. It uses a minimal environment,
umask `077`, one atomic output publication and a durable post-acquisition
terminal record. A pre-call authority, schema, root, prompt, tool, source or
ledger mismatch is `PREP_INVALID` with zero H calls.

## Source, runtime and provider authority

The source authority is the exact ordered eleven ordinary problem-owned CVRP
path/value pairs from fresh B0 at subject commit
`51f9bbd77f1e93777bbe9c401b8ba05c09a3e819`. Their order is:

```text
policies/baseline_modules/local_search.py
policies/baseline_algorithm.py
policies/baseline_modules/acceptance.py
policies/baseline_modules/config.py
policies/baseline_modules/construction.py
policies/baseline_modules/destroy_repair.py
policies/baseline_modules/route_first_heuristic.py
policies/baseline_modules/route_first_improvement.py
policies/baseline_modules/route_first_seeding.py
policies/baseline_modules/scheduler.py
policies/baseline_modules/state.py
```

The runner value-binds every path, byte value and ordinal against the clean
`51f9bbd7` archive and input file `frozen/source_context.json`, key
`editable_sources`, before provider delegation. No candidate or result value
may satisfy or replace a B0 source value. The H runtime, prompt carrier and tool authority are the clean values at
`47fe81ee6e17c04bc805197e2b2ad34e0fff4d14`.

The provider input contains only the clean production H system/instruction,
the canonical fresh-B0 context and source values, the exact research question,
and the separately labelled neutral exclusion ledger below. The exact frozen
`research_question` payload contains only its typed schema and
`current_question`; it has no `research_prior`. The context has no
`experiment_history`, `last_research_rejection` or `branch_current_code`.
Production carries the exact ordered eleven source values under
`champion_operators_code`.

The provider input contains no historical H or C response, historical
candidate source, raw metric, result, Decision or promotion record. In
particular, it does not send the ordinary outcome-rich
`CROSS_CAMPAIGN_RESEARCH_PRIOR` in addition to E01–E10. The ledger's neutral
summaries make the probe prior-informed; excluding raw historical responses
does not make it outcome-blind.

The exact request envelope is:

| Field | Frozen value |
|---|---|
| Provider | local Codex proxy at `http://127.0.0.1:8080` |
| Model | `gpt-5.6-terra` |
| H calls | at most one |
| H timeout | 180 seconds |
| Provider retries | zero |
| Outer hard wall | 300 seconds |
| Tool | `tools/hypothesis-tool-47fe81ee.json`, the exact production context-bound H tool, forced; no experiment-owned schema change |
| Frozen allowed `change_locus` | exactly `solver_design` |
| Explicit `prompt_cache_key` | absent |
| C and patch calls | zero |

Production `_split_direct_v3_hypothesis_context(context)` receives the exact
question above. `build_prompt_turn_snapshot()` uses production
`bind_hypothesis_tool_to_context`, whose exact context-bound tool freezes the
`change_locus` enum and description to the sole allowed value
`solver_design`; parsing reuses the same `allowed_change_loci`. There is no
experiment-owned tool-schema change. The production system blocks and base H
user prompt are unchanged. The runner then appends exactly two leading line feeds, the heading
`## Neutral Mechanism-Exclusion Ledger`, the preamble below, two line feeds,
and the E01–E10 rows in fixed order, one row per line, with no trailing prose.

## Canonical neutral mechanism-exclusion ledger

The exact one-line preamble is:

```text
Each row excludes only replay, tuning, repair, or a direct causal re-expression or extension in this probe. Sharing a file or operator name is not itself excluded if the causal mechanism is materially different and the response states that distinction.
```

The following is the single canonical documentation table and is value-bound
to `manifest.json` key `ledger`. The second column is serialized verbatim, one
row per line, into the provider-visible suffix.

| ID | Exact provider-visible row text |
|---|---|
| E01 | `E01 GLOBAL_EMBEDDED_VNS_SHARE — global embedded-VNS allocation/share, globally disabling embedded VNS, or a pure ALNS/global switch; directly evaluated without stable support.` |
| E02 | `E02 ELAPSED_BUDGET_SA — elapsed/deadline-normalized SA cooling/progress and setter/call-site repair; previously evaluated/iterated with mixed or implementation-invalid evidence.` |
| E03 | `E03 TWO_OPT_STAR_BOUNDARY_DELTA — O(1) two-opt-star boundary/cut delta evaluation, tuning it, or expanding/rerunning the same exact candidate; previously evaluated, with current exact-candidate minute observations tied.` |
| E04 | `E04 EJECTION_REPAIR — bounded depth-one/depth-two/completion-aware/route-cap/transactional ejection and direct refinements; previously mixed or negative.` |
| E05 | `E05 SWAP_CROSS_EXCHANGE — SWAP*, segment/cross exchange, double-bridge, route-pair-overlap and direct bounded variants; previously mixed/polarized.` |
| E06 | `E06 DESTROY_REPAIR_SELECTION — destroy-size/stagnation sizing, destroy/repair selector, lookahead/dual repair, seed/post-optimization selector, or route-first selection direct variants; explored without stable support.` |
| E07 | `E07 TIME_AWARE_OPERATOR_CREDIT — time-aware/post-repair operator credit or direct wiring repair; previously proposed but unused/unresolved, excluded here to avoid a repair loop.` |
| E08 | `E08 POOL_RECOMBINATION_VNS_ALLOCATION — pool/recombination, generic VNS allocation, or simple budget increases; previously rejected/unresolved or budget-confounded.` |
| E09 | `E09 TOOLING_PRESENTATION_REPAIR — prompt/tool/schema/self-review/source-navigation/sidecar/grader/retry/repair/host-gate changes; these are process/tooling, not the requested algorithm direction.` |
| E10 | `E10 OLD_CANDIDATE_REPLAY_OR_EXPANSION — recover, replay, select, or directly extend an R1/R2/R3/R53–R65 candidate, add only cases/seeds/time to it, or select it using prior outcomes; adaptive expansion is excluded.` |

This ledger is a scope constraint for this one probe, not an ontology, novelty
classifier, causal exclusion proof or permanent algorithm blacklist. A shared
file or operator name is allowed when the proposed causal mechanism is
materially distinct and the H observation states the distinction. Any later
human overlap review is descriptive only and cannot accept, reject, repair or
automatically continue the H.

## Mechanical terminal record

The runner records these six mechanical fields separately:

```text
provider_completed
expected_tool_call_count_eq_1
expected_tool_name
arguments_available
arguments_json_valid
hypothesis_schema_valid
```

`mechanically_complete` is true only when all six fields are true.
`hypothesis_schema_valid` means only that production `_parse_hypothesis` accepts
the typed H arguments. It is not a Hypothesis Contract, ContractGate, novelty,
scientific-quality or exclusion-compliance decision. Contract and ContractGate
calls remain zero.

When available through the production client abstraction, the durable result
also stores the parsed `h_observation`, raw/envelope availability and its
diagnostic, plus usage and latency availability. If the abstraction does not
expose raw content or a timing/usage value, the record says unavailable and
retains the production diagnostic rather than reconstructing data.

These fields remain exactly null under every terminal outcome:

```text
scientific_classification
algorithm_quality_evidence
decision
promotion
```

## Terminal and failure policy

- `PREP_INVALID`: a required local authority, fresh-root, schema, source,
  prompt, tool, ledger or request invariant fails before the provider call;
  H calls are zero.
- `TERMINAL_HYPOTHESIS_OBSERVATION`: the provider completes and all six
  mechanical fields are true. This records the typed H, not its novelty,
  correctness or adoption.
- `TERMINAL_HYPOTHESIS_FORMAT_REJECTED`: the provider completes terminally but
  supplies the wrong, missing or multiple tool calls, a wrong tool name,
  unavailable/invalid arguments or typed-schema-invalid H arguments.
- `TERMINAL_HYPOTHESIS_PROVIDER_FAILURE`: authentication, model, timeout,
  transport, upstream close or missing-terminal-response failure prevents a
  terminal H observation.
- `RUN_INVALID_LOCAL_INFRA`: a local runner, persistence or host exception
  invalidates the attempt outside the typed provider/format cases above.

Every terminal class stops R66. There is no retry, resume, substitution,
repair call or second H. A format or provider failure does not show that no
eligible mechanism exists.

## Exact zero-action boundary

R66 has zero actions or calls for each of the following:

```text
C
patch
materialize
Contract / ContractGate
V3 / V4
solver
canary
Protocol
quality / formal evaluation
Decision
promotion
repair
retry / resume / substitution
R67
```

No source file, candidate workspace, champion, gate or prior experiment is
changed. After the single terminal H attempt, the provider algorithm pause
resumes. There is no automatic C, solver design, preregistration, R67 or
authorization inheritance from the H observation.

## Claim boundary

R66 can claim only one prior-informed, non-blind, forced-tool H observation at
the frozen source, prompt and ledger. It cannot claim mechanism novelty,
ledger compliance, feasibility, correctness, executability, algorithm quality,
runtime benefit, case/seed generalization, model success rate, causal effect of
the ledger, prompt adoption, candidate selection or an S6 advance. Even a
well-formed and apparently distinct H remains an unaudited proposal until a
separate human/scientific design review determines whether a new question is
actually justified.

## Recorded authorization and launch rule

The user's exact raw authorization is recorded verbatim:

> 好，你准备好这份机制排除表和 H-only 预注册包之后直接进行后续操作，不用停下来等授权，直接开始，明确给你授权

For this preregistration, that authorization is resolved narrowly to bundle
preparation and exactly one H-only Terra call through the local proxy using the
fresh-B0 context and frozen ledger: H at most one, timeout 180 seconds, retry
zero. It does not authorize C, a patch, materialization, Contract, V3/V4, any
solver/canary/Protocol/quality/formal action, Decision, promotion, repair,
resume, substitution or R67.

Once the frozen input passes provider-free preflight and the output,
process-control and socket roots are confirmed absent, the one H-only launch
may proceed without another authorization pause. No preparation check may
itself call the provider, and no terminal result may trigger downstream work.

## Append-only terminal note — 2026-08-15

The preregistration above was committed before launch as
`4407e3371fc0ec44fa83391f9882a1ad0aacc1a2`; its pre-append SHA-256 was
`a52f96094bf8ed5886a578549350e14cd82017abe7770eb0e2809ff53f2d6837`.
The user's final exact authorization was:

> 我明确授权执行一次 v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814：将 commit 51f9bbd77f1e93777bbe9c401b8ba05c09a3e819 的 fresh-B0 CVRP problem-owned 11-source 源码派生上下文（local_search.py、baseline_algorithm.py、acceptance.py、config.py、construction.py、destroy_repair.py、route_first_heuristic.py、route_first_improvement.py、route_first_seeding.py、scheduler.py、state.py），连同冻结的研究问题、E01–E10 机制排除表以及 clean 47fe81ee6e17c04bc805197e2b2ad34e0fff4d14 production H system/tool，通过本机 Codex proxy http://127.0.0.1:8080 发送给 gpt-5.6-terra。最多执行 1 次 H、180 秒、retry 0；不得发送历史 H/C response、候选或结果；C、patch、solver、canary、Protocol、Decision、promotion 和 R67 均为 0。此授权仅覆盖这一 R66 H-only 调用。

The authorized action was invoked once after the UTC date changed to
2026-08-15. Its outer provider-free preflight rebuilt the production canonical
static context and found one exact drift at
`/problem_measurement_diagnostics/measurement_readiness/calibration_age_days`:
the frozen value was `64` and the launch-time value was `65`. It failed closed
before inner-process acquisition, proxy/model preflight or an H request.

R66 is terminal `PREP_INVALID / OuterPreflightFailure`. The durable record has
`h_attempts=0`, `hypothesis_provider_calls=0`, no provider terminal response,
`mechanically_complete=false`, and every downstream counter zero. The formal
output root, tmux socket/session and inner acquisition markers are absent;
only the mode-`0700` process-control root with `outer_failure.json` and
`exit.code=1` exists. No source, question or ledger was sent; no response or
candidate was produced. Under the frozen terminal policy there is no retry,
repair, resume, substitution, C, solver action or R67. Full artifact facts and
the claim boundary are recorded in the
[postrun](v0.4-cvrp-r66-h-only-mechanism-frontier-probe-postrun-20260815.md).

## Append-only recovery amendment — 2026-08-15

**Recovery status:** `RECOVERY1_PREPARED_AUTHORIZED_NOT_STARTED`

The terminal `PREP_INVALID` action and its consumed process-control root above
remain immutable historical facts. They are not reclassified, deleted,
overwritten or treated as a provider response. After that terminal record was
reviewed, the user gave this exact instruction:

> 是的，那你直接去掉，做好修复，然后继续实验

Recovery1 freezes three distinct authorization-chain fields without treating
them as interchangeable:

- `authorization_text` preserves the earlier broad instruction that
  authorized preparation and continuation after preflight;
- `source_send_authorization_text` is the later full eleven-source disclosure
  authorization quoted verbatim in the preceding terminal note, including
  the exact commits, source list, proxy/model and call/zero-action envelope;
  and
- `recovery_authorization_text` is the exact correction-and-continuation
  instruction quoted immediately above.

The historical broad field does not substitute for the full source-send
field. The full source-send field does not by itself amend the terminal
preflight policy. The recovery field authorizes only the diagnosed correction
and fresh continuation under that already explicit disclosure envelope.

In the context of the diagnosed one-field UTC-date rollover, this instruction
authorizes one narrowly corrected, separately rooted R66 recovery. It does not
remove source, runtime, prompt, tool, ledger, schema, root-freshness or request-
envelope checks. It authorizes only replacing the erroneous live-current-date
reconstruction of the time-derived readiness field with deterministic
reconstruction as of the original frozen date.

### Recovery identity and fresh roots

- Label:
  `v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-recovery1`
- Input root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-recovery1-input`
- Output root:
  `/home/clawd/research/scion-experiments/v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-recovery1-claw`
- Process-control root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-recovery1-control`
- Tmux socket:
  `/tmp/scion-r66-h-only-mechanism-frontier-probe-recovery1.tmux.sock`
- Tmux session: `scion-r66-h-only-frontier-recovery1`
- Manifest date authority: `static_context_as_of_date="2026-08-14"`
- Typed code authorities:
  `EXPECTED_STATIC_CONTEXT_AS_OF_TEXT = "2026-08-14"` and
  `EXPECTED_STATIC_CONTEXT_AS_OF =
  date.fromisoformat(EXPECTED_STATIC_CONTEXT_AS_OF_TEXT)`

All recovery output, process-control and socket roots must be absent before
launch. The original input and consumed control root remain read-only; the
original absent output is not reused. Recovery1 is an R66 subtype, not R67, a
same-label relaunch or a resume of an output workspace.

### Sole corrected invariant

Recovery preparation must call the production canonical-context construction
with the explicit typed `as_of` date `2026-08-14`. It must reconstruct
`/problem_measurement_diagnostics/measurement_readiness/calibration_age_days`
as `64`, equal to the frozen provider context. It must not use the wall-clock
date for that comparison, mutate the frozen value to the launch date, skip the
rest of static-context equality, or globally monkeypatch time.

Every other authority and provider-visible value remains as frozen above:
exact fresh-B0 eleven-source values at `51f9bbd7`, clean `47fe81ee` production
H system/prompt/tool, the exact question, E01–E10 ledger, sole
`change_locus=solver_design`, absent historical H/C responses/candidates/
results, and absent explicit `prompt_cache_key`. The correction changes only
how the date-sensitive local preflight is reproduced; it does not change the
intended provider payload or research estimand.

### Recovery call and stop boundary

The original terminal action consumed zero H attempts and zero provider
calls. Across original R66 plus recovery1, the maximum therefore remains one
H call. Recovery1 retains model `gpt-5.6-terra`, local proxy
`http://127.0.0.1:8080`, a 180-second provider timeout, provider retry zero and
a 300-second outer wall. A newly created client may make at most one forced H
request after all offline recovery checks pass.

Recovery1 still has zero C, patch, materialization, Contract/ContractGate,
V3/V4, solver, canary, Protocol, quality/formal evaluation, Decision,
promotion, provider repair/retry, resume, substitution and R67 actions. The
provider-free bundle correction is not an LLM repair call. No old response is
available to reuse, and no result-dependent continuation is permitted.

Any recovery terminal class stops recovery1. There is no recovery2 or further
automatic action. A mechanically complete H remains only one prior-informed,
non-blind proposal observation under the original claim boundary; it cannot
establish novelty, compliance, feasibility, correctness, executability,
algorithm quality, runtime benefit, generalization, adoption or S6 progress,
and it cannot automatically authorize C or R67.

At provider-free freeze, the recovery input-tree receipt SHA-256 is
`89e7994a05a0663f3bc95a9c2453ca28328159608a2cfc3749222f6747138aaf`
over 4,191 files and 72,632,636 bytes. The 97,863-byte public-call payload
receipt over exactly `{prompt, tool, system_blocks, model}` has SHA-256
`573d445126f41acab255d6f9bfa93d9aff43851fbc6458bca3cb0b9d9f5b470a`
and is not SDK kwargs. The 97,866-byte canonical counterfactual OpenAI
`chat.completions.create` kwargs receipt has SHA-256
`0dc1c5b4da1283915fcceb2b3b83e141cb70f1be87b589dcd7ff5d322947b0e0`;
its exact keys are `messages`, `model`, `timeout`, `tool_choice` and `tools`,
with float timeout `180.0` and `prompt_cache_key` absent. Recovery1 failed
before kwargs evaluation, so the latter is a reconstruction receipt, not an
emitted request. `manifest.json` SHA-256 is
`ba196da15b7a2d64b8b8918e2a56e6143846aba35a289c5a7e760f525d3fd4f1`.
The frozen suite passed 36 tests in 11.91 seconds together with runner check,
Ruff check/format, `bash -n` and `jq`; none contacted the provider. The fresh
recovery output, process-control and socket roots remained absent at freeze.
These are bundle-preparation facts, not an H observation.

Recovery1 may launch without another authorization pause only after its exact
bundle and these constants pass provider-free independent review, the fresh
output/control/socket roots are confirmed absent, and its recovery
preregistration state is durably recorded. Preparation, review and fake-client
tests may not contact the provider.

## Append-only recovery1 terminal note — 2026-08-15

Recovery1 acquired its inner process at `2026-08-15T03:23:06Z`, PID `591276`.
The launch-only state was `IN_FLIGHT` and outcome-unread. It later published
one atomic terminal output and exited `3`.

The immutable result/status emit
`TERMINAL_HYPOTHESIS_PROVIDER_FAILURE`, `h_attempts=1`,
`hypothesis_provider_calls=1`, no provider terminal response and
`mechanically_complete=false`. `result.json` records `LLMError`, exact error
`API error: No module named 'jiter'`, latency 637 ms, no raw response, envelope,
usage, tool arguments or H observation. Scientific classification, algorithm-
quality evidence, Decision and promotion are null; every downstream and R67
counter is zero.

Read-only causal audit keeps those durable fields unchanged but classifies the
cause as `RUN_INVALID_LOCAL_INFRA / MISSING_JITER_BEFORE_REQUEST`. The frozen
minimal Python environment lacked `jiter`; the OpenAI SDK's lazy `client.chat`
resource raised while importing it, before `client.chat.completions.create`
could emit a model request. Actual outbound model/H requests and provider-
visible research-payload sends are both zero. Control-plane proxy liveness and
model probes contained no research payload. There is no provider, model-
format, mechanism or algorithm observation.

The output `result.json`/`status.json` SHA-256 values are
`a781f1fb8444af8854536ff601416e0bef126c2d1020dff36b611b6fc0e75118`
and `af1608d8f0a530da153b359a99e2a7a8fbae19536643dd1a61b647fde6e7a5cb`.
The control root retains acquisition markers and `exit.code=3`; the inner PID
is gone. The recovery1 socket remains as a stale mode-`0600` artifact. Full
lifecycle facts and the durable-versus-causal distinction are recorded in the
[recovery1 postrun](v0.4-cvrp-r66-h-only-mechanism-frontier-probe-recovery1-postrun-20260815.md).

Recovery1 is terminal. Its explicit no-recovery2 rule remains the launch-time
policy, so neither the earlier source-send authorization nor the recovery1
instruction authorizes another provider attempt. The logical attempt/call
counters remain one even though the audited outbound H request count is zero.

## Append-only recovery2 preparation amendment — 2026-08-15

**Preparation status:**
`RECOVERY2_PREPARED_AWAITING_EXPLICIT_AUTHORIZATION`

The recovery2 authorization chain preserves the three earlier exact fields:
historical broad `authorization_text`, full eleven-source
`source_send_authorization_text`, and exact recovery1
`recovery_authorization_text` (`是的，那你直接去掉，做好修复，然后继续实验`).
It adds `recovery2_source_send_authorization_text=null`. The null field and
awaiting status are intentional; no earlier field is missing, replaced or
repurposed as recovery2 authority. Only a new exact user authorization may
populate that field prospectively.

Recovery2 is a separately rooted R66 local-infrastructure correction, not R67
and not a rewrite of recovery1. Its frozen identity is:

- Label:
  `v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-recovery2`
- Manifest schema:
  `scion.cvrp_r66_h_only_mechanism_frontier_probe.recovery2.manifest.v1`
- Result schema:
  `scion.cvrp_r66_h_only_mechanism_frontier_probe.recovery2.result.v1`
- Status schema:
  `scion.cvrp_r66_h_only_mechanism_frontier_probe.recovery2.status.v1`
- Input root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-recovery2-input`
- Output root:
  `/home/clawd/research/scion-experiments/v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-recovery2-claw`
- Process-control root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-recovery2-control`
- Tmux socket:
  `/tmp/scion-r66-h-only-mechanism-frontier-probe-recovery2.tmux.sock`
- Tmux session: `scion-r66-h-only-frontier-recovery2`

The sole new correction vendors exact `jiter==0.13.0` under
`vendor/python312` as nine frozen files and keeps `PYTHONNOUSERSITE=1`. The
minimal formal environment prepends that exact vendor root to `PYTHONPATH`;
it may not use user site-packages, install a package at launch, download a
dependency or mutate the sealed recovery1/runtime authorities. Provider-free
preflight must import the vendored module and exercise the exact OpenAI lazy
`client.chat` resource without HTTP. Recovery2 independently binds its own full
clean runtime and subject trees. Its live recovery1 binding is deliberately
narrow: the payload-authority core, terminal result/status, control roster and
stale socket, together with the fixed-`as_of`, payload-receipt and fake-client
checks. Recovery1 input-tree SHA `89e7994a...` remains a historical preparation
receipt only; recovery2 does not claim to recompute or revalidate it.

The vendored nine-file authority comes from the local pip-installed tree,
cross-checked to installed distribution metadata for CPython 3.12 on Linux
x86_64; no wheel artifact or source commit is claimed as independently
verified. The vendor root precedes the clean runtime on `PYTHONPATH`, user-site
loading is disabled, and the dependency preflight rejects any network send.

The provider-free freeze has 4,200 files and 73,523,973 bytes. Its
input-tree receipt uses sorted `relative_path\0file_sha256\0size\n` records and
has SHA-256
`f8e82201700cbfebea9ba26f5bfeb0d30f5ee694824722ab3a9fcdf861a050d5`.
The 14,315-byte `manifest.json` has SHA-256
`e056afcfe6e279bbc902c3b17b8e512eb466c9a28ae4e3e5ea4a4bf7e6fa8dc9`.
The suite passed 47 tests in 12.15 seconds, together with runner check, Ruff
check/format, `bash -n` and `jq`.
The formal preauthorization path exits `2`; fresh output, control and socket
roots are absent, and the recursive cache/symlink exclusion passes. None of
these checks contacted the provider or sent the research payload.

All provider-visible values and the research scope remain unchanged from
recovery1. Recovery2 preparation has zero H/model requests and zero research-
payload sends. If separately authorized, recovery2 may make at most one fresh forced H
request, timeout 180 seconds and provider retry zero. C, patch,
materialization, Contract, V3/V4, solver, canary, Protocol, quality/formal
evaluation, Decision, promotion, provider repair/retry, resume, substitution
and R67 remain zero. Any terminal recovery2 result stops with no recovery3 or
automatic downstream action.

No recovery2 launch authorization has been recorded. Because recovery1's
frozen policy explicitly excluded recovery2 and its durable logical call
counter is already one, the user must explicitly authorize the exact fresh
recovery2 source-send action after being told that recovery1 emitted a
provider-failure class with logical wrapper attempt/call `1/1` but actually
failed locally with zero model HTTP requests and zero research-payload sends.
That authorization must explicitly override the earlier no-recovery2 stop for
this one fresh recovery2 only. A generic "继续" or any earlier authorization is
insufficient. Until that exact new authorization is durably recorded,
recovery2 must remain provider-free and must not launch.

## Append-only recovery2 authorization note — 2026-08-15

**Launch-freeze status:** `RECOVERY2_PREPARED_AUTHORIZED_NOT_STARTED`

After the awaiting-authorization preparation record above and disclosure of
the recovery1 durable-versus-causal distinction, the user supplied this exact
recovery2 authorization:

> 我已知 R66 recovery1 的 durable label 是 TERMINAL_HYPOTHESIS_PROVIDER_FAILURE、wrapper attempt/call 计数为 1，但只读审计确认它在 client.chat 本地缺少 jiter 时、HTTP 之前终止，实际 H HTTP request、research payload send 和 H observation 均为 0。我明确授权仅为一次 fresh v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-recovery2 覆盖 recovery1 的 no-recovery2 stop：保持原 R66 已授权的 commit 51f9bbd77f1e93777bbe9c401b8ba05c09a3e819 fresh-B0 CVRP 11-source、研究问题、E01–E10 ledger、clean 47fe81ee6e17c04bc805197e2b2ad34e0fff4d14 production H system/tool 和全部 provider-visible request bytes 不变；保持 fixed as_of=2026-08-14 与 PYTHONNOUSERSITE=1，仅使用逐文件冻结的 experiment-owned jiter==0.13.0 修复本地 OpenAI SDK 依赖，然后通过本机 Codex proxy http://127.0.0.1:8080 向 gpt-5.6-terra 最多发送 1 次 H HTTP request。Recovery2 最多 1 次 wrapper attempt、最多 1 次 actual H HTTP request、180 秒、SDK/provider retry 0；不得修改、复用或重写原 R66/recovery1 的 input、control、output、socket 或终态，不得发送历史 response、候选或结果；C、patch、materialization、Contract、V3/V4、solver、canary、Protocol、quality/formal、Decision、promotion、repair、resume、substitution、R67 和 recovery3 均为 0。任何 recovery2 终态立即停止。此授权仅覆盖这一 R66 recovery2 H-only 调用。

This exact text prospectively populates
`recovery2_source_send_authorization_text`; the three earlier authorization
fields remain byte-for-byte preserved. It explicitly overrides recovery1's
no-recovery2 stop only for this one fresh R66 recovery2 H-only action and does
not alter the frozen payload bytes, source/context authorities or zero-action
boundary. Recovery2 has not launched or acquired. Launch remains blocked until
the authorization-bearing bundle is refrozen, independently checked, and its
fresh output, control and socket roots are confirmed absent; the following
refreeze record satisfies those launch-before conditions without launching.

At authorization-bearing refreeze, the exact UTF-8 value of
`recovery2_source_send_authorization_text` is 1,460 bytes with SHA-256
`25198479d96bc58b80e9bd0632ccb8f4bb55bf17d0d9c412070d2824eee46122`.
The preparation status is `RECOVERY2_PREPARED_AUTHORIZED_NOT_STARTED`, and the
formal launch sentinel equals the exact recovery2 label. The current input
tree contains 4,200 files and 73,527,199 bytes; its sorted
`relative_path\0file_sha256\0size\n` receipt has SHA-256
`a3178c2ddd5801ff2e6fff4ca69fa255311c24152340782c391436efabbb0779`.
The 15,825-byte `manifest.json` has SHA-256
`b91bbec446296a490db5b5c9c16e8b0f7f9dc1f17302cbb2fe937dfef65a365e`.
The exact environment passed 47 tests in 12.19 seconds together with runner
check, Ruff check/format, `bash -n` and `jq`; recursive cache/symlink exclusion
passed and the output, control and socket roots remain absent. No launch,
provider call or acquisition occurred during the refreeze. These receipts
supersede every earlier recovery2 candidate or preauthorization receipt for
launch authority.

## Append-only recovery2 launch-only history — 2026-08-15

**Observed state:** `IN_FLIGHT / OUTCOME_UNREAD`

The authorization-bearing launch-before documents were committed as
`a0a005a6`. The launcher then returned exactly
`LAUNCHED inner_pid=655763`, and the durable acquisition time is
`2026-08-15T05:35:19Z`. The launch preflight passed 47 tests in 13.53 seconds.

An `h-network-dispatch` marker exists, but neither its contents nor any H
response, usage, observation, classification, result, status or terminal
artifact was read at this launch-only boundary. Marker existence is recorded
only as lifecycle state and does not establish that an HTTP request completed
or that the provider returned. No outcome-dependent action, retry, repair,
downstream stage or R67 action is permitted while the run remains outcome-
unread.

## Append-only recovery2 terminal note — 2026-08-15

Recovery2 subsequently exited `0` and atomically published result/status SHA-
256 values
`19e01061df9e15749081a15834cf20d66fed6584103e976755c444a30a1da616`
and `d6d359b7b0634b8ed9a0869d501a8447a918f7047850e08cbb2f388379e3fe08`.
Both durable files emit `TERMINAL_HYPOTHESIS_OBSERVATION`. Wrapper attempts, H
attempts, logical provider calls and actual network requests are all exactly
one; a terminal response exists, retry is zero, and all six mechanical-vector
fields are true. Latency is 16,134 ms. Usage is 20,808 input, 687 output and
296 reasoning tokens, with all recorded cache counts zero and cache hit false.
Raw response and full response-envelope abstractions are unavailable.

The typed H proposes a deadline-polled bounded intra-route 3-opt neighborhood
after `_two_opt_intra`, with direct accepted-move deltas and final-best distance
as mechanism/outcome measurements. `scientific_classification`, algorithm-
quality evidence, Decision and promotion remain null. Every C, patch,
materialization, Contract, V3/V4, solver, canary, Protocol, quality/formal,
Decision, promotion, repair/retry/resume/substitution and R67 counter is zero.

A separate post-terminal historical audit leaves that primary record unchanged
but assigns only the non-gating descriptive label
`DESCRIPTIVE_DIRECT_OLD_BOUNDED_INTRA_ROUTE_3OPT_OVERLAP`. Successor5 had weak
below-MDE `bounded_intra_route_3opt` evidence; successor6 expanded the same
branch to `18W/22L/8T`, median `0`, summary effect `-0.75`, then abandoned it;
the earlier activated R3 `three_opt_intra` direction was also soft-abandoned on
negative median evidence. Seal
`NO_NOVEL_FRONTIER_EVIDENCE / ZERO_ALGORITHM_QUALITY_EVIDENCE` and
`NO_C / NO_R67 / NO_RECOVERY3` without changing a gate or rejection record.

The terminal output root is mode `0700` with only mode-`0600` `result.json`
and `status.json`. The control root retains acquisition, one payload-free
dispatch marker, empty log and `exit.code=0`. The inner PID is gone; the
audited live tmux server and mode-`0600` socket are retained and must not be
deleted. Full artifact,
observation, overlap and claim-boundary facts are in the
[recovery2 postrun](v0.4-cvrp-r66-h-only-mechanism-frontier-probe-recovery2-postrun-20260815.md).
