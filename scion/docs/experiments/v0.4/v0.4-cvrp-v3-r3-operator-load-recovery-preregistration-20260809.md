# CVRP V3 R3 Operator-Load Recovery Preregistration — 2026-08-09

**Status:** `PREREGISTERED_BEFORE_R3_QUALITY_EXPANSION_TERMINAL`; measurement
amendment frozen before R3 validation terminal

## Why this record exists

The active R3 campaign remains fixed at runtime `76f3e976` and campaign root
`/home/clawd/research/scion-experiments/v04-cvrp-v3-quality-screen-16stage-r3-gpt56terra-20260809T194031Z-claw/campaign`.
During its still-incomplete exact quality expansion, non-preregistered pytest
runs executed on the same host. The host exposes two logical CPUs on one
physical core. The solver is deadline-driven, so competing CPU work can change
the amount of search completed inside a fixed wall-clock budget even when the
solver source, case and seed are unchanged.

At least two conservative overlap windows are known from pytest temporary
artifacts and campaign progress timestamps:

- approximately `22:08:00Z–22:10:00Z`, overlapping part of the
  `X-n502-k39` expansion;
- `22:28:31Z–22:30:36Z`, overlapping the end of `CMT3` and the transition to
  `X-n280-k17`.

Other earlier test invocations cannot be mapped to exact pair boundaries with
the same confidence. Therefore this record does not delete a favorable or
unfavorable subset and does not claim that only the named pairs are affected.
The whole R3 formal chain is conservatively marked
`OPERATOR_LOAD_CONTAMINATED_FOR_STRICT_PROMOTION_CLAIM`.

All CPU-heavy repository tests and benchmarks were stopped once the overlap
was identified. No active R3 case, seed, budget, threshold, source, queue or
candidate is changed. No pair is retried.

## Authority and claim boundary

R3 may continue to measure the integrated agent research path and may discover
an exact candidate. Its H/C lineage, typed failures, implementations,
Verification results and complete formal observations remain valid records of
what occurred. The root alone may not close the TASK CVRP acceptance block,
even if its existing deterministic Decision promotes a candidate.

This is an experiment-operations correction, not a new Scion gate. It adds no
provider rule, candidate admission rule, Trust/Hash chain, deployment work or
Decision feature. The V3 H -> C -> Contract -> Verification -> problem
Protocol -> Safe Features -> deterministic Decision path is unchanged.

## Outcome-blind recovery rule

The recovery branch is selected only by the existing typed Protocol outcome,
not by distance magnitude, family, seed pattern or human preference:

1. If R3 produces no Protocol-complete promoted exact candidate, no candidate
   receives a promotion recovery replay. The root is retained as contaminated
   partial science and the next action is the provider-free case-by-seed and
   budget diagnosis already declared in `TASK.md`, followed by a fresh
   preregistered campaign if required.
2. If R3 produces one Protocol-complete promoted exact candidate, that exact
   promoted source is the only recovery candidate. No earlier, later or
   apparently stronger candidate may be substituted.
3. If R3 promotes more than once, each exact promotion step must be recovered
   against its immediate parent in original Decision order; cumulative final
   replay also compares the final champion with original B0.

The active root is never resumed or retried. Recovery is provider-free and
uses a fresh absent evaluation root.

## Fixed clean replay

Recovery is authorized only by a typed R3 `PROMOTE`. Use the ordinary promotion
snapshots: candidate B is `champions/champion_vN`, immediate parent A is
`champions/champion_v(N-1)`, and final baseline is original
`champions/champion_v1`. If the expected promotion snapshot or parent is absent,
recovery does not launch. Do not restore a formal-candidate recorder, invoke the
historical fixed-replay reader or reconstruct an already promoted source from
provider traces.

Run without concurrent pytest, benchmark, build or other CPU-heavy work. Use
the already frozen R3 populations, ordered seeds, dimension budgets,
estimators and thresholds without alteration:

1. complete `12 cases x 8 seeds` quality;
2. complete disjoint `12 x 8` validation;
3. complete disjoint `12 x 8` frozen;
4. complete predeclared disjoint `12 x 8` final B0 comparison.

The replay must execute fresh candidate and comparison sides; it may not reuse
the contaminated R3 runtime cache. Every pair must complete with zero solver,
runtime, feasibility or protected-fleet failure. Quality, validation and
frozen must each pass their original R3 rule. Before any final-population data
is read, the final B0 block reuses the frozen case-level retention rule: net
score `>=0.25`, loss rate `<=0.20`, case-effect median `>=1`, CI low `>=0`, and
no fleet, feasibility or subject-runtime failure.

### Analysis-complete paired execution amendment

The R45 measurement correction was frozen before this recovery became
launchable and changes no case, seed, budget, estimator or threshold. Before
recovery, add only a default-off paired-execution specification to the existing
Protocol; `LocalSubprocessRunner` remains unchanged. A small experiment-owned
driver supplies promotion snapshots, the four fixed blocks and aggregation.
It is not a second runner or Decision path.

All four formal blocks use `ExperimentProtocol` plus that same seam. Quality,
validation and frozen retain their existing 10-second canary and stage rules.
For final, the driver injects the already frozen final cases and seeds into an
experiment-owned split/seed configuration and evaluates it as a FROZEN stage;
`FROZEN_PASS` is interpreted only as B0 retention. It does not call the legacy
runner-backed final evaluator, whose fixed baseline-first order, single time
limit and row-level aggregate do not implement this preregistered case-level
estimand; calling it would also run the solver twice. Final adds no extra canary.

For 0-based candidate, block, case and seed ordinals, schedule AB when

```text
(candidate_ordinal + block_ordinal + case_ordinal + seed_ordinal) % 2 == 0
```

and BA otherwise. Disable champion caching and run both subjects fresh. Each
pair records only block/case/seed ordinals, scheduled and actual order, both
complete objective mappings, both feasible/success values, both elapsed/time
limits and one bounded failure or exit fact. State the provider-free and
Decision-excluded boundary once in the recovery manifest, not in every row.
The production paired-order/raw seam is capped at 100 added lines and the
experiment-owned driver at 220 lines; neither may add a ledger, digest,
identity, gate or resume state machine.

No result-dependent case family, seed, timeout or threshold adjustment is
allowed. If host load contamination recurs, the recovery root is terminal and
invalid; it is not resumed.

After R3 is terminal, seal it before any source change. If it promoted, land
only the paired-order/raw seam and focused fake-runner tests, then launch this
recovery from a fresh absent root. R44, V8, rejection diagnostics, provider
projection and C-expression work all wait until recovery completes. A single
promotion's four blocks carry about 12.7 hours of maximum solver budget, so the
host remains CPU-exclusive for the whole run.

## Interpretation

- Clean replay pass: `RETAINED_IMPROVEMENT_RECOVERED_BY_CLEAN_FIXED_REPLAY`.
  The exact Scion-generated algorithmic improvement is accepted, while the
  original R3 root remains annotated as operator-load contaminated.
- Clean replay fail or runtime failure: no CVRP promotion claim; preserve both
  roots and proceed to diagnosis/new research.
- No R3 promotion: R3 can support bounded research-behavior and implementation
  conclusions, but no negative claim about CVRP improvability or effects below
  the design's measurement reach.

Repository tests for pending framework edits remain deferred until recovery is
complete or inapplicable. Only focused fake-runner tests for the paired-order/raw
seam may run between R3 terminal and recovery launch; they do not execute the
solver or modify the exact promoted candidate snapshots.
