# CVRP R58 Expression-Corrected Seed-29 Minute One-Step Postrun — 2026-08-12

**Status:** `VALID_TERMINAL_RESEARCH_PROCESS_OBSERVATION / VERIFICATION_LIGHT_REJECTED / ZERO_PROTOCOL_EVIDENCE / NO_DECISION / NO_PROMOTION`

## Scope and V3 authority

This report closes the separately preregistered R58 minute-scale fresh-B0 CVRP
step. R58 asked for one new algorithm-research step after the provider-free R57
C-expression correction and prospectively changed the immediate minute-series
seed from `11` to `29`. It was not an R56 repair, retry or resume, an
exact-candidate replication, a validation/frozen run, a promotion campaign or
proof of solver improvement.

[`design/scion-architecture-v3.md`](../../../design/scion-architecture-v3.md)
remains the sole architecture authority. No deployment, packaging, build,
root/systemd, Trust/Hash, identity, registry, receipt, self-proof or new
research gate was added.

## Authorization, lifecycle and artifacts

- Frozen preregistration:
  [`v0.4-cvrp-r58-minute-expression-corrected-one-step-preregistration-20260812.md`](v0.4-cvrp-r58-minute-expression-corrected-one-step-preregistration-20260812.md)
- Clean source reference:
  `42535efc88582fdd7ab3ece13b349d3c3085813b`
- Input bundle:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r58-minute-expression-corrected-one-step-20260812-input`
- Output root:
  `/home/clawd/research/scion-experiments/v04-cvrp-r58-minute-expression-corrected-one-step-20260812-claw`
- Process-control root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r58-minute-expression-corrected-one-step-20260812-control`
- Driver result: `driver_result.json`
- Campaign summary: `campaign/campaign_summary.json`
- Lineage database: `campaign/scion.db`

The user supplied the exact fresh source-send authorization before launch. The
ordinary-user launcher acquired its inner process at
`2026-08-12T23:38:22Z`; the process-control exit code is `0`, and the bounded
run terminalized about 44 seconds after acquisition. Startup PID `316499` is an
observation only, not a durable process identity.

The durable lifecycle facts are:

- `run_one_step()` exactly once and `run()` zero times;
- one Terra H and one conditional Terra C, with zero retries and zero repair
  calls;
- both provider responses were terminal valid structured tool-call JSON;
- the recorded Contract passed;
- V1, V1b and V2 passed, then V3 executed and rejected the candidate;
- V4 and every later Verification check were not reached;
- zero canary and zero formal Protocol calls;
- zero screening, validation and frozen results;
- no raw metrics artifact and no Decision; and
- B0/v1 remained the sole champion, with no promotion.

The wrapper completed normally as `COMPLETED_ONE_STEP_WRAPPER`. Research
eligibility is `invalid_research_rejected_only / ineligible_zero_evaluated`:
there is a valid terminal research-process observation but no evaluated solver
outcome. This is not an infrastructure-invalid or interrupted wrapper.

## H research quality and provider-visible evidence

H received the exact frozen R58 question and the exact R53–R56 neutral prior
once. It again selected elapsed-budget-normalized simulated annealing, grounded
the proposal in the visible source and supplied an observable falsifier. The H
was valid and approved through the ordinary Hypothesis Contract.

The repeated mechanism is neither a host requirement nor evidence that the
algorithm is good or bad. R54, R56 and now R58 never produced a complete
quality comparison for an elapsed-budget-SA candidate. H relevance and
feedback uptake therefore remain research-reasoning facts only.

## C expression choice and incomplete applied source

C received the exact approved H and all 11 editable problem-owned source files.
Its tool schema included the optional R57 `exact_line_replace` intent and
ordinary guidance for whole-line replacement across indentation depths. C
nevertheless emitted six `exact_replace` edits and zero `exact_line_replace`
edits. Every supplied exact selector matched its source uniquely and all six
edits applied; there was no parse, schema, source-binding or ambiguous-selector
failure.

The frozen scheduler contained five calls to `annealing.cool()`. The applied
changes removed only two calls—one at 20-space indentation and the ordinary
loop-tail call at 12-space indentation—while deleting the
`_SimulatedAnnealing.cool()` method itself. Three calls at 16-space indentation
remained on the repair-error, candidate-infeasible and route-limit recovery
paths. All three were therefore invalid member calls. C's test hint named these
recovery paths, so the missing transformation cannot be attributed to absent
source or an unknown verification target.

The proposal preserved the broad approved mechanism but failed to express its
all-path API replacement. Unlike R56's single indentation-specific
`replace_all`, R58 had the corrected optional line-oriented expression form
available but did not choose it. This is evidence about C expression-form
selection and multi-site completeness, not evidence against the proposed
algorithm.

Independent static review also found a separate mechanism caveat: the elapsed
progress helper starts its numerator after initial VNS but uses a denominator
based on the full deadline minus reserve, so progress need not reach `1` at the
reserve boundary. Because the candidate failed before solver-quality
evaluation, this remains a static source observation. It is not a measured
quality, runtime or causal conclusion.

## Contract and executable Verification

The recorded Patch Contract passed. The executed Verification checks were:

| Check | Result | Durable meaning |
|---|---|---|
| V1 syntax | pass in 3 ms | The residual member calls are syntactically valid. |
| V1b undefined names | pass in 7 ms | A missing object member is not an undefined Python name. |
| V2 interface | pass/deferred in 2 ms | `solver_design` support module interface deferred to workspace smoke (AST). |
| V3 unit tests | fail in 568 ms | All three recovery fixtures raised `AttributeError` on the removed `cool()` method. |
| V4 regression tests | not reached | Verification stopped at V3. |
| V5–V8 | not reached | No later smoke, feasibility, oracle or nondeterminism check ran. |

The V3 pytest body reported three failures in 0.21 seconds. The failing applied
scheduler locations were lines 227, 249 and 269. Thus the problem-owned tests
again rejected an inconsistent multi-site edit in under one second and before
canary or formal solver spending. Candidate-workspace cleanup succeeded and
the retained source/diff state returned to the clean base.

This is ordinary executable runtime-consistency coverage, not an algorithm,
novelty, expression-style or quality gate.

## Zero Protocol evidence and seed-29 boundary

No candidate or champion canary or formal Protocol evaluation ran. The complete
scientific accounting is therefore:

```text
canary calls:             0
formal Protocol calls:    0
screening rows:           0
validation rows:          0
frozen rows:              0
admissible pairs:         0
seed-29 W/L/T:            undefined
delta / confidence band: undefined
fleet comparison:         undefined
Decision:                 none
champion:                 B0/v1
```

R58 supports no statement about final distance, feasibility under candidate
execution, fleet behavior, seed behavior, case quality, replication,
generalization or promotion. Seed `29` was already exposed on these exact cases
in R3 and differed only from the immediate R53–R56 minute-series seed `11`.
Because R58 never reached Protocol, even the preregistered different-seed
exploratory estimand remains entirely unobserved.

## Conclusion and next boundary

R58 is sealed as
`VALID_TERMINAL_RESEARCH_PROCESS_OBSERVATION / VERIFICATION_LIGHT_REJECTED /
ZERO_PROTOCOL_EVIDENCE / NO_DECISION / NO_PROMOTION`. It demonstrates another
clean bounded early-rejection lifecycle: exit `0`, terminal H/C, passing
Contract, executable V3 rejection and no expenditure on canary or the
94-subject-second formal screen.

It does not demonstrate that the C-expression problem is solved. R57 proved
provider-free that `exact_line_replace` could faithfully express the frozen
R56 multi-indent transformation; R58 shows that merely exposing the optional
form did not cause C to select it when facing the same class of edit. Repeating
another provider algorithm step now would test the unresolved expression
selection failure again rather than produce stable solver evidence.

Pause provider CVRP algorithm experiments. The next work is a minimal
provider-free redesign and test of the C expression-selection surface, without
adding a source-navigation loop, repair/retry call, patch grader, algorithm
direction rule, candidate-quality gate or Decision input. Do not launch R59, a
long campaign or a WSL run from this result.

The R58 root is not repaired, retried, resumed, screened, validated, frozen or
promoted. CVRP acceptance and S6 remain open.
