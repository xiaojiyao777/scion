# v0.4 Semantic Novelty Repair Sonnet 5R Smoke

Date: 2026-05-10 UTC

Run root:

```text
/home/clawd/research/scion-experiments/v04-semantic-novelty-repair-sonnet-5r-20260510T043306Z
```

This document records delegated raw-artifact analysis for the five-round
forced `main_search_strategy` CVRP formal-path smoke launched after the
singleton semantic novelty persistence repair. Raw experiment artifacts remain
in the run directory and should be referenced, not copied into project docs.

## Completion Status

```text
scion_commit=7111d69
worktree_dirty=false
status=completed
exit_record=EXIT_CODE:0
```

The campaign completed normally. `status.json`, `campaign_summary.json`,
SQLite, `run.log`, and `exit.txt` were internally consistent.

## Purpose

Validate the `7111d69` core governance repair:

- semantic singleton hypotheses must carry usable structured
  `novelty_signature`;
- old empty-signature singleton records must not poison later valid structured
  proposals;
- forced `main_search_strategy` control must stay fail-closed;
- APS feedback/memory reads must remain within the 48000-character observation
  cap;
- multiple code patches should reach screening before judging CVRP strategy
  efficacy.

This is a short diagnostic, not solver-quality evidence.

## Configuration

```text
model=claude-sonnet-4-6
problem=cvrp formal VRP
rounds=5
agentic_proposal=true
disable_early_stop=true
force_surface=main_search_strategy
cvrp_time_limit_sec=10
python=/home/clawd/miniconda3/envs/claw/bin/python
data_root=/home/clawd/research/or-autoresearch-agent/vrp
```

Artifact refs:

```text
launch=/home/clawd/research/scion-experiments/v04-semantic-novelty-repair-sonnet-5r-20260510T043306Z/launch.env
command=/home/clawd/research/scion-experiments/v04-semantic-novelty-repair-sonnet-5r-20260510T043306Z/command.txt
summary=/home/clawd/research/scion-experiments/v04-semantic-novelty-repair-sonnet-5r-20260510T043306Z/campaign/campaign_summary.json
status=/home/clawd/research/scion-experiments/v04-semantic-novelty-repair-sonnet-5r-20260510T043306Z/campaign/status.json
db=/home/clawd/research/scion-experiments/v04-semantic-novelty-repair-sonnet-5r-20260510T043306Z/campaign/scion.db
metrics=/home/clawd/research/scion-experiments/v04-semantic-novelty-repair-sonnet-5r-20260510T043306Z/campaign/metrics/
agentic_sessions=/home/clawd/research/scion-experiments/v04-semantic-novelty-repair-sonnet-5r-20260510T043306Z/campaign/agentic_sessions/
llm_traces=/home/clawd/research/scion-experiments/v04-semantic-novelty-repair-sonnet-5r-20260510T043306Z/campaign/llm_traces/
```

## Outcome

```text
total_rounds=5/5
steps=5
experiments=4
champion=v1
champion_weight_revision=0
promotions=0
frozen_budget_used=0/2
formal_ready=false
final_evidence_refs=missing
stopped_reason=max_rounds_exhausted
```

Coverage:

```text
action_locus_coverage=modify/main_search_strategy: 4
action_locus_coverage=create_new/proposal: 1
```

The `create_new/proposal` row was a proposal-failure placeholder from round 3,
not an actual off-surface candidate patch.

## Repair Acceptance

| Check | Result | Interpretation |
| --- | --- | --- |
| C10 empty-signature poisoning | pass | No `C10_novelty` failures occurred; later structured singleton proposals were not blocked by prior records. |
| Structured novelty identity | pass | All four approved hypotheses had complete structured `novelty_signature`. |
| Multiple code patches | pass | Rounds 1, 2, 4, and 5 generated patches and reached screening. |
| Observation budget | pass | No `result_too_large`, observation-budget exhaustion, or tool-budget failure recurred. |
| Forced surface | pass with one caught drift | Four evaluated candidates stayed on `main_search_strategy`; round 3 drifted to `baseline_policy` but was rejected before code generation. |
| Selected-surface audit | pass | All screened candidate pairs had complete `main_search_strategy` runtime audit. |
| Solver-quality evidence | fail | All screened candidates failed `SCREENING_FAIL_WIN_RATE`; best case-level win rate was 0.25. |

## Round Summary

| Round | Hypothesis / Research Session | Code / Implementation Session | Gate / Protocol | Interpretation |
| ---: | --- | --- | --- | --- |
| 1 | Forced `main_search_strategy`; complete structured novelty; both deep components. | Completed patch for `policies/main_search_strategy.py`. | Contract, Verification, canary passed; screening failed. | Governance path passed; quality insufficient. |
| 2 | Forced `main_search_strategy`; route-pair-only, baseline-heavy structured novelty. | Completed patch for same file. | Contract, Verification, canary passed; screening failed. | C10 allowed distinct structured identity; route-pair-only did not improve. |
| 3 | APS drafted `baseline_policy`. | Not reached. | Proposal failed forced-surface constraint. | Forced governance failed closed before code generation. |
| 4 | Forced `main_search_strategy`; construction ensemble plus route-pair-only strategy. | Completed patch for same file. | Contract, Verification, canary passed; screening failed. | Valid patch, but no case-level lift. |
| 5 | Forced `main_search_strategy`; both deep components, wider budget, restart/perturbation. | Completed patch for same file. | Contract, Verification, canary passed; screening failed. | Best signal, but still below screening threshold. |

## APS Per-Round Chain

The normal successful hypothesis session sequence was:

```text
context.read_surface(main_search_strategy)
-> duplicate read_surface skipped
-> context.list_surfaces
-> context.read_problem
-> memory.query
-> feedback.query_screening
-> feedback.query_runtime
-> proposal.schema_preview
-> proposal.target_permission_preview
```

The normal successful code session sequence was the same compact read path plus
`proposal.draft_patch` and `proposal.contract_preview`.

### Round 1

Hypothesis phase:

- Session used 8 tool calls and about `21317/48000` observation chars.
- It read the selected surface, compact surface list, problem summary, memory,
  screening feedback, and runtime feedback.
- Screening feedback had 0 rows and runtime feedback was unavailable because no
  prior screening had completed yet.
- Forced `modify/main_search_strategy` targeting
  `policies/main_search_strategy.py` was obeyed.
- Structured identity was complete: selected/deep components, budget pattern,
  destroy/repair pattern, baseline fraction pattern, acceptance/restart
  pattern, and perturbation pattern.

Code phase:

- Session used 9 tool calls and about `30596/48000` observation chars.
- Duplicate selected-surface read was skipped.
- Schema, target, and contract previews passed.
- Patch targeted `policies/main_search_strategy.py`.

Actual strategy:

- Whole-algorithm `main_search_strategy` surface change, not an operator.
- Construction used `nearest_neighbor` plus `demand_descending`, keeping two
  candidates.
- Baseline fraction was `0.75`.
- Improvement selected `route_pair_swap` and `bounded_destroy_repair`,
  `rounds=5`, `top_k=64`.
- Restart and perturbation were enabled; registry operators were disabled.

Gate/protocol:

- Contract, Verification, and canary passed.
- Screening ran 16 valid pairs and failed `SCREENING_FAIL_WIN_RATE`.

### Round 2

Hypothesis phase:

- Session used 8 tool calls and about `22066/48000` observation chars.
- Full compact context and feedback tools were called.
- Feedback still returned 0 screening rows and runtime unavailable.
- Forced surface was obeyed.
- Structured novelty was complete and distinct from round 1.

Code phase:

- Session used 9 tool calls and about `30454/48000` observation chars.
- Schema, target, and contract previews passed.

Actual strategy:

- Whole-algorithm `main_search_strategy` policy/config change.
- Route-pair-only strategy: construction `nearest_neighbor`, baseline fraction
  `0.85`, `route_pair_swap` only, requested 6 rounds and `top_k=32`.
- No bounded destroy/repair, restart, or perturbation.

Gate/protocol:

- Contract, Verification, and canary passed.
- Screening ran 16 valid pairs and failed `SCREENING_FAIL_WIN_RATE`.

### Round 3

Hypothesis phase:

- Session used 6 tool calls and about `17372/48000` observation chars.
- It read selected surface/list/problem/memory/screening/runtime context but
  stopped before schema and target previews.
- The model drafted `change_locus=baseline_policy` targeting
  `policies/baseline_policy.py`.

Code phase:

- Not reached.

Gate/protocol:

- Proposal failed the forced-surface constraint:
  `change_locus must be main_search_strategy, got baseline_policy`.
- No ContractGate, code generation, Verification, canary, or screening ran.

Causal read:

This is a successful fail-closed diagnostic control event. The model still had
an off-surface impulse, but the framework stopped it before code generation.

### Round 4

Hypothesis phase:

- Session used 8 tool calls and about `24227/48000` observation chars.
- Full compact context and feedback tools were called.
- Forced surface was obeyed.
- Structured novelty was complete.

Code phase:

- Session used 9 tool calls and about `33618/48000` observation chars.
- Schema, target, and contract previews passed.

Actual strategy:

- Whole-algorithm `main_search_strategy` change.
- Construction ensemble used `nearest_neighbor`, `demand_descending`, and
  `nearest_neighbor_demand_bias`, keeping three candidates.
- Baseline fraction was `0.80`.
- Route-pair-only improvement with `rounds=4`, `top_k=16`, and stricter
  minimum distance improvement.
- Perturbation was enabled; restart was disabled.

Gate/protocol:

- Contract, Verification, and canary passed.
- Screening ran 16 valid pairs and failed `SCREENING_FAIL_WIN_RATE`.

### Round 5

Hypothesis phase:

- Session used 8 tool calls and about `25791/48000` observation chars.
- Full compact context and feedback tools were called.
- Forced surface was obeyed.
- Structured novelty was complete.

Code phase:

- Session used 9 tool calls and about `35295/48000` observation chars.
- Schema, target, and contract previews passed.

Actual strategy:

- Whole-algorithm `main_search_strategy` change.
- Requested baseline fraction was `0.60`, but the formal baseline quality
  guard raised effective baseline fraction to `0.75`.
- Improvement selected both `route_pair_swap` and `bounded_destroy_repair`,
  requested 6 rounds and `top_k=128`.
- Restart and perturbation were more aggressive than prior rounds.

Gate/protocol:

- Contract, Verification, and canary passed.
- Screening ran 16 valid pairs and failed `SCREENING_FAIL_WIN_RATE`.
- This was the best candidate in the run, but still below threshold.

## Screened Candidate Evidence

| Round | Pair W/L/T | Case Win Rate | Median Delta | Median Runtime Ratio | Gate / Decision |
| ---: | ---: | ---: | ---: | ---: | --- |
| 1 | 3/2/11 | 0.00 | 0.0 | 0.9519 | `SCREENING_FAIL_WIN_RATE` |
| 2 | 3/1/12 | 0.00 | 0.0 | 1.0628 | `SCREENING_FAIL_WIN_RATE` |
| 4 | 3/1/12 | 0.00 | 0.0 | 1.0107 | `SCREENING_FAIL_WIN_RATE` |
| 5 | 5/1/10 | 0.25 | 0.0 | 0.9694 | `SCREENING_FAIL_WIN_RATE` |

Round 5 produced the strongest signal, but it still failed the screening
threshold (`win_rate < 0.3`) and had `median_delta=0.0`.

## Selected-Surface Runtime Audit

All four screened candidates selected `main_search_strategy`.

- Each screened candidate had all 44 required runtime fields observed across
  16/16 candidate pairs.
- `main_search_strategy_loaded=true`, `main_search_strategy_active=true`, and
  `main_search_strategy_errors=0` were present for all candidate pairs.
- `main_search_baseline_param_clamps` was present and non-empty for all pairs.
- This run covered clamp cases, not no-clamp cases.
- Round 5's requested low baseline fraction was raised by the formal baseline
  quality guard, so the candidate's requested 40% improvement budget did not
  execute literally.

## Component Telemetry

- Round 1 selected and attempted both deep components. `route_pair_swap`
  accepted 19 moves; `bounded_destroy_repair` accepted 0.
- Round 2 selected only `route_pair_swap`; it accepted 0 moves.
- Round 4 selected only `route_pair_swap`; it accepted 18 moves.
- Round 5 selected both deep components. `route_pair_swap` accepted 43 moves
  and `bounded_destroy_repair` accepted 11 moves.

Dominant skip reasons included:

- `route_pair_swap:no_improving_candidate`;
- `bounded_destroy_repair:repair_produced_no_improvement`;
- `bounded_destroy_repair:route_pair_phase_improved`;
- `bounded_destroy_repair:bounded_destroy_repair_accept_limit_reached`.

The important remaining attribution gap is that accepted component moves did
not clearly become final case-level benefit. Delegated analysis found
`main_search_objective_delta_by_phase.improvement_loop` still at 0.0 even when
accepted moves increased. Round 5's pair-level and case-level signal may be a
mix of baseline/effective-guard/parameter interaction and stochastic pair
outcome, not proven component-driven net improvement.

## Feedback Tool Finding

Every successful APS session called `feedback.query_screening` and
`feedback.query_runtime`, but later rounds still observed 0 screening feedback
rows and no safe runtime feedback, even after earlier screening results
existed in the campaign.

This suggests a proposal-feedback retrieval bug or scope mismatch. The model
may still be using memory/surface context to narrate prior attempts, but the
bounded feedback tools are not exposing campaign-level completed screening and
runtime summaries as intended.

## Causal Diagnosis

The `7111d69` repair fixed the immediate governance blocker:

- C10 did not reject later forced singleton hypotheses because of an old empty
  structured identity.
- Approved hypotheses persisted complete structured `novelty_signature`.
- Four patches reached screening.
- APS budget remained healthy.
- Forced-surface control stayed fail-closed.

The current blocker is no longer C10 or APS observation budget. It is now:

1. proposal-feedback attribution: `feedback.query_screening` and
   `feedback.query_runtime` are not returning same-campaign completed results;
2. CVRP surface efficacy: accepted component moves and local pair wins still
   do not reliably produce final case-level improvement.

## Next Action

Do not start long CVRP validation.

First repair or audit proposal feedback retrieval so forced diagnostics can see
same-campaign compact screening/runtime history after each completed screening
round. The next smoke should require:

- forced hypotheses stay on `main_search_strategy`;
- no off-surface proposal reaches code generation;
- five code patches or a clearly explained proposal failure;
- feedback rows become non-empty after prior screening exists;
- at least four screened candidates;
- component activity can be traced through phase delta to final case-level
  outcome.
