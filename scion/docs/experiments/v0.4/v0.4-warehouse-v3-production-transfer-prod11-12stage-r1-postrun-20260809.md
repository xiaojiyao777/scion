# Warehouse V3 Production Transfer prod-1.1 12-Stage R1 Postrun — 2026-08-09

**Classification:** `VALID_FUNNEL_FOR_24STAGE_PREREGISTRATION`

## Disposition

The preregistered production-style Warehouse shakedown is valid and complete.
It ran once in the declared fresh root, completed all 12 formal stages, and
stopped because the requested horizon was complete. The wrapper finished with
exit status `0`, no signal and final validity `valid`.

- campaign root:
  `/home/clawd/research/scion-experiments/v04-warehouse-v3-production-transfer-prod11-12stage-r1-gpt56terra-20260809T021245Z-claw/campaign`;
- executable source: `e1fbee8cd37aca31e445d0cc165422a1442cffb8`;
- started/ended: `2026-08-09T02:19:02Z` / `2026-08-09T03:10:56Z`;
- requested/completed formal stages: `12 / 12`;
- screening/validation/frozen: `11 / 1 / 0`;
- H/C calls: `9 / 9`, all terminal responses from `gpt-5.6-terra` through
  the local Codex proxy;
- attempted/valid/failed formal pairs: `211 / 211 / 0`;
- candidate/champion failed pairs: `0 / 0`;
- champion path: v1 only; no promotion occurred.

The final exact candidate reached validation and was deterministically queued
for validation expansion. The horizon ended with branch
`90120a5b-cf43-49c3-8f35-496482a4a319` in `validating_expand`. This is the
preregistered valid-funnel condition. The root is sealed and must not be
resumed; any 24-stage follow-up requires a separately preregistered fresh root.

## Input and execution audit

The executable/configuration/solver diff from the fixed source commit through
the registration-only `HEAD` was empty at launch and remained empty after the
run. The run used `problem-v1`, protocol `3.2-prod`, split `prod-1.1`, seed
ledger `3.0`, a 30-second solver limit and disabled parameter search.

The observed formal populations matched the fixed Protocol throughout:

- all seven initial modify screenings used the same six cases and seeds
  `42, 137`;
- all four expanded screenings used the same 14 cases and seeds `42, 137`;
- validation used all five declared validation cases and seeds `7, 19, 83`;
- every raw artifact recorded empty configured and effective priority-case
  lists, so no result-derived case channel affected this run;
- all 12 stage-level canary outcomes passed on the two declared canary cases
  with seed `999`.

The 211 formal pairs comprise 133 candidate wins, 15 losses and 63 ties. The
champion-result cache supplied 156 repeated v1 results and 55 champion results
were fresh. All cached and fresh objective pairs were complete; cached runtime
evidence was marked low confidence or excluded by the runtime policy. Runtime
was diagnostic only and did not enter the declared lexicographic solver-quality
decision.

There were zero infrastructure, resource, interruption, unknown or formal
solver-failure outcomes.

## Formal-stage evidence

Positive deltas below mean candidate improvement. Screening rows use
`total_cost` as the statistical metric after primary-objective ties; the final
validation row uses the primary `subcategory_splits` metric under the declared
hierarchical rule.

| Stage | Candidate | Protocol step | Valid | Case W/L/T | Pair W/L/T | Median improvement | CI | Decision |
|---:|---|---|---:|---:|---:|---:|---:|---|
| 1 | `8bbc8a1c` | screening | 12/12 | 3/0/3 | 8/0/4 | cost `+400` | `[150, 6225]` | expand screening |
| 2 | `8bbc8a1c` | expanded screening | 28/28 | 7/0/7 | 18/4/6 | cost `+450` | `[250, 3550]` | continue explore |
| 3 | `e423d82b` | screening | 12/12 | 1/1/4 | 4/4/4 | cost `0` | `[-2275, 650]` | continue explore |
| 4 | `4ab5e98d` | screening | 12/12 | 2/1/3 | 5/2/5 | cost `+450` | `[-1675, 3000]` | continue explore |
| 5 | `8d71c693` | screening | 12/12 | 3/0/3 | 7/1/4 | cost `+800` | `[0, 9375]` | expand screening |
| 6 | `8d71c693` | expanded screening | 28/28 | 7/0/7 | 18/1/9 | cost `+975` | `[400, 4500]` | continue explore |
| 7 | `090a770c` | screening | 12/12 | 3/0/3 | 7/1/4 | cost `+800` | `[0, 9375]` | expand screening |
| 8 | `090a770c` | expanded screening | 28/28 | 7/0/7 | 18/1/9 | cost `+975` | `[400, 4500]` | continue explore |
| 9 | `6d1e7748` | screening | 12/12 | 3/0/3 | 7/0/5 | cost `+950` | `[0, 9575]` | expand screening |
| 10 | `6d1e7748` | expanded screening | 28/28 | 7/0/7 | 18/0/10 | cost `+950` | `[375, 4700]` | continue explore |
| 11 | `38445224` | screening | 12/12 | 4/0/2 | 9/0/3 | cost `+950` | `[150, 9450]` | queue validation |
| 12 | `38445224` | validation | 15/15 | 5/0/0 | 14/1/0 | splits `0` | `[0, 1]` | expand validation |

Four earlier DestroyRebuild candidates produced positive cost evidence but
exhausted expanded screening with a case win rate of `0.5`, below the declared
`0.55` threshold because seven cases tied. The MoveOrder and MergeVehicles
branches each had a case loss and failed the initial screening win-rate rule.
These are scientific negatives, not framework stops.

## Final validation signal

The final candidate modifies DestroyRebuild's bounded first-target generation
and cross-batch beam so a deterministic new-vehicle trial can compete with
feasible existing destinations under the existing lexicographic score. Its
validation evidence was:

| Case | Pair W/L/T | Median split improvement | Median cost improvement |
|---|---:|---:|---:|
| `instance_prod_val_l01.json` | 3/0/0 | `+1` | `+15300` |
| `instance_prod_val_l02.json` | 3/0/0 | `0` | `+13300` |
| `instance_prod_val_l04.json` | 3/0/0 | `0` | `+14700` |
| `instance_prod_val_lx01.json` | 3/0/0 | `0` | `+10900` |
| `instance_prod_val_lx02.json` | 2/1/0 | `0` | `+7700` |

Across all 15 validation pairs, primary split improvement was positive on
three and tied on twelve; cost improvement was positive on fourteen and
negative on one. Median cost improvement was `+13200`. The hierarchical
primary median remained `0` with CI `[0, 1]`, so Protocol correctly returned
`VALIDATION_EXPAND_HIERARCHICAL_UNCERTAIN` rather than queuing frozen.

The runtime diagnostic was unfavorable: median candidate-minus-champion time
was `+9681 ms`, median ratio `1.473`, and all 15 validation pairs were slower.
This does not overturn the declared solver-quality result, but it must remain
visible in any follow-up analysis.

## H/C lineage, rejections and exact reuse

All 18 provider traces were persisted under one campaign ID. Every H and C
trace reports model `gpt-5.6-terra`, `ok=true` at the provider boundary and a
terminal response. Each C call carries its approved H hypothesis ID and the H
target file; there were no duplicate provider-call retries.

Two candidate-local paths were rejected exactly as allowed by the
preregistration:

- `4facc231...`: the C response was terminal, but its typed edit could not be
  source-bound because `old_string` was absent; it was recorded as
  `PROPOSAL_RESPONSE_INVALID` and a fresh H followed;
- `e43f9c47...`: syntax, undefined-name, interface and tests passed, but heavy
  Verification failed at V5 because the candidate called missing method
  `_prune_beam`; the isolated candidate workspace was cleaned and a fresh H
  followed.

Neither rejection consumed a formal stage, changed the champion or stopped
the campaign. The remaining path demonstrates same-branch refinement: the
agent repeatedly revised DestroyRebuild from greedy reconstruction through
batching and a bounded cross-batch beam before producing the validating
candidate.

Exact reuse is visible in both IDs and code hashes:

- stages 1/2 reuse `8bbc8a1c...` / `f5489ab5...`;
- stages 5/6 reuse `8d71c693...` / `e9b6f5e7...`;
- stages 7/8 reuse `090a770c...` / `e289974f...`;
- stages 9/10 reuse `6d1e7748...` / `46d25b51...`;
- stages 11/12 reuse `38445224...` /
  `06287a5604c8598061b144e55bab8946a56a96fc5762621ab4e0cc4cf9394d42`.

There was no provider call between the stage-11 screening decision and the
stage-12 validation. The terminal branch records `candidate_disposition` as
`exact_reuse`, and its current and last-clean code hashes are identical.

## Champion and pending state

The champion table still contains only v1, snapshot hash
`4271a0a44c9e8a7014ee0b73ff1982539f6ecae3fd76f1e14df85ca3e4322e27`.
The final candidate was not promoted and no frozen evidence exists. It is
pending the exact validation-expansion stage selected by Protocol, but the
12-stage root is complete and sealed.

`status.json` contains one legacy presentation inconsistency: alongside
`run_complete=true`, `completed_requested_rounds=true`, 12/12 formal results
and validity `valid`, its aggregate label says `valid_but_incomplete` because
one proposal-local rejection is counted as a non-evaluated outcome, and its
operator text suggests resume/rerun. That suggestion conflicts with the
preregistered accounting rule and the wrapper's final `valid / complete`
record. It is not authority to resume this root and does not invalidate the 12
formal results.

## Claim boundary

This run shows that the lightweight V3 path produced a real production-style
research funnel: an unconstrained Terra H/C sequence accumulated a
DestroyRebuild refinement, passed executable Verification, passed screening,
and reached complete positive-but-hierarchically-uncertain validation. It
therefore satisfies `VALID_FUNNEL_FOR_24STAGE_PREREGISTRATION`.

It does not establish a promotion, retained production transfer, frozen
generalization, a runtime improvement, a general absence of production
headroom, CVRP improvement or overall `TASK.md` completion. A later 24-stage
experiment is permitted by the preregistration but is neither part of this run
nor authorized by this postrun record.
