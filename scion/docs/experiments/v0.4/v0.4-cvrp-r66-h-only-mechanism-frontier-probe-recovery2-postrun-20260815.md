# CVRP R66 H-Only Mechanism-Frontier Probe Recovery2 Postrun — 2026-08-15

**Durable classification:** `TERMINAL_HYPOTHESIS_OBSERVATION`

**Mechanical endpoint:** `COMPLETE / SIX_OF_SIX_TRUE`

**Descriptive post-terminal overlap label:**
`DESCRIPTIVE_DIRECT_OLD_BOUNDED_INTRA_ROUTE_3OPT_OVERLAP`

**Research seal:** `NO_NOVEL_FRONTIER_EVIDENCE / ZERO_ALGORITHM_QUALITY_EVIDENCE`

**Stop:** `NO_C / NO_R67 / NO_RECOVERY3`

## Scope and authority

Recovery2 was the separately rooted, exactly authorized R66 H-only local-
dependency recovery recorded in the append-only
[preregistration](v0.4-cvrp-r66-h-only-mechanism-frontier-probe-preregistration-20260814.md).
It retained the exact fresh-B0 eleven-source context at `51f9bbd7`, fixed
research question and E01–E10 ledger, clean `47fe81ee` production H system and
tool, fixed `as_of=2026-08-14`, and every provider-visible request byte. Its
only local correction was the frozen nine-file experiment-owned
`jiter==0.13.0` authority under `vendor/python312`, with
`PYTHONNOUSERSITE=1`.

The exact 1,460-byte UTF-8 recovery2 authorization has SHA-256
`25198479d96bc58b80e9bd0632ccb8f4bb55bf17d0d9c412070d2824eee46122`.
The authorization-bearing input tree has 4,200 files and 73,527,199 bytes,
with canonical receipt SHA-256
`a3178c2ddd5801ff2e6fff4ca69fa255311c24152340782c391436efabbb0779`.
The 15,825-byte manifest SHA-256 is
`b91bbec446296a490db5b5c9c16e8b0f7f9dc1f17302cbb2fe937dfef65a365e`.
The original R66, recovery1 input/control/output/socket and their terminal
artifacts remain unchanged.

## Launch and lifecycle

The authorization-bearing documents were committed as `a0a005a6`. The formal
preflight passed 47 tests in 13.53 seconds. The launcher returned
`LAUNCHED inner_pid=655763`, and the inner process durably acquired at
`2026-08-15T05:35:19Z`. Recovery2 made no retry, repair, resume, substitution
or second launch. The wrapper exited `0`.

The output root is
`/home/clawd/research/scion-experiments/v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-recovery2-claw`.
It is mode `0700`, has terminal mtime
`2026-08-15T05:35:37.533732854Z`, and contains only:

- `result.json`: mode `0600`, 7,039 bytes, mtime
  `2026-08-15T05:35:37.530732829Z`, SHA-256
  `19e01061df9e15749081a15834cf20d66fed6584103e976755c444a30a1da616`;
- `status.json`: mode `0600`, 842 bytes, mtime
  `2026-08-15T05:35:37.532732845Z`, SHA-256
  `d6d359b7b0634b8ed9a0869d501a8447a918f7047850e08cbb2f388379e3fe08`.

The process-control root is
`/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r66-h-only-mechanism-frontier-probe-20260814-recovery2-control`.
It is mode `0700`, has terminal mtime
`2026-08-15T05:35:37.679734039Z`, and retains exactly:

- `inner.pid`: mode `0600`, 7 bytes, value `655763`, mtime
  `2026-08-15T05:35:19.274584763Z`, SHA-256
  `d8464e91fd2b71e3dbbfc5e6631761028338ec24bead54645b407969c4e4779a`;
- `inner-acquired-at`: mode `0600`, 21 bytes, value
  `2026-08-15T05:35:19Z`, mtime `2026-08-15T05:35:19.276584779Z`, SHA-256
  `e1c09e130e30bd71505e000ee89d2b1434330b9cb9d3147352ef597ffc08ea86`;
- `h-network-dispatch.json`: mode `0600`, 402 bytes, mtime
  `2026-08-15T05:35:21.438602299Z`, SHA-256
  `a9735e654abf1f49108ad4d16ccd0b384b40bd5a2e2a26b55e7595b621500e13`;
- `driver.log`: mode `0600`, zero bytes, mtime
  `2026-08-15T05:35:19.263584674Z`, empty-file SHA-256
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`;
- `exit.code`: mode `0600`, 2 bytes, value `0`, mtime
  `2026-08-15T05:35:37.678734031Z`, SHA-256
  `9a271f2a916b0b6ee6cecb2426f0b3206ef074578be55d9bc94f6f3fe3ab86aa`.

The dispatch marker contains only lifecycle counters and declares no payload
or secret. The inner PID is gone. The tmux server and mode-`0600` socket at
`/tmp/scion-r66-h-only-mechanism-frontier-probe-recovery2.tmux.sock` are
retained live as audited lifecycle state; the socket mtime is
`2026-08-15T05:35:19.242584504Z`. This postrun does not delete, detach, reuse or
otherwise mutate either one.

## Durable provider and mechanical record

The result and status both emit `TERMINAL_HYPOTHESIS_OBSERVATION`. Exact call
counts and transport facts are:

```text
wrapper_attempt_count:          1
h_attempts:                     1
hypothesis_provider_calls:      1
actual_network_request_count:   1
provider_terminal_response:     true
retry_calls:                    0
latency_available:              true
latency_ms:                     16134
raw_response_available:         false
response_envelope_available:    false
usage_available:                true
mechanically_complete:          true
```

The production public client exposes parsed tool arguments and mechanical
diagnostics, not the complete provider response envelope; `raw_response` is
therefore null. The selected choice finished with `tool_calls`, selected
exactly one `generate_hypothesis` call, and provided a 1,995-byte SDK argument
string that parsed as JSON.

All six mechanical-vector fields are true:

- provider completed;
- expected tool-call count equals one;
- expected tool name matched;
- arguments are available;
- arguments are valid JSON;
- the typed hypothesis schema is valid.

Usage is descriptive transport accounting only:

```text
model:                         gpt-5.6-terra
input_tokens:                  20808
output_tokens:                 687
reasoning_output_tokens:       296
cache_creation_input_tokens:   0
cache_read_input_tokens:       0
cache_miss_input_tokens:       0
prompt_cache_hit:              false
```

## Exact H observation

The durable typed observation is:

```text
action:              modify
change_locus:        solver_design
predicted_direction: improve
suggested_weight:    null
target_file:         policies/baseline_modules/local_search.py
target_weakness:     The VNS can only change one route through 2-opt reversal;
                     after 2-opt reaches a local optimum, route-internal edge
                     patterns that require reconnecting three arcs remain
                     unreachable unless a disruptive destroy/repair move
                     happens to recreate them. This leaves inexpensive route-
                     order improvements unavailable while preserving the same
                     customer-to-route assignment.
expected_effect:     At unchanged fleet_violation, reduce final-best
                     total_distance by escaping intra-route 2-opt local minima;
                     direct accepted 3-opt deltas and final-best distance
                     provide separate mechanism and outcome evidence.
```

The exact `hypothesis_text` is:

> Add a deadline-polled, bounded intra-route 3-opt neighborhood to the VNS, placed after `_two_opt_intra`. For each route, enumerate only a bounded set of ordered cut triples and evaluate the feasible non-2-opt reconnection patterns; accept a strictly improving reconnection, recalculate that route, rebuild the index, and restart its local search as the existing neighborhoods do. The mechanism can remove three-edge configurations that no single reversal can improve, yielding lower route distance from the same capacity-feasible customer partition and supplying stronger locally optimized ALNS candidates. Because it only permutes customers within one existing route, it preserves customer coverage, route loads, capacity feasibility, and route count, hence cannot worsen `fleet_violation`; all loops must retain the existing `remaining_time() > reserve` exit semantics. Measure final feasible fleet violation and final-best total distance, alongside accepted 3-opt moves and their direct deltas. The hypothesis is falsified if bounded 3-opt produces no practically meaningful reduction in final-best total distance (or worsens it) at equal fleet violation, despite being reached often enough to execute candidate checks.

This is one prior-informed, non-blind H observation. Mechanical completeness
does not establish novelty, implementation correctness, feasibility,
executability, algorithm quality, runtime benefit, generalization or adoption.

## Post-terminal descriptive overlap audit

The primary result's `scientific_classification` remains null and is not
rewritten. A separate, read-only historical comparison finds direct mechanism-
family overlap:

- Successor5 already evaluated `bounded_intra_route_3opt` in the same target
  file. It was weak-positive but below MDE: win rate `0.375`, median `1.25`,
  CI `[0,4]`, effect/MDE `0.126263`, with outcome
  `measured_no_positive_at_mde`.
- Successor6 expanded the same branch through 48 valid pairs: `18W/22L/8T`,
  raw-pair median `0.0`, branch summary effect `-0.75`; it was abandoned as a
  quality regression.
- The earlier R3 `three_opt_intra` direction entered the active path, with
  activation, runtime and effect observed `16/16`. Screening was `0W/0L/8T`
  with median delta `-0.25`, and it was soft-abandoned with
  `SCREENING_SOFT_ABANDON_NEGATIVE_DELTA`.

The historical sources are the
[successor5 review](v04-cvrp-successor5-3opt-followup-review-20260628.md),
[successor6 review](v04-cvrp-successor6-branch-handoff-rejection-review-20260628.md)
and [R3 lifecycle analysis](v0.4-branch-lifecycle-portfolio-sonnet-4r-analysis-20260520.md).
This comparison supports only the label
`DESCRIPTIVE_DIRECT_OLD_BOUNDED_INTRA_ROUTE_3OPT_OVERLAP`. It is not a formal
novelty gate, rejection rewrite or exact code-equivalence claim. No C response
or implementation exists here, so reconnection enumeration and implementation
differences cannot be adjudicated.

The research conclusion is `NO_NOVEL_FRONTIER_EVIDENCE`: the sole H returned a
directly old bounded intra-route 3-opt family rather than a new mechanism
outside the historical frontier. This leaves the primary mechanical record
intact and provides zero algorithm-quality evidence.

## Exact downstream zero record and stop

Both terminal files report zero for every downstream action:

```text
code_provider_calls
patch_calls
materialization_calls
contract_calls
v3_calls
v4_calls
solver_calls
canary_calls
protocol_calls
quality_calls
formal_calls
decision_calls
promotion_calls
repair_calls
retry_calls
resume_calls
substitution_calls
r67_launch_calls
```

`scientific_classification`, `algorithm_quality_evidence`, Decision and
promotion remain null. Recovery2 stops terminally with
`NO_C / NO_R67 / NO_RECOVERY3`; there is no repair, retry, candidate, solver measurement,
Protocol evidence or promotion. The provider algorithm pause remains, CVRP
still has no Protocol-complete promotion, and S6 remains open.
