# v0.4 Forced Blueprint Budget Sonnet Smoke, 2026-05-07

Run artifact root:

```text
/home/clawd/research/scion-experiments/v04-forced-blueprint-budget-sonnet-20260507T133711Z
```

This document records the delegated read-only analysis summary for a one-round
forced `algorithm_blueprint` CVRP formal smoke after
`af4ab5b Bound APS observation budget artifacts`. Raw experiment artifacts were
not opened for this documentation update.

## Configuration

```text
model=claude-sonnet-4-6
rounds=1
agentic_proposal=true
disable_early_stop=true
force_surface=algorithm_blueprint
cvrp_time_limit_sec=10
```

Command intent:

```text
--rounds 1 --force-surface algorithm_blueprint --agentic-proposal --disable-early-stop --time-limit-sec 10
```

Purpose: verify whether the APS budget/recovery repair closes the loop for a
forced `algorithm_blueprint` Sonnet CVRP formal smoke.

## Run Integrity

```text
exit_code=0
total_rounds=1
n_experiments=1
n_steps=1
stop_reason=max_rounds_exhausted
champion_version=1
weight_revision=0
active_branches=0
promotions=0
frozen_budget_used=0
frozen_budget_limit=2
frozen_budget_remaining=2
formal_ready=false
final_evidence_refs=missing
```

The run completed cleanly and stopped because the configured one round was
exhausted. It produced no promotion, no validation/frozen evidence, and no
formal-ready final evidence refs.

## Forced Surface

The diagnostic forced surface worked for round 1:

```text
change_locus=algorithm_blueprint
action=modify
target_file=policies/algorithm_blueprint.py
coverage=modify/algorithm_blueprint: 1
family=algorithm_blueprint: 1
```

## APS Budget And Recovery

The APS budget/recovery fix was effective enough to unblock the path:

```text
agentic recovery artifact invalid: no matches
tool budget exceeded: observation_chars: no matches
result_too_large: no matches
partial_session_observation_chars=23502/24000
completed_session_observation_chars=23990/24000
cli_inspect_validation.ok=true
cli_inspect_errors=[]
```

Compact transcripts still contain bounded `observation_budget_exhausted` stops,
but the sessions did not fail and the completed session produced a patch. The
remaining headroom is very low, so more compaction may still be useful later,
but it is no longer the blocking issue for the next slice.

The first session produced only a partial hypothesis. The second completed
session used the same idempotency key and the same hypothesis object, and
included a patch. No explicit `resume_context` or
`recovered_partial_hypothesis` was reported; this behaved as a new APS/code
session over the approved hypothesis, not as a semantically fresh proposal.

## Gate And Protocol Results

Contract, Verification, and canary all passed.

```text
screening_valid_pairs=16/16
screening_failed_pairs=0
win_rate=0.125
median_delta=0.0
statistical_status=tie
decision=abandon
reason=SCREENING_FAIL_WIN_RATE / T4 win_rate < 0.3
```

This is fail-closed screening evidence, not infrastructure failure. The
candidate did not promote.

## Algorithm Blueprint Candidate

The generated patch enabled the blueprint path with:

```text
construction_methods=nearest_neighbor, nearest_neighbor_demand_bias, demand_descending
construction_keep_top_k=2
baseline_time_fraction=0.75
local_search=intra_route_2opt, inter_route_relocate
local_search_rounds=2
local_search_top_k=32
restart_enabled=true
restart_stagnation=8
```

The V8 tiny runtime audit showed the algorithm path was loaded and active:

```text
algorithm_blueprint_loaded=true
algorithm_blueprint_active=true
algorithm_blueprint_errors=0
phases=plan_loaded, construction_ensemble, baseline, local_search
```

Formal screening raw metrics showed indirect effects such as
`baseline_time_fraction=0.75` and construction modes alternating between
`nearest_neighbor` and `nearest_neighbor_demand_bias`. However, explicit
`algorithm_*` fields were missing from screening pair runtime metrics and the
campaign screening summary.

## Interpretation

APS budget/recovery is fixed enough to unblock. The budget headroom is low, but
the next work should not be another compactness fix or a longer run.

This remains control-path evidence, not solver-quality evidence: there was no
promotion, no validation/frozen evidence, and formal readiness stayed false.

Recommended next slice: refine `algorithm_blueprint`
audit/contract/reporting, especially preserving and reporting required
`algorithm_*` runtime fields through formal screening metrics and campaign
summaries.
