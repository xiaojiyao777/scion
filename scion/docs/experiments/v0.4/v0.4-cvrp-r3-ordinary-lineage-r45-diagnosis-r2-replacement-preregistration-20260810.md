# CVRP R3 Ordinary-Lineage R45 Diagnosis R2 Replacement — 2026-08-10

**Status:** `R2 TERMINAL — RUN_INVALID_EXPERIMENT_DRIVER / ZERO_ACCEPTED_BLOCKS / NO_ADMISSIBLE_ANALYSIS`

## Terminal classification

R2 terminated at `2026-08-10 12:15:10 UTC` with driver exit code `2`. Its first
`MDE` raw record was complete at 96/96 attempted and valid pairs, with zero
candidate, champion or total failed pairs and no recorded failure. The driver
then rejected that complete record before atomic acceptance, so experiment
status remained zero of 37 accepted blocks with `last_block=null` and no
`analysis.json`.

The failure is a one-off driver defect, not an algorithm result or a Scion
Protocol failure. CVRP recomputation legitimately returns the two declared
metrics `fleet_violation` and `total_distance` plus the diagnostic field
`routes`. All 192 A/B objective mappings contained both declared metrics and
only that one extra field, while every other side-level success, feasibility,
failure, time-limit, elapsed-time and exit-code predicate passed. The driver
incorrectly required the objective key set to equal the declared metric set.

No R2 W/L/T, effect, gate, ranking or objective value was read or used. The R2
output and control roots are sealed without resume, completion, pair reuse or
analysis. A further replacement requires a fresh root, a corrected
experiment-owned driver and a separate preregistration.

## Launch history

R2 consumed its one allowed launch at `2026-08-10 09:50:29 UTC` through the
corrected explicit-socket tmux seam. The independent startup check found driver
PID `54089`, pane `dead=0`, and the exit observation pending. The first fresh
`MDE` raw record was structurally 2/96 attempted and valid rows, with candidate,
champion and total failed counts all zero; experiment status remained zero of
37 accepted blocks with `last_block=null`.

Until a terminal boundary, monitoring reads only process health and structural
counts. It does not read or report W/L/T, effect, gate or objective values.

## PRELAUNCH_CORRECTION

The first launcher invocation failed with `File name too long` before any tmux
server, driver or output root was created. The control-root-based absolute
socket path exceeded the AF_UNIX path limit. No driver started, zero pairs ran,
and R2 did not consume its one allowed experiment launch. The invocation made
only an empty mode-0700 control directory; it was removed with `rmdir`, so at
that prelaunch boundary the R2 output and control roots remained absent and the
status remained `R2 PREREGISTERED_NOT_STARTED`.

The corrected `launch-r2.sh` freezes the explicit short socket path
`/tmp/scion-r45-r2.tmux.sock` and fails if that path already exists. This was a
prelaunch operational correction only. It changed no runtime, manifest, cohort,
block, pair, budget, ordering or analysis input.

## Replacement boundary

R1 ended
`RUN_INVALID_INFRA / ZERO_ACCEPTED_BLOCKS / NO_ADMISSIBLE_ANALYSIS` after its
process disappeared during the first atomic block. Its cause is
`PROCESS_DISAPPEARANCE_CAUSE_UNIDENTIFIABLE`. This replacement changes only the
operator-visible launch and process-observation seam. It changes no scientific
input, comparison, analysis, V3 boundary or Scion production code.

The complete scientific design remains frozen in the
[R1 registration](v0.4-cvrp-r3-ordinary-lineage-r45-diagnosis-preregistration-20260810.md):
clean `c32f5b8a` runtime, exact experiment-owned driver and manifest, exact
six-candidate ordinary-lineage cohort, 37-block order, dimension budgets,
cache-off fresh subjects, deterministic AB/BA parity, completeness checks,
matched MDE and descriptive factor/budget analysis. The sole promotion
authority remains
`Contract -> Verification -> Protocol -> Safe Features -> Decision`; this
provider-free diagnosis never enters it.

## Frozen roots and fresh start

- Prepared input bundle:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r45-diagnosis-20260810-input`
- R2 output root:
  `/home/clawd/research/scion-experiments/v04-cvrp-r3-ordinary-lineage-r45-diagnosis-20260810-r2-claw`
- R2 process-observation control root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r45-diagnosis-20260810-r2-control`

The R2 output and control roots were both absent at registration. R2 creates
each once. It starts from a fresh compose and the first `MDE` block at 0/96,
with both subjects fresh and cache activity zero. It copies, appends, patches
or reuses no R1 raw pair, workspace, progress file, status or analysis. R1 and
R2 are separate execution regimes and may never be combined.

R2 fails closed unless the immutable expectation is exactly `N=6`, 37 accepted
blocks and 1,056 unique pair rows. It runs the independent full `12x8` B0 A/A,
then all `Q00`, all `Q01`, all `Q10`, all `Q11`, all `.5x`, and all `2x`, with
candidate ordinal ascending inside each block. Budgets, AB/BA parity, full
completeness/source-mutation checks and terminal-only analysis are unchanged
from R1.

## One-launch operational seam

A normal `clawd` shell launches R2 once through `launch-r2.sh` in the prepared
input bundle. The tracked documentation does not duplicate that script's
command body. The script uses the fail-if-present explicit tmux socket
`/tmp/scion-r45-r2.tmux.sock`, configures `remain-on-exit` before respawning the
driver pane, and writes only bounded log, PID and exit observations under the
R2 control root.

Operational health has exactly three observations:

1. the declared tmux socket/session/pane remains inspectable, including a dead
   pane retained after exit;
2. the control PID identifies a live R2 driver while execution is active;
3. the control log remains readable and the control exit record is absent while
   active, then records the driver exit once it terminates.

The control log, PID and exit record are operational observations only. They
are not a gate, Decision input, receipt, identity record, ledger or self-proof.
They cannot accept a block, repair raw evidence or change an analysis result.
Scientific progress remains owned by the experiment's atomic status and raw
records.

## Stop and interpretation

Launch exactly once after the launcher, input bundle, frozen cohort, absent R2
roots and CPU exclusivity are checked. If the driver, tmux pane or host is
interrupted, seal the R2 output and control roots without resume, pair reuse or
selective completion. Do not launch an automatic R3; a further replacement
would require a new terminal classification and preregistration.

Only a complete R2 with all 37 accepted blocks and 1,056 unique rows may create
the predeclared analysis. Intermediate W/L/T, effects, objectives and gates are
not read, reported or used to change execution. No deployment, distribution,
build, root/systemd, Trust/Hash, identity, signing, lease or receipt work is
introduced.
