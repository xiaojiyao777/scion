# CVRP R61 Provider-Free Semantic Audit Postrun — 2026-08-13

**Status:** `TERMINAL_DIAGNOSTIC / SEPARATE_ENDPOINT_TRADEOFF / ZERO_ALGORITHM_QUALITY_EVIDENCE`

## Scope and R60 boundary

R61 is a new provider-free semantic audit over the two sealed R60 materialized
workspaces. It asks two narrow implementation questions that R60's mechanical
`full_executable` endpoint intentionally did not answer:

1. does a controlled ordinary worsening iteration wire elapsed-budget progress
   into simulated annealing before acceptance; and
2. does that same iteration record the chosen destroy and repair weights once
   each?

R61 does not reopen, rewrite or augment the R60 primary result. R60 remains
`BOTH / BOTH_LINE_SELECTION / NO_IDENTIFIED_PRESENTATION_ADVANTAGE`; its tool
presentation estimand, fixed OLD-then-NEW confounding and zero algorithm-quality
evidence are unchanged. R61 is not a provider experiment, solver-quality case,
candidate comparison, Protocol stage or Decision input.

[`design/scion-architecture-v3.md`](../../../design/scion-architecture-v3.md)
remains the sole architecture authority. No production source, gate, Protocol
or lifecycle behavior changed.

## Inputs, output and execution boundary

- Label: `v04-cvrp-r61-provider-free-semantic-audit-20260813`
- Input bundle:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r61-provider-free-semantic-audit-20260813-input`
- Output root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r61-provider-free-semantic-audit-20260813-output`
- Result: `result.json`
- Terminal status: `status.json`
- OLD source workspace:
  `/home/clawd/research/scion-experiments/v04-cvrp-r60-c-tool-presentation-pair-20260813-claw/evaluation/cell_alpha/materializer/workspaces/fresh-b0`
- NEW source workspace:
  `/home/clawd/research/scion-experiments/v04-cvrp-r60-c-tool-presentation-pair-20260813-claw/evaluation/cell_beta/materializer/workspaces/fresh-b0`

The declared output root contains the single terminal R61 run. The runner
refuses an already-existing output root, so this formal result was neither
resumed nor overwritten; test executions use separate temporary outputs and
are not additional formal runs.

The two sealed R60 workspaces were read-only source authorities. Each cell and
each progress coordinate used a fresh temporary copy and a fresh subprocess.
The dynamic harness supplied a fake solution, fake instance, controlled clock
and patched operators so that the real candidate scheduler executed exactly
one ordinary worsening-candidate iteration. It evaluated progress coordinates
`0.1` and `0.9` separately.

This is a post-hoc synthetic counterfactual over frozen artifacts: each arm is
asked what it does under the same controlled ordinary-worsening setup at the
two progress coordinates. It is scheduler control-flow evidence only. The fake
instance is not a CVRP model, case, benchmark or feasibility test. Static
callsite counts corroborate the dynamic observations but do not decide either
endpoint.

The exact zero-call boundary is:

```text
provider calls:                    0
canary calls:                      0
formal Protocol solver calls:      0
quality solver calls:              0
benchmark or BKS inputs:           0
production gate changes:           0
Protocol changes:                  0
```

## Frozen endpoints

`deadline_wiring_pass` requires both progress probes to call the candidate
annealing setter before `accept`, forward the requested progress and expose a
strictly lower late temperature.

`ordinary_weight_accounting_pass` requires one ordinary scheduler iteration to
record the selected destroy weight exactly once and the selected repair weight
exactly once. The endpoints remain separate. The terminal artifacts explicitly
set `aggregate_algorithm_success=null`; no host-authored aggregate winner or
algorithm pass is inferred.

## OLD: wiring pass, accounting fail

OLD `cell_alpha / control_old_42535efc` exposes `set_progress` and not
`set_deadline_progress`. Both dynamic probes called `set_progress` exactly once
before the worsening-candidate acceptance decision and forwarded their exact
requested progress:

| OLD probe | Progress | Temperature at accept | Destroy records | Repair records |
|---|---:|---:|---:|---:|
| early | 0.1 | 2.6857958838184386 | 2 | 1 |
| late | 0.9 | 0.018616455666360693 | 2 | 1 |

The late temperature is strictly below the early temperature. OLD therefore
has `deadline_wiring_pass=true`.

The ordinary path records `destroy_weights.record(d_idx, score)` twice but the
repair weight once. This 2/1 count is stable across the two progress probes and
is also visible in the static scheduler callsites. OLD therefore has
`ordinary_weight_accounting_pass=false`.

## NEW: wiring fail, accounting pass

NEW `cell_beta / treatment_new_47fe81ee` defines
`set_deadline_progress`; `set_progress` is absent. The candidate scheduler calls
neither setter. Both early and late worsening-candidate acceptance decisions
therefore observe the unchanged start temperature:

| NEW probe | Progress | Setter calls | Temperature at accept | Destroy records | Repair records |
|---|---:|---:|---:|---:|---:|
| early | 0.1 | 0 | 5.0 | 1 | 1 |
| late | 0.9 | 0 | 5.0 | 1 | 1 |

Requested progress is unobserved and the late temperature is not lower. NEW
therefore has `deadline_wiring_pass=false` even though the candidate setter API
exists.

The ordinary path records the selected destroy and repair weights once each at
both progress coordinates. NEW therefore has
`ordinary_weight_accounting_pass=true`.

## Result vector and claim boundary

| Arm | Deadline wiring | Ordinary weight accounting |
|---|---:|---:|
| OLD `42535efc` | pass | fail (`2/1`) |
| NEW `47fe81ee` | fail (`5.0 -> 5.0`) | pass (`1/1`) |

The result is a crossed implementation tradeoff, not a ranking. Neither arm
passes both independent endpoints, and `aggregate_algorithm_success` remains
undefined (`null`). R61 does not say how often either path occurs in real
search, whether weight ratios materially change, whether a temperature
trajectory improves acceptance, or whether either candidate is feasible or
better on any CVRP instance.

There is no objective, route, fleet, runtime-quality, generalization or
promotion evidence. The audit cannot enter Protocol or Decision, change B0/v1,
or reinterpret R60's matched-pair tool-presentation outcome.

## Conclusion

R61 is sealed as
`TERMINAL_DIAGNOSTIC / SEPARATE_ENDPOINT_TRADEOFF /
ZERO_ALGORITHM_QUALITY_EVIDENCE`. It converts the two R60 post-hoc source
observations into bounded dynamic scheduler facts while preserving their
non-quality status.

Provider algorithm experiments remain paused. At R60 closeout, no R61 had been
preregistered, authorized or launched; that remains a correct historical fact.
R61 was later executed solely as this independent provider-free audit. No R62,
provider call, solver experiment, long campaign or WSL run is preregistered,
authorized or launched by this report.
