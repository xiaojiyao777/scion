# CVRP R60 C-Tool-Presentation Matched-Pair Postrun — 2026-08-13

**Status:** `TERMINAL_MATCHED_PAIR_OBSERVATION / BOTH / BOTH_LINE_SELECTION / NO_IDENTIFIED_PRESENTATION_ADVANTAGE / ZERO_ALGORITHM_QUALITY_EVIDENCE`

## Scope and authority

This report closes the separately preregistered R60 C-only matched pair. R60
held the complete frozen R58 approved-H C turn constant and changed only the
complete provider-visible `PATCH_TOOL` presentation: OLD from clean
`42535efc`, then NEW from `47fe81ee`. It was not a new H, algorithm experiment,
R58 repair/retry/resume, solver-quality screen, promotion run or causal test of
R59.

[`design/scion-architecture-v3.md`](../../../design/scion-architecture-v3.md)
remains the sole architecture authority. R60 added no production behavior,
research gate, retry, repair call or Decision input. Its provider-free V3/V4
microtests checked executable consistency only; no formal CVRP case-quality
evaluation ran.

## Authorization, lifecycle and artifacts

- Frozen preregistration:
  [`v0.4-cvrp-r60-c-tool-presentation-pair-preregistration-20260813.md`](v0.4-cvrp-r60-c-tool-presentation-pair-preregistration-20260813.md)
- R58 context/control source:
  `42535efc88582fdd7ab3ece13b349d3c3085813b`
- Treatment/evaluation source:
  `47fe81ee6e17c04bc805197e2b2ad34e0fff4d14`
- Input bundle:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r60-c-tool-presentation-pair-20260813-input`
- Output root:
  `/home/clawd/research/scion-experiments/v04-cvrp-r60-c-tool-presentation-pair-20260813-claw`
- Process-control root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r60-c-tool-presentation-pair-20260813-control`
- Terminal summary: `driver_result.json` and `status.json`
- Opaque primary records: `score_records/cell_alpha.json` and
  `score_records/cell_beta.json`
- Post-score mapping: `arm_reveal.json`
- Revealed per-arm records: `cells/cell_alpha.json` and
  `cells/cell_beta.json`

The user supplied the exact R60 source-send and two-call authorization before
launch. The frozen launcher acquired its inner process at
`2026-08-13T23:21:33Z`; process-control exit is `0`. The bounded wrapper
terminalized in about `69.043` seconds. Startup PID `380490` remains only an
acquisition observation.

The durable lifecycle facts are:

- fixed provider order OLD `42535efc` then NEW `47fe81ee`;
- two Terra C calls, zero H calls and zero provider retries;
- two of two terminal provider responses were stored before scoring;
- two of two opaque primary records were written before `arm_reveal.json`;
- two of two arms were evaluated;
- production parse, source binding, Contract and materialization passed for
  both arms;
- V3 and V4 executed non-skipped and passed independently for both arms; and
- canary, formal solver/Protocol, algorithm-quality and Decision calls were all
  zero.

Thus the wrapper is a complete identifiable matched-pair observation, not an
infrastructure failure. It creates no solver candidate, branch head, champion
change or promotion.

## Preregistered primary endpoint: BOTH

The arm reveal maps `cell_alpha` to OLD and `cell_beta` to NEW. Both opaque
primary records passed every component of the frozen `full_executable` vector:

| Primary component | OLD `42535efc` | NEW `47fe81ee` |
|---|---:|---:|
| parse | pass | pass |
| source binding | pass | pass |
| Contract | pass | pass |
| materialization | pass | pass |
| V3 non-skipped | pass, 555 ms | pass, 508 ms |
| V4 non-skipped | pass, 854 ms | pass, 844 ms |
| `full_executable` | true | true |

The preregistered classification is therefore `BOTH`: no mechanical
separation on this pair. The small V3/V4 elapsed-time differences are local
test timings, not provider latency or algorithm performance, and have no
comparative interpretation.

## Descriptive line-selection endpoint: BOTH_LINE_SELECTION

After the primary records and arm reveal, the descriptive diagnostics showed:

| Diagnostic | OLD `42535efc` | NEW `47fe81ee` |
|---|---:|---:|
| Raw `exact_replace` edits | 6 | 3 |
| Raw `exact_line_replace` edits | 1 | 1 |
| Target line | unindented `annealing.cool()` | unindented `annealing.cool()` |
| `replace_all` | true | true |
| Production selector matches | 5 | 5 |
| Residual scheduler `annealing.cool()` | 0 | 0 |
| Descriptive line selection | true | true |

NEW also emitted one no-op exact replacement; production normalization dropped
it before canonical materialization. That normalization fact did not prevent
parse, source binding, Contract, materialization or executable verification.

Both arms therefore receive `BOTH_LINE_SELECTION`. In this single call, the
old R58-era tool presentation selected the same indentation-neutral expression
that it had failed to select during historical R58. The historical R58 response
was not an R60 cell and was not sent to the provider. Its contrast with the new
OLD observation demonstrates provider variability, not an extra replicate.

## Usage and latency boundary

| Provider usage | OLD `42535efc` | NEW `47fe81ee` |
|---|---:|---:|
| Input tokens | 20,167 | 21,160 |
| Output tokens | 1,849 | 1,550 |
| Reasoning output tokens | 438 | 448 |

These are descriptive usage facts for different complete tool presentations.
The artifacts do not contain a comparable per-arm provider wall-latency
measure, so no latency contrast is reported. Total wrapper duration includes
both provider calls and provider-free evaluation and cannot be partitioned
post hoc into an arm-speed result.

## Independent post-hoc source observations

The following source observations were made only after the preregistered
primary records and arm reveal. They are not primary or secondary adoption
criteria and do not change `BOTH` or `BOTH_LINE_SELECTION`.

1. OLD's normalized scheduler contains two consecutive
   `destroy_weights.record(d_idx, score)` calls. Because the corresponding
   accumulated score and usage contributions often scale together, their ratio
   may frequently cancel, but the duplicate accounting is semantically
   unclean and was not tested as an algorithm mechanism.
2. NEW defines `_SimulatedAnnealing.set_deadline_progress`, but its normalized
   scheduler has zero calls to that method. Temperature therefore remains at
   `start_temp`; the approved elapsed-budget mechanism is not activated.

These facts show why executable consistency must not be relabeled as algorithm
fidelity or quality. Neither normalized proposal proceeds to canary, formal
solver testing, branch retention, Decision or promotion. No objective, route,
feasibility or runtime-quality evidence exists for either proposal.

## Single-pair claim boundary

R60 contains one fixed OLD-then-NEW pair, one fresh provider response per arm
and no provider seed. Presentation is confounded with call order, wall-clock
time, backend/model stochasticity and any unobserved provider variation. R60
therefore supports no p-value, provider population selection rate,
reproducibility claim or average causal effect.

The R58-to-R60 sequence establishes only that both complete tool presentations
could express and, in this R60 pair, did select the five-site indentation-neutral
replacement. Because OLD also succeeded, the pair does not identify an R59
presentation advantage. It neither proves that R59 caused better selection nor
proves that R59 is harmful or unnecessary in other calls.

## Zero algorithm-quality evidence and conclusion

```text
H calls:                    0
C calls:                    2
provider retries:           0
canary calls:               0
formal solver/Protocol:     0
algorithm-quality calls:    0
Decision calls:             0
admissible solver pairs:    0
W/L/T and effect:           undefined
champion:                   B0/v1
```

R60 is sealed as
`TERMINAL_MATCHED_PAIR_OBSERVATION / BOTH / BOTH_LINE_SELECTION /
NO_IDENTIFIED_PRESENTATION_ADVANTAGE / ZERO_ALGORITHM_QUALITY_EVIDENCE`.
It closes this bounded two-call diagnostic with a stable analyzable terminal
record, but it does not validate R59 causally and does not resolve whether
either returned algorithm implementation would improve CVRP.

Keep provider algorithm experiments paused. Neither R60 proposal enters an
algorithm test or promotion path. This report does not preregister, authorize
or launch R61, a minute one-step, a long campaign or a WSL run. Any next action
requires a separately prospective decision.
