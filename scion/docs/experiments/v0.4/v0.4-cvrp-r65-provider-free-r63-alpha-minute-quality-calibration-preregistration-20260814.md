# CVRP R65 Provider-Free R63-Alpha Minute Quality Calibration Preregistration — 2026-08-14

**Preparation status:** `PREPARED_NOT_STARTED`

**Authorization state:** `AWAITING_EXPLICIT_SOLVER_EXECUTION_AUTHORIZATION`

**Design disposition:** `GO_MINUTE_CALIBRATION / NO_GO_EFFECT_INFERENCE /
NO_MORE_PROVIDER_FREE`

No provider, H, C, canary, SCREENING, validation, frozen, Decision or solver
subprocess has started under R65. The input, output and process-control roots
were absent when this document was written. Offline bundle construction and
fake/static checks may proceed, but the canary and SCREENING action remain
blocked until the exact authorization paragraph below is recorded.

## Narrow calibration question

R65 asks only:

> When the exact sealed R63 alpha implementation is compared with exact B0 in
> one fresh, cache-disabled legacy minute roster, does the implementation
> expose a descriptive quality signal, no signal, a protected-objective
> regression, or a typed execution/comparator failure?

This is an implementation-specific, exposed-coordinate, lower-bound-style
minute calibration. It is not a test of a new mechanism, a held-out or
independent experiment, an effect estimate, or evidence that the R63 message
review treatment helped or harmed quality. The R63 and R64 outcomes were
visible before this design was frozen. The cases, budgets and mechanism have
all been exposed in earlier CVRP work. Seed 73 is a new coordinate for this
exact candidate on the legacy roster, but that single coordinate does not make
the post-hoc design held out, outcome-blind or independent.

## Historical evidence and adaptive status

The stronger historical R1 evaluation of the same boundary-edge-delta 2-opt*
mechanism ran `8 cases x 4 seeds`. Stage 1 itself recorded zero champion cache
hits; most later R1 B0 observations were cached. Stage 1 reported pair
`3W/0L/29T`, gate-case `0W/0L/8T`, median improvement `0` with 95% CI `[0,0]`,
and no feasibility or fleet regression. That result failed its declared gate
and was described only as a sparse no-loss signal.

R65 does not reinterpret or pool with R1. Its much smaller, fresh-subject
minute roster is an adaptive re-expression calibration of one later exact
implementation on an already exposed coordinate. A positive descriptive R65
signal would not overturn R1 or estimate a mechanism effect; a null or negative
result would not establish that every boundary-delta implementation is
ineffective.

"Lower-bound-style" is a claim-boundary description, not a mathematical lower
bound or a promised performance ordering. Deadline-driven solver trajectories
are nonmonotone, and R1's implementation was more concise; neither extra time
nor implementation detail guarantees that R1, R65 or another expression must
dominate another.

R64 separately reproduced finite-fixture semantic equivalence for both sealed
R63 cells, after exact-candidate outcomes had already been observed during
preparation. That result is non-gating and zero algorithm-quality evidence.
It supports only the decision to calibrate one executable exact implementation;
it supplies no quality prior or independent confirmation.

## Frozen identity and roots

- Label:
  `v04-cvrp-r65-provider-free-r63-alpha-minute-quality-calibration-20260814`
- Schema:
  `scion.cvrp_r65_provider_free_r63_alpha_minute_quality_calibration.v1`
- Prepared input root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r65-provider-free-r63-alpha-minute-quality-calibration-20260814-input`
- Formal output root:
  `/home/clawd/research/scion-experiments/v04-cvrp-r65-provider-free-r63-alpha-minute-quality-calibration-20260814-claw`
- Process-control root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r65-provider-free-r63-alpha-minute-quality-calibration-20260814-control`
- Tmux socket:
  `/tmp/scion-r65-r63-alpha-minute-quality-calibration.tmux.sock`
- Tmux session: `scion-r65-r63-alpha-minute-quality-calibration`

At launch, the input root must already exist as the frozen regular,
non-symlink bundle, while the formal output root, process-control root and
socket must be absent. Preparation must bind the preregistration, manifest,
frozen values, candidate workspaces, comparator, runtime, exact case roster,
orders and limits before a solver action. A mismatch is `PREP_INVALID` with
zero solver subprocesses. The output is written once and atomically; there is
no resume, overwrite, repair or second R65 launch.

## Frozen source and candidate selection

The exact comparator is CVRP B0 at subject commit
`51f9bbd77f1e93777bbe9c401b8ba05c09a3e819`. The only candidate is the sealed
R63 `cell_alpha`, revealed as `control_original_message`, from:

- R63 label:
  `v04-cvrp-r63-c-message-whole-patch-review-pair-20260814`;
- R63 schema:
  `scion.cvrp_r63_c_message_whole_patch_review_pair.v1`;
- approved-H trace:
  `20260809T082821069862_hypothesis_c6409fa4`; and
- frozen C-input trace:
  `20260809T082849758576_code_21402fe4`.

The solver/runtime authority is the sealed clean runtime commit
`47fe81ee6e17c04bc805197e2b2ad34e0fff4d14`. It is not the B0 subject or a
quality result. Preparation ordinary-value binds the exact B0 source, the
sealed alpha workspace and normalized source, and the complete runtime before
any candidate or comparator subprocess.

The frozen SHA-256 ordinary values for the changed path
`policies/baseline_modules/local_search.py` are:

```text
exact B0:       714fd09a6160bcad55f2f6590a7d4c3a267a85588a96955731ee29e2716124e5
R63 alpha:      ffe4a757dcd24802fc7bfaa3670c5835ce9e1b16e34170433a5d23f96ceb03bd
R63 beta:       8d1513526b3f00f68b8d723a6811266d5dab528bc5a2ef80d36a7c50862efdba
```

Beta's value is descriptive authority for exclusion, not an executable R65
arm. Preparation binds the R63 reveal and terminal status, the alpha opaque
cell record to its score record, the normalized alpha changed source to its
materialized workspace, the one changed path and ten unchanged paths to their
exact expected values, and all unchanged B0 paths to the subject baseline. R64
terminal facts are bound only as descriptive provenance; R64 is neither the
candidate authority nor a quality screen.

Alpha is selected by a neutral ordinal-0 tie-break because R63 and R64 supplied
no quality ranking and the two R64 semantic endpoint vectors were equal. The
selection is not evidence that alpha is better, more reviewed, or more likely
to succeed. R63 beta is excluded from R65 and cannot be substituted after any
result or failure.

## Frozen provider-free execution

R65 makes zero provider, H or C calls and sends no source-derived material off
machine. It runs fresh local solver subprocesses sequentially, with cache
disabled and concurrency exactly one.

### Veto canary

Before formal SCREENING, B0 and alpha each run once on
`controlled/data/synthetic_controlled_canary_5.vrp` with seed `101` and a
two-second per-subject limit. This is two solver subprocesses and four declared
subject-seconds at maximum. The exact order is B0 then `cell_alpha`. B0 runs as
the comparator and alpha as the candidate; no cached subject result is
admissible. If B0 is invalid, alpha is not executed.

The canary is veto-only and supplies no improvement evidence. A B0 execution,
feasibility or result failure has comparator-invalid precedence and stops as
`RUN_INVALID_COMPARATOR`. A sound B0 with an alpha execution, feasibility or
protected-result failure stops as
`VALID_TERMINAL_CANDIDATE_CANARY_NEGATIVE`. No SCREENING subject starts after
either terminal canary outcome.

### Single four-pair SCREENING

Only if both canary subjects are valid does R65 execute this exact roster once:

| Pair ordinal | Case | Seed | Per-subject limit | Order |
|---:|---|---:|---:|---|
| 0 | `P65` (`cvrplib/P/P-n65-k10.vrp`) | 73 | 8 s | `AB` |
| 1 | `E101` (`cvrplib/E/E-n101-k14.vrp`) | 73 | 12 s | `BA` |
| 2 | `X120` (`cvrplib/X/X-n120-k6.vrp`) | 73 | 12 s | `AB` |
| 3 | `X233` (`cvrplib/X/X-n233-k16.vrp`) | 73 | 15 s | `BA` |

`A` is exact B0 and `B` is sealed alpha. Every scheduled subject receives a
fresh workspace and process. Actual order must equal `AB/BA/AB/BA`; all caches
remain disabled. The four pairs therefore contain eight SCREENING subprocesses
and at most 94 declared subject-seconds.

Objective, feasibility and fleet observations never early-stop later valid
members of the frozen roster. A valid alpha loss or protected regression on an
early case is recorded and the remaining cases continue. No case, seed, budget,
order or subject may be appended, replaced or selectively rerun.

## Frozen measurement and interpretation

For each valid pair, the ordinary minute-screen comparison records B0 and alpha
status, feasibility, fleet count, total distance, signed paired distance effect
and pair direction. The exact signed effect is
`B0 total_distance - cell_alpha total_distance`; positive means alpha is
better.
Case W/L/T, pair W/L/T, median paired effect and its ordinary frozen interval
are computed only when all eight formal subjects are valid. Failed-subject
objective values are inadmissible and never enter W/L/T, median or interval.

With one seed per case and zero equivalence band, each valid case direction is
that case's paired direction. The exact descriptive ordinary minute rule is:

```text
case win rate:             wins / 4 >= 0.60
median paired distance:    median >= 2.0
bootstrap CI lower bound:  ci_low >= 0.0
net case score:            (wins - losses) / 4 >= 0.25
case loss rate:            losses / 4 <= 0.20
protected fleet regressions: 0
```

The interval is ordinary-value bound to the clean `47fe81ee` production
`scion.protocol.stats.bootstrap_ci` implementation with paired-distance values,
`n_boot=1000`, `alpha=0.05`, deterministic seed `42`, sorted bootstrap medians,
and the exact percentile-index rule in that source. The manifest and frozen
values must bind this implementation and every listed threshold before any
solver action; no post-result recomputation choice is permitted.

The complete all-valid terminal class is
`VALID_COMPLETE_EXPOSED_COORDINATE_MINUTE_CALIBRATION`. Within that class:

- any protected fleet regression adds reason
  `SCREENING_FAIL_PROTECTED_OBJECTIVE` and cannot be called a signal;
- otherwise the raw ordinary minute gate is reported descriptively as either
  `EXPLORATORY_SCREEN_SIGNAL` or `EXPLORATORY_SCREEN_NO_SIGNAL`.

These labels do not enter the production Decision path. In particular,
`EXPLORATORY_SCREEN_SIGNAL` is not a Protocol pass, candidate admission,
validation request, promotion recommendation, effect estimate or R1
reinterpretation. The result is descriptive even if every ordinary minute
criterion is positive.

## Frozen terminal and failure policy

- A pre-action authority, source, manifest, schema, fixture, roster, budget,
  root-freshness or binding failure is `PREP_INVALID`, with zero solver
  subprocesses and no scientific aggregate.
- A valid comparator and negative candidate canary is
  `VALID_TERMINAL_CANDIDATE_CANARY_NEGATIVE`; formal SCREENING stays absent.
- Any B0 canary or formal-subject execution/feasibility/result failure takes
  precedence as `RUN_INVALID_COMPARATOR`.
- With a sound B0, an alpha formal-subject execution/feasibility/result failure
  is `VALID_TERMINAL_CANDIDATE_EXECUTION_FAILURE`.
- Detectable competing host CPU work or an invalid load envelope is
  `RUN_INVALID_HOST_LOAD`; no partial comparison becomes a quality result.
- A harness, process-control, timeout-delivery, serialization or other local
  infrastructure failure is `RUN_INVALID_INFRA`.
- Only an all-valid four-pair roster is
  `VALID_COMPLETE_EXPOSED_COORDINATE_MINUTE_CALIBRATION`.

For every incomplete, canary-terminal or failure class, pair/case W/L/T,
median and interval aggregates are `null` or undefined. Valid observations may
be retained as typed subject facts, but they do not form a partial comparison.
No failure is retried, repaired, resumed, substituted, or followed by another
seed. Candidate failure is a measured exact-implementation negative only when
the comparator and harness are sound; infrastructure and comparator failures
carry no candidate-quality conclusion.

## Exact resource and zero-action envelope

The complete R65 maximum is:

```text
canary solver subprocesses:       2
formal SCREENING subprocesses:    8
total solver subprocesses:       10
declared subject-seconds:         98
hard wall:                       300 seconds
concurrency:                       1
provider calls:                    0
H calls:                           0
C calls:                           0
Contract calls:                    0
V3 calls:                          0
V4 calls:                          0
retry/repair/resume/substitute:    0
validation calls:                  0
frozen calls:                      0
Decision calls:                    0
promotion actions:                 0
beta executions:                   0
R66 actions:                       0
```

The hard wall is an outer safety ceiling, not extra solver budget. No cache,
parallel subject, provider repair, formal Protocol continuation, Decision,
validation, frozen holdout, BKS comparison, promotion or production mutation
is reachable.

This deadline-driven calibration requires the frozen two-core host-load guard.
Before launch and throughout both canary and SCREENING, no other CPU-heavy
solver, pytest, benchmark, compilation or validation process may overlap.
Every solver child receives a minimal environment with
`OMP_NUM_THREADS=1`, `OPENBLAS_NUM_THREADS=1`, `MKL_NUM_THREADS=1`, and
`NUMEXPR_NUM_THREADS=1`; any additional thread-control variables frozen by the
input manifest must also remain at their exact prepared values. A detectable
prelaunch or in-run overlap, thread-boundary mismatch or invalid host-load
envelope is `RUN_INVALID_HOST_LOAD`, retains no partial quality aggregate, and
is never retried.

## Explicit execution-authorization boundary

R64's authorization was consumed by its single provider-free formal
publication and its terminal documents explicitly authorized no R65. A generic
"好的", "继续", or prior "可以啊，你继续吧" can authorize only read-only
design work and provider-free offline/fake/static preparation here. It does not
authorize even the two-second CVRP solver canary or any SCREENING subprocess.

R65 may launch only after the user records this exact paragraph:

> 我明确授权执行一次 `v04-cvrp-r65-provider-free-r63-alpha-minute-quality-calibration-20260814`：以 `51f9bbd77f1e93777bbe9c401b8ba05c09a3e819` 的 exact B0 为对照，只使用 sealed R63 `cell_alpha/control_original_message` 候选和 sealed `47fe81ee6e17c04bc805197e2b2ad34e0fff4d14` runtime；先串行运行 B0/alpha 的 seed-101、每个 2 秒 veto canary，通过后仅在 P65/E101/X120/X233、seed 73、每 subject 8/12/12/15 秒、AB/BA/AB/BA、fresh 且 cache disabled 下运行一次四对 SCREENING。最多 10 个 solver subprocess、98 subject-seconds、单并发、300 秒 hard wall；provider/H/C/Contract/V3/V4/retry/repair/validation/frozen/Decision/promotion/beta/R66 均为 0。失败不重试、恢复、替换或追加 seed；无论结果如何 R65 停止。此授权仅覆盖这一 R65。

That paragraph has not been recorded at document time. It authorizes exactly
one fresh R65 action under the frozen envelope and nothing else. It does not
authorize a provider send, a long campaign, validation, frozen evaluation,
Decision, promotion, repair, second R65, beta arm or R66.

## Claim boundary and mandatory stop

R65 may report the typed terminal class, complete valid raw subject facts and,
only for a complete all-valid roster, the frozen descriptive W/L/T and paired-
effect summary. It may describe an implementation-specific exposed-coordinate
minute signal or no signal. It may not claim:

- a causal or population quality effect;
- a held-out, prospective, unbiased or independent result;
- semantic correctness beyond R64's finite fixtures;
- a new mechanism or a replication of R1;
- general CVRP improvement, validation, promotion or retained B0 superiority;
- benefit, harm or ineffectiveness of the R63 review instruction; or
- evidence sufficient to resume provider experiments or close `TASK.md`.

R65 is the final provider-free action contemplated by this design. Under every
terminal outcome, including an exploratory signal, R65 stops with
`NO_GO_EFFECT_INFERENCE / NO_MORE_PROVIDER_FREE`. No R66 is preregistered,
authorized or launched by this document.
