# v0.4 Algorithm Blueprint Sonnet Smoke, 2026-05-07

Run artifact root:

```text
/home/clawd/research/scion-experiments/v04-algorithm-blueprint-sonnet-20260507T115141Z
```

This was a short 3-round control-path smoke after adding the CVRP
`algorithm_blueprint` surface. It is not solver-quality evidence: it was too
short to support efficacy claims, and it did not produce validation or frozen
evidence.

Observed behavior:

- APS could list the declared `algorithm_blueprint` surface.
- Tool traces attempted `context.read_surface {"surface":"algorithm_blueprint"}`.
- The read observation exceeded the configured session observation budget /
  result budget, so the agent did not get usable selected-surface context.
- Final choices retreated to existing `route_local` / `search_policy` surfaces
  rather than exercising `algorithm_blueprint`.

Engineering conclusion:

- The surface declaration was visible to APS, but surface-context payloads were
  too large for the v0.4 session observation budget.
- The next check should happen after compact `context.list_surfaces` /
  `context.read_surface` behavior is in place.
- Use a forced or strongly steered smoke to verify that APS can inspect and
  select `algorithm_blueprint`; do not interpret that smoke as solver-quality
  evidence unless it reaches the normal evaluation gates with adequate budget.
