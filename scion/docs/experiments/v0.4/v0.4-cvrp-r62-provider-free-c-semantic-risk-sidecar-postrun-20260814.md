# CVRP R62 Provider-Free C Semantic-Risk Sidecar Postrun — 2026-08-14

**Status:** `TERMINAL_CALIBRATION / SEPARATE_SYNTAX_RISK_SIGNALS /
ZERO_ALGORITHM_QUALITY_EVIDENCE`

## Scope

R62 is an experiment-owned, provider-free static calibration over the frozen
fresh-B0 source projection, the two sealed R60 materialized workspaces, and
ephemeral in-memory controls. It does not reopen, rewrite, repair, rank, or
augment R60 or R61. It changes no production source, prompt, tool schema,
Contract, materializer, V3/V4 check, Protocol, Decision, or gate.

R62 calibrates two independent `TRUE / FALSE / UNKNOWN` syntax signals:

1. `introduced_internal_method_has_same_name_direct_attribute_call` asks
   whether every introduced internal method name has a definition-external,
   same-name direct `receiver.method()` Attribute Call. Receiver type is not
   resolved, so `TRUE` is only a name-match signal and may be a false positive.
2. `changed_span_adjacent_identical_call_risk_absent` asks whether candidate
   changed spans contain no increased adjacent AST-identical `Expr(Call)` pair.
   It is syntax-only: a repeated pure function or unresolved callback also
   produces `FALSE`, which does not prove side effects or a defect.

The signals remain separate. `UNKNOWN` is not coerced to pass or fail, and both
`aggregate_semantic_completeness` and `aggregate_algorithm_success` are `null`.

## Durable execution

- Input bundle:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r62-provider-free-c-semantic-risk-sidecar-20260814-input`
- Output root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r62-provider-free-c-semantic-risk-sidecar-20260814-output`
- Terminal files: `result.json`, `status.json`

The declared output root contains the single formal run. The runner refuses an
existing root; an independent second launch was rejected without changing
either terminal file. Test runs used temporary roots and are not formal runs.

The source binder ordinary-value checked the frozen R58 C input's source
commit, trace ID, ordered 11 editable-source values, and the R60 runtime copy.
It then checked R60 reveal-after-opaque-score, terminal status, each cell record
against its opaque score, each cell's two normalized changed-file values against
the materialized workspace, and all nine unchanged files against fresh B0. R60
and R61 remained unchanged.

The exact zero-call/change boundary is:

```text
provider calls:                 0
canary calls:                   0
Contract calls:                 0
V3 calls:                       0
V4 calls:                       0
formal Protocol calls:          0
Decision calls:                 0
quality solver calls:           0
benchmark or BKS inputs:        0
production gate changes:        0
production source changes:      0
R60 or R61 artifact changes:    0
```

## Frozen calibration vector

| Case | Same-name direct Attribute Call | Adjacent identical call risk absent |
|---|---:|---:|
| Fresh B0 baseline | `UNKNOWN` | `UNKNOWN` |
| Sealed R60 alpha / OLD | `TRUE` | `FALSE` |
| Sealed R60 beta / NEW | `FALSE` | `TRUE` |
| Temporary cleaned alpha | `TRUE` | `TRUE` |
| Temporary cleaned beta | `TRUE` | `TRUE` |
| Legitimate branch control | `UNKNOWN` | `TRUE` |
| Callback control | `UNKNOWN` | `FALSE` |

For sealed alpha, the first signal sees direct name matches for
`_ALNSVNSSolver._usable_budget_progress` and
`_SimulatedAnnealing.set_progress`. The second signal finds the changed-span
adjacent pair at scheduler lines 313–314. For sealed beta,
`_SimulatedAnnealing.set_deadline_progress` has no same-name direct call or
indirect reference, while no changed-span adjacent identical call pair is
present.

The cleaned controls exist only in memory and are not candidate repairs. The
branch control preserves `UNKNOWN` for an indirect branch-selected method
reference. The callback control deliberately demonstrates that the syntax-only
adjacent-call signal reports `FALSE` even when callee semantics are unresolved.

## Claim boundary and next step

R62 calibrates sensitivity and known false-positive/ambiguity behavior against
post-hoc R60-informed fixtures. It does not estimate general C completeness,
provider quality, algorithm correctness, feasibility, objective/runtime quality,
or generalization. It cannot enter Protocol or Decision, change B0/v1, promote
a candidate, or reinterpret R60/R61.

The design note mentions a possible future one-shot generic C self-review
instruction, but R62 does not implement or authorize it. No prompt or tool
change, test/search feedback, provider call, retry, or repair loop ran.

Provider algorithm experiments remain paused. No R63 provider experiment,
solver experiment, long campaign, WSL run, or successor is preregistered,
authorized, or launched by this report.
