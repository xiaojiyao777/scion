# v0.4 Solver-Design Problem-Adaptation Contract Repair Sonnet 4R Diagnostic

*Date: 2026-05-12*
*Status: terminated early after validating contract repair; solver quality still blocked*

## Run

```text
run_root=/home/clawd/research/scion-experiments/v04-solver-design-problem-adaptation-contract-repair-sonnet-4r-20260512T091752Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=4
rounds_completed_before_termination=2
screened_experiments=2
time_limit_sec=30
agentic_proposal=true
agentic_session_timeout_sec=600
force_surface=none
SCION_PROBLEM_DATA_ROOT=/home/clawd/research/or-autoresearch-agent/vrp
exit_status=terminated_after_two_valid_zero_movement_screenings
finished_utc=2026-05-12T09:49:40Z
```

The first `nohup` launch did not persist and produced no exit marker. The
validation launch used `setsid -f run.sh`; it was terminated manually after two
completed screenings because both had already validated the repaired contract
path and both showed the same zero-movement quality failure.

Raw artifacts checked: `campaign/status.json`, `campaign/scion.db`, APS
`output.json` files, metrics JSON files, selected LLM traces, process status,
and the manual `exit.txt` / `finished_utc.txt` markers.

## Completion Status

```text
status.total_rounds=2
status.n_steps=2
status.n_experiments=2
status.last_result.reason=T4: win_rate < 0.3
agentic_outputs=5
completed_code_sessions=2
partial_hypothesis_only_sessions=3
completed_sessions_schema_valid=true
completed_sessions_contract_preview_passed=true
```

A third branch and hypothesis were created during shutdown, but they were not
evaluated and should be treated as a discarded partial branch.

## Results

| Order | Hypothesis | Surface | Target | Outcome |
|---:|---|---|---|---|
| 1 | `e0ded8ee-5660-4c9f-883d-3413e6042f79` | `solver_design` | `policies/main_search_strategy.py` | Contract passed; Verification passed; screening failed with `win_rate=0.0`, `median_delta=0.0`; abandoned |
| 2 | `bb83605c-84bb-4b81-ad3c-363e7c551d90` | `solver_design` | `policies/main_search_strategy.py` | Contract passed; Verification passed; screening failed with `win_rate=0.0`, `median_delta=0.0`; abandoned |

Both completed code sessions stayed on `solver_design`, targeted
`policies/main_search_strategy.py`, passed APS schema/target/Contract previews,
and did not copy `novelty_signature` into the generated
`main_search_plan()` return dictionary.

Both experiment rows recorded:

```text
contract_result=passed
verification_result=passed
decision=abandon
decision_reasons=["SCREENING_FAIL_WIN_RATE"]
screening_win_rate=0.0
screening_median_delta=0.0
```

## Runtime Interpretation

The framework repair was validated. `problem_adaptation.component_roles` can
now describe lifecycle role targets such as construction, repo-local baseline,
strict-improvement acceptance, restart, perturbation, and package-owned
main-search components. `problem_adaptation.evidence_targets` can now name
actual runtime audit fields such as
`main_search_component_accepted`,
`main_search_component_phase_improvement_counts`,
`main_search_restart_count`, and `main_search_perturbation_count`.

Both screened candidates used declared problem adaptation successfully:

```text
main_search_strategy_active=true
main_search_strategy_errors=0
main_search_problem_adaptation_source=declared
runtime_regression_rate=0.0
runtime_ratio_median=about_0.94
```

The remaining failure is solver execution semantics, not exposure or contract
plumbing. Both candidates completed all 16 screening pairs with valid
selected-surface evidence, but the main-search phase produced no phase-best
movement:

```text
main_search_objective_delta_by_phase.improvement_loop=0 across all pairs
main_search_component_phase_delta_sum=0 for selected components
main_search_component_phase_improvement_counts=0 for selected components
stop_reason=no_main_search_improvement
```

The first candidate recorded route-pair accepted current moves, but they did
not refresh phase best. `bounded_destroy_repair` remained non-beneficial in
both candidates. The second candidate reacted to the first failure with lower
baseline budget, destroy-repair-first ordering, pre-improvement perturbation,
and restarts; it still produced zero phase-best movement.

## Follow-Up

1. Keep `solver_design` as the CVRP top-level problem-object research boundary.
2. Stop spending optimization cycles on additional exposure or prompt-only
   contract fixes unless a new live failure appears.
3. Next repair should target the CVRP main-search execution path: why accepted
   current moves do not refresh phase best, why destroy/repair produces no
   phase-level benefit, and whether the package-owned primitives are strong
   enough to improve over the repo-local baseline under the screening budget.
4. Do not run long CVRP quality validation until a short diagnostic shows
   nonzero main-search phase-best movement and screening-quality movement.
