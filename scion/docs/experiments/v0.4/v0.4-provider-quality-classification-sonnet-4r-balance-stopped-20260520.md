# v0.4 Provider Quality Classification Sonnet 4R Balance-Stopped Analysis - 2026-05-20

Experiment path:
`/home/clawd/research/scion-experiments/v04-provider-quality-classification-sonnet-4r-20260520T213126Z`

This is a short stop-reason and classification audit. The run produced no
counted screening result, so this document does not evaluate algorithm quality.

## Run Summary

- Commit: `a477ab0`
- Campaign id: `fd7d7cbf-8391-4cde-8c08-f813e761a406`
- Branch id: `c76b22d5-b2d4-413a-9e78-eeafc7f0f242`
- Configured max rounds: 4
- `n_experiments`: 0
- `screened_experiments`: 0
- `proposal_attempts`: 4
- `n_steps`: 4
- `stopped_reason`: `api_balance_exhausted`
- `stop_category`: `provider_error`
- `balance_exhausted`: `true`
- `circuit_breaker_tripped`: `true`

Primary status evidence:

- `campaign/status.json`
- `campaign/campaign_summary.json`
- `run.log`
- `campaign/scion.db`

## Stop Chain

The campaign started normally and opened one explore branch. Attempt 1 reached a
provider premise check and was blocked before code. Attempts 2-4 then failed
during LLM proposal generation/tool selection because the upstream LLM provider
returned HTTP 403 insufficient balance errors.

`run.log` records repeated transient provider errors with the provider message
that the account balance is insufficient. After the third consecutive LLM
proposal-session failure, the campaign circuit breaker stopped the run:

- Attempts 2, 3, and 4 each failed after two transient API attempts.
- The final status records `provider_error.category = balance_exhausted`.
- The campaign stopped with `stopped_reason = api_balance_exhausted`.

No code generation, Contract validation, smoke test, telemetry validation, or
formal screening phase ran after the balance errors.

## Step Classification

### Attempt 1

- Session: `65fdf309-5e3b-426d-a102-798d824d6f60`
- Session status: `partial_hypothesis_only`
- Termination reason: `premise_contradicted`
- Step failure stage: `agent_quality_blocked`
- Primary failure code: `proposal_premise_contradicted`
- Contract not run reason: `proposal_only_agent_quality_blocked`
- Branch DB failure code: `PROPOSAL_PREMISE_CONTRADICTED`

This was a real CVRP provider premise block, not an API-balance failure. The
classification is consistent with the recent P0 repair: it did not persist as
`hypothesis_contract`, `FAILED_HYPOTHESIS_CONTRACT`, or branch `CONTRACT`.

The rejection also carried an exact span (`contradicted_span` /
`matched_span`) and digest/provenance fields in `campaign_summary.json`, so this
attempt is a useful sanity check of provider-quality persistence. It is not a
screening result.

### Attempt 2

- Session: `eb40912e-4fc8-4d2c-b1ed-332cbcbac108`
- Session status: `failed`
- Termination reason: `hypothesis_generation_failed`
- Session failure category: `llm_transient_api_error`
- Step failure stage: `proposal`
- Contract not run reason: `proposal_generation_failed`
- Rejection constraint: absent
- Screened experiment: `false`

The failure detail is an upstream HTTP 403 insufficient-balance error. It was
not classified as provider premise, Contract, telemetry, or activation
diagnostic.

### Attempt 3

- Session: `bdb2956b-997a-469f-9865-4c597f161e25`
- Session status: `failed`
- Termination reason: `hypothesis_generation_failed`
- Session failure category: `llm_transient_api_error`
- Step failure stage: `proposal`
- Contract not run reason: `proposal_generation_failed`
- Rejection constraint: absent
- Screened experiment: `false`

This repeats the same upstream HTTP 403 insufficient-balance failure. No
provider-quality or telemetry gate was involved.

### Attempt 4

- Session: `309dd683-76bb-4a1d-bae2-3d5b88ae4727`
- Session status: `failed`
- Termination reason: `hypothesis_generation_failed`
- Session failure category: `llm_transient_api_error`
- Step failure stage: `proposal`
- Contract not run reason: `proposal_generation_failed`
- Rejection constraint: absent
- Screened experiment: `false`

This was the third consecutive LLM provider failure. The circuit breaker then
stopped the campaign with `api_balance_exhausted`.

## DB and History Check

The SQLite branch table contains one active explore branch:

- `state = explore`
- `failure_codes = ["PROPOSAL_PREMISE_CONTRADICTED"]`
- `infra_block_count = 0`
- `consecutive_llm_retries = 0`

The branch failure history was not polluted with `CONTRACT`, `TELEMETRY`, or an
infra block for the API-balance failures. The only branch failure code came from
Attempt 1's real provider-quality premise block.

The `experiment_events` table contains `agentic_proposal_session` and
`proposal_fail` events for the balance failures. The `proposal_fail` records use
stage `proposal` and include the upstream 403 insufficient-balance detail. They
do not claim a provider premise rejection, Contract failure, telemetry failure,
or screened experiment.

## Classification Assessment

Pass at campaign/status level:

- `status.json` and `campaign_summary.json` correctly classify the run-level
  stop as `provider_error` with `api_balance_exhausted`.
- `run.log` clearly attributes the stop to repeated upstream 403 insufficient
  balance errors and the circuit breaker.
- Counted screening remained zero, so no solver-quality conclusion was recorded.

Pass for provider-quality persistence:

- The real Attempt 1 provider block persisted as
  `agent_quality_blocked:proposal_premise_contradicted`.
- No API-balance attempt was converted into a provider premise contradiction,
  activation diagnostic, Contract failure, or telemetry failure.
- Branch DB failure codes do not contain `CONTRACT` for this run.

Residual observability issue:

- The per-session `agentic_proposal_session` audit payload for the 403 failures
  still records a nested `primary_failure.category` of
  `contract_boundary_failure` with reason `schema_or_target_preview_failed`.
  That label appears to come from the APS self-check/preview path wrapping the
  failed LLM call.
- This did not affect campaign stop classification, StepRecord
  `contract_not_run_reason`, branch failure codes, or screening counts, but it
  is mildly misleading for raw event consumers. A future cleanup should map LLM
  provider-balance failures in session audit payloads to a provider/API category
  rather than a contract-boundary category.

## P0 Validation Implication

This run cannot validate the P0 provider-quality classification repair beyond
the one pre-balance sanity check in Attempt 1. There were no code candidates,
smoke checks, telemetry diagnostics, formal screenings, or branch-continuation
decisions.

After the API balance is replenished, rerun the intended 4R validation. The next
run should specifically verify:

- provider premise blocks stay agent-quality in StepRecord, DB, branch history,
  and planner-facing history;
- API/provider infrastructure errors remain distinct from Scion proposal quality
  and problem-provider quality;
- activation diagnostics enter compact negative memory without becoming ordinary
  screened failures;
- at least one counted screening occurs so branch lifecycle can be evaluated.
