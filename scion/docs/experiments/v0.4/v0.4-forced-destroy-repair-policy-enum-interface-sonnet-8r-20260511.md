# v0.4 Forced Destroy Repair Policy Enum Interface Sonnet 8R Diagnostic

*Date: 2026-05-11*
*Status: analyzed; move next diagnostic to route_pair_candidate_policy*

## Run

```text
run_root=/home/clawd/research/scion-experiments/v04-forced-destroy-repair-policy-enum-interface-sonnet-8r-20260511T114551Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=8
rounds_completed=8
time_limit_sec=20
agentic_proposal=true
agentic_session_timeout_sec=480
force_surface=destroy_repair_policy
force_action=modify
force_target_file=policies/destroy_repair_policy.py
SCION_PROBLEM_DATA_ROOT=/home/clawd/research/or-autoresearch-agent/vrp
launcher=nohup+setsid
scion_commit_at_launch=b8da8a6d0023909957a85db91da6eda225535f38
worktree_dirty_at_launch=false
```

Raw artifacts were checked only for bounded experiment analysis:
`exit.txt`, `run.log`, `status.json`, `campaign_summary.json`,
`agentic_session_index.json`, compact APS transcripts, LLM trace string
counts, the seven screening metrics files referenced by
`campaign_summary.json`, and compact completed proposal artifacts.

## Completion Status

```text
exit_code=0
stopped_reason=max_rounds_exhausted
total_rounds=8
steps=8
screened_experiments=7
champion_version=1
champion_weight_revision=0
frozen_budget_used=0/2
circuit_breaker_tripped=false
active_branches=0
```

The run completed normally. The enum-interface repair was effective: no
candidate failed Verification with invalid `destroy_repair_policy` runtime
evidence.

## Summary

What worked:

- Forced-surface control stayed healthy. All 8 hypotheses targeted
  `modify/destroy_repair_policy` and `policies/destroy_repair_policy.py`.
- All 8 hypothesis traces contained the forced task line for
  `destroy_repair_policy`, and 0 contained the old generic "Choose a research
  surface from ..." line.
- `proposal.contract_preview` passed for all 7 completed code sessions.
- Verification passed for all 7 completed code candidates.
- The prior invalid-selector failure mode disappeared:
  `verification_failure_breakdown={}`.
- Seven candidates reached screening, compared with two in the previous
  selector-semantics rerun.

What did not work:

- All 7 screened candidates failed `SCREENING_FAIL_WIN_RATE`.
- Every screened candidate had `win_rate=0.125` and `median_delta=0.0`.
- One round failed at hypothesis Contract with `C10_novelty` because the
  proposed structured novelty signature omitted required
  `destroy_repair_pattern` and `budget_pattern` fields.
- Destroy/repair mechanism movement remained exactly zero despite valid,
  varied policies.

## Deterministic Results

| Round | Surface/action/target | Gate outcome |
|---:|---|---|
| 1 | `destroy_repair_policy` / `modify` / `policies/destroy_repair_policy.py` | Screening failed `SCREENING_FAIL_WIN_RATE`; 4 wins / 2 losses / 10 ties |
| 2 | same | Screening failed `SCREENING_FAIL_WIN_RATE`; 4 wins / 1 loss / 11 ties |
| 3 | same | Screening failed `SCREENING_FAIL_WIN_RATE`; 4 wins / 2 losses / 10 ties |
| 4 | same | Hypothesis Contract failed `C10_novelty` |
| 5 | same | Screening failed `SCREENING_FAIL_WIN_RATE`; 4 wins / 1 loss / 11 ties |
| 6 | same | Screening failed `SCREENING_FAIL_WIN_RATE`; 4 wins / 2 losses / 10 ties |
| 7 | same | Screening failed `SCREENING_FAIL_WIN_RATE`; 4 wins / 1 loss / 11 ties |
| 8 | same | Screening failed `SCREENING_FAIL_WIN_RATE`; 4 wins / 1 loss / 11 ties |

Screening summary:

```text
screened_rounds=7
win_rate=0.125 for all screened rounds
median_delta=0.0 for all screened rounds
runtime_ratio_median range=0.9992053525288107..1.0006430521357996
runtime_regression_rate range=0.3125..0.6875
```

The per-case wins/losses are not attributable to the selected mechanism:
selected-surface runtime records zero accepted destroy/repair moves and zero
phase delta for every screened pair.

## APS Control Plane

```text
APS sessions: 15 total
  partial_hypothesis_only: 8
  completed: 7
LLM traces:
  hypothesis: 8
  code: 7
  tool_selection: 48
proposal.contract_preview:
  passed: 7/7 completed code sessions
result_too_large: 0 observed in traces
observation_budget_exhausted: 0 observed in traces
```

The prior enum confusion is closed. The remaining proposal issue is small: one
hypothesis failed C10 novelty because it did not provide the required
structured novelty identity fields for this surface.

## Runtime Interpretation

Across screened rounds:

```text
runtime_observed_pairs=112
destroy_repair_attempts=7168
repair_budget_used=7168
destroy_repair_accepted_current=0
destroy_repair_accepted_recovery_only=0
destroy_repair_accepted_phase_best=0
destroy_repair_phase_delta_sum=0.0
repair_budget_exhausted=46/112 pairs
repair_produced_no_improvement=66/112 pairs
removed_customers values=2,3,4,5,6
reinserted_customers values=2,3,4,6
```

The candidates varied the advertised levers:

```text
destroy selectors:
  ["route_diverse_worst"]
  ["route_diverse_worst","worst_removal"]
  ["worst_removal"]
  ["worst_removal","route_diverse_worst"]
repair selectors:
  ["regret_2"]
  ["cheapest"]
budget patterns:
  max_destroy=2..10
  repair_budget_per_customer=6..16
```

This is enough coverage to treat the forced `destroy_repair_policy` diagnostic
as exhausted for the current solver implementation. `cheapest` can reinsert
more customers and avoid budget exhaustion, but those repaired candidates still
produce no strictly improving route set. `regret_2` often exhausts budget on
larger destroy sets. Neither path produced accepted current, recovery-only, or
phase-best movement.

## Interpretation

The enum-interface repair is validated. The active blocker is no longer
control-plane routing, selector implementation, or selector value clarity. The
remaining blocker is that the package-owned bounded destroy/repair mechanism
does not produce useful post-baseline improvement on this formal screening
slice.

Do not spend another forced run on `destroy_repair_policy` without changing the
solver-owned mechanism itself. The next short diagnostic should force
`route_pair_candidate_policy`, because it controls a different route-editing
mechanism and has not yet received this level of targeted validation.
