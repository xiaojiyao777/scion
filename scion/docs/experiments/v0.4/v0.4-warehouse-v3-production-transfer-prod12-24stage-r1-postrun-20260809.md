# Warehouse V3 Production Transfer prod-1.2 24-Stage R1 Postrun — 2026-08-09

**Classification:** `PRODUCTION_PROMOTION_OBSERVED_REPLAY_PENDING`

## Terminal account

The preregistered campaign completed in
`/home/clawd/research/scion-experiments/v04-warehouse-v3-production-transfer-prod12-24stage-r1-gpt56terra-20260809T044055Z-claw/campaign`.
The wrapper exited 0, the run is `valid / complete`, and all 24 requested
typed Protocol stages completed. The source campaign used executable commit
`309a65b62ca5cfc2858393350ae903cc3bc835dd`, the local Codex proxy and
`gpt-5.6-terra`.

- formal stages: 22 screening, one validation and one frozen;
- formal execution: 299 attempted pairs, 297 valid, two candidate failures
  belonging to one later abandoned beam candidate, and zero champion failures;
- proposal path: 23 H and 23 C calls, all with terminal provider responses;
- execution outcomes: 24 evaluated, one candidate-local research rejection,
  zero not-evaluated, blocked-infra, resource or interruption outcomes;
- final champion: v2 after one exact promotion.

The one non-formal rejection was a SplitVehicle implementation that raised
`OverflowError: cannot convert float infinity to integer` during V5. It was
correctly discarded and a fresh H was scheduled. It does not invalidate the
formal evidence.

## Promoted mechanism

The first H replaced DestroyRebuild's shuffled first-fit rebuild with a
subcategory-aware best-fit rebuild. The exact candidate traversed the full
funnel without another H/C call:

| Stage | Cases W/L/T | Pairs W/L/T | `total_cost` median and CI | Verdict |
|---|---:|---:|---:|---|
| screening | 4/0/2 | 9/1/2 | `+500`, `[-50, 3300]` | `SCREENING_PASS` |
| validation | 5/0/0 | 15/0/0 | `+11500`, `[4200, 18900]` | `VALIDATION_PASS` |
| frozen | 4/0/0 | 12/0/0 | `+17000`, `[6500, 29600]` | `FROZEN_PASS` |

The v2 and v1 snapshots differ only in `operators/destroy_rebuild.py`, so the
promotion has clean mechanism-level source attribution. This establishes a
real in-campaign production-style algorithm improvement. Retention remained
pending until the separately preregistered replay.

## Research after v2

The agent continued for 21 screening candidates after promotion, spanning
DestroyRebuild, MergeVehicles, SwapOrders, MoveOrder, ChangeVehicleType,
SplitVehicle and a newly created EjectionChain. None passed screening. Across
these candidates the case account was 6/43/81 and the pair account was
49/92/119; 13 of 21 candidates had zero median effect.

This is valid negative research, but it does not isolate the absence of a
second improving mechanism:

1. The then-current scheduler repeatedly selected the oldest EXPLORE branch.
   After one branch was abandoned, branch `c16a7fec...` received stages 11–24
   while two schedulable siblings received only one screening each.
2. Fourteen later candidates on that branch were cumulative and explicitly
   recorded `incremental_effect_isolated=false`; an early 0/3/3 MoveOrder
   result therefore remained in all later source ancestry.
3. Two independently authored MergeVehicles candidates each produced sparse
   no-loss evidence: 2/0/4 cases, 6/0/6 pairs, median `+650`, CI low `0`.
   The old initial-screening rule failed them immediately at case win rate
   1/3, so the predeclared expanded population was never measured.
4. Eighteen of 21 post-v2 candidates showed runtime-budget saturation. Large
   cases carried most of the losses, while every recorded `size_bucket` was
   `unknown` and phase-level runtime attribution was absent.

These observations support a search-organization and measurement-escalation
limitation; they do not prove that any rejected candidate would have become a
second promotion. They also do not justify a negative-result rollback gate.

## Provider-context audit

All 46 provider traces were parseable and used `gpt-5.6-terra`. H and C each
made exactly one structured provider call; there was no hidden critique or
tool loop. The final H input was dominated by current source and structured
research history, not framework rules: its instructions were about 0.3% of
rendered characters, and an independent sentence/line audit found no
Trust/Hash, gate or provider mechanics in the agent's algorithm reasoning or
candidate source.

The fixed semantic history projection therefore worked as intended. The
remaining context defect was narrow: a Verification traceback was reduced to
`[ENV] Traceback...`, hiding the actionable exception and candidate-source
location from the next H.

## Prospective consequences

Commit `8909f635` makes three lightweight, result-independent corrections for
future campaigns:

- EXPLORE siblings rotate by persisted least-recently-served time while
  validation/frozen state priority and FIFO remain unchanged;
- a sparse practical initial signal with wins, no losses and no candidate
  execution failures receives one predeclared screening expansion, but does
  not pass screening without the original threshold;
- Python Verification failures expose one typed exception and relative
  candidate-source location instead of an unhelpful traceback header.

No deployment, package, root, Trust/Hash, identity, lease, new algorithm
quality gate, parameter tuning or result-derived population was added.

## Claim boundary

This campaign proves one production-style Warehouse promotion and meaningful
algorithm exploration after it. It does not prove continuous production
multi-promotion, and it does not prove CVRP improvement. Production retention
is decided only by the independent held-out replay.
