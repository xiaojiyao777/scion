# CVRP V3 Quality-Screen R3 Reboot Postrun — 2026-08-10

**Classification:** `RUN_INVALID_INFRA / VALID_PARTIAL_SCIENCE`

## Terminal boundary

R3 did not reach a campaign-level Protocol terminal state. The host boot time is
`2026-08-10 05:44:07 UTC`; the last campaign status update is
`2026-08-10 05:43:44 UTC`, immediately before that reboot. The wrapper record
therefore remains stale at `running`, with no end time, exit status or signal.
This is an external infrastructure interruption, not a Scion Decision.

At the final durable checkpoint:

- formal validation had attempted 52 of 96 pairs and completed 51;
- the partial metric contained 51 valid pairs, with zero candidate, champion
  or total execution failures;
- the durable Protocol record contained 10 screening stages, zero completed
  validation stages and zero frozen stages;
- champion remained the original B0/v1.

The partial validation is deliberately not adjudicated. Its win/loss/tie,
deltas and gate outcome are excluded from this postrun and later candidate
selection, none is reported here, and the partial stage cannot be combined with
a later execution regime.

While extracting the allowed progress counters, one documentation worker
accidentally rendered the complete in-flight status object. No partial effect
was propagated or used. That worker is permanently excluded from subsequent
cohort selection and factor adjudication, and the partial validation remains
excluded from every scientific conclusion.

## Science retained

All evidence that became terminal before the reboot remains valid partial
science. In particular, the exact route-removal candidate completed its
predeclared 12-case by 8-seed quality expansion with 96/96 valid pairs, zero
execution or fleet failure, 7W/0L/5T cases, median distance improvement
`+3.5 [0, 6.25]`, and the matching
`SCREENING_PASS -> queue_validate` route. The earlier chronological H/C and
completed screening evidence also remains available for result-bounded
research analysis.

The reboot does not show that the candidate was good or bad on validation. It
shows only that the planned comparison was not completed. There is therefore
no Protocol-complete promotion, no promoted snapshot pair and no candidate
eligible for the R3 clean promotion-recovery replay.

## Disposition and next work

This campaign root is sealed. It will not be resumed, retried or modified. A
resume would mix pre- and post-reboot machine conditions; a retry would violate
the one-launch R3 design and still would not create the missing promotion
lineage.

The next provider-free work is:

1. add the default-off R45 fresh-subject, counterbalanced AB/BA and minimal raw
   comparison seam with focused fake-runner tests;
2. freeze the chronological ordinary-lineage cohort from exactly
   reconstructable Verification-passed R3 candidates;
3. run the predeclared case-by-seed factor and budget diagnosis, reporting any
   missing source, base, order or measurement fact as `UNIDENTIFIABLE`;
4. use that diagnosis to design a fresh promotion campaign only if the evidence
   warrants one.

No provider call, formal-candidate recorder, gate, Trust/Hash layer, deployment
or packaging work is introduced by this disposition.
