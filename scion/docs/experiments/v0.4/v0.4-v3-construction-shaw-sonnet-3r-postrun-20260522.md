# v0.4 v3 construction/Shaw Sonnet 3R postrun 分析 - 2026-05-22

实验路径：
`/home/clawd/research/scion-experiments/v04-v3-construction-shaw-sonnet-3r-20260522T150933Z`

代码仓库：
`/home/clawd/research/or-autoresearch-agent`

验证 commit：
`a8143f2`

本报告只读取实验产物并新增分析文档，不修改 Scion 源码。依据包括
`command.txt`、`exit.txt`、`launch.env`、`run.log`、`campaign/status.json`、
`campaign/campaign_summary.json`、`campaign/scion.db`、`campaign/metrics/*.json`、
`campaign/llm_traces/*.json`、`campaign/agentic_sessions/*/{output,transcript}.json`、
prompt manifest，以及 workspace 中的 `active_solver_facts.py`。

## 0. 执行摘要

实验正常返回，`exit.txt` 为 `EXIT_CODE:0`。命令请求 `--rounds 3`，最终完成
3 个 effective screening；另有 3 个 proposal-quality block。停止原因是
`max_rounds_exhausted`，不是 proposal-quality loop、budget 或 circuit breaker。没有 champion
晋升，最终仍为 `champion_version=1`、`champion_weight_revision=0`，所有 branch
最终都被清理为 `abandoned`。

上一轮修复的 construction/Shaw false positive 在本 run 没有复现。全 run 没有
`construction_seed_strategy` 或 `shaw_related_removal` structured rejection。尤其 Step 4 的
`geographic_cluster_removal` 明确提到 Shaw 并试图区分“grid spatial cluster”与 `_shaw_removal`，
gate 没再把它硬判为 Shaw/proximity-cluster missing；construction 相关 active facts 也被正常
包含，未触发 nearest-neighbor-only 误杀。

剩余主要问题是 random-removal premise matcher 仍过宽。Step 4 hypothesis 承认已有
`random`/`shaw`/`worst`/`route` destroy，只说缺少 spatial patch/grid cluster removal；但
MechanismNoveltyGate 抓取 `"random removal is purely uniform. No..."`，判成
“random customer-removal destroy is missing”。这是不合理 qblock，属于 CVRP provider
premise matcher 误伤，不是 agent 没看到事实。

其他两类拦截基本合理。`regret_insertion_repair` block 拦下了一个对 regret repair 语义有误的
proposal，但失败文案说“claims regret insertion repair is missing”不够准确；更准确应是
“mischaracterizes existing regret repair semantics”。`alns_post_repair_2opt` code session 连续
3 次未产生 `solver_algorithm_context_records.alns_post_repair_2opt_iterations` activation，
`proposal.algorithm_smoke` 拦截合理。

最后一个 effective screening `noise_greedy_repair` 的 `CONTINUE_EXPLORE` 合理：case gate
仅 1/0/7，pair 4/2/10，median delta 0.0，runtime regression rate 0.5625。它有弱正向信号，
但远不够 promotion；campaign 随后因已完成 3 个 requested effective rounds 正常停止。

## 1. 总体状态

启动配置：

| 项 | 值 |
|---|---|
| command | `/home/clawd/miniconda3/envs/claw/bin/python -m scion.cli.main run --problem scion/problems/cvrp/problem.yaml --protocol scion/problems/cvrp/formal/protocol.yaml --split scion/problems/cvrp/formal/split_manifest.yaml --seeds scion/problems/cvrp/formal/seed_ledger.yaml --campaign-dir .../campaign --rounds 3 --time-limit-sec 10 --disable-early-stop --agentic-proposal` |
| model | `claude-sonnet-4-6` |
| git commit | `a8143f2` |
| data root | `/home/clawd/research/or-autoresearch-agent/vrp` |
| requested rounds | 3 |
| time limit | 10s |
| agentic proposal | enabled |
| early stop | disabled |

`status.json` / `campaign_summary.json` 核心状态：

| 指标 | 值 | 判断 |
|---|---:|---|
| `total_rounds` / `n_steps` | 6 | 6 个 campaign step / proposal attempt |
| `proposal_attempts` | 6 | 与 steps 一致 |
| `screened_experiments` / `n_experiments` | 3 | 3 个 effective screening |
| `campaign_loop.effective_rounds_completed` | 3 / 3 | 达到 requested rounds |
| `proposal_quality_blocks_consumed` | 3 / 6 | 未耗尽，remaining=3 |
| `stopped_reason` | `max_rounds_exhausted` | 正常完成请求轮次 |
| `telemetry_failed_experiments` | 1 | Step 1 effect telemetry failure |
| `telemetry_failed_experiments_by_category` | `effect: 1` | 不是 activation/runtime/import failure |
| `verification_failure_breakdown` | `{}` | verification 无失败 |
| `balance_exhausted` | false | balance 未触发 |
| `circuit_breaker_tripped` | false | 无 circuit breaker |
| `budget_utilization` | 0.003 | 预算未耗尽 |
| `frozen_budget` | used 0 / limit 2 | 未进入 frozen |
| `formal_readiness.status` | `non_formal_final_evidence_closed` | 正常 campaign，没有 formal final package |

DB 核心事实：

| 表 | 观察 |
|---|---|
| `hypotheses` | 5 条，机制为 `sa_adaptive_reheat`、`vns_neighbor_list_insertion`、`geographic_cluster_removal`、`alns_post_repair_2opt`、`noise_greedy_repair` |
| `experiment_events` | 3 条 `experiment`，3 条 `decision`，10 条 `agentic_proposal_session` |
| `branches` | 4 个 branch，最终全为 `abandoned`；`07ea...` 额外带 `ALGORITHM_SMOKE_FAILURE`，`3eb...` / `76ba...` 带 `PROPOSAL_PREMISE_CONTRADICTED` |

## 2. Campaign Step 时间线

| Step | Branch | Agentic session(s) | 机制 / target | Contract / smoke | Screening / decision | 判断 |
|---:|---|---|---|---|---|---|
| 1 | `a1642e0f...` | `cec515df...` hypothesis, `28474e00...` code | `sa_adaptive_reheat` / `acceptance.py` | hypothesis semantic + schema retry 后 pass；code 两次 smoke repair 后 pass；formal contract/verification/canary pass | pair 1/0/15，case 0/0/8；telemetry effect zero；`abandon` | 合理 abandon |
| 2 | `07ea1ad6...` | `ad6129a3...` hypothesis, `f11d5cd2...` code | `vns_neighbor_list_insertion` / `local_search.py` | hypothesis schema retry 后 pass；code 一次 smoke repair 后 pass；formal contract/verification/canary pass | pair 4/2/10，case 1/1/6；`continue_explore` | 弱混合信号，继续探索合理 |
| 3 | `3eb22b58...` | `75fc88eb...` hypothesis only | `cluster_removal` / `destroy_repair.py` | novelty gate block，无 code | 无 screening | 拦截方向可理解，但文案/分类不准 |
| 4 | `76ba5e17...` | `72391f91...` hypothesis only | `geographic_cluster_removal` / `destroy_repair.py` | semantic retry + schema retry 后被 novelty gate block，无 code | 无 screening | construction/Shaw 修复有效；random-removal matcher 误伤 |
| 5 | `07ea1ad6...` | `79bbe034...` hypothesis, `48153583...` code | `alns_post_repair_2opt` / `scheduler.py` | hypothesis schema + semantic retry 后 pass；code 3 次 smoke 均缺 activation，failed closed | 无 screening | 合理 qblock |
| 6 | `3eb22b58...` | `6529bb80...` hypothesis, `651a54db...` code | `noise_greedy_repair` / `destroy_repair.py` + `scheduler.py` | hypothesis schema retry 后 pass；code 一次 smoke repair 后 pass；formal contract/verification/canary pass | pair 4/2/10，case 1/0/7；`continue_explore` | 弱正向，继续探索合理；随后 max rounds 停止 |

## 3. 逐 Step / LLM 调用细节

### Step 1 - `sa_adaptive_reheat`

Sessions：

- Hypothesis：`campaign/agentic_sessions/cec515df-053e-4e88-8808-cd32b57c214f`
- Code：`campaign/agentic_sessions/28474e00-3932-4162-a53e-3166d70d5cd9`

Hypothesis 阶段：

| 调用 | trace | 结果 |
|---|---|---|
| tool-selection x8 | `20260522T150934...` 到 `20260522T150957...` | required preface 读取 surfaces/problem/algorithm files/active solver design/call graph；随后读 memory、screening feedback、`local_search.py`、`destroy_repair.py`、`acceptance.py`、`config.py`、`baseline_algorithm.py` |
| hypothesis #1 | `20260522T150959867229_hypothesis_3f2c755261_2ca0fb31.json` | 初稿被 mechanism novelty gate semantic retry |
| hypothesis #2 | `20260522T151028239933_hypothesis_518dfe2e56_d01dcb50.json` | schema preview 指出 activation 使用 aggregate/activity fields |
| hypothesis #3 | `20260522T151055557717_hypothesis_6aaf154e7b_ec9bce5d.json` | 修正 telemetry，schema/target permission pass，等待 code |

Code 阶段：

| 调用 | trace | 结果 |
|---|---|---|
| tool-selection x9 | `20260522T151128...` 到 `20260522T151152...` | 复用已读文件，读取 `context.read_surface`、`context.read_branch_state` |
| code #1 | `20260522T151154751236_code_8b13ba62f5_b73e6d87.json` | contract preview pass；algorithm smoke 要求 `context.record_move('sa_adaptive_reheat', ..., delta=..., best_improved=...)` |
| code #2 | `20260522T151241960331_code_52984c8203_fc55449b.json` | contract preview pass；smoke 仍缺 `solver_algorithm_phase_runtime_ms.sa_adaptive_reheat` activation |
| code #3 | `20260522T151336373473_code_003de00d19_c2601809.json` | contract preview pass；algorithm smoke pass |

Formal gate / screening / decision：

| 项 | 结果 |
|---|---|
| contract / verification / canary | `passed` / `passed` / `passed` |
| verification checks | V1 syntax 到 V9 perf guard 全 pass；V8 adapter canonical signature equal；perf ratio 1.00x |
| raw metrics | `metrics/420f08ca-35a3-4562-bf1f-d0ba4775cc28.json` |
| pair pattern | 1 win / 0 loss / 15 tie；唯一 win 为 `B-n31-k5.vrp` seed 29，delta +3 |
| case gate | 0 win / 0 loss / 8 tie |
| runtime | median ratio 1.0005，delta +4.5 ms，regression rate 0.5625 |
| telemetry guard | activation/runtime observed；effect zero |
| decision | `abandon` |
| reason | `SCREENING_TELEMETRY_FAILED` |

判断：完整链路闭合。算法产生了可观测 activation/runtime，但 declared effect 没有正值；
case-level gate 也没有任何 decisive win，因此 abandon 合理。telemetry detail 仍把
present-but-zero 和 missing 混在 `missing_fields`/`candidate_missing` 表达里，建议后续降噪，
但不影响本次决策方向。

### Step 2 - `vns_neighbor_list_insertion`

Sessions：

- Hypothesis：`campaign/agentic_sessions/ad6129a3-9b3a-4b5a-b302-22bfff1e2413`
- Code：`campaign/agentic_sessions/f11d5cd2-e55d-4915-b1da-484f74be450a`

Hypothesis 阶段：

| 调用 | trace | 结果 |
|---|---|---|
| tool-selection x8 | `20260522T152258...` 到 `20260522T152319...` | 读取 required preface、memory、screening feedback、`local_search.py`、`destroy_repair.py`、`acceptance.py`、`config.py`、`construction.py`、runtime feedback |
| hypothesis #1 | `20260522T152322681349_hypothesis_0b207f1214_f3c28bf7.json` | schema preview 指出 activation 引用 aggregate/effect fields |
| hypothesis #2 | `20260522T152352284955_hypothesis_a9f50ed9d6_51e91a92.json` | 修正为 mechanism-specific fields，schema/target pass |

Code 阶段：

| 调用 | trace | 结果 |
|---|---|---|
| tool-selection x10 | `20260522T152423...` 到 `20260522T152448...` | 读取 code phase surface/branch state，工具路径正常 |
| code #1 | `20260522T152451379216_code_38949929ee_680bc805.json` | contract preview pass；algorithm smoke 要求 effect telemetry `record_move` |
| code #2 | `20260522T152539225913_code_cdff77d0e0_e5c79067.json` | contract preview pass；algorithm smoke pass |

Formal gate / screening / decision：

| 项 | 结果 |
|---|---|
| contract / verification / canary | `passed` / `passed` / `passed` |
| raw metrics | `metrics/913dfd6a-2321-40e8-ba3e-3614b26819ea.json` |
| pair pattern | 4 win / 2 loss / 10 tie |
| case gate | 1 win / 1 loss / 6 tie |
| wins | `B-n31` seed 29 +3，`E-n101` seeds 11/29 +5/+28，`P-n101` seed 11 +16 |
| losses | `B-n52` seeds 11/29 -2/-2 |
| runtime | median ratio 0.9996，delta -3.5 ms，regression rate 0.4375 |
| telemetry guard | activation/runtime/effect all positive |
| decision | `continue_explore` |
| reason | `SCREENING_FAIL_WIN_RATE`, `SCREENING_WEAK_SIGNAL_CONTINUE` |

判断：继续探索合理。信号不是 neutral，确实有 positive mechanism evidence 和若干 pair wins；
但 case-level 有 1 win 和 1 loss，median delta 仍为 0.0，不能晋升。

### Step 3 - `cluster_removal` / regret-insertion qblock

Session：
`campaign/agentic_sessions/75fc88eb-6c52-418d-b270-16bb6f4b00f8`

Hypothesis / tool 链路：

| 调用 | trace | 结果 |
|---|---|---|
| tool-selection x8 | `20260522T153459...` 到 `20260522T153521...` | required preface、memory、screening feedback、runtime feedback、`local_search.py`、`destroy_repair.py`、`acceptance.py`、`baseline_algorithm.py`、`config.py` |
| hypothesis #1 | `20260522T153524252956_hypothesis_3711f1c903_3fc82826.json` | novelty gate semantic retry |
| hypothesis #2 | `20260522T153552378860_hypothesis_d82275cd65_9d9b81ac.json` | MechanismNoveltyGate hard block before code |

Structured rejection：

| 字段 | 值 |
|---|---|
| mechanism | `regret_insertion_repair` |
| fact id | `cvrp.destroy_repair.regret_insertion_repair` |
| fact digest | `b70c282acdce90d310880877b2e4c19c488d1180e2722c36e6da64edb91b2ed2` |
| contradicted span | `regret insertion repair operators always insert customers at globally cheapest positions without` |
| reason | active solver already wires `_regret2_insertion` / `_regret3_insertion` through repair portfolio |
| allowed guidance | may add/modify destroy/removal using existing regret portfolio; do not claim regret repair itself is missing/new |

判断：拦截结果大体可接受，因为 hypothesis 确实把 regret repair 说成“globally cheapest
positions”，与 active regret-2/3 机制不符。但 rejection 文案不准确：它不是清楚地声称 regret
repair 缺失，而是把已有 regret repair 语义说错。应作为 grounding contradiction / semantic
retry，而不是简单归因为 missing-premise。没有 code、contract、screening。

### Step 4 - `geographic_cluster_removal` / random-removal qblock

Session：
`campaign/agentic_sessions/72391f91-a298-4bb9-801a-afe45581b660`

Hypothesis / tool 链路：

| 调用 | trace | 结果 |
|---|---|---|
| tool-selection x8 | `20260522T153626...` 到 `20260522T153646...` | required preface、memory、feedback、`local_search.py`、`destroy_repair.py`、`acceptance.py`、`config.py`、`baseline_algorithm.py`、runtime feedback |
| hypothesis #1 | `20260522T153649542301_hypothesis_c8776c5114_8b2a3cd2.json` | novelty gate semantic retry |
| hypothesis #2 | `20260522T153719754920_hypothesis_5649ce62ca_d5ef486f.json` | schema preview 指出 C11 activation aggregate issue |
| hypothesis #3 | `20260522T153749701445_hypothesis_af8d003979_58fd2e8d.json` | novelty gate hard block before code |

Final hypothesis 的关键文本：

- 承认 active destroy phase uses `random`, `shaw`, `worst`, `route` removal。
- 目标是新增 `_geographic_cluster_removal`：grid cell spatial patch removal。
- 明确区分 Shaw：Shaw 是 seed-biased relatedness；route removal 是 whole-route。
- target weakness 说 existing operators 不能系统性暴露 cross-route geographic patch。

Structured rejection：

| 字段 | 值 |
|---|---|
| mechanism | `random_removal_destroy` |
| fact id | `cvrp.destroy_repair.random_removal_destroy` |
| fact digest | `bbd22bdc444c0de0f4713fc80ddaa36a1a9963443092436a202837528355466f` |
| contradicted span | `random removal is purely uniform. no` |
| reason | active solver already has `_random_removal` wired as scheduler `"random"` |
| allowed guidance | modify existing `_random_removal` distribution/trigger/budget; do not claim random removal missing |

判断：这是本 run 的主要 P1。hypothesis 没有说 random removal 缺失；相反，它明确把
random removal 当作现有 baseline 并提出 spatial grid cluster variant。这个 qblock 说明
random-removal premise matcher 对 “random is uniform; no operator does X” 的句式仍过宽。
不过上一轮 construction/Shaw false positive 已消失：同一 hypothesis 提到 construction/Shaw
事实时没有触发 `construction_seed_strategy` 或 `shaw_related_removal` rejection。

### Step 5 - `alns_post_repair_2opt` algorithm-smoke qblock

Sessions：

- Hypothesis：`campaign/agentic_sessions/79bbe034-f6c0-412a-a600-7318902190a2`
- Code：`campaign/agentic_sessions/48153583-0d17-44aa-9bdd-34661b5c6c6f`

Hypothesis 阶段：

| 调用 | trace | 结果 |
|---|---|---|
| tool-selection x8 | `20260522T153823...` 到 `20260522T153843...` | required preface、screening/runtime feedback、memory、核心 owner files |
| hypothesis #1 | `20260522T153845955172_hypothesis_847dfcc0eb_9390e204.json` | schema preview 指出 aggregate activation |
| hypothesis #2 | `20260522T153914270558_hypothesis_6149aebfe2_e9203ab0.json` | novelty gate semantic retry |
| hypothesis #3 | `20260522T153944913298_hypothesis_594a20f59b_5e5cecc8.json` | schema/target pass，进入 code |

Code 阶段：

| 调用 | trace | 结果 |
|---|---|---|
| tool-selection x9 | `20260522T154022...` 到 `20260522T154046...` | 读取 `scheduler.py`、surface、branch state |
| code #1 | `20260522T154050104104_code_247f793cb2_46abd61c.json` | contract preview pass；smoke 缺 `solver_algorithm_context_records.alns_post_repair_2opt_iterations` |
| code #2 | `20260522T154137824135_code_6919803598_b510c5e7.json` | 增加 phase/move 记录，但仍未产生 context record activation |
| code #3 | `20260522T154225398735_code_77583abd13_2d374f1d.json` | 继续增加 phase/move early-path 记录，但仍无 `record_iteration('alns_post_repair_2opt', ...)`；failed closed |

判断：algorithm smoke activation qblock 合理。LLM 的修复集中在 `record_phase` /
`record_move`，没有满足 expected activation 中的
`solver_algorithm_context_records.alns_post_repair_2opt_iterations`。`proposal.algorithm_smoke`
明确指出缺 activation evidence，并且没有让无 activation 的 algorithm patch 进入 formal
screening。没有 contract/screening/decision，因为 code generation failed closed。

### Step 6 - `noise_greedy_repair`

Sessions：

- Hypothesis：`campaign/agentic_sessions/6529bb80-c49c-4d60-87c4-30c6bff0dc76`
- Code：`campaign/agentic_sessions/651a54db-a056-48f4-b4ac-2e57537a2691`

Hypothesis 阶段：

| 调用 | trace | 结果 |
|---|---|---|
| tool-selection x9 | `20260522T154314...` 到 `20260522T154335...` | required preface、memory、2 条 screening feedback、runtime feedback、owner files |
| hypothesis #1 | `20260522T154338449924_hypothesis_0337504a08_e83213ca.json` | schema preview 指出 aggregate/effect activation |
| hypothesis #2 | `20260522T154412843335_hypothesis_fbd94fd2da_c2a63f88.json` | 修正为 `noise_greedy_repair` mechanism-specific telemetry，schema/target pass |

Code 阶段：

| 调用 | trace | 结果 |
|---|---|---|
| tool-selection x9 | `20260522T154446...` 到 `20260522T154509...` | 读取 code phase surface/branch state，复用 owner files |
| code #1 | `20260522T154512571157_code_2db93c7b86_1ef49148.json` | contract preview pass；smoke 缺 `solver_algorithm_context_records.noise_greedy_repair_iterations` |
| code #2 | `20260522T154626564646_code_1a90e1c241_73b54fb8.json` | 添加 `context.record_iteration('noise_greedy_repair', 1)` 和 effect moves；algorithm smoke pass |

Formal gate / screening / decision：

| 项 | 结果 |
|---|---|
| contract / verification / canary | `passed` / `passed` / `passed` |
| raw metrics | `metrics/aa857450-bbf8-45dc-ae70-7b3c20e5b41b.json` |
| pair pattern | 4 win / 2 loss / 10 tie |
| case gate | 1 win / 0 loss / 7 tie |
| wins/losses | `E-n101` 两 seed +11/+19；`P-n101` seed 11 +15、seed 29 -16；`B-n52` seed 11 -1、seed 29 +1 |
| runtime | median ratio 1.0013，delta +10.5 ms，regression rate 0.5625 |
| telemetry guard | activation/runtime observed，effect positive；effect positives 18/32，zeros 14/32 |
| decision | `continue_explore` |
| reason | `SCREENING_FAIL_WIN_RATE`, `SCREENING_WEAK_SIGNAL_CONTINUE` |

判断：最后 `CONTINUE_EXPLORE` 是弱信号而非误晋升。它没有 loss at case-level，但有 pair-level
loss，case win 只有 `E-n101`，median delta 仍为 0.0，runtime 也偏慢。继续探索同 branch
合理；campaign 结束是因为 requested rounds 已完成。

## 4. Active Algorithm Facts 与 Gate 同源性

本 run 的关键点不是 agent 看不到 facts。每个 hypothesis/code prompt manifest 都包含
`agentic_active_algorithm_facts`，约 12.3k chars，状态为 `included`，未截断。`agentic_tool_observations`
常被截断，但 active facts section 没被截断。

同源 digest 例子：

| Session | 角色 | prompt fact digest | gate / code 使用 |
|---|---|---|---|
| `75fc88eb...` | regret qblock hypothesis | `b70c282a...b91b2ed2` | rejection 同 digest，fact id `cvrp.destroy_repair.regret_insertion_repair` |
| `72391f91...` | random qblock hypothesis | `bbd22bdc...355466f` | rejection 同 digest，fact id `cvrp.destroy_repair.random_removal_destroy` |
| `79bbe034...` / `48153583...` | `alns_post_repair_2opt` hypothesis/code | `f1196d9d...f38b819d` | code prompt 同 digest，smoke activation 基于 declared mechanism |
| `6529bb80...` / `651a54db...` | `noise_greedy_repair` hypothesis/code | `b70c282a...b91b2ed2` | code prompt 同 digest，smoke repair 后 formal telemetry 同 mechanism |

`active_solver_facts.py` 已显式包含：

- `cvrp.construction.diverse_feasible_seed`: sweep、Clarke-Wright、capacity-balanced、nearest-neighbor fallback。
- `cvrp.destroy_repair.shaw_related_removal`: `_shaw_removal` 已是 seed-based related/proximity destroy。
- `cvrp.destroy_repair.random_removal_destroy`: `_random_removal` 已是 scheduler `"random"` destroy，并给出 allowed variant guidance。
- `cvrp.destroy_repair.regret_insertion_repair`: `_regret2_insertion` / `_regret3_insertion` 已 wired。
- local search Or-opt / two-opt / two-opt-star facts。

结论：construction/Shaw/random/regret facts 对 agent 和 gate 是同源暴露的。Step 4 的 random
误伤不是 context 缺失，而是 matcher 对“已有 random 是 uniform；缺少 spatial patch”这种
对比句处理过宽。

## 5. Qblock 合理性

| Block | 结论 | 依据 |
|---|---|---|
| Step 3 `regret_insertion_repair` | 部分合理，文案不准 | Hypothesis 对 regret insertion 语义说错；但 rejection 说“missing”过强，应改成 semantic contradiction / mischaracterization |
| Step 4 `random_removal_destroy` | 不合理 P1 | Hypothesis 承认 random 已存在，目标是 geographic cluster variant；matcher 把 “random is uniform; no spatial patch” 误判为 random removal missing |
| Step 5 `algorithm_smoke_failure` | 合理 | 三次 code 都没产生 `solver_algorithm_context_records.alns_post_repair_2opt_iterations`；smoke 阻止无 activation patch 进入 screening |

Proposal-quality budget 本 run 没被这些 block 耗尽：consumed 3 / limit 6，仍完成 3 个
effective screening。因此没有 P0 级运行失败。

## 6. v3 框架边界

本 run 符合 v3 边界：

- Generic campaign/decision 只处理 surface、contract、verification、telemetry、screening、
  decision reason code；CVRP mechanism facts 由 problem-owned provider 暴露。
- Prompt context 通过 exposure-controlled tools 读取 allowlisted solver_design files，
  target 都在 `policies/baseline_modules/*.py` / `policies/baseline_algorithm.py`，没有越界写 generic core。
- Contract/smoke 是 proposal 阶段 tainted preview；formal screening 使用 campaign protocol metrics，
  `raw_metrics_ref` 以 public artifact ref 记录。
- MechanismNoveltyGate rejection 带 fact id、snapshot digest、fact provenance 和 allowed guidance；
  这是 v3 需要的同源事实链。

剩余问题应归入 CVRP provider / premise matcher 质量，而不是 generic Scion core 边界破坏。

## 7. P0 / P1

P0：无。实验正常结束，完成 3 个 effective screening；无 orphan runtime、无 verification
breakdown、无 proposal-quality exhaustion、无 champion/promotion integrity 问题。

P1：random-removal premise matcher 误伤 `geographic_cluster_removal`。它把一个明确承认
`_random_removal` 存在、并提出 spatial grid cluster variant 的 hypothesis 判成
“random customer-removal destroy missing”。建议把 random-removal contradiction 限定在显式
missing/new-random-removal claims；对 “random is uniform / lacks X” 句式改为 semantic retry
或 allowed-variant guidance，不直接消耗 hard qblock。

P1：regret qblock 的 reason 过度简化为 “regret insertion repair missing”。实际更像
“existing regret repair semantics mischaracterized”。建议 rejection reason 区分
`missing_premise`、`duplicate_mechanism`、`mischaracterized_existing_mechanism`，避免把
不准确反馈写入后续 agent memory。
