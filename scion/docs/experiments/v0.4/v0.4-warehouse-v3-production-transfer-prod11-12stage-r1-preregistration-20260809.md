# Warehouse V3 Production Transfer prod-1.1 12-Stage R1 Preregistration — 2026-08-09

**Status:** `PRE_REGISTERED_READY_NOT_LAUNCHED`

Immediately before this record was written, the local Codex proxy reported
`authenticated=true` with one active account. The declared campaign root was
absent and has not been pre-created.

## Question

Can a fresh `gpt-5.6-terra` research agent, using the lightweight V3 path,
produce a valid algorithmic improvement funnel on production-style Warehouse
data during one uninterrupted 12-formal-stage shakedown?

This is the first production-transfer rung after retained synthetic Warehouse
continuity was confirmed. It tests production behavior, where
`subcategory_splits` has little headroom and secondary `total_cost` effects may
be noisy. It is not a repeat of the synthetic continuity experiment and does
not require multiple promotions.

## Fixed source and inputs

| Input | Pre-registered value |
|---|---|
| Runtime source | `/home/clawd/research/or-autoresearch-agent-v3-hotpath/scion` |
| Source commit | `e1fbee8cd37aca31e445d0cc165422a1442cffb8` |
| Branch at registration | `codex/v04-production-cvrp-research` |
| Campaign root | `/home/clawd/research/scion-experiments/v04-warehouse-v3-production-transfer-prod11-12stage-r1-gpt56terra-20260809T021245Z-claw/campaign` |
| Problem launcher | `problems/warehouse_delivery/problem.yaml` |
| ProblemSpec | `problems/warehouse_delivery/problem-v1.yaml` (`problem-v1`) |
| Champion/solver root | `/home/clawd/research/or-autoresearch-agent-v3-hotpath/surrogate` |
| Protocol | `problems/warehouse_delivery/protocol_prod.yaml` (`3.2-prod`) |
| Split | `problems/warehouse_delivery/split_manifest_prod.yaml` (`prod-1.1`) |
| Seeds | `problems/warehouse_delivery/seed_ledger.yaml` (`3.0`) |
| Provider/model | local Codex proxy at `127.0.0.1:8080` / `gpt-5.6-terra` |
| Formal-stage horizon | 12 |
| Solver subprocess limit | 30 seconds per case/seed execution |
| Parameter search | disabled |
| Scheduler | default lightweight V3 state priority/FIFO, at most three active branches |

Input resolution before registration found no missing cases. The fixed split
contains 2 canary, 16 screening, 5 validation and 8 frozen cases. Fixed seeds
are:

- canary: `999`;
- screening: `42`, `137`;
- validation: `7`, `19`, `83`;
- frozen: `256`, `512`, `1024`.

Protocol selection is deterministic from the declared split, action and stage:
initial screening uses 6 cases for modify or 10 for create; expanded screening
uses 14 or 16; validation and expanded validation use the same five declared
cases; frozen uses four declared cases. Current candidate results do not add,
remove or choose cases.

The objective is the ProblemSpec's lexicographic order:
`subcategory_splits`, then `total_cost`. Runtime is comparative diagnostic
evidence, not a substitute objective. Gates, practical deltas, bootstrap rules,
case populations, seeds and the time limit remain those declared above.

## V3 composition and exclusions

The active path is one structured H call, structural Hypothesis Contract, one
approved-H-bound C call, structural Patch Contract, isolated Workspace,
executable Verification, problem-owned Protocol and deterministic Decision.

- No surface, action, target file or mechanism is forced.
- There is no auxiliary quality/critique loop and no provider retry.
- There is no resume, prior campaign state, copied branch, H/C response or
  champion import.
- Parameter and weight search remain off.
- There are no result-derived or dynamically generated formal cases.
- No Protocol, Decision, threshold, scheduler or objective change is part of
  this run.

## Frozen launch command

At launch, all executable, configuration and solver paths must match the fixed
source commit above and have no tracked or untracked worktree changes. `HEAD`
may be later than the executable source when the intervening commits change
documentation only, including the commit that records this preregistration.
The proxy authentication and model check must pass before the campaign starts.
The campaign root must remain absent; the CLI creates it.

```bash
set -euo pipefail

SOURCE_COMMIT=e1fbee8cd37aca31e445d0cc165422a1442cffb8
CAMPAIGN_ROOT=/home/clawd/research/scion-experiments/v04-warehouse-v3-production-transfer-prod11-12stage-r1-gpt56terra-20260809T021245Z-claw/campaign

cd /home/clawd/research/or-autoresearch-agent-v3-hotpath/scion
test -z "$(git diff --name-only "$SOURCE_COMMIT" HEAD -- scion problems ../surrogate)"
test -z "$(git status --porcelain --untracked-files=all -- scion problems ../surrogate)"
test ! -e "$CAMPAIGN_ROOT"

PROXY_KEY_VALUE="$(curl -fsS http://127.0.0.1:8080/auth/status | \
  jq -er '.proxy_api_key | select(type == "string" and length > 0)')"
trap 'unset PROXY_KEY_VALUE' EXIT

curl -fsS -o /dev/null \
  -H "Authorization: Bearer $PROXY_KEY_VALUE" \
  http://127.0.0.1:8080/v1/models

PYTHONPATH=/home/clawd/research/or-autoresearch-agent-v3-hotpath/scion \
SCION_MODEL=gpt-5.6-terra \
SCION_BASE_URL=http://127.0.0.1:8080 \
SCION_API_KEY="$PROXY_KEY_VALUE" \
  /home/clawd/miniconda3/envs/claw/bin/python -m scion.cli.main run \
  --problem problems/warehouse_delivery/problem.yaml \
  --protocol problems/warehouse_delivery/protocol_prod.yaml \
  --split problems/warehouse_delivery/split_manifest_prod.yaml \
  --seeds problems/warehouse_delivery/seed_ledger.yaml \
  --campaign-dir "$CAMPAIGN_ROOT" \
  --rounds 12 \
  --time-limit-sec 30
```

## Stop and continuation policy

- Launch exactly once into the absent root. Never resume or retry this root or
  the same H/C call.
- Candidate-local schema, Contract, edit-application or Verification rejection
  releases that proposal and schedules a fresh H on clean current source; it
  consumes no formal stage.
- Completed screening negatives retain the evaluated provisional branch head
  under V3 §11.2 and scheduling continues.
- Queued screening expansion, validation, validation expansion and frozen use
  the exact candidate without another provider call.
- Do not stop on the first scientific negative or first promotion. Continue
  until 12 formal stages complete or a genuine terminal infrastructure,
  provider-without-terminal-response, resource or interruption outcome occurs.
- If stage 12 leaves an exact candidate queued, seal the root and report the
  pending stage. Do not extend or resume it under this preregistration.
- A 24-stage matched matrix is not automatic. It requires a new root and a new
  preregistration, and is allowed only if this shakedown exposes a valid funnel
  or a concrete powered production headroom/noise question.
- Any promoted champion requires a separate preregistered independent
  comparison against its original v1 base before a retained production-
  transfer claim.

## Outcome classification and claim boundary

- `PRODUCTION_PROMOTION_OBSERVED_REPLAY_PENDING`: an exact candidate completes
  screening, validation and frozen and receives `PROMOTE`; independent replay
  is still required.
- `VALID_FUNNEL_FOR_24STAGE_PREREGISTRATION`: complete evidence reaches a real
  expansion or later-stage funnel but the 12-stage root does not establish a
  retained promotion.
- `POWERED_QUESTION_FOR_24STAGE_PREREGISTRATION`: complete case-level evidence
  identifies a specific production headroom/noise question suitable for a
  separately fixed matched matrix.
- `VALID_SHAKEDOWN_NO_TERMINAL_CLAIM`: 12 stages complete with interpretable
  scientific negatives but neither condition above.
- `RUN_INVALID_INFRA`: the declared horizon is truncated by provider,
  configuration, framework, runner or resource failure.
- `INTERRUPTED`: an explicit external interruption ends the run.

This campaign can support valid production-style research behavior, negative
mechanism evidence, a continuation decision, or an observed Protocol-complete
promotion. By itself it cannot establish retained production transfer, a
general absence of production headroom, synthetic multi-promotion continuity,
CVRP improvement, parameter-search value or overall `TASK.md` completion.
