# CVRP R56 Corrected-Runtime Minute One-Step Postrun — 2026-08-12

**Status:** `VALID_TERMINAL_RESEARCH_PROCESS_OBSERVATION / VERIFICATION_LIGHT_REJECTED / ZERO_PROTOCOL_EVIDENCE / NO_DECISION / NO_PROMOTION`

## Scope and V3 authority

This report closes the separately preregistered R56 minute-scale fresh-B0 CVRP
step. Its immediate purpose was to observe one short research lifecycle after
the R55 persistence and problem-owned Verification corrections. It was not an
R54 retry, validation run, frozen evaluation, promotion campaign or proof of
solver improvement.

[`design/scion-architecture-v3.md`](../../../design/scion-architecture-v3.md)
remains the sole architecture authority. No deployment, packaging, build,
root/systemd, Trust/Hash, identity, registry, receipt, self-proof or new
research gate was added.

## Authorization, lifecycle and artifacts

- Frozen preregistration:
  [`v0.4-cvrp-r56-minute-corrected-one-step-preregistration-20260812.md`](v0.4-cvrp-r56-minute-corrected-one-step-preregistration-20260812.md)
- Clean source reference:
  `acdc80ba5de27fad5f6c56cbb6daf56fef2bbbb1`
- Input bundle:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r56-minute-corrected-one-step-20260812-input`
- Output root:
  `/home/clawd/research/scion-experiments/v04-cvrp-r56-minute-corrected-one-step-20260812-claw`
- Process-control root:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r56-minute-corrected-one-step-20260812-control`
- Driver result: `driver_result.json`
- Campaign summary: `campaign/campaign_summary.json`
- Lineage database: `campaign/scion.db`

The user supplied the exact fresh source-send authorization before launch. The
ordinary-user launcher acquired its inner process at
`2026-08-12T15:05:33Z`; the control root recorded exit `0` by
`2026-08-12T15:06:24Z`, about 51 seconds later. The startup PID `277510` is an
observation only, not a durable process identity.

The durable lifecycle facts are:

- `run_one_step()` exactly once and `run()` zero times;
- one Terra H call and one conditional Terra C call, with zero retries;
- the recorded Contract passed;
- V1, V1b and V2 passed, then V3 executed and rejected the candidate;
- V4 and all later Verification checks were not reached;
- zero canary and zero formal Protocol calls;
- zero screening, validation and frozen results;
- no Decision and no raw metrics artifact; and
- B0/v1 remained the sole champion, with no promotion.

The wrapper completed normally with exit `0`, classification
`COMPLETED_ONE_STEP_WRAPPER`, execution outcome `research_rejected` and reason
`VERIFICATION_LIGHT_REJECTED`. The generic campaign-validity projection is
`invalid_research_rejected_only` because there are zero evaluated outcomes.
That is an algorithm-conclusion exclusion, not an infrastructure failure or an
incomplete wrapper.

## H research quality

H used the neutral R53–R54 algorithm evidence and chose an elapsed-budget
normalized annealing schedule. It was source-grounded and directly addressed
the earlier observation that iteration-count behavior can diverge from actual
time allocation. Crucially, H explicitly required progress to be updated after
every completed ALNS iteration, including early destroy, repair and
feasibility-recovery paths, and explicitly prohibited stale calls to a removed
cooling API.

This is strong research-reasoning and feedback-uptake evidence. It is not
algorithm-quality evidence because the candidate never reached canary or
formal Protocol execution.

## C fidelity and expression failure

C preserved the approved mechanism's intent: it removed the old
`_SimulatedAnnealing.cool()` API, introduced elapsed-fraction temperature
handling and attempted to replace scheduler cooling calls. The failure was in
the mechanical completeness of the typed exact edit.

The frozen scheduler contained five `annealing.cool()` calls. C used one
indentation-specific `replace_all` expression whose 20-space search matched
only one occurrence. Four calls remained after application: three at
16-space indentation in the repair-error, candidate-infeasible and route-limit
recovery paths, plus one at 12-space indentation on the ordinary loop-tail
path. Because the method itself had been removed, all four residual calls were
invalid. This violates H's explicit all-path/no-stale-API obligation even
though the central mechanism and target files were faithful.

The three recovery fixtures exercised the first three residual calls. The
fourth ordinary-path residual is source-proven but remained latent because V3
stopped at the three failures. This is therefore a C expression/application
completeness defect, not evidence against the elapsed-budget algorithm.

## Contract and executable Verification

The recorded Contract passed. The executed checks were:

| Check | Result | Meaning |
|---|---|---|
| V1 syntax | pass | The residual member calls are syntactically valid. |
| V1b undefined names | pass | A missing object member is not an undefined Python name. |
| V2 interface | pass/deferred | The support-module interface remained eligible for workspace smoke. |
| V3 unit tests | fail | All three problem-owned recovery fixtures raised `AttributeError` on the removed `cool()` method. |
| V4 regression tests | not reached | Verification stopped at the first failed check. |
| V5–V8 | not reached | No later smoke, feasibility, oracle or nondeterminism check ran. |

V3 took 519 ms in the recorded Verification result; pytest itself reported
three failures in 0.19 seconds. The failing candidate-workspace scheduler
locations were lines 230, 252 and 272. Thus the R55 problem-owned tests did
exactly what they were intended to do: reject an inconsistent multi-site edit
in well under one second, before a canary or minute-scale solver screen.

This is not a new algorithm, novelty or quality gate. It is ordinary executable
runtime-consistency coverage, and it avoided spending the four-case formal
budget on a source-proven broken candidate.

## Zero Protocol evidence and claim boundary

No candidate or champion formal Protocol evaluation ran. There is therefore:

```text
canary calls:             0
formal Protocol calls:    0
screening rows:           0
validation rows:          0
frozen rows:              0
admissible pairs:          0
W/L/T:                    undefined
effect / confidence band: undefined
Decision:                 none
champion:                 B0/v1
```

R56 supports no statement about final distance, feasibility under candidate
execution, fleet behavior, case quality, replication or promotion. In
particular, the candidate's rejection cannot be interpreted as an algorithm
loss: it never received an algorithm-quality evaluation.

## Conclusion and next boundary

R56 is sealed as
`VALID_TERMINAL_RESEARCH_PROCESS_OBSERVATION / VERIFICATION_LIGHT_REJECTED /
ZERO_PROTOCOL_EVIDENCE / NO_DECISION / NO_PROMOTION`. It demonstrates a clean,
bounded early-rejection lifecycle after R55: the wrapper exited normally, the
new problem-owned test executed, and no broken candidate consumed canary or
formal Protocol time.

The scientific bottleneck remains proposal expression rather than H's
algorithm focus. R56 supplies evidence for a future minimal redesign of the C
editing/tool surface so that one intended multi-site transformation cannot be
silently narrowed by indentation-specific exact replacement. That redesign
must not become another algorithm-direction gate or rule layer. It should be
reviewed and tested provider-free before any further short solver experiment.

The root is not repaired, retried, resumed, screened, validated, frozen or
promoted. No R57, tool redesign or successor H/C is preregistered, authorized
or launched by this report. CVRP acceptance and S6 remain open.
