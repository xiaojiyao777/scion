# CVRP Minute Feedback One-Step Exploratory Screen Postrun — 2026-08-12

**Status:** `VALID_TERMINAL_CANDIDATE_RUNTIME_FAILURE / DECISION_ABANDON / FRAMEWORK_POST_DECISION_CONTEXT_PERSISTENCE_ERROR / NO_PROMOTION`

## Scope and V3 authority

This report closes the separately preregistered R54 minute-scale CVRP
experiment. R54 asked whether Scion could consume R53's negative mechanism
evidence and use one fresh direct-V3 step to propose and test a materially
different solver change. It does not validate or promote the candidate,
establish CVRP improvement, authorize a retry, or complete `TASK.md`.

[`design/scion-architecture-v3.md`](../../../design/scion-architecture-v3.md)
remains the sole architecture authority. The experiment used the existing
problem-owned chain:

```text
problem facts + complete fresh-B0 source + R53 neutral evidence
  -> one structured H
  -> Contract
  -> one approved-H-bound C
  -> isolated Workspace
  -> executable Verification
  -> canary + paired SCREENING Protocol
  -> Safe Features
  -> deterministic Decision
```

No deployment, distribution, packaging, build, root/systemd, Trust/Hash,
identity, lease, signing, receipt, registry, self-proof or new research gate
was added.

## Lifecycle and artifacts

- Frozen preregistration:
  [`v0.4-cvrp-minute-feedback-one-step-preregistration-20260812.md`](v0.4-cvrp-minute-feedback-one-step-preregistration-20260812.md)
- Clean source reference:
  `e4b6b98d63eac7e088d9a9be960a6b5df08bca9d`
- Input bundle:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-minute-feedback-one-step-20260812-input`
- Output root:
  `/home/clawd/research/scion-experiments/v04-cvrp-minute-feedback-one-step-20260812-claw`
- Process observation:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-minute-feedback-one-step-20260812-control`
- Driver result: `driver_result.json`
- Campaign summary: `campaign/campaign_summary.json`
- Lineage database: `campaign/scion.db`
- Raw screening evidence:
  `metrics/3c235923-6855-45e1-b4cc-a92c2245d1e5.json`

Fresh source-send authorization was granted before the one launch. The
ordinary-user inner process was acquired at `2026-08-12 13:27:54 UTC`; the
control root recorded exit `1` at `13:30:13 UTC`, about 139 seconds later. No
process remained afterward.

The durable lifecycle facts are:

- one `CampaignManager.run_one_step()` invocation and zero `run()` calls;
- one H call and one C call, with zero provider retries;
- one canary invocation and one formal `SCREENING` invocation;
- four of four formal pairs attempted and the raw Protocol artifact completed;
- zero validation and frozen rows;
- Decision durably recorded `abandon` before the later exception;
- B0/v1 remained the sole champion, with no promotion row.

The experiment-owned driver records
`RUN_INVALID_INFRA_OR_INTERRUPTED / ValueError` because `run_one_step()` raised
while persisting post-Decision proposal context and therefore did not return to
the wrapper's normal terminalizer. That wrapper label describes the lifecycle
exception only. It does not overwrite the earlier typed candidate runtime
failure, completed Protocol row or deterministic Decision.

## H feedback uptake and research quality

Both provider calls used `gpt-5.6-terra` through the local Codex proxy.

| Call | Count | Provider timeout | Wall time | Input / output / reasoning tokens |
|---|---:|---:|---:|---:|
| H / `generate_hypothesis` | 1 | 90 s | 10.863 s | `21,087 / 462 / 184` |
| C / `generate_patch` | 1 | 180 s | 38.810 s | `19,917 / 1,892 / 296` |

The H trace contains the exact frozen R54 question and the exact R53 feedback
as the fifth and final typed prior item. H explicitly used the relevant R53
observation—more ALNS iterations but fewer best updates and no final-quality
gain—to reject another global runtime-share-cap adjustment. It instead chose
elapsed-budget-normalized simulated-annealing temperature in
`policies/baseline_modules/acceptance.py`, with supporting scheduler changes.

The hypothesis was source-grounded and falsifiable: it predicted worsening
acceptances concentrated early in the elapsed ALNS budget, fewer such
acceptances late, more late best updates and lower final distance, while
preserving feasibility and fleet semantics. Thus R54 shows good uptake of the
R53 feedback at H. This is proposal-quality evidence, not solver-performance
success.

## C fidelity and the incomplete applied edit

C was conceptually faithful to the approved H. Its typed response:

1. removed the iteration-count temperature state and `cool()` method from
   `_SimulatedAnnealing`;
2. changed `accept` to compute temperature from an explicit elapsed fraction;
3. removed the estimated-iteration constructor input;
4. added an ALNS elapsed-budget denominator and helper; and
5. supplied that fraction at the ordinary annealing acceptance call.

However, the multi-site edit was mechanically incomplete. The frozen B0
scheduler contains five calls to `annealing.cool()`. Replaying the seven
recorded exact replacements in memory against the frozen source applies each
replacement exactly once, removes the normal loop-tail call and the
`destroy_empty` call, but leaves three calls after the C edit:

- the `repair_error` path;
- the `candidate.is_feasible() == false` path; and
- the `route_limit` path.

At the same time, C removed `_SimulatedAnnealing.cool`. The resulting candidate
therefore contains three calls to a method that no longer exists. This is a
real active-path implementation defect, not unused scaffolding. The exact one
of those three paths reached by P-n65 is not retained in durable stderr and is
therefore not identified here. The defect is consistent with, and provides a
source-level explanation for, the typed runtime-audit failure observed on that
case; the report does not invent a missing traceback.

## Contract, Verification and canary coverage

The durable experiment row reports Contract passed with no diagnostic. All
nine recorded Verification entries also have `passed=true`, but two are
explicit skips:

| Check | Durable result | Relevance to the missed defect |
|---|---|---|
| V1 syntax | pass | the residual method calls are syntactically valid |
| V1b undefined names | pass | it checks names, not object member existence on every branch |
| V2 interface | pass/deferred | support-module interface deferred to workspace smoke |
| V3 unit tests | pass/skipped | no unit-test path configured |
| V4 regression tests | pass/skipped | no regression-test path configured |
| V5 solution consistency | pass | two-second controlled-case smoke |
| V6 feasibility | pass | two-second controlled-case smoke |
| V7 objective oracle | pass | two-second controlled-case smoke |
| V8 nondeterminism | pass | two controlled runs had equal canonical signatures |

The typed canary used
`controlled/data/synthetic_controlled_canary_5.vrp`, seed `101`, and passed one
attempted candidate pair. Neither the controlled canary nor the configured
Verification smoke exposed the three residual scheduler calls. The formal
P-n65 run was the first retained test to report a corresponding typed
solver-algorithm runtime failure; its durable evidence does not identify which
residual branch was reached. R54 therefore exposes a concrete Verification
branch-coverage gap: light syntax/name checks and the tiny happy-path smoke
were insufficient for a coordinated method-removal edit. This is a diagnosis
of existing coverage, not a new candidate gate or a production change in this
report.

## Exact formal screen and admissible result

`A` is fresh B0/v1 and `B` is the fresh R54 candidate. All case paths and
limits match preregistration; case ordinals are `0..3`, seed ordinal and block
ordinal are zero, and actual order equals scheduled `AB/BA/AB/BA`. Champion
caching was disabled with zero hits, misses, writes or cached-runtime pairs.

| Ordinal | Case / seed | Order | Limit | A result | B result | Admissible comparison |
|---:|---|---|---:|---|---|---|
| 0 | `P/P-n65-k10` / 11 | `AB` | 8 s | success, feasible, exit 0 | process success, feasible, exit 0; typed `solver_algorithm_runtime_error` | invalid; no delta or W/L/T |
| 1 | `E/E-n101-k14` / 11 | `BA` | 12 s | `(0,1130,14)` | `(0,1130,14)` | tie, distance delta 0 |
| 2 | `X/X-n120-k6` / 11 | `AB` | 12 s | `(0,14204,6)` | `(0,14204,6)` | tie, distance delta 0 |
| 3 | `X/X-n233-k16` / 11 | `BA` | 15 s | `(0,20112,17)` | `(0,20112,17)` | tie, distance delta 0 |

Objective tuples are `(fleet_violation, total_distance, routes)`. The raw P row
contains numeric subject objectives, but the candidate side is explicitly
failed by the solver runtime audit. Those numbers are not an admissible paired
effect and must not be converted into a distance delta, win, loss or tie.

The exact structural counts are:

```text
total / attempted:       4 / 4
valid / failed:          3 / 1
candidate failures:      1
champion failures:       0
valid-subset W/L/T:      0 / 0 / 3
four-case W/L/T:         undefined (P is invalid)
```

Protocol's conditional valid-subset statistics are median `0`, CI `[0,0]`
and three ties. They describe only the three admissible rows; they are not a
complete four-case quality estimate and cannot support an improvement claim.
All three valid candidate results preserve zero fleet violation. The failed P
row is not used to extend that protected-metric statement to all four cases.

## Decision and independent framework failure

Protocol persisted one completed screening result with gate outcome `fail`.
The auxiliary Protocol reason is `SCREENING_FAIL_CASE_QUALITY`; the effective
Decision reason is the stronger typed `CANDIDATE_RUNTIME_FAILURE`. Decision
durably recorded `abandon`, the branch state/code status became `abandoned`,
and the champion table remained exactly B0/v1. This yields the scientific
terminal classification `VALID_TERMINAL_CANDIDATE_RUNTIME_FAILURE /
DECISION_ABANDON / NO_PROMOTION`.

A separate framework error then occurred while `_record_step` tried to persist
canonical screening context for a future proposal. The durable step contains
three valid `pair_feedback` items, while Protocol stats contain
`valid_pairs=3` and `candidate_failed_pairs=1`. The context projection requires
`len(pair_feedback) == valid_pairs + candidate_failed_pairs`, so it demanded
four feedback rows even though the failed P comparison is correctly absent
from W/L/T feedback. It raised:

```text
ValueError: screening pair feedback cardinality conflicts with
valid/candidate-failure pair counts
```

This happened after the experiment and Decision events were stored. It caused
wrapper exit `1`, the exception stop reason
`minute_feedback_one_step_exception`, and prevented the wrapper from writing
its normal completion-scope fields. It did not cause the candidate runtime
failure, change Decision, promote anything, invoke a second step, or make the P
objective admissible. The correct independent label is
`FRAMEWORK_POST_DECISION_CONTEXT_PERSISTENCE_ERROR`.

## Conclusion and sealed claim boundary

R54 provides a mixed but analyzable result about Scion's research behavior:

- H consumed the R53 feedback well and produced a relevant, source-grounded,
  falsifiable mechanism materially different from the tested cap;
- C preserved the central mechanism but failed to complete a coordinated
  multi-site method-removal edit;
- existing Verification passed without covering the three affected negative
  branches;
- formal Protocol exposed the defect as one candidate runtime-audit failure,
  retained three valid ties, and Decision correctly abandoned the candidate;
- post-Decision context persistence then failed independently on the typed
  candidate-failure cardinality.

The experiment is sealed as
`VALID_TERMINAL_CANDIDATE_RUNTIME_FAILURE / DECISION_ABANDON /
FRAMEWORK_POST_DECISION_CONTEXT_PERSISTENCE_ERROR / NO_PROMOTION`. No R54
candidate is retained, replicated, validated, frozen or promoted. The root is
not retried or resumed. This report does not preregister or authorize a next
experiment, and CVRP acceptance plus S6 remain open.
