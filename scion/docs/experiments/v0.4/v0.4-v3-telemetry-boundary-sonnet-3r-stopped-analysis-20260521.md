# v0.4 v3 Telemetry Boundary Sonnet 3R Stopped Analysis - 2026-05-21

实验路径：
`/home/clawd/research/scion-experiments/v04-v3-telemetry-boundary-sonnet-3r-20260521T200645Z`

本分析只读取 artifacts、SQLite、APS sessions、LLM traces 和 metrics；没有修改实验 raw，也没有启动新实验。

## P0/P1 结论

### P0

无 P0。

没有证据表明 Scion 把不完整 formal screening 计为有效实验，也没有看到 validation/frozen raw 泄漏、telemetry 修复导致错误计数、或 partial metrics 被用于正式 Decision。

### P1

1. **Framework reporting gap**: 最新 APS code session `25b2d0a7` 已 `completed`，并启动 formal screening 写出了 partial metrics，但 `SIGTERM` 在 3/16 pairs 后打断 protocol。因为没有返回 `ProtocolResult`，所以没有正式 `experiment` event，`n_experiments=0` 正确。问题不是 protocol correctness，而是 status/reporting 容易误导：`status.last_result` 仍显示上一条 `agent_quality_blocked`，没有显示 in-flight formal screening 和 partial metrics ref。
2. **Framework lifecycle ambiguity**: 第一次 `adaptive_sa_reheat` code preview C9e 失败后，框架似乎保留了 pending hypothesis 进入下一次 APS；但下一次又被 repeated mechanism gate 当作 duplicate proposal 拦截。这对 fresh proposal 合理，对 approved hypothesis 的 code repair retry 不清晰。需要明确：要么允许 code repair 并绕过同 hypothesis duplicate gate，要么第一次 code_generation failure 后不创建 pending retry。
3. **CVRP adapter C9e false-positive / unclear guidance**: `reheat_count` 是 `@property` getter，通过 `annealing.reheat_count` 属性读取被 scheduler 使用；C9e 只按 call edge 判断 helper reachability，把 property getter 判成 inert。该 gate 过严或需要明确禁止 property helper。agent patch 本身也有质量问题，但直接失败点是 CVRP C9e 判定。

## 为什么 completed session 有 metrics 但没有正式 experiment event

结论：这是预期的 protocol 边界行为，不是 `n_experiments` 计数 bug；但 status 可观测性需要修。

相关 artifacts：

- APS session: `campaign/agentic_sessions/25b2d0a7-0e2a-489e-9347-8c11b1231d22/output.json`
  - `status=completed`
  - `termination_reason=completed`
  - Contract preview passed
  - algorithm smoke passed
- Smoke/canary metrics:
  - `campaign/metrics/v8_run1_099b834b.json`
  - `campaign/metrics/v8_run2_099b834b.json`
  - 这两份是 APS/smoke 阶段 artifacts，不是正式 experiment event。
- Formal partial metrics:
  - `campaign/metrics/21133b6c-3703-4ced-8989-055255c6bebc.json`
  - `stage=screening`
  - `complete=false`
  - `total_pairs=16`
  - `attempted_pairs=3`
  - `valid_pairs=3`
  - `failed_pairs=0`
- DB:
  - `experiment_events` 只有 5 条 `agentic_proposal_session`
  - 没有正式 `experiment` row
- Final status:
  - `n_experiments=0`
  - `screened_experiments=0`
  - `last_result.reason=agent_quality_blocked`
  - `stopped_reason=signal:SIGTERM`

解释：

1. `25b2d0a7` 的 `completed` 只表示 APS proposal/code 阶段完成，patch 通过 Contract preview 和 smoke，可以进入 formal protocol。
2. formal screening 启动后，protocol pair loop 每完成一个或几个 pair 会写 partial metrics snapshot，所以出现了 `21133b6c...json`。
3. 正式 experiment event 需要 formal screening 完成全部 pairs、构建 telemetry guard summary、返回 `ProtocolResult`，再由 pipeline 记录 StepResult/DB event。
4. `SIGTERM` 在 20:21:56 到达时，formal screening 只有 3/16 pairs 完成，因此没有 `ProtocolResult`，没有 Decision，没有正式 `experiment` event，也不应增加 `n_experiments`。
5. `status.last_result` 仍是上一条 completed StepResult，也就是 repeated mechanism 的 `agent_quality_blocked`。这不是计数错误，但缺少 `in_flight_protocol` 状态，容易把最终失败点误读为 agent_quality_blocked。

框架判定：

- **不是 P0 correctness bug**：Scion 没有把 partial formal metrics 错计成正式实验，符合 v3。
- **是 P1 observability/reporting bug**：stop 时应该在 status 中显式记录 in-flight formal screening、partial metrics ref、attempted pairs 和 current phase，而不是只留下旧的 `last_result`。

## Run 状态摘要

来自 `launch.env`：

- Commit: `991535f Move runtime telemetry behind surface declarations`
- Model: `claude-sonnet-4-6`
- Rounds: `3`
- Formal time limit: `10`
- `AGENTIC_PROPOSAL=1`
- `DISABLE_EARLY_STOP=1`
- Started UTC: `2026-05-21T20:06:45Z`
- Exit: `EXIT_CODE=143`, `EXIT_REASON=signal_SIGTERM`, `EXIT_UTC=2026-05-21T20:21:56Z`

来自 `campaign/status.json`：

- `stopped=true`
- `stopped_reason=signal:SIGTERM`
- `n_experiments=0`
- `screened_experiments=0`
- `telemetry_failed_experiments=0`
- `n_steps=2`
- `proposal_attempts=2`
- `total_rounds=2`
- `last_result.reason=agent_quality_blocked`

SQLite：

- `hypotheses`
  - `0077a73e-8b23-4783-ae76-f69c034e4b39`: `adaptive_sa_reheat`, status `rejected`
  - `66597c2d-6fee-4583-80f9-261e852d6eb9`: `random_segment_removal`, status `active`
- `experiment_events`: 5 条 `agentic_proposal_session`，0 条正式 `experiment`

## 每个 APS session 时间线

| UTC | Session | 阶段 | 状态 | 关键结果 |
|---|---|---|---|---|
| 20:08:28 | `b9478e1a` | hypothesis | `partial_hypothesis_only` | 最终提出 `adaptive_sa_reheat`，schema/target preview 通过，等待 approval |
| 20:11:10 | `3aeb76dc` | code | `failed` | `adaptive_sa_reheat` patch 三次 C9e 失败，inert helper `reheat_count` |
| 20:11:31 | `9523b040` | hypothesis/retry | `partial_hypothesis_only` | same `adaptive_sa_reheat` 被 repeated mechanism gate 拦截，StepResult 为 `agent_quality_blocked` |
| 20:13:15 | `277fc06a` | hypothesis | `partial_hypothesis_only` | 最终提出 `random_segment_removal`，schema/target preview 通过 |
| 20:17:10 | `25b2d0a7` | code | `completed` | `random_segment_removal` patch 通过 Contract preview 和 smoke，随后进入 formal screening |
| 20:21:55 | formal protocol | screening | in-flight | partial metrics `21133b6c...json`, `complete=false`, `attempted_pairs=3/16` |
| 20:21:56 | campaign | stop | stopped | `SIGTERM`，未形成 `ProtocolResult` 或正式 experiment event |

## Session 重点分析

### `b9478e1a` - `adaptive_sa_reheat` hypothesis

- 工具读取了 problem/surface/active solver design/call graph，以及 `local_search.py`、`destroy_repair.py`、`acceptance.py`、`construction.py`、`baseline_algorithm.py`。
- screening/runtime feedback 返回 0 safe rows，合理。
- LLM 前两次 hypothesis 尝试集中在 spatial/cluster removal，telemetry activation 使用 aggregate/effect 字段，被 C11/novelty feedback 拦截。
- 第三次提出 `adaptive_sa_reheat`，使用机制专属 telemetry，schema/target preview 通过。
- v3 判定：正常。无效 telemetry 没有越过 schema/preview 边界。

### `3aeb76dc` - `adaptive_sa_reheat` code

- code prompt 包含 active algorithm facts、完整 target file、branch integration files 和 prior context。
- 三次 patch 都在 Contract preview 阶段失败 C9e。
- 失败点是 `reheat_count`：
  - agent 新增 `@property reheat_count`
  - scheduler 通过 `annealing.reheat_count` 属性读取
  - C9e helper reachability 没把 property attribute load 识别为 integrated use
- 判定：
  - CVRP adapter C9e 过严或 guidance 不清，是直接失败点。
  - agent 没有根据两轮 C9e feedback 修掉 property，也有 patch 质量问题。
  - 框架正确 failed closed，没有进入 smoke/formal。

### `9523b040` - repeated `adaptive_sa_reheat`

- session 有 tool context，但没有新的实质 hypothesis LLM prompt manifest。
- gate 基于 campaign history 拒绝同 mechanism id `adaptive_sa_reheat`，理由是刚刚同 branch/target 在 code_generation 失败。
- 对 fresh proposal 合理。
- 对 code repair retry 不清晰，因为上一轮 code failure 后 pending hypothesis 仍进入后续尝试。这里是 P1 framework lifecycle ambiguity。

### `277fc06a` - `random_segment_removal` hypothesis

- 工具读取 destroy/acceptance/local search/config/entrypoint 等文件。
- 前两次 hypothesis 尝试的 telemetry activation 仍不合规或语义不足。
- 第三次 `random_segment_removal` 使用：
  - activation: `solver_algorithm_context_records.random_segment_removal_iterations`, `solver_algorithm_phase_runtime_ms.random_segment_removal`
  - effect: `solver_algorithm_phase_improvement_counts.random_segment_removal`, `solver_algorithm_phase_best_delta.random_segment_removal`
- schema/target preview 通过。
- v3 判定：正常。telemetry descriptor/surface 约束有效地把 agent 从聚合字段引导到机制专属字段。

### `25b2d0a7` - `random_segment_removal` code

- code prompt 有完整 target file 和 integration context。
- 第一次 patch 加 segment destroy operator，Contract 过，但 smoke 缺 declared activation。
- 第二次仍缺 activation。
- 第三次加上 `record_iteration("random_segment_removal", 1)`、phase runtime，以及 accepted move effect telemetry；Contract 和 smoke 通过。
- APS session `completed` 后启动 formal screening。
- formal screening 被 `SIGTERM` 截断，留下 partial metrics，但没有正式 experiment event。
- v3 判定：proposal/code/smoke 阶段正常；formal protocol 没完成，所以不计实验也正常。

## telemetry/provenance 修复是否异常

未见异常。

- C11 正确拒绝 aggregate/effect-as-activation。
- smoke 正确拒绝缺 declared activation 的 patch，并在第三次 code repair 后通过。
- partial formal metrics 中只记录 selected surface runtime summary，符合 runtime telemetry behind surface declarations。
- `validation_repair_required` 没有触发，因为 formal protocol 没完成到 validation/Decision。
- active facts digest 在 hypothesis/code prompt manifests 中稳定；repeated mechanism rejection 的 provenance 为空可解释为 campaign-history duplicate，而非 active-fact contradiction。但建议增加 previous session/hypothesis id 便于审计。

## 建议修复与测试

1. **P1 status/reporting**: stop 时记录 `in_flight_protocol`：
   - phase: `formal_screening`
   - metrics ref: `metrics/21133b6c-3703-4ced-8989-055255c6bebc.json`
   - progress: `attempted_pairs=3`, `total_pairs=16`
   - last completed result 与 current in-flight attempt 分开显示
2. **P1 lifecycle**: 明确 code_generation failure 后的 pending policy：
   - 若允许 repair，同一 approved hypothesis 的 code retry 不走 repeated mechanism duplicate gate
   - 若不允许 repair，第一次 code_generation failure 后直接关闭 pending hypothesis
3. **P1 CVRP C9e**: 为 property getter reachability 增加测试，或在 prompt/contract 中禁止 property helper 并给出短反馈。
4. **测试**:
   - `test_sigterm_during_formal_screening_keeps_n_experiments_zero_and_reports_inflight`
   - `test_completed_aps_partial_metrics_no_experiment_event`
   - `test_pending_code_retry_not_blocked_as_duplicate`
   - `test_cvrp_c9e_property_getter_reachability_or_guidance`

最高优先级：先修 status/reporting 的 in-flight protocol 可观测性，因为它直接解释本次“completed session 有 metrics 但无 event”的困惑；随后修 pending code retry 与 duplicate mechanism gate 的生命周期冲突。
