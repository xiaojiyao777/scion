# CVRP R56 Corrected-Runtime Minute One-Step Preregistration — 2026-08-12

## Append-only launch note — 2026-08-12T15:05:33Z

The user supplied the exact fresh authorization:

> 我明确授权将 acdc80ba 的 fresh-B0 CVRP problem-owned 源码派生上下文、R53–R54
> 中性算法证据先验和 R56 预注册研究问题，通过本机 Codex proxy 发送给
> gpt-5.6-terra，最多执行 1 次 H 和条件式 1 次 C，并运行一次 R56
> 分钟级 one-step screen。

The frozen ordinary-user launcher was then invoked exactly once. Its control
marker records successful inner-process acquisition at `2026-08-12T15:05:33Z`
with inner PID `277510`. The PID is only a startup observation, not a durable
identity, continued-liveness claim or terminal result. The acquisition-time
lifecycle state is `AUTHORIZED / LAUNCH_ACQUIRED / IN_FLIGHT`; this note reads
no H/C response, raw result, gate, Decision or scientific outcome. A terminal
classification must be appended separately after bounded read-only postrun
inspection. The pre-launch fields and frozen scientific body below remain the
historical preregistration and are not rewritten by this note.

**Preparation status:** `PREPARED_NOT_STARTED`

**Authorization state:** `AWAITING_FRESH_SOURCE_SEND_AUTHORIZATION`

No provider or formal Protocol call has started. The frozen input bundle exists,
while the future output root, process-control root and tmux socket were absent
when this preregistration was written. R56 is one short post-R55 observation;
it is not an R54 retry, a long campaign or a promotion run.

## Question, purpose and V3 authority

The H request will ask exactly:

> Starting from fresh B0 under the fixed CVRP deadlines, what one CVRP-owned
> algorithm change is most likely to improve final-best total_distance while
> preserving feasibility, fleet semantics, and useful search within the
> available budget? Use the visible source and the supplied R53–R54 algorithm
> evidence to choose one causal mechanism and state the observable trajectory
> that would falsify it.

The immediate purpose is to observe whether one fresh-B0 algorithm-research
step completes cleanly after the narrow R55 persistence and Verification
corrections. The question remains about the algorithm. It does not require a
repair of R54, a novel or materially different mechanism, a particular file,
parameter, phase or implementation shape. Repeating, refining or avoiding a
prior idea is not a host-side acceptance condition.

[`design/scion-architecture-v3.md`](../../../design/scion-architecture-v3.md)
is the sole architecture authority. The direct-runtime addendum may describe
the lightweight implementation but cannot replace V3 ownership. The active
chain remains:

```text
problem-owned facts + complete fresh-B0 source + safe algorithm evidence
  -> at most one structured H
  -> Hypothesis Contract
  -> at most one approved-H-bound C
  -> Patch Contract
  -> isolated Workspace
  -> executable Verification
  -> canary + problem-owned paired SCREENING Protocol
  -> Safe Features
  -> deterministic Decision
```

No novelty, implementation-style, activation, telemetry-quality or host-authored
algorithm gate is added. LLM output remains tainted and cannot choose cases,
seeds, Protocol results, Decision actions or promotion.

## R55 host-side correction boundary

The clean R56 runtime includes the two already-completed R55 corrections:

- canonical screening context can persist the exact valid-pair feedback when a
  separate candidate-failed pair has no W/L/T feedback; and
- CVRP V3/V4 now execute the problem-owned subsecond recovery-path and public
  solver-entrypoint tests instead of skipping.

These are runtime and Verification facts, not algorithm evidence for H. The
R55 persistence defect, test implementation and provider-free R54 fixture
replay are deliberately excluded from the provider-visible question and prior.
The tests retain their ordinary V3 semantic-consistency role and add no quality,
novelty, research-direction or promotion gate.

Before source-send authorization, provider-free static bundle checks and the
subsecond V3/V4 microtests may run. They must not call the proxy or provider and
must not invoke canary or formal Protocol evaluation. This preparation evidence
cannot be reported as an R56 algorithm result.

## Fresh source-send authorization boundary

The H request would send problem-owned source-derived context from the frozen
runtime, problem facts, the exact question above and the exact R53–R54 prior
through the local Codex proxy to `gpt-5.6-terra`. If H passes Contract, C would
receive that exact approved H plus the ordinary complete editable-source
context.

This is a new provider experiment. R53 and R54 authorizations do not carry
forward, and a generic acknowledgement such as “可以” does not identify or
authorize this exact R56 source send. Launch requires fresh explicit user
authorization covering the `acdc80ba` fresh-B0 CVRP source-derived context,
the R53–R54 algorithm prior, the R56 question, at most one H and conditional
one C call to `gpt-5.6-terra`, and this one minute-scale screen.

## Frozen source, model and call envelope

| Input | Frozen value |
|---|---|
| Campaign label | `v04-cvrp-r56-minute-corrected-one-step-20260812` |
| Schema | `scion.cvrp_r56_minute_corrected_one_step.v1` |
| Input bundle | `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r56-minute-corrected-one-step-20260812-input` |
| Runtime | one bundle-owned clean `git archive`; no dirty-worktree overlay |
| Exact source reference | `acdc80ba5de27fad5f6c56cbb6daf56fef2bbbb1` |
| Problem/solver source | `runtime/scion/scion/problems/cvrp` |
| External case root | `/home/clawd/research/or-autoresearch-agent/vrp` |
| Champion | original CVRP B0, version `1` |
| Provider | local Codex proxy at `http://127.0.0.1:8080` |
| Model | `gpt-5.6-terra` |
| H calls | at most one; 90-second provider timeout |
| C calls | at most one, only after approved H; 180-second provider timeout |
| Provider retries | zero |
| Campaign-manager calls | `run_one_step()` exactly once; `run()` never |

Relative to `e4b6b98d`, the R56-relevant executable changes are the
problem-spec test bindings, the two problem-owned Verification tests and the
context-persistence correction; the B0 solver policy source is unchanged. The commit is an
ordinary scientific source reference, not an identity, receipt, signature,
registry, Trust/Hash or authority object.

The provider surface remains the direct-runtime structured H and typed exact
edit/full-file C surface. Complete visible problem-owned source is provided
through the ordinary context. No provider shell, network, execution,
file-navigation or same-call edit/repair loop is added.

## Frozen provider-visible R53–R54 prior

H receives one additional guidance block with id
`cvrp_r53_r54_algorithm_evidence_prior`, title
`R53-R54 algorithm evidence`, and the following text exactly once:

> R53 started from B0 and changed only the global embedded-VNS runtime-share
> cap from 0.0 to 0.35. On four fresh seed-11 pairs it completed without
> execution, feasibility, or fleet failure: P-n65 was 16 distance units worse
> and E-n101, X-n120, and X-n233 tied. The cap activated and ALNS iterations
> increased from 44 to 151, but best updates fell from 7 to 4 and final quality
> did not improve. This is negative evidence only for that tested cap on that
> roster, not for phase allocation or any other mechanism. R54 again started
> from B0. Its H used R53 evidence to propose elapsed-budget-normalized simulated
> annealing, but C left three recovery-path calls to a method it removed. The
> formal screen retained three valid ties and one typed candidate runtime failure;
> the failed row has no admissible delta, so the quality of the proposed R54
> mechanism is unidentifiable rather than negative. These facts show feedback
> uptake followed by incomplete code expression. They do not prescribe
> repairing simulated annealing, selecting any mechanism, parameter, or file,
> or repeating or avoiding either prior proposal.

The failed R54 row remains excluded from delta and W/L/T. The prior therefore
does not convert R54 into an algorithm negative or positive, and it cannot
enter Decision or relax Contract, Verification, feasibility, fleet or Protocol
semantics.

## Verification, canary and frozen screening population

Verification uses a two-second runtime limit and the configured problem-owned
V3/V4 tests. Protocol canary uses
`controlled/data/synthetic_controlled_canary_5.vrp`, seed `101`, with a
two-second per-subject limit. Canary is veto-only and is not improvement
evidence.

If Contract and Verification pass and canary does not veto, the only reachable
formal stage is one initial, non-expanded `SCREENING` call over seed `11`:

| Ordinal | Case | Per-subject limit | Scheduled order |
|---:|---|---:|---|
| 0 | `cvrplib/P/P-n65-k10.vrp` | 8 s | `AB` |
| 1 | `cvrplib/E/E-n101-k14.vrp` | 12 s | `BA` |
| 2 | `cvrplib/X/X-n120-k6.vrp` | 12 s | `AB` |
| 3 | `cvrplib/X/X-n233-k16.vrp` | 15 s | `BA` |

`A` is fresh B0/v1 and `B` is the fresh R56 candidate. Candidate and block
ordinals are zero; case ordinals are `0..3`; seed ordinal is zero. Actual order
must equal scheduled order. Champion caching is disabled, so both subjects in
all declared pairs execute fresh. The paired block id is
`v04-cvrp-r56-minute-corrected-one-step-20260812-screening`; maximum declared
formal solver time is 94 subject-seconds.

The estimator remains paired-effect median with zero equivalence band.
`total_distance` is lower-better and `fleet_violation` is protected. The
practical screening delta remains `2`; the unchanged screening rule retains
case win rate `0.60`, bootstrap-CI lower bound `0`, minimum net case score
`0.25`, and maximum case loss rate `0.20`.
`require_expanded_for_pass=false` because this is a one-step observation, not
an expansion campaign.

Schema-valid, disjoint placeholders remain unreachable: validation uses
`cvrplib/B/B-n67-k10.vrp`, seed `47`; frozen uses
`cvrplib/P/P-n76-k4.vrp`, seed `61`. The wrapper rejects expanded screening,
validation and frozen execution. It calls `finalize_requested_stop()`
immediately after the single returned step; a queued Decision is recorded but
never drained.

## Why seed 11 is retained and what it cannot show

R56 retains the exact R53/R54 seed, cases, limits, fresh-subject policy and
AB/BA schedule to minimize measurement changes during the first post-R55
short lifecycle observation. This makes process and exact-roster behavior more
comparable; it does not create an independent algorithm comparison across the
three candidates.

R53 evidence shaped R54, and both R53 and R54 now shape R56. The seed-11 roster
is therefore twice adaptive. Even a positive complete R56 screen cannot
establish independent replication, seed robustness, case generalization,
validation, frozen retention, promotion, retained B0 improvement or readiness
for a long campaign. Seed `29` is not reachable here. Any exact-candidate
replication or further short run must be separately preregistered and
authorized after read-only postrun analysis.

## Execution and process envelope

- Local solver subprocess limits are 45 seconds, 2,048 MiB and 256 file
  descriptors. The driver hard wall is 720 seconds, delivered through
  foreground `SIGINT` with a bounded kill-after fallback.
- Launch is ordinary-user `clawd` only through the user-owned tmux socket
  `/tmp/scion-r56-minute-corrected.tmux.sock` and session
  `scion-r56-minute-corrected`, with remain-on-exit and bounded acquisition
  observation. No root, systemd, deployment, distribution, packaging or build
  step is involved.
- This two-core host runs no concurrent solver campaign, pytest, benchmark or
  other intentionally CPU-heavy task during R56. The pre-launch conflict check
  is load hygiene, not a candidate, research or quality gate.
- The inner process starts from minimal `env -i`. The non-`SCION_*` ephemeral
  `CODEX_PROXY_CLIENT_KEY` is removed before candidate/champion subprocesses
  and passed explicitly only to the provider client. It is not an argv, log or
  scientific artifact field.
- Proxy authentication and model acquisition are bounded. Output, control and
  socket roots must be fresh. The driver creates output once, writes terminal
  status atomically and has no resume or retry path.

Frozen future roots are:

- output:
  `/home/clawd/research/scion-experiments/v04-cvrp-r56-minute-corrected-one-step-20260812-claw`;
- process control:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r56-minute-corrected-one-step-20260812-control`;
- input:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r56-minute-corrected-one-step-20260812-input`.

## Completion, terminal classification and claim boundary

A normal return from the single `run_one_step()` followed by successful
finalization is `COMPLETED_ONE_STEP_WRAPPER`, regardless of H/C rejection,
canary veto, candidate failure or Protocol quality result. The hot-path driver
does not parse raw metrics into another gate. Only an exception or violation
of the declared one-step lifecycle makes the wrapper invalid. The normal
terminal stop reason is `r56_minute_corrected_one_step_complete`.

Independent read-only postrun analysis may apply only these bounded
interpretations:

| Terminal evidence | Postrun boundary |
|---|---|
| Four complete fresh successful pairs plus a typed Decision | One valid twice-adaptive exploratory screen only |
| Typed candidate canary veto | Bounded candidate-canary negative; no screen comparison |
| Complete formal attempt containing candidate-side failure | Terminal candidate-execution negative; failed objectives excluded and four-case W/L/T undefined |
| Complete formal attempt containing champion-side failure | Partial champion evidence; candidate comparison unavailable |
| Typed terminal H/C rejection, including a malformed/schema-invalid response, or Contract/Verification rejection before Protocol | Valid terminal research/process observation; no solver-quality conclusion |
| Partial, interrupted, resource or configuration failure; malformed/untyped artifact; provider call without a terminal response; or framework exception | `RUN_INVALID_*` at the exact failed layer; no scientific comparison |

None of these labels may alter Decision, request another provider call, resume
the root or enter another formal stage. A complete clean run is one
post-correction lifecycle observation, not proof of stability or authorization
for a long campaign.

Champion must remain B0/v1. No `PROMOTE` is admissible; no expanded screening,
validation, frozen holdout, final replay or independent retention runs. A
positive screening Decision may remain queued for evidence purposes, but this
wrapper cannot drain it. `campaign_summary.run_complete`, if present, refers
only to the bounded wrapper lifecycle.

## Launch and postrun rule

Launch remains blocked on fresh explicit R56 source-send authorization. Once
that exact authorization exists, the frozen launcher may run once without
changing source, question, prior, model, call/time limits, tests, cases, seed,
order, cache policy, Protocol or claims. Acquisition, provider, candidate,
champion or host failure does not authorize a retry.

After terminalization, read-only analysis must separate H algorithm reasoning,
C expression fidelity, applied source, Contract, each Verification check,
canary, raw pair evidence, Protocol, Decision and wrapper/process stability.
The result may motivate another separately preregistered short step. It cannot
launch that step, a seed-29 replication or a long promotion campaign itself.

## Append-only terminal note — 2026-08-12

R56 terminalized normally with launcher exit `0` and wrapper classification
`COMPLETED_ONE_STEP_WRAPPER`. It used exactly one H and one conditional C; the
recorded Contract passed, then V3 executed the three problem-owned recovery
fixtures and rejected all three on residual calls to the removed
`_SimulatedAnnealing.cool()` method. V4 and all later checks, canary, formal
Protocol and Decision were not reached. B0/v1 remained champion.

Independent source/expression audit found four residual `annealing.cool()`
calls in total: the three paths observed by V3 plus one latent ordinary
loop-tail path. The sealed classification is
`VALID_TERMINAL_RESEARCH_PROCESS_OBSERVATION / VERIFICATION_LIGHT_REJECTED /
ZERO_PROTOCOL_EVIDENCE / NO_DECISION / NO_PROMOTION`. See the
[postrun](v0.4-cvrp-r56-minute-corrected-one-step-postrun-20260812.md). The
pre-launch fields and scientific body above remain historical frozen text;
this terminal note authorizes no retry or successor.
