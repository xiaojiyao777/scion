# CVRP R3 Ordinary-Lineage R45 Diagnosis R1 — 2026-08-10

**Status:** `RUN_INVALID_INFRA / ZERO_ACCEPTED_BLOCKS / NO_ADMISSIBLE_ANALYSIS`

## Post-run terminal note

R1 launched once into
`/home/clawd/research/scion-experiments/v04-cvrp-r3-ordinary-lineage-r45-diagnosis-20260810-claw`
and is now sealed. Its terminal structural state is `complete=false`,
`accepted_blocks=0/37` and `last=null`. The sole in-progress `MDE` raw file
stopped at 46/96 attempted and valid rows with zero recorded subject failures;
its last write was `2026-08-10T08:42:03Z`. No analysis artifact exists, and no
driver or solver process remains on the host.

The classification is
`RUN_INVALID_INFRA / ZERO_ACCEPTED_BLOCKS / NO_ADMISSIBLE_ANALYSIS`; the cause
is `PROCESS_DISAPPEARANCE_CAUSE_UNIDENTIFIABLE`. No W/L/T, effect, gate or
objective value was read or reported. The partial raw file is not an accepted
atomic block and cannot be analyzed, resumed, completed, copied, concatenated
or reused by a replacement run. This root will not be modified.

The remainder of this document is the frozen pre-launch R1 registration. Its
future-tense launch statements are historical design text, not current status.

## Question and authority boundary

This provider-free diagnosis asks whether CVRP conclusions change with case
count, seed count or dimension budget when every exact R3 candidate is compared
fresh with its immediate base. It also estimates the sensitivity of the exact
12-case by 8-seed case-median rule. It does not rejudge R3, select a candidate
for promotion or enter Safe Features or Decision.

The sole architecture authority remains `design/scion-architecture-v3.md` and
the only promotion authority remains
`Contract -> Verification -> Protocol -> Safe Features -> Decision`. This run
adds no provider call, candidate gate, formal-candidate recorder, identity,
ledger, lease, signing, receipt, hash chain, deployment, distribution, build,
root or systemd work. R3 remains sealed
`RUN_INVALID_INFRA / VALID_PARTIAL_SCIENCE`; its incomplete validation is not
read, resumed, retried, combined or adjudicated.

## Frozen implementation and source inputs

- R45 is the default-off paired-diagnosis seam at `c32f5b8a`. Its four
  production files have net growth of exactly 100 lines. Forty-six focused and
  adjacent tests and an independent review passed. With the option absent, the
  ordinary Protocol API, cache path, raw schema and Decision-facing result stay
  unchanged.
- The prepared, existing input bundle is
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r45-diagnosis-20260810-input`.
  It contains the clean ordinary Git-archive runtime at `c32f5b8a`, the exact
  experiment-owned driver and test source, and `diagnosis-manifest.json`. The
  driver and its test are not committed to Scion and are not framework
  lifecycle authority.
- The earlier 220-line cap was an engineering warning against growing a second
  orchestration framework. Complete formatting, source checks and bounded
  diagnostics make the one-off driver larger than that number. Before launch
  and without reading outcomes, the constraint is therefore narrowed: the
  Protocol seam remains at its 100-line production ceiling, while the complete
  one-off driver/test source is preserved only with this experiment and does
  not enter Scion history.
- The sole launch and output root is
  `/home/clawd/research/scion-experiments/v04-cvrp-r3-ordinary-lineage-r45-diagnosis-20260810-claw`.
  It remains absent and must be created once by the run, as user `clawd`, only
  after CPU exclusivity and the source checks below pass. The prepared input
  bundle is not an output root and must not receive run results.

No digest or self-attestation is needed: ordinary Git source, preserved tool
source, exact cohort inputs and frozen byte snapshots of the composed
workspaces provide the necessary deterministic boundary.

## Frozen outcome-blind cohort

The cohort evidence root is
`/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-r3-ordinary-lineage-diagnosis-20260810-cohort-r3`.
Its materializer replayed every Contract- and Verification-passed terminal C
response in proposal-time order, including the primary edit and all ordered
`additional_changes`. It selected no Protocol metric or solver result.

The frozen result is:

- six chronological, unique exact candidates (`candidate-01` through
  `candidate-06`), with ordinals `0` through `5`;
- each candidate has its true exact immediate base, which is either B0 or the
  preceding verified candidate on that branch;
- all three branch terminal reconstructions are byte-equal to their durable
  workspaces across 53 ordinary `*.py` files each;
- one C classified as `research_rejection` is recorded as an omission and is
  not materialized;
- an independent outcome-blind review passed.

Earlier r1/r2 materializer outputs were wrong because that offline analyzer
applied only each response's primary edit and omitted `additional_changes`.
They are excluded. That defect is not Scion runtime drift and creates no reason
to add a recorder or source-identity layer.

## Immutable populations and execution order

Case and seed ordinals always refer to the complete frozen R3 quality roster;
they never restart inside a block:

| Block ordinal | Label | Case ordinals | Seed ordinals | Budget |
|---:|---|---|---|---|
| 0 | `Q00` | 0–7 | 0–3 | `1x` |
| 1 | `Q01` | 0–7 | 4–7 | `1x` |
| 2 | `Q10` | 8–11 | 0–3 | `1x` |
| 3 | `Q11` | 8–11 | 4–7 | `1x` |
| 4 | `.5x` | 0–7 | 0–3 | `0.5x` |
| 5 | `2x` | 0–7 | 0–3 | `2x` |
| 6 | `MDE` | 0–11 | 0–7 | `1x` |

There are exactly 37 atomic executions, in this order:

1. independent B0-versus-B0 A/A over `MDE`, using diagnostic candidate
   ordinal `6`;
2. `Q00` for candidate ordinals `0..5`, then `Q01`, `Q10` and `Q11` in the
   same ordinal order;
3. `.5x` for candidate ordinals `0..5`;
4. `2x` for candidate ordinals `0..5`.

The complete expectation is 1,056 unique pair rows and 2,112 fresh solver
executions. Every call uses the existing `ExperimentProtocol` with stage
`SCREENING`, `expand=False`, problem measurement enabled, the frozen split and
seed subsets, and `selected_surface="solver_design"`. A is always the exact
immediate base and B is always the exact candidate; B0 A/A uses the same exact
B0 on both sides. Cache hit, miss, write and cached-runtime counts must all be
zero.

Scheduled order is AB when
`(candidate_ordinal + block_ordinal + case_ordinal + seed_ordinal) mod 2 == 0`
and BA otherwise. Actual order must exactly equal the schedule. Only the R45
raw record may contain block/case/seed ordinals, scheduled/actual order, both
full objective mappings, success, feasibility, elapsed/time limit and one
bounded failure or exit fact. None of those fields becomes a Protocol result,
Safe Feature or Decision input.

## Completeness and stop rules

After composing every subject workspace, freeze one byte snapshot of all
ordinary `*.py` files across the complete composed-workspace set. Before and
after every block, compare every composed workspace globally with that frozen
in-run snapshot. The external cohort directories are input sources, not a
per-block comparison authority. Before analysis, require exactly `N=6`, all 37
blocks and all 1,056 unique pair rows, plus exact ordinal coverage, correct
order, complete A/B records, successful and feasible subjects, full objectives,
zero cache activity and no source mutation. CPU-heavy tests, other solver runs
and other deadline-driven work are prohibited for the whole execution.

Any missing or duplicate row, schedule mismatch, cache activity, subject
failure, infeasibility, absent objective, exception, source mutation or CPU
overlap stops the run as incomplete. An overlapped block is contaminated as a
whole. No intermediate W/L/T, effect, gate or candidate ranking is read or
changed, and no favorable-result retry or selective pair deletion is allowed.
The progress file records only completed/expected block counts and the last
block label; it contains no intermediate effect.

## Frozen analyses

### Case and seed factors

Aggregate immutable blocks only after complete execution:

- `Q00` reconstructs `8x4`;
- `Q00 + Q01` reconstructs `8x8`;
- `Q00 + Q10` reconstructs `12x4`;
- `Q00 + Q01 + Q10 + Q11` reconstructs `12x8`.

For each candidate and cumulative cell, report pair W/L/T and the ordered case
medians in frozen case-ordinal order, followed by case W/L/T, net score, loss
rate, median effect and the existing deterministic bootstrap interval.
Candidate is the cohort unit (`N=6`); the 96 pairs are not independent
candidates.

At cohort level, report only ordered per-candidate descriptive contrasts:

- seed-added: `8x8` versus `8x4`;
- case-added: `12x4` versus `8x4`;
- joint-to-full: `12x8` versus `8x4`;
- budget response: `0.5x` versus `1x`, and `2x` versus `1x`, on the same
  `8x4` core.

These contrasts are descriptive, not causal factor estimates. `Q11` remains a
joint added-case/added-seed block, the case-by-seed interaction remains
`UNIDENTIFIABLE`, and whole-budget-arm machine drift remains
`UNIDENTIFIABLE`. Family/size contrasts are likewise bounded by the fixed
roster and its confounding.

### Matched MDE

Use only the independent B0 A/A `12x8` block. Perform 2,000 sign swaps of its
matched pair noise using random seed `4506`. Within each swap and each
homogeneous additive effect on a 0.25 distance-unit grid, take the median across
eight seeds within each of 12 cases, then apply the full expanded quality rule:

- case net score at least `0.25`;
- case loss rate at most `0.20`;
- median distance improvement at least `2`;
- deterministic 95% bootstrap-median CI low at least `0`, using 1,000 samples
  and seed `42`;
- no failed, infeasible or protected-fleet-regressing subject.

The empirical MDE is the smallest grid value with at least 80% pass
probability, equivalently the 80th percentile of per-swap minimum passing
effects at 0.25 resolution. It supports only a homogeneous additive effect on
this fixed 12-case roster. The independent unit is the case, not each seed or
pair, and this calibration is not a candidate futility gate.

### Dimension-budget response

Reuse `Q00` as the `1x` arm. The fixed half-up dimension bands are:

| Arm | Dimension-budget bands (seconds) |
|---|---|
| `0.5x` | `15/23/30/45/60` |
| `1x` | `30/45/60/90/120` |
| `2x` | `60/90/120/180/240` |

The subprocess hard guard is 300 seconds. Because the frozen order completes
all `1x` blocks before all `0.5x` and `2x` blocks, it cannot counterbalance
whole-arm machine drift. Mark that attribution `UNIDENTIFIABLE`; cross-arm
budget trends are descriptive only. Within each arm, fresh AB/BA comparison of
candidate with its immediate base remains the supported contrast. Missing
activation telemetry limits only mechanism attribution, not a complete paired
objective response.

## Launch and interpretation

Launch once only from the prepared input bundle after its clean Git-archive
runtime, exact external driver/test source and manifest are rechecked together
with the frozen cohort, the still-absent sole output root and CPU exclusivity.
The solver-time upper bound is approximately 36 hours for six candidates. The
run is not yet started.

Only a complete terminal analysis may inform the design of a separately
preregistered fresh Terra promotion campaign. It cannot promote, reject or
recover an R3 candidate, and it cannot change R3's reboot classification or
use its incomplete validation.
