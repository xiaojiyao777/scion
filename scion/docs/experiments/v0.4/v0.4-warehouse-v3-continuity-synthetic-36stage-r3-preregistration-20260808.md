# Warehouse V3 Continuity Synthetic 36-Stage R3 Preregistration — 2026-08-08

**Status:** `PRE_REGISTERED_NOT_RUN`

## Repair boundary

R1 failed before campaign creation because the synthetic data root was absent.
R2 fixed that input but its sole H request received 401 because the inherited
`SCION_API_KEY` was unrelated to the local proxy. R2 has no evaluated candidate
and no scientific result. This R3 changes only the fresh campaign root and the
client-key source: the key is read ephemerally from the local proxy's
localhost-only authenticated status endpoint and is never printed or written.

All scientific inputs, execution/stop policy, analysis plan, outcome classes
and exclusions remain frozen by the R1/R2 preregistrations.

## Frozen inputs

| Input | Pre-registered value |
|---|---|
| Runtime source/code checkpoint | `/home/clawd/research/or-autoresearch-agent-v3-hotpath/scion`, `68413a34` |
| Campaign directory | `/home/clawd/research/scion-experiments/v04-warehouse-v3-continuity-synth-36r-r3-gpt56terra-20260808T163049Z-claw/campaign` |
| Problem/Protocol | `problem.yaml` / `protocol.yaml` (`3.0`) |
| Split/seeds | `split_manifest.yaml` (`4.0`, 47/47 paths present) / `seed_ledger.yaml` (`3.0`) |
| Synthetic data root | `/home/clawd/research/or-autoresearch-agent/surrogate` |
| Provider/model | local Codex proxy at `127.0.0.1:8080`, `gpt-5.6-terra` |
| Proxy client credential | ephemeral `/auth/status.proxy_api_key`; verified against `/v1/models`, never printed/persisted |
| Protocol-stage horizon | 36 typed evaluated stages |
| Solver subprocess limit | 30 seconds per case/seed execution |

## Frozen command

```bash
cd /home/clawd/research/or-autoresearch-agent-v3-hotpath/scion

PROXY_KEY_VALUE="$(curl -fsS http://127.0.0.1:8080/auth/status | \
  jq -er '.proxy_api_key | select(type == "string" and length > 0)')"

PYTHONPATH=/home/clawd/research/or-autoresearch-agent-v3-hotpath/scion \
SCION_MODEL=gpt-5.6-terra \
SCION_BASE_URL=http://127.0.0.1:8080 \
SCION_API_KEY="$PROXY_KEY_VALUE" \
SCION_WAREHOUSE_DATA_ROOT=/home/clawd/research/or-autoresearch-agent/surrogate \
  /home/clawd/miniconda3/envs/claw/bin/python -m scion.cli.main run \
  --problem problems/warehouse_delivery/problem.yaml \
  --protocol problems/warehouse_delivery/protocol.yaml \
  --split problems/warehouse_delivery/split_manifest.yaml \
  --seeds problems/warehouse_delivery/seed_ledger.yaml \
  --campaign-dir /home/clawd/research/scion-experiments/v04-warehouse-v3-continuity-synth-36r-r3-gpt56terra-20260808T163049Z-claw/campaign \
  --rounds 36 \
  --time-limit-sec 30

unset PROXY_KEY_VALUE
```

Run once from a clean worktree. Do not resume R1/R2, retry provider calls,
modify inputs, stop on intermediate scientific outcomes, or expose the proxy
client key in logs or reports.
