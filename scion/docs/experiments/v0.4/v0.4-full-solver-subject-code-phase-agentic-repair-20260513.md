# v0.4 Full Solver Subject Code-Phase Agentic Repair

*Date: 2026-05-13*

## Experiment

```text
run_root=/home/clawd/research/scion-experiments/v04-full-solver-subject-validation-feedback-repair-sonnet-8r-20260513T160209Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=8
rounds_observed_before_repair=5
screened_experiments=3
time_limit_sec=60
agentic_proposal=true
agentic_session_timeout_sec=1800
force_surface=none
started_utc=2026-05-13T16:02:09Z
status=manually_terminated_for_code_phase_agentic_repair
```

The run was stopped after the problem was clear enough to repair. Continuing it
would have mixed old APS behavior with the framework changes.

## Results

- Round 1 generated a direct `policies/solver_algorithm.py` full-algorithm
  candidate and reached screening. It was valid but worse than champion:
  `win_rate=0.0`, `median_delta=-98.5`, and very low runtime ratio. The
  candidate moved, but its from-scratch construction/search quality was below
  the repo-local ALNS+VNS champion.
- Round 2 generated a baseline-seeded solver body. It reached screening with
  16/16 valid pairs but did not beat champion: `win_rate=0.0`,
  `median_delta=0.0`, runtime ratio around `0.756`.
- Round 3 generated baseline plus adaptive LNS/SA. It also reached screening
  with 16/16 valid pairs and no wins: `win_rate=0.0`, `median_delta=0.0`,
  runtime ratio around `0.880`.
- Round 4/5 formed a more distinct population/recombination hypothesis, but
  code generation timed out. No patch reached screening for that branch.

All screened candidates stayed on `solver_design` and targeted
`policies/solver_algorithm.py`. Contract and Verification passed for the three
screened candidates. This is a boundary success, not solver-quality success.

## Trace Finding

The experiment exposed an APS architecture gap. The hypothesis/planning stage
used proposal tools such as `context.read_surface`, `context.read_problem`,
`memory.query`, `feedback.query_screening`, and `feedback.query_runtime`.

The code stage did not. Code traces were single `generate_patch` calls with
large static prompts, growing from roughly 49k to 58k characters. They received
the observations gathered earlier, but could not interactively query memory,
branch state, runtime feedback, or the full problem object while drafting code.
The later timeout is a direct symptom of this static prompt pressure.

## Interpretation

The failure is not explained by tool access alone.

The framework-side failure was that Scion treated the second round as a
single-shot structured code generator. That is too weak for whole-algorithm
research. Scion should control boundaries, permissions, taint, audit trail, and
protocol state; the research core still needs to be an agent that can inspect
the problem object, history, and feedback while implementing the algorithm.

The algorithm-side failure was also real. The first candidate was too far below
champion despite being fast. The later candidates leaned heavily on the
repo-local baseline and only had limited post-baseline budget, so they mostly
tied the champion rather than escaping its local optimum. Runtime is part of
the optimization target: spending most of the budget on baseline leaves too
little room for a genuinely different search body, but from-scratch search
quality is currently too weak to replace it.

## Repair

The APS repair adds a code-phase tool loop after ContractGate-approved
hypothesis context and before final patch generation:

- code phase can call bounded proposal tools such as `context.read_surface`,
  `context.read_problem`, `context.read_objective_policy`,
  `context.read_champion_summary`, `context.read_branch_state`,
  `memory.query`, `feedback.query_screening`, and `feedback.query_runtime`;
- code phase receives a full selected-surface read within budget instead of
  relying only on an 800-character compact preview;
- final patch generation still emits a normal `PatchProposal`;
- deterministic Contract preview remains the gate before workspace
  materialization;
- a failed Contract preview is fed back into one bounded patch-regeneration
  attempt before the session fails closed.

The repair keeps validation/frozen/raw metrics hidden and does not allow the
proposal agent to mutate workspaces, promote, abandon, or change objective and
constraint semantics.

## Next Validation

Run a short direct `solver_design` experiment from the repaired code path.
Success criteria are:

- code-phase traces include `phase="draft_patch"` tool-selection contexts;
- code observations include a full `context.read_surface` for
  `policies/solver_algorithm.py`;
- generated candidates continue to pass Contract/Verification without touching
  original CVRP runtime files;
- screening shows either real wins or a clearer algorithmic failure pattern,
  not another static-prompt timeout loop.

## Follow-Up Micro Smoke

```text
run_root=/home/clawd/research/scion-experiments/v04-code-phase-agentic-repair-sonnet-4r-20260513T175917Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=4
rounds_completed=3 scheduler steps, 0 experiments
time_limit_sec=60
agentic_session_timeout_sec=1800
git_commit=f77b263c56239b18d1319fc72acc0fa187a686d8
finished_utc=2026-05-13T18:11:33Z
status=stopped_by_api_balance_exhaustion
```

Observed:

- The code-phase tool loop did run. The approved-hypothesis session transcript
  contains `Starting code-phase proposal tool loop for approved hypothesis`,
  followed by `context.read_surface` with
  `selection_source=code_phase_planner`.
- LLM tool-selection traces
  `20260513T180044737444_tool_selection_bebe2a2d95_26e2cf67.json` and
  `20260513T180047375717_tool_selection_a96b281f2d_118d4865.json` have
  `code_phase=true` and request
  `context.read_surface(detail="full", max_code_chars=12000,
  target_file="policies/solver_algorithm.py")`.
- The final `generate_patch` call then timed out after three attempts. Its
  code prompt was still around 49k characters.
- The retry repeated the code-phase full surface read, then the provider
  returned an insufficient-balance error. The campaign ended with
  `balance_exhausted=true`, `circuit_breaker_tripped=true`, and no screening
  experiments.

Interpretation:

- The framework repair worked for the original issue: code phase is no longer
  purely blind/static and can use exposure-controlled tools after hypothesis
  approval.
- The next blocker is final patch generation size/latency. The code prompt
  still duplicated the full solver target file with the whole champion policy
  bundle. A follow-up prompt-slimming repair now omits the duplicate champion
  policy bundle for `solver_design`; the full selected target file remains in
  the `Target File` section and code-phase full-surface read remains audited.
- Do not start the 5-8 round validation until API balance is restored and a
  1-2 round smoke shows final patch generation reaches Contract/Verification.

## Balance-Restored 2-Round Smoke

```text
run_root=/home/clawd/research/scion-experiments/v04-code-phase-slim-smoke-sonnet-2r-20260513T182058Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=2
rounds_completed=2
screened_experiments=2
time_limit_sec=60
agentic_session_timeout_sec=1800
git_commit=d5a57df382d12e1413508d15ff681edbdfeeb104
started_utc=2026-05-13T18:20:58Z
finished_utc=2026-05-13T19:06:41Z
status=max_rounds_exhausted
balance_exhausted=false
circuit_breaker_tripped=false
```

This run validated the repaired code-phase path after API balance was restored.
All final `generate_patch` calls returned successfully:

- round 1 code prompts were about 49.5k and 50.0k characters, including one
  Contract-preview repair;
- round 2 code prompt was about 55.6k characters;
- all generated patches targeted `policies/solver_algorithm.py`;
- code-phase tool-selection traces used
  `context.read_surface(detail="full", max_code_chars=12000,
  target_file="policies/solver_algorithm.py")`;
- Contract and Verification passed for both screened candidates.

The run also validated the feedback loop. Round 1 produced a from-scratch
algorithm that was fast but weak: 16/16 valid pairs, 0 wins, 16 losses,
median pair delta `-119.5`, and median runtime ratio about `0.029`. Decision
abandoned it with `SCREENING_FAIL_WIN_RATE`.

Round 2 then explicitly used the round-1 screening/runtime feedback in its
hypothesis: it identified the prior candidate as spending only about 3% of the
time budget and proposed a baseline-plus-ILS post-processing solver. That
candidate produced 16/16 valid pairs, 1 win, 15 ties, 0 losses, median pair
delta `0.0`, and median runtime ratio about `1.00045`. It was still abandoned
by the win-rate gate, but it did produce a real nonzero improvement on
`cvrplib/E/E-n101-k8.vrp` seed `11` (`delta=+1.0`,
`solver_algorithm_best_delta=1.0`).

Interpretation:

- The framework path is now healthy enough for longer validation: code-phase
  tools, full solver-surface reads, patch generation, Contract preview repair,
  Contract, Verification, screening, feedback ingestion, and subsequent
  hypothesis adaptation all executed.
- The algorithmic signal is weak but positive. The system moved from a fast
  low-quality replacement solver to a quality-preserving baseline-plus local
  search solver with one small win.
- Prompt slimming remains incomplete. The follow-up code prompt still reached
  roughly 55.6k characters. It no longer timed out in this smoke, but prompt
  pressure remains a cost and latency risk.
- The next long validation should let APS continue exploring whole-solver
  algorithm variants, especially stronger post-baseline neighborhoods and
  runtime allocation, while watching for candidates that merely wrap the
  baseline without nonzero `solver_algorithm_best_delta`.

## 5-Round Exploratory Hang

```text
run_root=/home/clawd/research/scion-experiments/v04-code-phase-slim-exploratory-sonnet-5r-20260513T190909Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=5
rounds_observed_before_termination=4
screened_experiments=3
time_limit_sec=60
agentic_session_timeout_sec=1800
git_commit=febca19
started_utc=2026-05-13T19:09:09Z
terminated_utc=2026-05-13T23:06:48Z
exit_code=143
status=manually_terminated_for_preview_hang
```

The run improved slightly over earlier broken paths but exposed a sharper
runtime-control failure. The campaign reached three screened candidates and
then stopped making progress after a successful code-generation trace around
`2026-05-13T20:30:56Z`. `status.json` stayed stale at
`2026-05-13T20:29:23Z`, the Python process remained CPU-active for more than
two hours, and no new status, DB, metrics, or session artifacts appeared.

Screened results before the hang:

- round 1: 16/16 valid, 0 wins, 16 losses, median delta `-152`, median runtime
  ratio about `0.0079`;
- round 2: 16/16 valid, 2 wins, 13 ties, 1 loss, median delta `0`, median
  runtime ratio about `0.945`;
- round 3: 16/16 valid, 2 wins, 11 ties, 3 losses, median delta `0`, median
  runtime ratio about `0.668`.

The final generated code trace contained several natural but risky
improvement-flag loops such as `while improved` and did not seed from
`context.baseline()`. The likely failure point was deterministic Contract
preview or CVRP synthetic preview calling `solve(...)` before workspace
materialization. The previous static C9c rule was too permissive: it counted
any assignment to the loop condition variable as evidence that the loop was
shrinking a collection. That allowed boolean-flag loops to pass even when they
had no bounded counter, shrinking collection, or `context.remaining_time()`
guard.

Interpretation:

- Code-phase tool access is not the only remaining problem. The proposal
  agent can now inspect the full solver subject and feedback, but Scion also
  needs a fail-closed execution boundary around preview-time candidate code.
- The boundary failure is framework-side: preview is a deterministic gate and
  must never be able to consume unbounded wall time.
- The algorithmic failure is still real: the candidates showed weak positive
  signal but mostly tied or underperformed the champion, and they still often
  oscillated between from-scratch weak solvers and shallow baseline wrappers.

## Preview Timeout Repair

The follow-up repair keeps the original CVRP runtime and checked-in VRP
algorithm files untouched. It only changes Scion boundary code and tests:

- Contract C9c no longer treats reassignment of the loop-condition variable as
  collection shrinkage. Boolean-flag loops like `while improved` must now have
  an explicit bounded counter, shrinking collection operation, bounded break,
  or runtime guard.
- CVRP synthetic preview wraps `solver_design.solve(...)` in a hard main-thread
  SIGALRM timeout. A timeout becomes a failed preview issue telling the
  candidate to use bounded loops and poll `context.remaining_time()`. The
  timeout sentinel is not a normal `Exception`, so broad candidate
  `except Exception` handlers cannot swallow the preview guard.
- APS wraps the `proposal.contract_preview` tool in a hard timeout before
  workspace materialization. If preview hangs, the session receives a
  controlled `tool_error` observation instead of blocking the campaign.

Focused validation:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m py_compile \
  scion/scion/contract/gate.py \
  scion/scion/problems/cvrp/adapter.py \
  scion/scion/proposal/agentic_session.py

/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/unit/test_research_surfaces.py::test_cvrp_solver_algorithm_complexity_allows_bounded_algorithm_while_patterns \
  scion/scion/tests/unit/test_research_surfaces.py::test_cvrp_solver_algorithm_complexity_rejects_unbounded_improvement_flag_loop \
  scion/scion/tests/test_cvrp_adapter.py::test_cvrp_solver_algorithm_preview_bounds_remaining_time_guarded_loop \
  scion/scion/tests/test_cvrp_adapter.py::test_cvrp_solver_algorithm_preview_times_out_unbounded_solve \
  scion/scion/tests/unit/test_agentic_proposal_tools.py::test_agentic_session_contract_preview_timeout_returns_tool_error -q

5 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/test_contract.py \
  scion/scion/tests/test_cvrp_adapter.py \
  scion/scion/tests/unit/test_research_surfaces.py \
  scion/scion/tests/unit/test_agentic_proposal_tools.py \
  scion/scion/tests/unit/core/test_proposal_pipeline.py -q

329 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/scion/tests -q

1618 passed, 1 skipped
```

Next validation should be a smaller 1-2 round independent smoke before another
5-8 round run. The first success criterion is not promotion; it is that
unbounded preview candidates fail closed quickly and the campaign continues to
the next branch/round instead of hanging after code generation.

## Preview-Repair Smoke Failure

```text
run_root=/home/clawd/research/scion-experiments/v04-preview-timeout-repair-smoke-sonnet-2r-20260513T232347Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=2
rounds_completed=2 APS attempts, 0 screened experiments
time_limit_sec=60
agentic_session_timeout_sec=1800
git_commit=0361d1a
started_utc=2026-05-13T23:23:47Z
finished_utc=2026-05-13T23:43:19Z
exit_code=0
status=max_rounds_exhausted
terminal_reason=code_generation_failed
```

This run did not validate the preview-timeout repair. The campaign exited
cleanly, but both rounds failed before Contract preview or Verification.

Detailed trace findings:

- Both code-generation calls timed out after three provider attempts:
  `20260513T232443802265_code_cafca85811_6e480e8b.json` and
  `20260513T233429537279_code_a0077b8022_347841cb.json`.
- Code prompt sizes were still about 49k and 51k characters.
- The approved hypothesis was a heavy ALNS-style full solver implementation,
  but final code generation saw duplicated target/surface content: the target
  file appeared in the normal `Target File` section and again inside the full
  `context.read_surface` observation payload.
- Code phase also spent planner calls repeating the selected-surface read
  instead of stopping after the required full read was available.
- Model-facing planner context contained an empty tool name because strict
  sanitization removed the string `feedback.query_holdout_summary` from
  `allowed_tools` / `allowed_tool_specs`.
- Retry context added "previous attempt failed" but did not shrink the prompt
  or reduce the task shape, so the retry reproduced the same timeout failure.

Interpretation:

- The preview guard may still be correct, but this run never reached the code
  path where it could be tested.
- The real blocker in this slice was still the agent/code boundary: Scion had
  given code phase tool access, but then fed the second-round model a static
  duplicated prompt large enough to time out.
- The empty tool name was a governance/audit bug. Even when a tool is safe as a
  deterministic direct call, a model-facing planner prompt must not contain
  names that sanitization will erase.

## Prompt/Tool-Loop Repair

The follow-up repair keeps original VRP/CVRP algorithm files untouched and
changes only Scion APS/prompt assembly:

- Code generation now receives compact tool-observation payloads. Full
  selected-surface reads remain executed and counted in APS evidence, but
  `current_artifact.content_preview`, duplicated interface text, and other
  full-code fields are omitted from the final `generate_patch` prompt because
  the target file is already supplied once in the `Target File` section.
- Code-phase observation context is capped more tightly than hypothesis
  context (`10k` observation JSON, `8k` diagnosis JSON).
- Code phase stops the planner loop once a successful full selected-surface
  read for the approved target is available, instead of asking for another
  read before patch generation.
- Model-facing planner allowed-tool lists/specs filter
  `feedback.query_holdout_summary`. The direct tool remains callable by
  deterministic tests and callers, but it is no longer rendered into planner
  prompts where holdout sanitization turns the name into an empty string.
- Timeout retry prompt text now recognizes provider/code-generation timeouts
  and asks for one compact bounded algorithm body rather than a large helper
  forest.

Focused validation:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/unit/test_agentic_proposal_tools.py -q

102 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/unit/test_research_surfaces.py \
  scion/scion/tests/test_cvrp_adapter.py \
  scion/scion/tests/test_contract.py -q

198 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/unit/test_g4_plumbing.py \
  scion/scion/tests/unit/test_sprint_j3_prompt_plumbing.py -q

29 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/scion/tests -q

1619 passed, 1 skipped
```

## Code Self-Check Smoke And No-Op Feedback Repair

```text
run_root=/home/clawd/research/scion-experiments/v04-code-self-check-smoke-sonnet-2r-20260514T091556Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=2
rounds_completed=2
screened_experiments=2
time_limit_sec=60
agentic_session_timeout_sec=1800
git_commit=06e9365
started_utc=2026-05-14T09:15:56Z
finished_utc=2026-05-14T10:16:00Z
exit_code=0
status=max_rounds_exhausted
```

The run validated framework behavior after code self-check repair:

- code-phase tool selection and full `solver_design` reads executed;
- first generated code drafts in both rounds were rejected by Contract preview
  for `C9c_complexity_bound` uncapped loops;
- one bounded Contract-preview repair produced code that reached screening;
- both screened candidates activated `solver_algorithm` and had
  `solver_algorithm_errors=0`;
- both candidates were abandoned by Decision with `T4: win_rate < 0.3`.

Screening outcomes:

- Round 1: 16/16 valid, 3 wins, 1 loss, 12 ties, median runtime ratio about
  `1.234x`. The candidate had nonzero internal LNS movement but several cases
  hit `time_limit`.
- Round 2: 16/16 valid, 1 win, 2 losses, 13 ties, median runtime ratio about
  `1.063x`. The candidate attempted moves on all pairs but accepted almost no
  moves (`solver_algorithm_accepted_moves=0` on 15/16 pairs) and had
  `solver_algorithm_best_delta=0.0` on 15/16 pairs.

Interpretation:

- This is not primarily a model-tooling failure. The model obeyed the protocol,
  repaired static preview issues, generated valid code, and activated the
  solver hook.
- The algorithmic failure is that the candidates are still baseline-heavy and
  mostly no-op on formal CVRPLIB instances. They pay runtime overhead without
  enough phase-best movement.
- The previous Contract preview was too weak for this failure mode: it checked
  validity and shallow baseline wrappers, but it did not include a synthetic
  instance where the preview baseline is intentionally improvable.
- Feedback ordering also under-emphasized `solver_algorithm_*` no-op telemetry,
  so the next hypothesis could see runtime ratio but not the most important
  move/no-move facts.

Repair:

- `context.read_problem` now exposes adapter-rendered `solver_mechanics` next
  to `problem_object`, so code phase sees the full fixed research object and
  direct `solve(...)` lifecycle from one problem-object tool response.
- CVRP `solver_design` Contract preview now runs a third synthetic instance,
  `synthetic_preview_improvement_trap`, where nearest-neighbor preview baseline
  distance is `51.0` and a simple route reordering reaches `43.0`.
- Baseline-seeded candidates that fail to improve that trap fail Contract
  preview with a `solver_design micro-eval no-op` issue before workspace
  materialization.
- Screening/runtime feedback now prioritizes
  `solver_algorithm_move_attempts`, `solver_algorithm_accepted_moves`,
  `solver_algorithm_best_delta`, `solver_algorithm_search_iterations`,
  `solver_algorithm_elapsed_ms`, and `solver_algorithm_phase_runtime_ms`, so
  the next agent turn can distinguish "active but no phase-best movement" from
  syntax/runtime failures.

Validation so far:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/test_cvrp_adapter.py -q

53 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/unit/test_agentic_proposal_tools.py -q

105 passed
```

## Aggressive Compact Smoke

```text
run_root=/home/clawd/research/scion-experiments/v04-code-phase-aggressive-compact-smoke-sonnet-2r-20260514T061603Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=2
rounds_completed=2
screened_experiments=1
time_limit_sec=60
agentic_session_timeout_sec=1800
git_commit=14f7f29
started_utc=2026-05-14T06:16:03Z
exit_code=0
status=max_rounds_exhausted
```

Observed signals:

- Both code-generation calls completed; final code prompt user text was about
  30.1k and 32.9k characters with an 8.8k system block.
- Model-facing tool-selection traces contained no empty tool names and no raw
  `content_preview` payloads.
- Round 1 passed Contract/Verification and reached screening, but failed
  Decision with `win_rate=0.0`, `median_delta=0.0`,
  `runtime_ratio_median=0.343`, and 16/16 valid pairs.
- Round 1 was active and fast, but weak: `solver_algorithm_best_delta` was
  positive on only 4/16 pairs, and most phase deltas remained zero.
- Round 2 generated a baseline-first + Or-opt/2-opt* post-baseline solver, but
  failed heavy Verification at `V5_solution_consistency`.

Round-2 failure analysis:

- Campaign summary recorded `solver run failed or no output`.
- Replaying the generated `policies/solver_algorithm.py` in the correct
  workspace/canary context exposed the underlying candidate exception:
  `solve failed: list index out of range`.
- The traceback was inside the generated Or-opt pass after stagnation
  perturbation, where a stale segment position could produce an empty segment
  and then index `seg[0]`.
- The existing problem preview ran only a very small synthetic instance with a
  1-second synthetic time window, so it did not reach the stagnation/perturbation
  path that triggered the bug.
- Runtime audit did not have a dedicated `solver_algorithm_errors` failure
  category, so solver-design exceptions were less legible than operator/policy
  exceptions.

Repair:

- `runtime.audit` now recognizes `solver_algorithm_errors > 0` as a first-class
  `solver_algorithm_runtime_error`, including path, active/loaded state, stop
  reason, and the first solver-algorithm events.
- CVRP solver-design problem preview now runs the solver hook on both the
  original synthetic preview instance and a controlled-canary-shaped instance
  (`synthetic_preview_canary_5`).
- CVRP solver-design preview synthetic time limit increased from 1s to 5s while
  retaining the 2s wall-clock preview timeout. This lets bounded search loops
  reach deeper stagnation/perturbation paths without allowing unbounded previews
  to hang the session.
- The exact round-2 generated solver now fails Contract preview before
  workspace materialization with:
  `synthetic_preview_canary_5: solve raised during synthetic preview: list index out of range`.

Validation:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/test_cvrp_adapter.py::test_cvrp_solver_algorithm_preview_accepts_valid_solution \
  scion/scion/tests/test_cvrp_adapter.py::test_cvrp_solver_algorithm_preview_runs_canary_shaped_instance \
  scion/scion/tests/test_cvrp_adapter.py::test_cvrp_solver_algorithm_preview_rejects_shallow_baseline_wrapper \
  scion/scion/tests/test_cvrp_adapter.py::test_cvrp_solver_algorithm_preview_bounds_remaining_time_guarded_loop \
  scion/scion/tests/test_cvrp_adapter.py::test_cvrp_solver_algorithm_preview_times_out_unbounded_solve -q

5 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/test_cvrp_adapter.py \
  scion/scion/tests/test_cvrp_solver_operator_runtime.py \
  scion/scion/tests/test_verification.py -q

209 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/unit/test_agentic_proposal_tools.py -q

102 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/scion/tests -q

1621 passed, 1 skipped
```

Next validation remains a 1-2 round independent smoke. The first expected
signal is that the run reaches Contract preview/Verification instead of
timing out in final `generate_patch`; solver-quality movement is a later gate.

## Solver Preview Repair Smoke

```text
run_root=/home/clawd/research/scion-experiments/v04-solver-preview-repair-smoke-sonnet-2r-20260514T073717Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=2
rounds_completed=2
screened_experiments=0
time_limit_sec=60
agentic_session_timeout_sec=1800
git_commit=c011ac2
started_utc=2026-05-14T07:37:17Z
finished_utc=2026-05-14T07:55:11Z
exit_code=0
status=max_rounds_exhausted
```

Observed signals:

- Both rounds stayed on `modify/solver_design` targeting
  `policies/solver_algorithm.py`.
- Tool-selection prompts remained clean: no empty tool names and no raw
  `content_preview` payloads.
- Round 1 failed during final code generation after three provider timeout
  attempts. The code prompt was about 31.3k user characters plus an 8.8k system
  block.
- Round 2 generated a 16.3k-char patch, but Contract preview rejected it with
  `C9c_complexity_bound` because the generated solver contained an uncapped
  `while` loop.
- The repair attempt generated a 19.0k-char patch, but Contract preview
  rejected it with `C6_ast_syntax` at line 341.

Round-2 repair failure detail:

```python
for entry in relatedness:
    d, eri, epi, ec in relatedness:
        pass
```

The model also returned this `test_hint`:

```text
The _destroy_repair_regret function has a syntax error in the Shaw removal
section that needs fixing.
```

Interpretation:

- The solver-design preview/audit repair worked in the important boundary
  sense: bad full-solver code failed closed before Verification and Screening.
- Code-phase is still too willing to emit known-bad code. A model response that
  explicitly says the generated patch has a syntax error, needs fixing, is
  incomplete, or is otherwise unresolved must not be treated as a normal
  candidate patch.

Follow-up repair:

- APS now runs a cheap code-response self-check on generated patches before
  Contract preview.
- If `test_hint` self-reports an unresolved code issue such as `syntax error`,
  `invalid syntax`, `does not compile`, `needs fixing`, `still broken`,
  `not implemented`, `incomplete`, `TODO`, or `FIXME`, APS consumes at most one
  configured code-repair attempt and regenerates with explicit
  `agentic_code_self_check_feedback`.
- If the repair result still self-reports an unresolved code issue, APS fails
  closed with `code_generation_failed` and does not pass the patch to Contract
  preview as a candidate.
- Contract-preview repair now shares the same repair-attempt budget, so one
  session cannot exceed `max_code_repair_attempts` by doing both self-check and
  preview repairs.

Validation:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/unit/test_agentic_proposal_tools.py::test_agentic_session_repairs_self_reported_unresolved_patch_issue \
  scion/scion/tests/unit/test_agentic_proposal_tools.py::test_agentic_session_rejects_self_reported_unresolved_patch_after_repair \
  scion/scion/tests/unit/test_agentic_proposal_tools.py::test_agentic_session_contract_preview_failure_fails_closed -q

3 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/unit/test_agentic_proposal_tools.py -q

104 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/scion/tests -q

1623 passed, 1 skipped
```

## Compact-Prompt Smoke

```text
run_root=/home/clawd/research/scion-experiments/v04-code-phase-compact-smoke-sonnet-2r-20260514T050915Z
model=claude-sonnet-4-6
problem=cvrp
protocol=formal
rounds_requested=2
rounds_completed=2
screened_experiments=1
time_limit_sec=60
agentic_session_timeout_sec=1800
git_commit=7f7ef04
started_utc=2026-05-14T05:09:15Z
finished_utc=2026-05-14T05:38:23Z
exit_code=0
status=max_rounds_exhausted
```

The run partially validated the compact code-phase path:

- Round 1 still failed in final `generate_patch` after three provider timeout
  attempts.
- Round 2 generated a patch, passed Contract, passed Verification, and reached
  screening.
- Both rounds stayed on `modify/solver_design` and targeted
  `policies/solver_algorithm.py`.
- Code prompt user text fell to roughly 35.5k-35.8k characters, down from the
  previous 49k-51k range.
- Model-facing planner traces no longer contained empty tool names.
- Final code prompts did not contain raw `"content_preview":` code payloads;
  they contained `content_preview_omitted=true` metadata instead.
- Code-phase transcripts recorded `code_surface_context_satisfied` after one
  successful full selected-surface read.

Screening outcome for the completed candidate:

- 16/16 valid pairs;
- `win_rate=0.0`;
- `median_delta=-71.0`;
- `runtime_ratio_median=0.192`;
- `solver_algorithm_best_delta` weighted sum `698.0`;
- stop reasons: 12 `completed`, 4 `time_limit`.

Interpretation:

- The framework path is now capable of reaching Contract/Verification and
  screening again from the direct full-algorithm boundary.
- Prompt pressure is materially lower but still high enough to cause a first
  round timeout.
- The completed candidate was active and recorded substantial internal
  improvement telemetry, but it remained much weaker than the repo-local
  champion.

Follow-up repair:

- Final code-generation observations now keep only code-relevant proposal
  observations: memory, screening/runtime feedback, branch state, errors, and
  the latest full selected-surface read metadata. Initial compact
  `context.read_surface`, `context.list_surfaces`, `context.read_problem`, and
  schema/target preview observations are omitted from the code prompt.
- Solver-design code prompts now cap the problem object, solver execution
  model, surface interface, and hypothesis detail separately. The full target
  file remains present.
- Code-phase agentic observation and diagnosis caps are both reduced to 6k.
- Timeout retry guidance now explicitly asks for one construction path plus one
  bounded improvement loop and discourages large ALNS helper forests.

Validation:

```text
/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/unit/test_agentic_proposal_tools.py -q

102 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/unit/test_research_surfaces.py \
  scion/scion/tests/test_cvrp_adapter.py \
  scion/scion/tests/test_contract.py -q

198 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest \
  scion/scion/tests/unit/test_g4_plumbing.py \
  scion/scion/tests/unit/test_sprint_j3_prompt_plumbing.py -q

29 passed

/home/clawd/miniconda3/envs/claw/bin/python -m pytest scion/scion/tests -q

1619 passed, 1 skipped
```
