# Warehouse V3 Continuity Synthetic 36-Stage R6 Preregistration — 2026-08-08

**Status:** `PRE_REGISTERED_NOT_RUN`

## Question

Can a fresh `gpt-5.6-terra` research agent, running under the lightweight V3
loop, improve the canonical synthetic Warehouse solver at least twice in one
uninterrupted campaign? The primary endpoint is champion v3 or later through
two complete screening -> validation -> frozen promotions. The run must target
all 36 typed Protocol stages; an intermediate promotion or a shorter partial
campaign does not answer the continuity question.

R6 starts from a fresh champion v1 campaign. It does not resume or inherit
champions, branches, workspaces, proposal sessions, request state or campaign
state from R1 through R5.

## R5 terminal boundary and matched-run semantics

R5's credential extraction and silent `/v1/models` check succeeded, and its
first Hypothesis call completed. Its first Code call then received HTTP 504
`Upstream closed stream without a terminal event`. R5 exited `20` with
`invalid_infra_only`, zero evaluated candidates and zero experiments. The proxy
still reported authenticated state and one active account after the stop.

R6 changes no scientific input, framework source or launch mechanism. It is a
fresh matched campaign after an infrastructure-only R5 result. R6 will create a
new H and, if H succeeds, a new C call in a new campaign; it does not replay,
resume or retry the failed R5 Code call.

Proxy diagnostics identified R5 request `cd4f705e` as an occasional upstream
termination class already present in historical logs. The 49,280-byte Terra
Code-call input produced 2,022 partial stream events, but no terminal event;
the proxy failed fast without retry after 64,689 ms and recorded
`StreamUpstreamPrematureClose` at 17:33:48Z. Those partial events are not a
valid C output and must not be reconstructed, replayed or supplied to R6. The
account is currently active, and no proxy restart or configuration change is
required for this matched campaign.

R6 is therefore ready but not run. Its new campaign will make a new H call and,
if that succeeds, a new C call. This is fresh experimental sampling after an
infrastructure-only run, not replay or retry authorization for R5's failed C.

## Framework boundary retained from R5

The executable framework revision is
`88c1bc2ba0d14d17a4b0f3b39ac934d7029b9d68` (`88c1bc2b`). Relative to the R3
runtime, this revision changes only post-promotion stale-branch control flow:

- a stale branch with no replayable patch is retired as a non-attempt lifecycle
  action and scheduling continues without consuming a formal Protocol stage or
  recording a research rejection;
- an already typed `RESEARCH_REJECTED` result remains scheduler-forward and its
  audit projection no longer depends on a separate disposition object being
  present.

The scheduler-forward lifecycle predicate remains strict: a reconcile result
carrying a Decision, Protocol stage, formal-evaluation flag, failure payload,
execution outcome or attempt disposition is not treated as patchless
housekeeping.

This revision does **not** change the Warehouse solver or data, statistical
thresholds, Contract or Verification gates, Protocol rules, Decision mapping,
candidate disposition, branch-count limit, Scheduler priority order or FIFO
selection. STALE remains V3 Priority 3 after READY_FROZEN and READY_VALIDATE.

## Frozen scientific inputs

| Input | Pre-registered value |
|---|---|
| Runtime source | `/home/clawd/research/or-autoresearch-agent-v3-hotpath/scion` |
| Runtime revision | `88c1bc2ba0d14d17a4b0f3b39ac934d7029b9d68` |
| Campaign directory | `/home/clawd/research/scion-experiments/v04-warehouse-v3-continuity-synth-36r-r6-gpt56terra-20260808T173413Z-claw/campaign` |
| Problem | `problems/warehouse_delivery/problem.yaml` (canonical synthetic Warehouse) |
| Protocol | `problems/warehouse_delivery/protocol.yaml` (`3.0`) |
| Split | `problems/warehouse_delivery/split_manifest.yaml` (`4.0`, 47 declared paths) |
| Seeds | `problems/warehouse_delivery/seed_ledger.yaml` (`3.0`) |
| Synthetic data root | `/home/clawd/research/or-autoresearch-agent/surrogate` |
| Provider | local Codex proxy at `http://127.0.0.1:8080` |
| Model | `gpt-5.6-terra` |
| Protocol-stage horizon | 36 typed evaluated stages |
| Solver subprocess limit | 30 seconds per case/seed execution |
| Scheduler | default V3 state priority and FIFO, at most three active branches |

These are exactly the R5 scientific inputs, framework revision, provider/model
choice and execution horizon. Only the fresh R6 campaign directory differs.

## Frozen command

Run exactly once from the runtime source:

```bash
set -euo pipefail

cd /home/clawd/research/or-autoresearch-agent-v3-hotpath/scion

PROXY_KEY_VALUE="$(curl -fsS http://127.0.0.1:8080/auth/status | \
  jq -er '.proxy_api_key | select(type == "string" and length > 0)')"
trap 'unset PROXY_KEY_VALUE' EXIT

curl -fsS -o /dev/null \
  -H "Authorization: Bearer $PROXY_KEY_VALUE" \
  http://127.0.0.1:8080/v1/models

PYTHONPATH=/home/clawd/research/or-autoresearch-agent-v3-hotpath/scion \
SCION_MODEL=gpt-5.6-terra \
SCION_BASE_URL=http://127.0.0.1:8080 \
SCION_API_KEY="$PROXY_KEY_VALUE" \
SCION_WAREHOUSE_DATA_ROOT=/home/clawd/research/or-autoresearch-agent/surrogate \
  /home/clawd/miniconda3/envs/claw/bin/python -m scion.cli.main run \
  --problem problems/warehouse_delivery/problem.yaml \
  --protocol problems/warehouse_delivery/protocol.yaml \
  --split problems/warehouse_delivery/split_manifest.yaml \
  --seeds problems/warehouse_delivery/seed_ledger.yaml \
  --campaign-dir /home/clawd/research/scion-experiments/v04-warehouse-v3-continuity-synth-36r-r6-gpt56terra-20260808T173413Z-claw/campaign \
  --rounds 36 \
  --time-limit-sec 30
```

The credential and `/v1/models` checks are silent. Response content goes to
`/dev/null`, the key value is never printed or persisted, and the extracted
value is passed unchanged to the campaign child. Extraction or HTTP-check
failure ends the shell before the child starts.

## Execution and stop policy

- Launch once into the declared fresh R6 campaign directory.
- Do not resume or retry R5, and do not resume a partial R6 root. Do not copy
  prior campaign or provider-call state into R6.
- Do not retry a provider H or C call. A scheduler-forward research rejection
  starts a new H attempt from the selected clean base; it is not a retry of the
  rejected call.
- Do not stop at champion v2, at a negative candidate, or when a patchless old
  branch is retired. Continue until 36 typed stages complete or the invocation
  reaches a genuine terminal outcome.
- Do not modify framework source, solver source, problem/protocol/split/seed
  inputs, thresholds, model, proxy route, time limit or campaign state while
  the run is active.
- Observe process and campaign status without using partial results to alter
  execution. Any later repaired or matched run requires a new preregistration
  and a new campaign root.
- Record provider, framework, solver, resource or interruption failures as
  observed. They do not authorize restarting the same root.

## Analysis plan

After terminal exit, record:

1. silent credential and `/v1/models` health-check success, process exit,
   campaign stop reason, run validity and exact requested/completed typed-stage
   counts;
2. H/C call counts and every provider, Contract, Verification or execution
   failure without treating a scheduler-forward rejection as a formal stage;
3. screening, expanded-screening, validation, expanded-validation and frozen
   funnel counts with exact Protocol and Decision reason codes;
4. each promotion's stage index, H, branch, candidate source, immediate parent
   champion and resulting champion version;
5. all post-promotion STALE selections, including whether patchless branches
   retire without a research-rejection count and whether scheduling reaches a
   new branch based on the current champion;
6. branch depth, provisional-head continuity, pair completeness,
   candidate/champion failures and runtime evidence role;
7. whether the campaign reaches champion v3 through two promotions and whether
   all 36 typed stages complete.

If the run promotes, independently replay each promoted champion against its
immediate parent and replay the final champion against v1 on the declared
held-out evidence. Those are separate eval-only experiments and do not mutate
or extend this campaign.

## Outcome classification

- `CONTINUOUS_OPTIMIZATION_CONFIRMED`: R6 reaches champion v3 or later through
  at least two complete Protocol promotions, and separate independent replays
  support retained improvement over each immediate parent and v1.
- `PROMOTION_CONTINUITY_OBSERVED_REPLAY_PENDING`: R6 completes the required
  promotion path to v3 or later, but independent replay evidence is incomplete.
- `VALID_FUNNEL_BELOW_V3`: the run completes 36 typed stages with valid
  scientific evidence but ends below champion v3.
- `SCIENTIFIC_NO_PROMOTION`: the 36-stage campaign completes with interpretable
  evaluated candidates and no promotion.
- `RUN_INVALID_FRAMEWORK`: the horizon is cut short by framework control flow,
  including recurrence of the R3 patchless-stale stop or failure to continue
  after a typed research rejection.
- `RUN_INVALID_INFRA`: provider, credential, upstream stream, runner, resource
  or other infrastructure failure prevents interpretation of the declared
  horizon.
- `INTERRUPTED`: an explicit user/system interruption ends the invocation
  before the declared horizon.

An incomplete framework or infrastructure result neither confirms nor refutes
the scientific endpoint. Completed formal stages may be reported as partial
evidence, but they cannot be presented as completion of the pre-registered
36-stage question.
