# v0.4 Algorithm-Body Lifecycle Short Diagnostic

Date: 2026-05-12

Status: stopped before completion and superseded by the execution-semantics
repair documented in
[`v0.4-algorithm-body-execution-semantics-repair-20260512.md`](v0.4-algorithm-body-execution-semantics-repair-20260512.md).

Run root:
`/home/clawd/research/scion-experiments/v04-algorithm-body-lifecycle-sonnet-8r-20260512T145345Z`

Supersedes the earlier launch
`/home/clawd/research/scion-experiments/v04-algorithm-body-lifecycle-sonnet-8r-20260512T143836Z`,
which was terminated after the runtime contract was tightened so that the
verification run matches the final code under test.

## Setup

- Model: `claude-sonnet-4-6`
- Problem/protocol: CVRP formal
- Surface: free `solver_design`
- Rounds requested: 8
- Time limit: 60 seconds
- Agentic proposal timeout: 1200 seconds
- Agentic proposal: enabled
- Force surface: none
- Launch script:
  `/home/clawd/research/scion-experiments/v04-algorithm-body-lifecycle-sonnet-8r-20260512T145345Z/run.sh`

## Code Change Under Test

This run tests the explicit CVRP algorithm-body repair:

- enabled `solver_design` proposals must return `algorithm_body`;
- adapter preview and ContractGate fail closed when `algorithm_body` is
  missing;
- runtime records `main_search_algorithm_body*` and route-pool lifecycle
  controls;
- adaptive auto-added `route_pool_recombination` is scoped out on small formal
  `.vrp` instances below the declared customer threshold unless the plan
  explicitly asks for `route_pool_activation="always"`;
- route-pool activation scope is now part of the solver-design lifecycle
  hypothesis rather than hidden behavior.

## Acceptance Checks

After completion, inspect bounded artifacts first:

- `exit.txt` exists and reports exit code 0, or `run.log` explains an explicit
  terminal failure.
- Completed APS code sessions targeting `solver_design` include
  `algorithm_body` in `main_search_plan()`.
- Contract preview no longer fails because the generated plan omitted the
  algorithm body.
- Screening/runtime metrics include `main_search_algorithm_body`,
  `main_search_algorithm_body_source`,
  `main_search_route_pool_auto_added`,
  `main_search_route_pool_invocations`,
  `main_search_route_pool_activation`,
  `main_search_route_pool_min_customers`, and
  `main_search_route_pool_max_rounds`.
- Quality interpretation remains conservative: this run can validate
  generation and runtime behavior, but long CVRP validation still requires
  repeated screening wins or median movement from the whole solver lifecycle.

## Initial Status

Started as an independent process from the current working tree after the
algorithm-body repair passed the full local test suite.

Superseded-launch sanity check before restart:

- APS completed a `solver_design` code session with Contract preview passing.
- The generated `main_search_plan()` included `algorithm_body`.
- Verification/runtime metrics recorded
  `main_search_algorithm_body_source="declared"`.
- The declared body used `route_pool_activation="medium_large_only"` with
  `route_pool_min_customers=60`; on small verification `.vrp` cases runtime
  skipped route-pool with `algorithm_body_route_pool_scope`, confirming that
  execution is now following the declared lifecycle scope.

The active run uses the stricter final runtime contract where enabled
`main_search_plan()` also fails closed inside the solver if `algorithm_body` is
missing.

Active-run sanity check:

- First completed code session passed schema and Contract preview.
- Patch target is `policies/main_search_strategy.py`.
- Verification metrics show `main_search_strategy_active=true`,
  `main_search_strategy_errors=0`, and
  `main_search_algorithm_body_source="declared"`.
- The declared body again uses `route_pool_activation="medium_large_only"` with
  `route_pool_min_customers=60`, and small verification `.vrp` runs skip
  route-pool through `algorithm_body_route_pool_scope`.

## Stop Update

The active run was manually stopped before completion after the partial eighth
round made the failure mode clear enough to repair. The campaign had validated
that APS could return `algorithm_body`, but it also showed that field exposure
was not yet enough:

- completed screenings remained sparse and mostly tied (`win_rate` values
  0.125, 0.0, 0.125, and 0.125, with `median_delta=0.0`);
- the partial round-8 screening was again all ties with runtime regression;
- candidates could declare lower baseline fractions, but the formal runtime
  still silently applied the legacy 0.75 baseline floor;
- phase sequence, cleanup coupling, adaptive component budgets, and
  construction-pool reuse were not sufficiently operational.

The follow-up repair makes those lifecycle controls executable and required in
runtime evidence instead of only visible in prompt/audit text.
