# Warehouse V3 Continuity Synthetic 36-Stage R8 Preregistration — 2026-08-08

**Status:** `PRE_REGISTERED_READY`

Immediately before this record was frozen, the local proxy reported
`authenticated=true`, one active account and no quota/rate/refresh condition.
The declared R8 root was absent.

## Question

Can a fresh `gpt-5.6-terra` agent improve the canonical synthetic Warehouse
solver at least twice in one uninterrupted lightweight-V3 campaign? The
primary endpoint remains champion v3 or later through two complete screening
-> validation -> frozen promotions over 36 formal Protocol stages.

R8 is a fresh champion-v1 campaign. It imports no branch, champion, workspace,
H/C response or campaign state from R1-R7.

## Matched-repeat boundary

R6 provided one promotion plus post-promotion research continuity, but its old
runtime stopped at 17/36 on a now-corrected proposal-content classification.
R7 used the corrected runtime and completed 4/36 stages with two valid
candidate negatives. Its third C had no terminal provider response, so Scion
correctly stopped `BLOCKED_INFRA`; the proxy remained authenticated and active.

R8 keeps every scientific and framework input from R7 and changes only the
fresh campaign root and naturally sampled H/C calls. It does not replay the R7
C, reconstruct its partial stream, resume R7, change proxy configuration or
alter the model. This is the second and final matched repeat before changing
framework or problem context under the current S4 plan.

The executable code remains
`b346b278d5ca6a0e3997b0447b267e24b1d5b395`. It keeps exact source-bound edit
validation. A terminal malformed/schema-invalid H/C or unapplicable typed edit
is rejected and scheduler-forward starts a fresh H; a provider call without a
terminal response remains invocation-terminal. No same-call retry is allowed.

## Frozen inputs

| Input | Pre-registered value |
|---|---|
| Runtime source | `/home/clawd/research/or-autoresearch-agent-v3-hotpath/scion` |
| Executable code revision | `b346b278d5ca6a0e3997b0447b267e24b1d5b395` |
| Campaign directory | `/home/clawd/research/scion-experiments/v04-warehouse-v3-continuity-synth-36r-r8-gpt56terra-20260808T201033Z-claw/campaign` |
| Problem | `problems/warehouse_delivery/problem.yaml` |
| Protocol | `problems/warehouse_delivery/protocol.yaml` (`3.0`) |
| Split | `problems/warehouse_delivery/split_manifest.yaml` (`4.0`, 47 declared paths) |
| Seeds | `problems/warehouse_delivery/seed_ledger.yaml` (`3.0`) |
| Synthetic data root | `/home/clawd/research/or-autoresearch-agent/surrogate` |
| Provider/model | local Codex proxy / `gpt-5.6-terra` |
| Formal-stage horizon | 36 |
| Solver subprocess limit | 30 seconds per case/seed execution |
| Scheduler | default V3 state priority/FIFO, at most three active branches |

R8 changes no Warehouse source/data, Contract, Verification, Protocol,
Decision, case/seed population, threshold, time limit or scheduling rule. The
code checkpoint passed `1965 passed, 1 skipped` and a 92-test focused adjacent
set before R7/R8.

## Frozen launch command

Run exactly once:

```bash
set -euo pipefail

R8_ROOT=/home/clawd/research/scion-experiments/v04-warehouse-v3-continuity-synth-36r-r8-gpt56terra-20260808T201033Z-claw/campaign
test ! -e "$R8_ROOT"
cd /home/clawd/research/or-autoresearch-agent-v3-hotpath/scion

PROXY_KEY_VALUE="$(curl -fsS http://127.0.0.1:8080/auth/status | \
  jq -er '.proxy_api_key | select(type == "string" and length > 0)')"
trap 'unset PROXY_KEY_VALUE' EXIT

curl -fsS -o /dev/null \
  -H "Authorization: Bearer $PROXY_KEY_VALUE" \
  http://127.0.0.1:8080/v1/models

PYTHONPATH=/home/clawd/research/or-autoresearch-agent-v3-hotpath/scion \
SCION_MODEL=gpt-5.6-terra \
SCION_BASE_URL=http://127.0.0.1:8080 \
SCION_API_KEY="$PROXY_KEY_VALUE" \
SCION_WAREHOUSE_DATA_ROOT=/home/clawd/research/or-autoresearch-agent/surrogate \
  /home/clawd/miniconda3/envs/claw/bin/python -m scion.cli.main run \
  --problem problems/warehouse_delivery/problem.yaml \
  --protocol problems/warehouse_delivery/protocol.yaml \
  --split problems/warehouse_delivery/split_manifest.yaml \
  --seeds problems/warehouse_delivery/seed_ledger.yaml \
  --campaign-dir "$R8_ROOT" \
  --rounds 36 \
  --time-limit-sec 30
```

The credential is used only in the launch-process environment and is never
printed or persisted. The authorized model check must succeed before Scion
starts.

## Execution and analysis policy

- Launch once into the absent R8 root; never resume or retry the root.
- Never retry the same H or C. Scheduler-forward proposal/Contract/Verification
  rejection means a new H on the clean base and consumes no formal stage.
- Drain queued validation/frozen stages on the exact candidate without a
  provider call.
- Continue through scientific negatives until 36 formal stages complete or a
  genuine terminal outcome occurs.
- Do not alter code, data, thresholds, model, proxy route or campaign state
  while running. Observe only.
- After terminal exit, record formal-stage and H/C counts, exact failure class,
  pair completeness, funnel/Decision reasons, branch continuity and every
  promotion with its immediate parent.
- If any champion promotes, independently replay it against its immediate
  parent and replay the final champion against v1. Replay is separate and does
  not mutate R8.

## Outcome classification

- `CONTINUOUS_OPTIMIZATION_CONFIRMED`: v3 or later through two complete
  promotions, with independent retained-improvement replays.
- `PROMOTION_CONTINUITY_OBSERVED_REPLAY_PENDING`: v3 or later before replay.
- `VALID_FUNNEL_BELOW_V3`: 36 complete stages below v3.
- `SCIENTIFIC_NO_PROMOTION`: 36 complete stages with no promotion.
- `RUN_INVALID_FRAMEWORK`: framework control flow truncates the horizon.
- `RUN_INVALID_INFRA`: provider-without-terminal-response, credential,
  transport, runner or resource failure truncates the horizon.
- `INTERRUPTED`: explicit external interruption.

An incomplete R8 may provide bounded partial science but cannot satisfy the
continuous-optimization endpoint.
