# CVRP Minute One-Step Exploratory Screen Postrun — 2026-08-12

**Status:** `VALID_COMPLETED_ONE_STEP_SCREEN / SCREENING_FAIL_CASE_QUALITY / NO_PROMOTION`

## Scope and V3 authority

This report closes the separately preregistered minute-scale CVRP experiment.
The run tests whether the simplified direct-V3 path can support one real,
source-grounded algorithm research step and leave complete evidence before a
long campaign is considered. It does not validate or promote the candidate,
establish broad CVRP improvement, or complete `TASK.md`.

[`design/scion-architecture-v3.md`](../../../design/scion-architecture-v3.md)
remains the sole architecture authority. The run used the existing path:

```text
problem-owned facts + complete editable source + prior safe evidence
  -> one structured H
  -> Contract
  -> one approved-H-bound C
  -> isolated Workspace
  -> executable Verification
  -> canary + paired SCREENING Protocol
  -> Safe Features
  -> deterministic Decision
```

No deployment, distribution, packaging, build, root/systemd, Trust/Hash,
identity, lease, signing, receipt, registry, self-proof or new research gate
was added.

## Lifecycle and artifacts

- Preregistration:
  [`v0.4-cvrp-minute-one-step-preregistration-20260812.md`](v0.4-cvrp-minute-one-step-preregistration-20260812.md)
- Clean source reference:
  `e4b6b98d63eac7e088d9a9be960a6b5df08bca9d`
- Output root:
  `/home/clawd/research/scion-experiments/v04-cvrp-minute-one-step-20260812-claw`
- Process observation:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-minute-one-step-20260812-control`
- Driver result: `driver_result.json`
- Campaign summary: `campaign/campaign_summary.json`
- Lineage database: `campaign/scion.db`
- Raw screening evidence:
  `metrics/9805125e-b0fc-4c29-90ba-e186c5920f16.json`

Fresh source-send authorization was granted before the single launch. The
ordinary-user inner process was acquired at `2026-08-12 12:02:19 UTC` and
wrote its terminal driver artifact at `12:04:13.687 UTC`, about 114.7 seconds
later. The process-control exit code is `0`.

The terminal driver record is `COMPLETED_ONE_STEP_WRAPPER` and records:

- one `CampaignManager.run_one_step()` call and zero `run()` calls;
- one H call and one C call;
- one canary call and one formal `SCREENING` call;
- zero validation and frozen calls;
- `finalize_requested_stop("minute_one_step_complete")` reflected as the
  terminal stop reason;
- no exception, retry, resume or promotion.

`campaign_summary.run_complete=true` follows the one-step wrapper's zero
requested-round accounting. It is not evidence that validation, frozen
holdout or a complete promotion campaign ran. The driver therefore persists
the narrower scope
`one_step_wrapper_only_not_validation_frozen_or_campaign_completion`.

## H, C and applied candidate

Both provider calls used `gpt-5.6-terra` through the local Codex proxy.

| Call | Count | Provider timeout | Wall time | Input / output / reasoning tokens |
|---|---:|---:|---:|---:|
| H / `generate_hypothesis` | 1 | 90 s | 15.299 s | `20,940 / 643 / 340` |
| C / `generate_patch` | 1 | 180 s | 7.676 s | `19,912 / 370 / 104` |

Both calls returned exactly one valid structured tool call. H received the
frozen research question and five typed prior items; the experiment-owned R45
prior appeared exactly once. C received the exact approved H and one ordinary
editable-source context containing 11 current solver paths.

H proposed activating the existing embedded-VNS runtime-share cap while
retaining repair-improvement rescue. C expressed that hypothesis as one exact
single-file replacement. The only semantic workspace difference from B0 is:

```diff
-EMBEDDED_VNS_MAX_RUNTIME_SHARE = 0.0
+# Retain a bounded share for selective polishing while leaving most ALNS-loop
+# time for destroy/repair exploration and simulated-annealing acceptance.
+EMBEDDED_VNS_MAX_RUNTIME_SHARE = 0.35
```

Generated `__pycache__` content aside, the candidate and champion trees differ
only in `policies/baseline_modules/config.py`. The active scheduler imports the
constant, reads it in `_should_run_embedded_vns`, and retains
`EMBEDDED_VNS_CAP_REPAIR_IMPROVEMENT_RESCUE=True`. The patch is therefore a
real algorithm configuration change, not unused scaffolding or framework work.

## Contract, Verification and canary

The durable experiment record reports Contract passed with an empty diagnostic
list. It does not persist a separate per-check H-Contract artifact, so H
acceptance is supported by the stored typed hypothesis, the C call's exact
approved-H binding and the aggregate Contract result rather than by an
independent H-check inventory.

Verification passed all nine recorded checks:

| Check | Result | Durable detail |
|---|---|---|
| V1 syntax | pass | syntax ok |
| V1b undefined names | pass | no undefined names in the changed file |
| V2 interface | pass | support-module interface deferred to workspace smoke |
| V3 unit tests | pass/skipped | no unit-test path configured |
| V4 regression tests | pass/skipped | no regression-test path configured |
| V5 solution consistency | pass | adapter consistency, 8 ms, 2 s limit |
| V6 feasibility | pass | 4 ms, 2 s limit |
| V7 objective oracle | pass | 3 ms, 2 s limit |
| V8 nondeterminism | pass | canonical signatures equal across two runs, 2,421 ms |

The one typed canary call used
`controlled/data/synthetic_controlled_canary_5.vrp`, seed `101`, and passed
with one attempted pair and `candidate_status=passed`. Its durable object has
`raw_metrics_ref=null` and `champion_status=not_applicable`; the generic raw
unavailability field also says `canary_veto_before_formal_protocol` despite the
pass. This report therefore treats the artifact as one candidate-side canary
pass, not as independently auditable paired champion/candidate canary evidence.
That observability limit did not prevent the declared formal screen.

## Exact formal screen

`A` is the fresh B0/v1 champion and `B` is the fresh candidate. Positive
`total_distance` delta means the candidate is shorter because the raw metric is
`A - B`. Objective tuples below are
`(fleet_violation, total_distance, routes)`.

| Ordinal | Case / seed | Scheduled -> actual | Limit | A objective / time | B objective / time | Distance delta | Result |
|---:|---|---|---:|---|---|---:|---|
| 0 | `P/P-n65-k10` / 11 | `AB -> AB` | 8 s | `(0,814,10)` / 6,532 ms | `(0,830,10)` / 6,601 ms | -16 | loss |
| 1 | `E/E-n101-k14` / 11 | `BA -> BA` | 12 s | `(0,1130,14)` / 9,711 ms | `(0,1130,14)` / 9,638 ms | 0 | tie |
| 2 | `X/X-n120-k6` / 11 | `AB -> AB` | 12 s | `(0,14204,6)` / 10,321 ms | `(0,14204,6)` / 9,666 ms | 0 | tie |
| 3 | `X/X-n233-k16` / 11 | `BA -> BA` | 15 s | `(0,20112,17)` / 15,817 ms | `(0,20112,17)` / 13,187 ms | 0 | tie |

All eight subject executions report `success=true`, `feasible=true`, exit
`0`, no failure and the exact per-case time limit. Raw completeness is
`4 total / 4 attempted / 4 valid / 0 failed`, with zero candidate or champion
failures and no protected `fleet_violation` regression. Case ordinals are
`0..3`, seed ordinal is zero, block ordinal is zero, and actual order equals
the preregistered `AB/BA/AB/BA` schedule. Candidate ordinal and block id remain
preregistration facts because this raw schema does not persist them.

Champion caching was disabled. Every pair reports the champion result source
as `fresh`; cache hits, misses, writes and cached-runtime pairs are all zero.
The four candidate subjects also executed normally and have no reused-result
path in the paired Protocol.

## Independent statistical recomputation

The exact `total_distance` effects are `[-16, 0, 0, 0]`. Because there is one
seed per case, pair and case outcomes are both:

```text
W/L/T:              0/1/3
win rate:           0.00
loss rate:          0.25
net case score:    -0.25
paired median:      0
```

Re-running the checked-in 1,000-sample median bootstrap with random seed `42`
gives `95% CI [-16,0]`, exactly matching Protocol. All four protected-metric
effects are zero. The result fails the ordinary case-quality rule rather than
an infrastructure or wrapper rule: win rate is below `0.60`, median is below
the practical delta `2`, CI lower bound is below zero, net score is below
`0.25`, and loss rate is above `0.20`.

Protocol therefore records `SCREENING_FAIL_CASE_QUALITY`; deterministic
Decision records `continue_explore`. This is a valid completed negative screen,
not a hidden pass.

## Search-allocation telemetry

The mechanism-evidence columns are candidate, champion and
candidate-minus-champion:

| Observation | Candidate | Champion | Difference |
|---|---:|---:|---:|
| Solver algorithm elapsed | 37,727 ms | 40,987 ms | -3,260 ms |
| ALNS-core runtime share | 0.382246 | 0.195208 | +0.187038 |
| Embedded-VNS runtime share | 0.287778 | 0.419914 | -0.132136 |
| Initial-VNS runtime share | 0.318260 | 0.373533 | -0.055273 |
| ALNS iterations | 151 | 44 | +107 |
| ALNS accepted | 49 | 31 | +18 |
| ALNS best updates | 4 | 7 | -3 |
| VNS attempts | 404 | 913 | -509 |
| VNS improvements | 90 | 192 | -102 |
| Post-repair polish improvements | 16 | 34 | -18 |

The aggregate phase shift is consistent with the proposed cap, but useful
quality conversion fell. ALNS best-update density is about `2.65%` (`4/151`)
for the candidate versus `15.91%` (`7/44`) for B0. The candidate also records
six route-limit rejections versus zero for B0, although all final solutions
preserve fleet feasibility.

The shift is concentrated in the non-X cases:

| Case | Candidate/champion ALNS core ms | Candidate/champion embedded-VNS ms | Candidate/champion ALNS iterations |
|---|---:|---:|---:|
| `P-n65-k10` | `3992 / 862` | `2184 / 5258` | `123 / 31` |
| `E-n101-k14` | `4778 / 1187` | `4244 / 7872` | `26 / 10` |
| `X-n120-k6` | `454 / 1150` | `4427 / 4078` | `1 / 2` |
| `X-n233-k16` | `5197 / 4802` | `2 / 3` | `1 / 1` |

Thus the P and E rows show cap-consistent reallocation, but P becomes 16 units
worse and E ties. The X rows remain dominated by initial VNS and one or two
ALNS iterations; they do not establish cross-family mechanism benefit.

Candidate-minus-champion wall deltas are `[+69,-73,-655,-2630]` ms. Their
median is `-364` ms and the median runtime ratio is `0.9645099544`. The runtime
model is budget-exhausting and both sides trigger the informational tiny-budget
saturation diagnostic. Runtime evidence is therefore observational and is not
a standalone optimization signal or Decision feature.

The stored mechanism object explicitly marks `hypothesis_attribution=unbound`,
`interpretation_constraint=association_only` and `gate_influence=false`.
Accordingly the telemetry supports a cap-consistent allocation observation,
not strict event-level causal attribution.

## Decision, champion and conclusion

The database contains exactly one champion row, B0/v1, with no promotion
experiment, dossier or promotion timestamp. The candidate remains the
provisional head of its `explore` branch, as required by V3 for a verified
completed screen, but it is not a champion. Screening expansion count and
validation expansion count are both zero.

This run establishes a narrower but useful result: after the research-hot-path
subtraction, Terra formed a source-grounded, falsifiable solver hypothesis; C
implemented it as a faithful minimal active-path patch; and Scion completed a
stable Contract, Verification, canary, paired Protocol and Decision cycle with
analyzable evidence. It does not establish that the candidate improved the
solver. On this exact four-case, one-seed screen, the intended allocation shift
did not convert into more best updates or lower final distance.

The experiment is sealed as
`VALID_COMPLETED_ONE_STEP_SCREEN / SCREENING_FAIL_CASE_QUALITY / NO_PROMOTION`.
It is not resumed, retried, validated, frozen or promoted by this report. CVRP
acceptance and S6 remain open. Any later experiment requires a separate design,
preregistration and authorization; this postrun creates none.
