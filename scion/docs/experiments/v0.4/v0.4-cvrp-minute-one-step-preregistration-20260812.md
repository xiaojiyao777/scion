# CVRP Minute One-Step Exploratory Screen Preregistration — 2026-08-12

**Preparation status:** `PREPARED_NOT_STARTED`

**Authorization state:** `AWAITING_FRESH_SOURCE_SEND_AUTHORIZATION`

No provider or solver call has started. The experiment output, process-control
root and tmux socket were absent when this preregistration was written.

## Question and V3 authority

This run asks exactly:

> Under a fixed solver deadline, what one CVRP-owned algorithm change can
> improve allocation of useful search between ALNS and local-search phases, so
> destroy/repair/acceptance receives enough opportunity without discarding
> useful local-search polishing, while preserving feasibility, fleet and final
> total_distance semantics? Choose the mechanism from the visible source and
> runtime evidence.

The immediate purpose is to test one short, real direct-V3 research step after
the research-hot-path subtraction: can one Terra H/C sequence form and
implement a source-grounded algorithm hypothesis, traverse the existing
Contract and executable Verification boundaries, and produce an analyzable
canary or four-case screening outcome without lifecycle instability?

[`design/scion-architecture-v3.md`](../../../design/scion-architecture-v3.md)
is the sole architecture authority. The v0.4 direct-runtime addendum explains
the lightweight implementation but cannot replace V3 ownership. The active
chain remains:

```text
problem-owned facts + complete editable source + safe prior evidence
  -> at most one structured H
  -> Hypothesis Contract
  -> at most one approved-H-bound C
  -> Patch Contract
  -> isolated Workspace
  -> executable Verification
  -> canary + problem-owned paired SCREENING Protocol
  -> Safe Features
  -> deterministic Decision
```

This diagnostic adds no novelty, activation, mechanism, telemetry-quality or
host-style gate. LLM output stays tainted; it cannot choose cases, seeds,
Protocol results, Decision actions or promotion.

## Fresh source-send authorization boundary

The H request will send problem facts and source-derived context from the
frozen runtime through the local Codex proxy to `gpt-5.6-terra`. If H is
approved, C will receive that approved H plus the ordinary editable source
context. This is a new provider experiment. The prior authorization for the
eight completed C-expression A/B calls does not authorize these H/C sends.

Launch therefore requires a new explicit user authorization covering this
one-step H/C source-derived context. Until that authorization is recorded, only
offline manifest/config checks and fake/static tests may run. Those checks do
not call the proxy, provider or solver.

## Frozen source, model and call envelope

| Input | Frozen value |
|---|---|
| Input bundle | `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-minute-one-step-20260812-input` |
| Runtime | bundle-owned ordinary-user clean archive under `runtime/scion` |
| Exact source reference | `e4b6b98d63eac7e088d9a9be960a6b5df08bca9d` |
| Problem/solver source | `runtime/scion/scion/problems/cvrp` |
| External case root | `/home/clawd/research/or-autoresearch-agent/vrp` |
| Champion | original CVRP `B0`, version `1` |
| Provider | local Codex proxy at `http://127.0.0.1:8080` |
| Model | `gpt-5.6-terra` |
| H calls | at most one; 90-second provider timeout |
| C calls | at most one, only after approved H; 180-second provider timeout |
| Provider retries | zero |
| Campaign-manager calls | `run_one_step()` exactly once; `run()` never |

The commit is an ordinary exact scientific source reference, not an identity,
signature or authority object. The run does not introduce a hash chain, source
acceptance, receipt, registration, lease or repeated self-proof closure.

The provider tool surface is the unchanged direct-runtime structured H and
typed exact/full-file C surface in the selected source. Complete visible source
is pushed in context. No provider shell, network, execution, file-navigation or
same-call edit/repair loop is added.

## Frozen neutral prior

H receives the following experiment-owned neutral evidence item exactly once:

> The current source enables initial VNS and considers embedded VNS at cadence
> 1, including repair-improvement triggers. Completed R45 diagnosis found broad
> immediate-base effects mostly zero while the strongest
> scheduler/elapsed-budget candidates became directionally positive only at 2x
> budgets. This is evidence of phase-allocation/implementation-fidelity
> opportunity, not proof of any required cadence, cap, SA rule or file.

This prior motivates investigation of useful work inside the existing budget.
It neither prescribes a module or mechanism nor enters Decision.

## Canary, Verification and screening population

Verification uses a two-second runtime budget. Protocol canary uses
`controlled/data/synthetic_controlled_canary_5.vrp`, seed `101` and a
two-second per-subject limit. Canary remains veto-only; it is not improvement
evidence.

If Contract and Verification pass and canary does not veto, the only reachable
formal stage is one initial `SCREENING` call over seed `11`:

| Ordinal | Case | Per-subject limit | Scheduled order |
|---:|---|---:|---|
| 0 | `cvrplib/P/P-n65-k10.vrp` | 8 s | `AB` |
| 1 | `cvrplib/E/E-n101-k14.vrp` | 12 s | `BA` |
| 2 | `cvrplib/X/X-n120-k6.vrp` | 12 s | `AB` |
| 3 | `cvrplib/X/X-n233-k16.vrp` | 15 s | `BA` |

`A` is the fresh B0 champion subject and `B` is the fresh candidate. Candidate
ordinal and block ordinal are both zero; case ordinals are `0..3` and seed
ordinal is zero. Order follows the existing parity rule. Actual order must
equal scheduled order. Champion caching is disabled, so the declared four
pairs execute both subjects fresh. The maximum declared screening solver time
is therefore 94 subject-seconds.

The case estimator is paired-effect median with zero equivalence band.
`total_distance` is the effect metric, lower is better; `fleet_violation` is
the protected objective. The practical screening delta is `2`. The unchanged
screening rule retains the declared `0.60` case win rate, nonnegative bootstrap
CI lower bound, `0.25` minimum net case score and `0.20` maximum case loss
rate. `require_expanded_for_pass=false` because this is a one-step diagnostic,
not an expansion campaign.

The production schema also contains disjoint unreachable placeholders:
validation `cvrplib/B/B-n67-k10.vrp`, seed `47`; frozen
`cvrplib/P/P-n76-k4.vrp`, seed `61`. The wrapper rejects validation, frozen and
expanded screening before execution. It calls `finalize_requested_stop()`
immediately after the single returned step. A queued Decision is recorded but
never drained by this run.

## Execution and process envelope

- Local solver subprocess limits are 45 seconds, 2,048 MiB and 256 file
  descriptors. The driver hard wall is 720 seconds, delivered through
  foreground `SIGINT` with a bounded kill-after fallback so Python can write a
  terminal artifact.
- Launch is ordinary-user `clawd` only. It uses a user-owned tmux server at the
  explicit short socket `/tmp/scion-minute-one-step.tmux.sock`, with
  remain-on-exit and an acquired/exit observation. No root, systemd, deployment,
  distribution, packaging or build step is involved.
- The inner process starts from `env -i` with only the minimal home, locale,
  path and Python runtime variables plus the non-`SCION_*` ephemeral
  `CODEX_PROXY_CLIENT_KEY`. The driver removes that key from the environment
  before any candidate/champion subprocess and passes it explicitly only to the
  provider client. The key is not an argv, log or artifact field.
- Proxy authentication/model acquisition is bounded. Output, control and socket
  roots must all be fresh. The driver creates the output once, writes terminal
  status atomically and has no resume or retry path.

Frozen future roots are:

- output: `/home/clawd/research/scion-experiments/v04-cvrp-minute-one-step-20260812-claw`;
- process control:
  `/home/clawd/research/or-autoresearch-agent/campaign_out/v04-cvrp-minute-one-step-20260812-control`;
- launcher: `launch.sh` in the input bundle.

## Wrapper completion, postrun classification and claim boundary

The hot-path driver does not parse raw metrics into a scientific acceptance
gate. A normal return from the single `run_one_step()` followed by successful
finalization is `COMPLETED_ONE_STEP_WRAPPER`, regardless of whether H/C is
rejected, canary vetoes, a subject fails, or the ordinary Protocol gate is
positive or negative. The wrapper records bounded call counts, the returned
typed result summary and any raw reference. Only an exception or violation of
the declared one-step lifecycle makes the wrapper `RUN_INVALID`.

After the process is terminal, a separate read-only postrun audit may classify
the retained evidence:

| Terminal evidence | Read-only postrun class/status | Allowed interpretation |
|---|---|---|
| Four complete fresh successful screening pairs and a typed non-promotion Decision | `VALID_COMPLETED_ONE_STEP_SCREEN` | One valid exploratory screen only |
| Typed candidate canary veto | `VALID_TERMINAL_SUBJECT_FAILURE_EVIDENCE` / `candidate_canary_failure` | Controlled-canary candidate negative; no screen comparison |
| Four complete attempted screening pairs with candidate-side failure | `VALID_TERMINAL_SUBJECT_FAILURE_EVIDENCE` / `candidate_screen_failure` | Terminal candidate execution negative; no improvement claim |
| Four complete attempted screening pairs with champion-side failure | `VALID_TERMINAL_SUBJECT_FAILURE_EVIDENCE` / `champion_screen_failure` | Partial champion evidence; comparison unavailable |
| Typed pre-Protocol H/C, Contract or Verification rejection | `VALID_TERMINAL_RESEARCH_REJECTION` | Valid attempt-terminal research/process observation; no solver conclusion |
| Partial, interrupted, resource, configuration, malformed or untyped evidence | `RUN_INVALID_*` | No scientific comparison; report the exact infrastructure/evidence boundary |

These read-only labels cannot alter the recorded Decision, resume a candidate,
request another provider call or change the wrapper exit. In particular,
`VALID_TERMINAL_SUBJECT_FAILURE_EVIDENCE` means only that a bounded
subject-negative outcome terminalized and was durably attributable. It does not
mean a valid comparison, a stable solver, or an algorithm improvement.

Even a complete positive screen cannot establish validation, frozen holdout,
promotion, retained B0 improvement, broad CVRP improvement or `TASK.md`
completion. `campaign_summary.run_complete`, if present, is ignored outside the
one-step wrapper scope. Champion must remain B0/v1, no `PROMOTE` is admissible,
and no validation/frozen/final replay is executed.

## Launch and postrun rule

The sole launch remains blocked on fresh source-send authorization. Once the
user explicitly grants it, run the prepared launcher once. Do not alter source,
question, prior, model, call/time limits, cases, seeds, order, cache policy,
Protocol or claim rules. Do not start another run automatically if acquisition,
provider, candidate, champion or host execution fails.

After terminalization, analyze the exact H, C, normalized patch, Contract,
Verification, canary, raw four-pair evidence and recorded Decision. Separate
agent research quality, code-expression fidelity, subject execution, Protocol
measurement and process stability. The result may justify a separately
preregistered next short step or the larger CVRP promotion campaign; it cannot
launch either one itself.
