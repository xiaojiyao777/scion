# Warehouse V3 Continuity Synthetic 36-Stage R2 Preregistration — 2026-08-08

**Status:** `PRE_REGISTERED_NOT_RUN`

## Repair boundary

The first preregistered launch exited in CLI preflight because the synthetic
split's relative `data/...` paths were resolved through the production data
root. No campaign directory, provider call, H/C, solver run or scientific result
was created. This R2 changes only the explicit problem data-root environment and
the fresh campaign directory. All scientific inputs and policies remain those
of the first preregistration.

Read the complete question, analysis plan and outcome classes in
`v0.4-warehouse-v3-continuity-synthetic-36stage-preregistration-20260808.md`.

## Frozen inputs

| Input | Pre-registered value |
|---|---|
| Runtime source | `/home/clawd/research/or-autoresearch-agent-v3-hotpath/scion` |
| Runtime code checkpoint | `68413a34` |
| Campaign directory | `/home/clawd/research/scion-experiments/v04-warehouse-v3-continuity-synth-36r-r2-gpt56terra-20260808T162816Z-claw/campaign` |
| Problem | `problems/warehouse_delivery/problem.yaml` |
| Protocol | `problems/warehouse_delivery/protocol.yaml` (`3.0`) |
| Split | `problems/warehouse_delivery/split_manifest.yaml` (`4.0`, 47/47 paths present) |
| Seeds | `problems/warehouse_delivery/seed_ledger.yaml` (`3.0`) |
| Synthetic data root | `/home/clawd/research/or-autoresearch-agent/surrogate` |
| Provider/model | local Codex proxy at `127.0.0.1:8080`, `gpt-5.6-terra` |
| Protocol-stage horizon | 36 typed evaluated stages |
| Solver subprocess limit | 30 seconds per case/seed execution |

## Frozen command

```bash
cd /home/clawd/research/or-autoresearch-agent-v3-hotpath/scion

PYTHONPATH=/home/clawd/research/or-autoresearch-agent-v3-hotpath/scion \
SCION_MODEL=gpt-5.6-terra \
SCION_BASE_URL=http://127.0.0.1:8080 \
SCION_WAREHOUSE_DATA_ROOT=/home/clawd/research/or-autoresearch-agent/surrogate \
  /home/clawd/miniconda3/envs/claw/bin/python -m scion.cli.main run \
  --problem problems/warehouse_delivery/problem.yaml \
  --protocol problems/warehouse_delivery/protocol.yaml \
  --split problems/warehouse_delivery/split_manifest.yaml \
  --seeds problems/warehouse_delivery/seed_ledger.yaml \
  --campaign-dir /home/clawd/research/scion-experiments/v04-warehouse-v3-continuity-synth-36r-r2-gpt56terra-20260808T162816Z-claw/campaign \
  --rounds 36 \
  --time-limit-sec 30
```

The first preregistration's execution/stop policy, analysis plan, acceptance
endpoint and exclusions apply unchanged. In particular: run once, do not retry
provider calls, do not alter framework/config/data/thresholds, do not stop on a
promotion or negative candidate, and do not reuse the failed R1 root.
