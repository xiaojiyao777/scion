"""Internal proposal context assembly package.

`scion.proposal.context_manager` remains the public compatibility facade.
Modules in this package own focused context responsibilities and must stay
problem-independent unless they call an adapter/provider hook.
"""

