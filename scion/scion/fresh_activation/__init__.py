"""Dormant fresh-only activation primitives.

The package deliberately exports no composition root.  Production campaign
initialization must not import it until the complete frozen activation path has
passed its independent acceptance sequence.
"""
