# Scion v0.4 Effective Research Repair Design

*Date: 2026-06-23*
*Status: Designs A-J, typed proposal failure routing, weak-positive
runtime-pressure follow-up policy, target-intent authority resolution,
post-validation actionability classifier repair, warehouse guidance-binding
repair, launcher running-status repair, and launcher lifecycle contract
modularization are implemented and focused-tested;
clean CVRP scheduler-status validation accepted; CVRP target-intent authority
validation is accepted as framework evidence, not solver progress*

This document defines the next repair slice needed before another long CVRP or
warehouse campaign. It is intentionally design-first: the goal is to restore
generic Scion research behavior without turning Scion into a CVRP-specific
framework or adding another layer of helper-function patches.

## 1. Scope

v0.4 must prove that Scion can support effective research before v0.5 starts
the broad comparison matrix. The current blocker is not a new CVRP heuristic.
The current blocker is framework semantics:

- branch scheduling conflates live branch records, schedulable work, and scarce
  active slots;
- prepared research focus is copied through launch/readiness/postrun tools by
  ad hoc projection helpers and string probes;
- problem-specific CVRP contract details have leaked into generic projection
  and readiness code;
- free-form opportunity diagnostics can create lifecycle or scheduler pressure
  unless reduced to typed evidence signals;
- branch lifecycle policy blocks can be over-interpreted from failure-detail
  prose unless mutation paths consume typed policy-block signals;
- live campaign accounting can misclassify proposal blocks when `reason` prose
  happens to contain legacy control-plane keywords;
- proposal-boundary failure routing can misclassify typed agentic failures when
  diagnostic text mentions legacy timeout or transient-service keywords;
- runtime-evidence completeness pressure can preempt a current weak-positive
  branch even when the branch has no case-level loss and still needs
  same-mechanism follow-up;
- prepared launch focus can hard-bind target intent to a new mechanism even
  after the scheduler has selected an existing branch whose branch-local policy
  requires protected same-mechanism follow-up;
- typed research guidance can confuse context-only research/evidence axes with
  hard hypothesis mechanism ids, blocking valid problem-owned operator
  telemetry identities;
- root launcher status can remain `prepared` during a live campaign, making
  operator monitoring disagree with `campaign/run_status.json`;
- very large scripts and tests are accumulating repeated contract fixtures.

The design below keeps v3 boundaries intact:

- LLM output remains tainted proposal material.
- Decision still reads only `DecisionFeatures`.
- Problem diagnostics may guide proposal context, readiness, runtime
  governance, and lifecycle policy only through deterministic, schema-validated
  fields.
- CVRP, VRP, warehouse, and future problem semantics stay in problem-owned
  providers.

## 2. Evidence Trigger

The WSL root
`/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-missingprimary-8d28bc30-narrowavoid-4r-gpt55-20260622T171659Z-claw`
finished wrapper/postrun execution with no postrun failures, but the campaign
itself stopped as `valid_partial_interrupted` after 3 of 4 requested effective
rounds:

- `last_stop_reason=scheduler_active_slot_blocked`;
- `postrun_acceptance` readiness is `ready`;
- `active_slots.used=3`, `max=3`, with
  `bba3d45f`, `ec052599`, and `9faaf70b`;
- the current-run branch `9faaf70b` is correctly classified as
  `last_screening_feedback_tier=inactive`;
- copied or resumed weak-positive branches still consume active slots and can
  block a new branch even when they are not the next useful research action.

This is a generic scheduling and state-reconciliation problem. CVRP exposed it,
but the fix must apply to any problem package that resumes from prior branches.

The later clean scheduler-status validation root
`/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-schedstatus-d0dded44-clean-missingprimary-4r-gpt55-20260623T025241Z-claw`
accepted the active-slot repair: 4 of 4 effective rounds,
`last_stop_reason=max_rounds_exhausted`, and no scheduler active-slot blocks.
It did not show solver progress. Its postrun evidence exposed the next generic
research-quality blocker: same-mechanism follow-up was available in 4 places
but selected once, because runtime-evidence completeness pressure preferred a
clean fork over current weak-positive branches with no case-level loss. The
design response is a scheduler policy repair, not a CVRP mechanism repair.

After the weak-positive scheduler-policy repair, the continuity root
`/home/xjy-ubuntu/research/scion-experiments/v04-cvrp-continuity-77f4abe7-postweakpressure-4r-gpt55-20260623T051921Z-claw`
confirmed the scheduler can select an existing weak-positive branch under
`exploit_weak_positive`, but failed before Protocol rows. Prepared launch focus
required one mechanism while the selected existing branch's protected
mechanism authority required another. The result was a deterministic loop
between `same_mechanism_only_violation` and
`target_intent_binding_mismatch`. The design response is generic
target-intent authority resolution, not a CVRP target exception.

## 3. Design A: Branch Scheduling Status

### Problem

Active-slot accounting currently answers "does this branch count?" by walking
branch state, `branch_code_status`, feedback tier, lifecycle blocks, and
scheduler-owned release predicates. The same facts are reconstructed in the
scheduler, branch cards, status summaries, and postrun reports. The result is
string-driven and hard to audit.

### Target Model

Introduce one deterministic status object owned by scheduling, not by CVRP:

```python
@dataclass(frozen=True)
class BranchSchedulingStatus:
    branch_id: str
    branch_state: str
    evidence_tier: str
    lineage_status: str
    lane: str
    schedulable: bool
    consumes_active_slot: bool
    release_reason: str
    next_action_reason: str
    promotion_boundary: str = "not_a_promotion_or_validation_decision"
    decision_features_excluded: bool = True
```

The `lane` is a generic scheduling lane, for example:

- `promotion_stage`
- `validation_stage`
- `stale_reconcile`
- `weak_positive_followup`
- `diagnostic_followup`
- `low_signal_followup`
- `clean_exploration`
- `not_schedulable`

The scheduler, active-slot inventory, branch cards, and status summaries should
consume this status object instead of recomputing separate helper predicates.

### Rules

- Terminal, abandoned, and decision-parked branches never consume active slots.
- A branch can be reportable while not consuming an active slot.
- A branch can be active-slot consuming only if it has a schedulable lane or a
  deliberate recovery reason that still needs capacity.
- Scheduler-origin reclaim may park only branches that are explicitly eligible
  by deterministic status, and the status must explain why this is not a
  promotion or quality judgment.
- Resumed branches must be reconciled into `BranchSchedulingStatus` before
  active-slot inventory is computed. Copied weak-positive state is not enough;
  the current status must reflect the latest evidence tier and current
  schedulability.
- If all slots are consumed but no branch can make forward progress, the
  campaign should report an actionable scheduling-state failure rather than
  looking like a successful research stop.

### Acceptance

- Unit tests build generic dummy branches; no CVRP mechanism names are required.
- A resumed weak-positive branch with superseding inactive or non-actionable
  current evidence does not block all clean exploration capacity.
- A true weak-positive branch with current actionable evidence remains
  schedulable and can consume a slot.
- Quality-regression and hard-negative branches still fail closed or release
  slots according to deterministic lifecycle evidence.
- Branch cards and `status.json` agree with scheduler selection because both
  are rendered from the same status object.

## 4. Design B: Research Guidance Contract

### Problem

Prepared launch guidance is currently shaped by problem launchers but projected
through generic tools by helper functions. The generic context manager and
readiness tools know keys such as `large_instance_two_opt_constraints`, and
readiness checks look for strings such as `CMT2`, `CMT4`, or
`large_instance_intra_route_two_opt_seed`. That is a boundary leak.

### Target Model

Problem packages should expose a typed research-guidance contract through a
stable port:

```python
class ProblemResearchGuidanceProvider(Protocol):
    def build_guidance_contract(self, context: GuidanceContext) -> ResearchGuidanceContract:
        ...
```

The generic contract shape should be problem-neutral:

```python
@dataclass(frozen=True)
class ResearchGuidanceContract:
    schema_version: str
    problem_family: str
    current_question: str
    required_mechanisms: tuple[RequiredMechanism, ...]
    evidence_requirements: tuple[EvidenceRequirement, ...]
    avoid_rules: tuple[AvoidRule, ...]
    continuity_requirements: tuple[ContinuityRequirement, ...]
    guidance_blocks: tuple[GuidanceBlock, ...]
    measurement_summary: MeasurementGuidanceSummary | None
    decision_boundary: str
    proposal_visibility_only: bool = True
    decision_features_excluded: bool = True
```

Problem-specific content lives inside typed blocks with stable ids and
categories. Generic code may validate and render these blocks, but it must not
interpret CVRP case names or mechanism ids.

### Ports

- Problem-owned provider:
  `scion/scion/problems/<problem>/research_guidance.py`
- Generic schema and renderer:
  `scion/scion/research_guidance/schema.py`
  and `scion/scion/research_guidance/rendering.py`
- Generic validators check schema, visibility flags, required block ids, and
  rendered-path coverage.
- Problem-owned validators check CMT2/CMT4, bounded two-opt, warehouse
  champion-v2 evidence, or future domain-specific requirements.
- Tool scripts become thin CLI wrappers around these ports.

This is modularization by responsibility, not a new pile of helper files.

### Acceptance

- `rg "large_instance_intra_route_two_opt_seed|CMT2|CMT4|large_instance_two_opt_constraints" scion/scion/core scion/scion/proposal/context_manager`
  has no production hits outside tests or problem-owned providers.
- Generic prompt projection accepts a dummy problem guidance contract and
  renders its required mechanisms and evidence blocks without knowing the
  domain.
- CVRP launcher/readiness tests still prove the CVRP provider emits the
  bounded large-instance two-opt contract, but those assertions live in CVRP
  provider or launcher tests.
- Existing prepared roots remain readable during one compatibility window by
  adapting old `research_focus` manifests into the new contract at the
  problem-owned boundary.

## 5. Design C: Fresh Runtime Opportunity Signal

### Problem

Fresh-runtime follow-up is a useful control-plane mechanism, but it must not be
created from proposal prose. Before this cleanup, generic lifecycle and
scheduler paths treated non-empty `opportunity_diagnostics` strings as an
actionable-loss signal. That made free-form diagnostics capable of creating
fresh-runtime markers or replay pressure.

### Target Model

`FreshRuntimeOpportunitySignal` is the shared generic interpretation boundary:

- It records whether structured phase evidence or structured reason-code
  families support an actionable-loss follow-up.
- It records when free-form `opportunity_diagnostics` text was present but
  ignored as a scheduler/lifecycle source.
- It stays proposal/control-plane visible and explicitly excluded from
  `DecisionFeatures`.

Lifecycle marker creation and fresh-runtime replay materialization both consume
this typed signal. Pair-level weak-positive replay remains supported, but
`RUNTIME_TIE_FRESH_CHAMPION_REQUIRED` alone is only a freshness precondition; it
is not an actionable diagnostic source.

### Acceptance

- Text-only `opportunity_diagnostics` plus fresh-runtime-required status does
  not create an actionable-loss follow-up marker.
- A stale or text-only pending marker is not replayed unless the current summary
  carries pair-level or structured actionable-loss signal sources.
- Structured phase activation/effect evidence and diagnostic/telemetry reason
  codes still allow actionable-loss fresh-runtime follow-up.

## 6. Design D: Lifecycle Policy-Block Signal

### Problem

Lifecycle policy blocks are legitimate framework control-plane events, but
branch state must not be mutated because arbitrary failure prose happens to
contain policy keywords. Reporting and run-validity classifiers may still
recognize legacy strings, but scheduler/lifecycle state mutation needs a typed
signal.

### Target Model

`BranchLifecyclePolicyBlockSignal` is derived from the exact
`RepairPolicyCheck` machine detail shape or passed directly by the policy
checker. `record_branch_lifecycle_policy_block()` can record an explicit
generic block, but clean-fork routing fields such as `candidate_routing` and
`clean_fork_signal` are set only from the typed signal or explicit structured
arguments, never by substring search in `detail`.

Proposal failure lifecycle and circuit-breaker accounting use an exact
policy-check violation parser for machine-generated `RepairPolicyCheck.detail`
payloads, including the agentic wrapper form. This keeps repair-first and
branch-lifecycle policy blocks out of proposal streaks without letting free-form
agent notes suppress failures by mentioning policy keywords.

Agentic proposal-boundary routing uses one `AgenticFailureRoutingSignal`.
Typed agentic output routes timeouts and transient-service failures only from
`termination_reason` / `failure_category`; failure detail is report material.
Framework-boundary circuit suppression requires either an exact policy-check
payload or an exact structured machine rejection such as
`failure_code=forced_surface_constraint`. A typed failure whose prose merely
mentions framework-boundary words remains a proposal/circuit failure.

### Acceptance

- Text that merely mentions lifecycle policy keywords does not become a
  `branch_lifecycle_policy` attempt in the explore pipeline.
- A valid `RepairPolicyCheck` lifecycle violation produces a typed signal and
  records clean-fork routing metadata.
- Valid repair-first and branch-lifecycle policy-check details stay outside
  proposal/circuit-breaker streaks even when wrapped by agentic failure detail.
- Broad `is_branch_lifecycle_policy_block_detail()` classification remains
  available for report/run-validity compatibility, not as the mutation gate.
- Typed agentic failure text that mentions timeout, transient-service, or
  framework-boundary keywords does not change routing unless the corresponding
  typed category or exact machine payload is present.

## 7. Design E: Live Attempt-Kind Accounting

### Problem

`CampaignLoop` is the live budget and stop-reason accounting layer. It must not
infer control-plane attempt kinds from arbitrary `StepResult.reason` prose. A
proposal block that mentions `branch_lifecycle_policy_violation`,
`repair_first_policy_violation`, or `schema_quality_block` must not become a
lifecycle, repair, or schema-quality accounting event unless the step carries
the typed `attempt_kind` emitted by the producer.

### Target Model

Live campaign accounting trusts explicit `StepResult.attempt_kind` and existing
structured state such as `action == "reconcile"` or scheduler active-slot
diagnostics. Reason text remains report material. Legacy reason-text
classification may exist only in postrun/report compatibility helpers, not in
the running campaign loop.

### Acceptance

- A non-counting step whose reason merely mentions lifecycle, telemetry-repair,
  same-family retry, or schema-quality keywords remains a generic proposal
  block unless `attempt_kind` is set.
- Explicit `attempt_kind` values continue to drive lifecycle, repair, schema
  quality, and scheduler accounting.
- The repair is generic and introduces no CVRP, warehouse, or solver-specific
  policy.

## 8. Design F: Weak-Positive Runtime-Pressure Follow-Up

### Problem

Runtime-evidence completeness pressure is useful when a branch has low,
cached, incomplete, or aggregate-excluded runtime evidence. But it must not
override the scheduler's research-continuity obligation when the branch is a
current weak-positive follow-up with no case-level loss. Otherwise Scion can
validate that active slots are not blocked while still failing to do effective
research: a clean fork repeatedly wins over the low-SNR mechanism that should
receive one more bounded follow-up.

### Target Model

Runtime pressure is a generic resource-policy signal. It may prefer a clean fork
only after the scheduler checks whether the pressured branch still carries a
current weak-positive signal without case-level loss. Current weak-positive
signal can come from problem-neutral evidence tier/status fields, case wins,
or pair-level wins. The scheduler audit must record when clean-fork pressure is
suppressed so the decision is visible in branch cards and postrun summaries.

### Rules

- A weak-positive lineage branch with any case-level loss remains eligible for
  runtime-evidence clean fork pressure.
- A weak-positive lineage branch with no case-level loss and current
  weak-positive evidence remains schedulable as `weak_positive_followup`, even
  when runtime confidence is low, incomplete, or aggregate-excluded.
- Pair-level wins may justify one weak-positive follow-up when case-level wins
  are zero, but they do not become a promotion or `DecisionFeatures` input.
- The audit record must include the suppressed clean-fork reason, pressure
  count, case wins/losses, pair wins, evidence tier, and runtime pressure
  triggers.
- The rule is problem-neutral. Generic scheduler code must not mention CVRP
  cases, BKS, CMT, two-opt, warehouse order semantics, or mechanism ids.

### Acceptance

- Focused scheduler tests cover both sides of the boundary: weak-positive with
  a case loss still clean-forks, while weak-positive with no case loss follows
  up under `weak_positive_signal_followup`.
- A replay of the clean CVRP validation database selects a current weak-positive
  branch for `exploit_weak_positive` instead of creating a clean fork.
- Audit metadata explains the suppression as
  `runtime_evidence_clean_fork_suppression=weak_positive_exception`.
- The implementation stays below the code-quality guard and does not create
  problem-specific projection helpers.

## 9. Design G: Target-Intent Authority Resolution

### Problem

Target-intent preflight previously treated prepared
`launch_research_focus.required_mechanism_ids` as absolute authority. That is
valid for open exploration or clean-fork contexts, but it conflicts with an
existing selected branch whose branch hygiene requires branch-local protected
mechanism follow-up. In that case Scion can freeze selected target intent to a
prepared new mechanism, then ask schema preview to rewrite formal hypotheses
back to the protected branch mechanism. The proposal loop fails before code or
Protocol even though both individual guards are correct.

### Target Model

Introduce one proposal-layer authority resolver before final hypothesis
generation consumes target intent. The resolver compares only generic fields:

- prepared required mechanism ids;
- branch protected or allowed mechanism ids;
- branch follow-up mode and same-mechanism hygiene;
- the selected target-intent action and mechanism id.

### Rules

- Open exploration and clean-fork contexts may bind target intent to the
  prepared required mechanism id.
- Existing branch follow-up with an intersection between prepared and protected
  mechanism ids binds the intersecting id.
- Existing branch follow-up with disjoint prepared and protected ids defers the
  prepared focus and binds target intent to the branch-local protected id.
- Protected and allowed branch mechanism ids form one ordered authority set;
  allowed ids must not shadow protected ids when both are present.
- If branch-local authority takes over a target intent that was drafted as a
  new action, the selected intent becomes an existing-file `modify` follow-up
  rather than a `create_new` placeholder.
- Deferred prepared focus remains auditable as proposal-layer diagnostics and
  `deferred_required_mechanism_ids`, but the final hypothesis context must not
  still hard-require the deferred id.
- Host transport details such as `tool_context_overrides` stay outside the
  intent body; the intent carries only selected target fields and tainted audit
  diagnostics.
- The resolver is not a Decision input and must not read problem-specific
  mechanism names, case ids, BKS/gap facts, or CVRP/warehouse semantics.

### Acceptance

- Focused unit tests cover open exploration, disjoint existing-branch follow-up,
  intersecting follow-up, and no-required-focus cases.
- A session regression verifies that branch protected id `B` plus prepared id
  `A` can produce a valid branch-local partial hypothesis around `B` without
  tripping the original required-mechanism guard.
- Existing prepared-focus target-intent behavior remains valid for
  non-conflicting contexts.
- Local and WSL conda `scion` focused proposal tests pass.
- The WSL authority validation root finishes current-run-ready with 4 effective
  Protocol rows, 0 proposal quality blocks, and 0 active-slot blocks, proving
  branch-local weak-positive follow-up can cross target intent, formal
  hypothesis/code, and Protocol instead of cycling between target-intent
  binding and same-mechanism guards.

## 10. Report-Only Actionability Classifier Addendum

The accepted authority root exposed two postrun actionability false positives,
not new CVRP-specific framework requirements:

- accepted clean-fork policy choices were counted as missed same-mechanism
  opportunities;
- voluntary `branch_lesson_usage` without an active requirement was counted as
  a semantic gap.

The repair is deliberately report-only. Same-branch follow-up classification
now separates selected follow-up, true missed follow-up, and accepted
clean-fork policy choices. Research-continuity semantic gaps are computed from
explicit semantic failures/blocks, not from `present_count - satisfied_count`
or global requirement projection deltas. Existing completed summaries can be
reclassified from their step scheduler metadata during postrun research
efficiency reporting, without mutating campaign, scheduler, Protocol,
promotion, or `DecisionFeatures` state.

Acceptance:

- Focused observability and research-efficiency tests cover accepted
  clean-fork policy choices, legacy summary reclassification, voluntary
  branch-lesson usage, and true semantic failure/block gaps.
- The accepted authority root can be rebuilt in a temporary postrun acceptance
  directory and reports `actionability_gaps=[]`,
  `same_mechanism_missed=0`, `branch_lesson_semantic_gap_count=0`, and
  `accepted_clean_fork_policy_choice_count=1`.
- No CVRP/warehouse mechanism semantics enter generic core; only generic
  scheduler/actionability reason codes are interpreted.

## 11. Design H: Research Guidance Binding Semantics

### Problem

The current warehouse positive-control root
`/home/xjy-ubuntu/research/scion-experiments/v04-wh-v2-positive-65115459-current-8r-gpt55-20260623T084049Z-claw`
finished wrapper/postrun-ready but stopped after 1 effective round with
`repeated_quality_block_signature`. The quality blocks were not a warehouse
plateau signal. They exposed a contract contradiction:

- Warehouse typed guidance mechanisms such as
  `warehouse_champion_v2_checkpoint`, `validation_transfer_continuation`, and
  `warehouse_runtime_model_handoff` are research-context and evidence axes.
- Existing warehouse operator patches must use concrete runtime export keys
  such as `operator_diagnostics.move_order.*`.
- Generic launch-focus projection treated every typed required mechanism as a
  hard `required_mechanism_ids` guard.

Thus a hypothesis using the concrete operator id was rejected by generic
launch-focus binding, while a hypothesis using the context id was rejected by
warehouse telemetry identity.

### Target Model

`RequiredMechanism` now carries a generic
`hypothesis_mechanism_binding` field:

- `required` means the mechanism id must appear in hypothesis
  `mechanism_changes`.
- `context_only` means the mechanism renders into proposal guidance and
  readiness path coverage but does not become a hard schema-preview mechanism
  guard.

The default is `required` for compatibility. CVRP keeps
`large_instance_intra_route_two_opt_seed` hard-bound. Warehouse marks its
champion-v2, validation-transfer, and runtime handoff mechanisms as
`context_only` while keeping all rendered guidance and evidence requirements.

### Acceptance

- Generic research-guidance tests prove a context-only mechanism renders and
  appears in rendered paths, but is omitted from
  `launch_research_guidance_payload()["required_mechanism_ids"]`.
- Warehouse provider/launcher tests prove warehouse guidance is context-only
  for hard binding while still rendered.
- CVRP provider/launcher tests prove CVRP still hard-binds
  `large_instance_intra_route_two_opt_seed`.
- No warehouse-specific condition is added to generic schema-preview guard
  code.

## 12. Design I: Launcher Running Status

### Problem

Prepared roots write root `run_status.json` as `prepared/prepared_only=true`.
During a live campaign, `campaign/run_status.json` becomes `running`, but root
status can remain prepared until the campaign exits and the wrapper copies or
annotates final status. This makes operator monitoring ambiguous and can make a
live root look like an unlaunched root.

### Target Model

CVRP and warehouse generated `run.sh` call a shared
`tools/write_launcher_running_status.py` writer after launch/auth/git/data-root
guards pass and before completion preflight or campaign execution. The writer
sets generic outer-wrapper lifecycle fields:

- `status=running`
- `prepared_only=false`
- `run_root`
- `campaign_dir`
- `git_commit`
- `model`
- `scion_base_url`
- `started_utc`
- `pid`
- completion-preflight and postrun-report booleans

Failure to write this running status is a pre-campaign infra failure. Final
campaign/postrun status still overwrites or annotates the same root status file
after execution.

### Acceptance

- A shared writer unit test covers the running payload.
- CVRP and warehouse launcher tests prove generated `run.sh` writes running
  root status before completion preflight and campaign execution.
- postrun artifact inventory classifies running-status writer failure as
  generic pre-campaign infra failure.
- Launcher lifecycle status remains report/operator material and does not
  enter Decision, Protocol, scheduler, solver semantics, or `DecisionFeatures`.

## 13. Design J: Launcher Lifecycle Contract

### Problem

The CVRP and warehouse agentic launchers now share the same outer lifecycle
requirements: read `launch.env`, resolve auth, guard the runtime checkout,
write campaign-execution and running-status artifacts, execute `scion run`,
rebuild postrun acceptance, and annotate final wrapper status. These
requirements are generic operator semantics, not CVRP or warehouse semantics.

Today that lifecycle is duplicated inside two large launcher scripts. Recent
repairs proved the behavior, but the implementation shape makes future fixes
too easy to apply as one-off string patches in both launchers. That increases
the chance that one problem gets a different wrapper contract from another and
violates the v3 goal of deterministic, auditable runtime boundaries.

### Target Model

Introduce a generic launcher lifecycle model, not a bag of helper snippets.
The model owns the outer wrapper contract and shell generation around a
problem-provided command:

- `LauncherLifecyclePlan`: typed Python object for run root, campaign dir,
  repo root, git commit, Python executable, model route, API-key env binding,
  postrun-report policy, runtime guard paths, exported environment names, and
  optional pre-campaign guards.
- `CampaignCommandPlan`: typed problem-owned command payload containing
  problem/protocol/split/seeds/rounds/runtime limits, agentic limits, forced
  target arguments where applicable, and problem-owned data-root exports.
- `PreCampaignGuard`: typed shell guard with a generic failure key and
  problem-owned condition body. CVRP normally needs no extra guard; warehouse
  owns the warehouse data-root guard.
- `render_run_sh(plan)`: a deterministic renderer that writes one outer
  lifecycle script. It may render shell, but the lifecycle facts come from the
  typed plan.

The generic lifecycle layer may know about wrapper status, launch env loading,
auth resolution, git runtime guards, campaign execution markers, running
status, completion preflight, postrun acceptance, and final wrapper status. It
must not know CVRP case ids, BKS facts, warehouse operator ids, mechanism
names, or problem-specific research guidance. Those remain in problem-owned
providers and command plans.

### Acceptance

- CVRP and warehouse generated `run.sh` keep the same observable lifecycle:
  pre-campaign failures write `exit.txt` and root `run_status.json`, running
  status is written after guards and before completion preflight, campaign
  execution marker is written before `scion run`, postrun acceptance annotates
  final wrapper status, and effective wrapper exit semantics are unchanged.
- A generic unit test covers the lifecycle renderer with a dummy problem
  command and a dummy extra guard. CVRP/warehouse tests assert only their
  problem-owned command/env/extra-guard differences plus a small set of common
  lifecycle markers, instead of copying large run-script assertions.
- Existing prepared manifest, launch readiness, and postrun inventory tests
  pass. No `DecisionFeatures`, scheduler, Protocol, promotion, or solver
  semantics consume launcher lifecycle fields.
- The new module stays cohesive and below 1000 lines. The launcher tool scripts
  become orchestration adapters; they do not own the lifecycle contract.

## 14. Code Quality Guard

Large files are now a design smell for this area. The next repair should not
increase the helper-script surface.

Guidelines:

- New production modules should be cohesive and normally stay well below 1000
  lines.
- Existing files above 1000 lines may be touched for compatibility shims, but
  new behavior should move behind named ports or typed models.
- Test fixtures for prepared roots and research guidance should move into
  reusable fixture builders instead of being copied through multiple large
  tests.
- Tool scripts should orchestrate; they should not own domain contracts.

## 15. Migration Order

1. Add `BranchSchedulingStatus` and route scheduler, active-slot inventory, and
   branch-card active-slot fields through it. Keep behavior equivalent except
   for the current slot-block bug.
2. Add regression tests for the WSL failure shape using dummy mechanisms:
   two stale or copied weak-positive branches plus one inactive diagnostic
   branch must not terminate the campaign by slot blockage when a clean
   exploration lane is available.
3. Add the generic `research_guidance` schema and renderer with a dummy problem
   fixture.
4. Move CVRP prepared focus construction to a CVRP provider and make the
   launcher call it. Keep a compatibility adapter for existing prepared
   manifests.
5. Move warehouse prepared focus construction to a warehouse provider.
6. Replace generic projection/readiness string probes with contract validators.
7. Split repeated prepared-root test builders into fixture modules.
8. Remove live campaign accounting fallbacks that infer control-plane attempt
   kinds from reason text; keep compatibility text parsing in reporting only.
9. Route agentic proposal failure handling through one typed routing signal so
   circuit-breaker and lifecycle categories trust structured output before
   legacy detail compatibility.
10. Apply weak-positive runtime-pressure follow-up policy and audit clean-fork
    suppression for current weak-positive branches without case-level loss.
11. Add target-intent authority resolution so prepared launch focus cannot
    hard-bind a disjoint new mechanism after the scheduler selects an existing
    branch-local follow-up.
12. Reclassify postrun research-context actionability using report-only
    same-branch and branch-lesson semantics before treating completed roots as
    prompt/context failures.
13. Add research-guidance mechanism binding semantics so problem-owned
    context/evidence axes can render without becoming hard hypothesis mechanism
    ids.
14. Add root launcher running status so live campaigns are not reported as
    prepared-only roots until they finish.
15. Move duplicated outer launcher lifecycle generation behind a typed generic
    lifecycle plan and renderer. Keep CVRP/warehouse command construction and
    problem guards problem-owned.

No new long CVRP relaunch should be used as acceptance until steps 1 and 2 are
complete. No broad prepared/readiness refactor should start until steps 3 and 4
have characterization tests.

## 16. Non-Goals

- Do not tune CVRP heuristics in generic core.
- Do not move BKS, case gap, CMT case details, warehouse domain facts, or raw
  calibration rows into `DecisionFeatures`.
- Do not add broad proposal budgets, truncation, compression, or decorative
  gates to mask scheduler or context-design problems.
- Do not solve v0.5 governance ablation in this slice.
