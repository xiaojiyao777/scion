[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -m "not slow"
markers =
    slow: long-running MILP solver tests excluded from the default surrogate suite
    integration: Scion/surrogate integration tests that may exercise subprocess solvers
