# CLAUDE.md — compatibility entry

This file exists only for agents that discover `CLAUDE.md` automatically.

Before any analysis, edit, experiment, or Git action, read and follow
[`AGENTS.md`](AGENTS.md). It defines the canonical reading order, current
handoff, source precedence, allowed actions, and project boundaries.

Do not reconstruct current behavior from historical instructions formerly kept
in this file. In particular, this file does not prescribe a model, provider,
machine path, process carrier, solver, problem-specific mechanism, packaging,
deployment, build, systemd/nohup setup, Trust/Hash chain, object identity,
lease, signing, registration, receipt, or repeated closure.

Current task and status sources are reached through `AGENTS.md`; when code and
dated prose differ, investigate the current source and report the discrepancy
instead of inventing another authority layer.
